# PIXIU 两大核心功能命名

2026-09-15 按用户要求确立产品名称，用于当前32页科技风演示稿与项目总览。

| 产品名称 | 功能说明 | 工程对应 |
|---|---|---|
| **记忆焕新** | 后台理解与记忆整合 | Dreaming |
| **记忆互联** | 分布式多设备 Agent 记忆共享 | P2P 同步与 Agent 记忆适配 |

## 记忆焕新

“焕新”强调已有记忆能够结合新资料得到整理和更新。当前授权目录是触发入口；功能主体是逐块理解资料、检索关联记忆、读取原记录，提出新建、更正或合并方案，并保留来源。

新建遵循目录授权，更正与合并先冻结目标版本，再交用户逐计划审批。只处理获授权的私人范围，不自动将结果转为共享记忆。当前命名不意味着睡眠调度、自由联想、模型训练或无审批覆盖原记忆。

代码依据：`backend/agent/dreaming/runner.py` 的受控模型循环，`session.py` 的 create/update/merge 方案与冻结版本；公共接口见 `docs/API.md` 的文档暂存和 Dreaming 计划记录。更正截图不能单独证明合并已完成实测。

## 记忆互联

“互联”强调多台设备上的 Agent 可使用授权共享的知识和来源。各设备独立保存副本，通过可信配对、共享范围控制、对等同步和重连对账实现连续使用。

仅共享范围参与同步，私人记忆不出端。设备断连时保留本地记忆能力；跨端副本在重连后收敛，不宣称离线期间即时一致或共享模型参数、完整会话及任意文件。离线记忆能力不等于模型推理完全离线。

依据：`docs/ARCHITECTURE.md` 第4节、`backend/foundation/docs/ARCHITECTURE.md` 的 P2P CRDT 同步章节，以及现有三端验证页。代码标识、API 路径、数据库表名仍使用现有命名。

## 演示稿主线

- 封面、第2页目录、第5页定位：点明两大功能。
- 第7页：双主角布局，两项并列，其他通用能力作为支撑。
- 第8页：记忆焕新的理解、关联与整合流程，使用审批界面说明更新能力。
- 第11页：记忆互联，保留三台电脑完整网状连接。
- 第24、25页：记忆焕新的跨会话召回与更正案例。
- 第28页：记忆互联的三端收敛验证。
- 结尾：统一回扣两大名称。

目录授权、文件采集仍可作为具体步骤出现，不能作为 Dreaming 的总名称。历史31页正式稿未在本轮替换。

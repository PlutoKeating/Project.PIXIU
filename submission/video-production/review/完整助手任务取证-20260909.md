# 完整助手任务取证与章节制作

本轮未修改产品业务代码。现有完整影片仍为第20版；s16章节修订后的制作时间轴为575.100秒。

## 网关路径

- 完整助手任务01漏传模型，网关实际请求hermes-agent；HTTP200正文为初始化失败10。不是任务成功。
- 官方SDK通用错误码10是MODEL_NOT_FOUND。同一最小问候请求，hermes-agent返回502；kylin-qwen-plus返回200和“你好。”。原始对照保留。采集脚本现强制传入已列出的模型，不把此采集错误归因于SDK不可用。
- 02完成计算与记忆保存，但文件包含字面量换行，终端检索无结果后echo官网地址；不能作搜索成功证据。
- 运行时已支持DDGS但缺少可选依赖。演示B机安装ddgs9.16.0、lxml6.1.3、primp2.0.0，版本固定在scripts/requirements-agent-demo.txt；未改变发布包依赖。重启演示网关后载入web_search。
- DDGS后端实测01是未初始化插件注册表的孤立调用，返回未配置；02直接调用官方插件提供者，取得真实官网结果。两份均保留，不把01当作网关最终配置结论。
- 完整助手任务03实际调用terminal、write_file、read_file、web_search、pixiu_memory_remember；四行文件和官网结果已核对。04新会话检索包含03新来源，但最终回复引用02的相同预算；该边界保留在网关验证JSON。

## 原生客户端路径

使用唯一样本“秋季阅读采购复核单”：24+36+40=100。原生会话f1cf364c-a676-405c-8864-b4a0d66de79d执行两轮，原始录屏、每帧PNG及时间表全部保留。

首轮计算、写入四行、回读完成，但搜索错误地加入openkylin.com域名并多次超时，未保存记忆。第二轮先成功调用记忆保存，随后搜索仍失败，最终错误声称openkylin.com为官网。不能把“已完成工作”卡片数视作工具全部成功，也不能把本次原生搜索作为官网成功证据。

新原生会话ab3218c1-1c19-486a-a50a-52dec11cb36b仅按标题调用记忆检索，正确回复24/36/40及100，最终引用knw_01M22B0S2378RFPM5DNSJZVT1N，与显式保存的evd_01M22B0S20WPHCY44J60ETGP5J一致。其检索也包含自动沉淀的前次会话文本；不声称所有候选内容均正确。验证JSON和最终原生截图已核对。

## 后续制作约束

s06可以使用已验证的原生文件任务、显式保存与新会话复用；联网搜索成功另有网关第三轮的真实记录。若同章组合两批素材，必须明确分段来源，不拼成一次全部成功。本轮任务章节已制作为25秒样片，04修正页脚后有限独立复核通过；未听审或放行全片；首轮自动预取与原生长启动问题未由本轮解决。

录屏采用每秒2次真实采样、按实际时刻复制到30fps，不生成中间动画。用户已授权全部素材保留与推送，故失败样本、重复预算和原始录屏也纳入版本管理。

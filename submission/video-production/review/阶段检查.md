# 首批制作检查

本记录仅覆盖制作工程和开场审阅样片，不代表最终视频通过。

- 赛方三张附件已查看，官方 PPTX 实际解包读取全部页面文字；两个赛题记录摘要与 `docs/OFFICIAL_SOURCES.sha256` 一致。`DELIVERY_PLAN.md` §0 未修改。
- 38 张原素材与原仓库截图逐文件 SHA-256 相同；清单在 `raw/manifest.json`。新增一张 V11 当前桌面原始截图，作为状态记录，尚未选入片。
- `npm run check` 在视频工程目录通过。Remotion 固定 4.0.484，Node.js 24.16.0，npm 锁文件保存。
- 开场使用准确的 `BrandInkOpen.tsx` 示例适配，中文品牌／副标、浅色产品配色，延长落定段承载解说。制作参考源与适配源分开保留。
- 实际 MP4 为 H.264、1920×1080、30 帧，AAC 48 kHz，容器时长 11.050667 秒，694636 字节。视频为 330 帧；额外容器时长来自音轨封装。
- 从实际 MP4 抽取第 25、80、200 帧并查看，品牌和中文均可读、无裁切；第 25 帧为预期的压印入场模糊，第 80、200 帧落定。三个抽帧均保留。
- FFmpeg 完整解码成功；声轨最大音量为 -7.3 dB，无削波。中文原音 9.36 秒，序列从 0.5 秒开始。试听及逐词对齐仍待最终混音核对，不以峰值检测替代听感检查。
- 另生成 s01、s02 的逐词原始时间信息。早期 SRT 两句边界存在 50ms 重叠，开场显示已按下一句起点截断前句；完整字幕生成需要统一处理边界并留存原始值。
- 最终 5–10 分钟时间线、全部核心功能、真实跨节点录屏、全部中文声轨、最终 ZIP 和独立终检均未完成。

## 三端采集与配音进展（2026-09-09，仍未交付）

- 三台独立 V11 虚拟机使用独立数据、签名身份及 TLS 证书。原设备正常数据库与配置保留，私钥、运行数据库和磁盘链记录仅存在忽略目录。
- 实际发现暴露 Zeroconf 回调签名错误，已在 `ba3e5c6` 修复；33 项发现/同步服务/收敛回归通过。原安装包仍是 0.1.9，当前采集环境另外加载了该修复，正式包尚待重建。
- 三端配对完成；相同共享记忆和证据 ID 均可检索，原始结果在 `raw/network/三端首次共享查询.json`。
- 录下笔记本实际检索：12 秒、48 张原始帧及逐帧开始/结束时间，编码为 H.264 30 帧。素材含系统边框、维护模式水印及连接状态，最终剪辑须据实裁切与标注，不能误称完整 Agent 会话已连接。
- 同步 TCP 断连时第三端保留旧版本，恢复后补齐；并发正文曾达到三端一致。但随后遗忘重连步骤失败，发现 `Mainline` 仲裁提前写入缺少远端 evidence 的知识，引发外键错误；整套网络回归仍为失败，不能把中间一致状态提升为完整稳定性结论。临时防火墙规则已全部移除。
- 30 段当前稿配音齐全，保留 s20/s21 修改前后的原音；当前稿净时长 444.024 秒。`prepare_timeline.py` 按当前讲稿选择音频并核验 SHA-256，生成 543.100 秒的逐镜时间线与无重叠字幕草稿；静态类型检查通过，尚未完成试听、完整渲染和逐镜审阅。
- 当前优先工作：复现并修复仲裁路径的迟到证据问题，再重跑三端遗忘和重放场景；随后补齐其他素材、剪辑、混音、独立复审及正式 ZIP。不得标记任务完成。

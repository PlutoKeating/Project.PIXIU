# 跨端共享章节与工作台检查

s20 使用笔记本真实请求局部、客厅完成后核对录屏及真实结果定格。
第 239 帧随“换到客厅”切换画面，300 帧原速录屏后保留工具结果。
该素材不是请求至回答的全程录像，画面明确标注“完成后核对”。
s21 则明确切换到家庭节能约定，避免把先前的断连试验误讲为书房约定。

30 秒审阅 MP4 整段解码通过；实际 100/320/620 帧已经查看，用户请求、
工具完成、回答与知识来源可见，字幕与注记没有覆盖业务正文。
源像素放大仍偏软；已另采集原生 4K 截图/录屏，尚未入本轮全片。

配音互相关 0.99996，测得音源实际起点为第 15.29 帧，目标第 15 帧，
误差约 0.29 帧；解码峰值约 -3.25 dBFS，无数值削波。
这是信号测量，不是主观试听。见 `shared-agent-01-audio-sync.json`。

工作台清单与主片同源，画面关闭内嵌语音和字幕，分别建立独立轨道。
标题、字幕内容及相关字号/颜色可编辑；原镜头运动参数未开放。
在开场、普通镜头、共享三阶段与片尾六个抽样帧，导入与原合成的差异像素为零。
校验工具、结果和两侧 PNG 保存在 `workbench-parity-01/`。

第四版渲染启动后，为工作台抽取了共享组件、音频/字幕开关和审阅叠加层，
默认样式保持一致；渲染时源码原件与摘要保留在 `source-snapshots/full-draft-04/`
和 `full-draft-04-source.json`，可区分原始渲染版本与随后工作台接入版本。

本轮没有修改产品实现，不宣称自动预取或会话初始化慢的问题已修复。
全局余项见 `稳定交付剩余工作.md`。

第四版完整输出 554.154667 秒、54735605 字节；整片解码通过，实际
10317/10537/10837/11220 帧已查看。两段新解说波形相关均超过 0.9999，
起点相对目标均约晚 0.27 帧。结果与原数据见 `full-draft-04-technical.json`
和 `full-draft-04-audio-sync.json`。这不代表逐段试听或完整功能终检通过。

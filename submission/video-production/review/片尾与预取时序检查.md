# 片尾与预取时序检查（2026-09-09）

仍为制作阶段证据，不代表最终交付完成。

## 片尾

- 已在 Gallery 索引确认 `outro-group-photo-launch` 同名变体，阅读完整镜头卡及原模板 `live/SceneOutroLive.tsx` 后适配；原参考源码保持不变。
- 新组件 `src/SceneOutro.tsx` 保留九元素每 3 帧错峰、12 帧飞行、真实过冲曲线、4° 落机位、字标压印、舞台光与确定性粒子。替换为蓝白产品色、中文字体和九张真实产品切片，裁切及 SHA-256 清单为 `outro-crops.json`。
- 字标为“貔貅”，副标为“让经验，成为下一次行动的起点”。因用户要求口白与中文字幕对应，片尾仍保留字幕；飞入层预留底部 140px，避免遮挡字幕。此为相对于模板无字幕片尾的明确适配。
- 第一版静帧 20/50/120 已查看；修正中文字标偏左、元素侵入字幕区。第二版实际 MP4 的 20/120/265 帧已查看，字标居中、字幕区完整、落定后留有充分停留。
- 两版原 MP4 均保留。第二版完整解码通过。旁白与音效处于独立声明的音频序列，未添加背景音乐。

## 声画测量

- `scripts/measure_audio_sync.py` 使用 FFmpeg 解码为 48kHz 单声道，再在声明位置附近以归一化波形互相关定位音源。NumPy 2.5.3；这是信号测量，不能代替试听。
- 第一版解说与落印音效分别测得输出滞后 1.28/1.270625 帧。按同管线约 1.28 帧补偿旁白；按音源 10ms RMS 峰值延迟另补偿落印和余韵。
- 第二版：旁白预期第 15 帧开始，测得 15.29；落印目标第 50 帧，测得主峰约 49.94；余韵目标第 70 帧，估计约 69.53。
- 余韵音源相关系数较低（0.052），被混合声轨掩盖，其对齐值仅为弱证据；管线偏移主要依据相关性较高的解说和落印探针。
- 第二版解码声轨峰值约 -4.96 dBFS，无数值削波。当前工具返回“不支持音频输入”，未完成主观听感审阅，不宣称试听通过。
- 音效沿用 skill 提供的素材及许可记录，见 `reference/audio-attribution.md`。其中 riser/sparkle 的逐文件原始 URL 未记录，保留来源记录的这一限制；未把无法反查的地址编造为已核对。

## 新会话预取

- `scripts/probe_prefetch_timing.py` 调用真实 Provider，在测试 API 返回处设置受控闸门；没有修改产品代码，也没有调用真实模型。
- 两次默认运行均退出 1：新会话第一次 `prefetch` 返回空字符串，查询完成后可以取到正确家庭约定。
- `--ready-before-prefetch` 只改变查询完成时机，运行退出 0，第一次缓存读取取得正确记忆。三份 JSON 分别保留。
- 这证明既有“后台查询 + 立即读缓存”调用顺序存在首次召回时间窗口，不等于已证明上次模型错误回答的完整根因。当前契约明确要求 `prefetch` 只读缓存，未擅自改成同步 HTTP 调用。
- 客厅错误回答的原始 evidence 已读取，provenance 指向会话 `37d7d030-675b-49c2-93e5-182dc836185d`、首轮，scope 为 `shared:home`。其自动写入范围与预期一致；实际模型请求是否收到正确上下文仍待追踪。
- 下一步核对实际模型输入与显式检索工具执行，再选择符合契约的最小修复，并重跑真实跨端会话；失败原数据不删除。

## 实际会话请求补证

已定位客厅端该会话的 details 日志，只提取匹配标志、工具名及时间差，完整日志不出库；原日志 SHA-256 绑定在 `raw/network/跨端实际模型请求核对.json`。实际请求包含 `system` 与 `user`，正确约定文本和 memory-context 均未出现，hard scope shared:home 指令存在，五个 PIXIU 工具均已提供，模型仅调用一次、未返回工具调用。由此可排除“正确约定已在该请求里但模型忽略了它”这一分支；仍需修复首次召回时间窗口并验证工具使用。Agent 创建耗时 985.565 秒，模型响应 7.495 秒，初始化耗时须单独定位。

## 显式工具路径真实通过

随后在客厅创建新会话 `bce05021-099a-4685-bc5c-885572af7f2c`，明确要求使用记忆检索工具，依据实际返回作答。实际日志显示 `pixiu_memory_search` 开始、成功完成、第二次模型调用给出正确约定，引用的是笔记本先前沉淀的 `knw_01M212PEJJ5EBTG8FAXS7JKZEX`。原工具参数/结果、最终回答与日志摘要保存在 `raw/network/客厅智能体实际工具检索成功.json`；界面原图及展开工作卡的 10 秒录屏、40 张采样帧也已保存。该次创建仅 0.209 秒，整轮 6.791 秒。它证明显式工具调用下真实跨端 Agent 复用成功，不证明首次自动召回已修复，也不推翻前一次初始化过慢的记录。

## 全片第三版

`renders/全片初剪-03.mp4`：543.146667 秒、40689930 字节，H.264/AAC、1080p/30fps，整片解码通过。改动绑定源码 SHA-256，实际抽查遗忘区与片尾入场画面，见 `full-draft-03-technical.json` 和对应帧。该版已接入组装片尾及声轨补偿，但尚未接入刚取得的客厅 Agent 成功素材；核心场景仍需统一讲稿、画面与来源。

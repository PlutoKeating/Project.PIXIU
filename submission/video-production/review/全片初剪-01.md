# 全片初剪 01 检查（2026-09-09）

仅为制作阶段检查，尚未通过最终交付验收。

- 实际输出 1920×1080、30fps、H.264/AAC；543.146667 秒，39129425 字节。
- FFmpeg 对全片完整解码退出 0，无错误输出。
- 实际 MP4 已抽取 s04/1457、s14/7093、s20/10307、s21/11027、s24/12887、s30/16083 帧。
- 已查看 s20、s24、s30：中文标题与字幕完整；共享段只有结果截图，遗忘确认按钮靠近下沿，片尾尚为字卡。
- 后续源码已接入共享检索 12 秒和遗忘后检索 10 秒的真实录屏，原速播放后明确标注结果定格；遗忘页调整为操作区取景。初剪 02 仍需整片重渲与抽帧核对。
- 页面像素来自 1440×900 原生桌面，尚未达到模板建议的 2x 纹理清晰度；特征镜头和片尾尚未补齐。
- 音频试听、字幕逐段核对、正式安装包重建及独立终检未完成。

## 接口采集证据边界

- `结构化知识与记忆晋升-有效记录.json`：执行完整脚本退出 0，四类知识只读类型核对与两类显式晋升后检索均通过。原始返回不改写。
- `结构化知识与记忆晋升.json`：未完成：脚本误以为公共 context 返回 kind，首条检索后停止。
- `结构化知识与记忆晋升-复核.json`：未完成：只读类型核对 SQL 尚未正确实现。
- `结构化知识与记忆晋升-完整.json`：未完成：只读类型核对 SQL 含误加字符；文件名不代表完成。
- `智能体会话自动沉淀与跨端证据.json`：支持自动回合沉淀及跨端可见；不支持将模型自述当作已执行记忆工具的证据。

原始失败记录保留供追踪，不与成功记录合并，不仅根据文件名判断有效性。

## 跨端会话后续结果

- 客厅端请求最终完成，但回答了无关的公共书房活动，未回答家庭约定；失败原图及判定单独保留，未入片。
- 完成后以相同问题调用公共 `/agent/context`，返回原本的正确约定，同时返回新自动沉淀的错误回答（排名在前）。这支持后端可检索正确记忆，不能证明当时模型收到了该上下文；也暴露错误回答进入记忆后影响后续检索的风险。
- 端到端原因尚待核对实际模型路径、注入上下文与轮次记录。未删除失败数据或重试到成功来覆盖失败结论。
- 初剪 01 音轨 mean_volume -28.4 dB、max_volume -4.8 dB；无数值削波，但不代表听感和字幕同步已验收。

## 下一轮排查入口

- `integrations/kylin_agent/pixiu/provider.py` 的 `on_turn_start` 将 context 请求交给后台队列；`prefetch` 只取已有缓存。
- 上游 `agent/conversation_loop.py` 在 `on_turn_start` 后立即调用 `prefetch_all`，结果缓存用于本轮循环。新会话存在来不及取回首次 context 的可能，需通过受控时序复现和实际模型请求证据验证；尚未认定为本次错误回答的完整根因。
- 正式提交目录检查仅提示缺少演示视频 ZIP，9 项目录规则测试通过，官方文档 SHA-256 均匹配。

## 初剪 02 验证

- `renders/全片初剪-02.mp4` 绑定源码 `2ba42bf`，543.146667 秒、38334954 字节；1080p/30fps、H.264/AAC，整片解码成功。
- 已查看实际输出 s20/10427、s24/12887、s25/13607 帧：共享检索真实输入过程与遗忘后无结果可见，确认按钮完整保留。遗忘区下缘仍有极细的原始水印残边，下一版应精确调整取景并复核。
- 该版仍为带审阅标记的初剪，不能提交为最终 ZIP；剩余事项延续上述记录。

# PIXIU 中文演示视频制作工程

2026-09-10 正在按当前 0.1.12 界面重新采集和制作。新版分镜在 `storyboard/shots.json`，新增 Dreaming 更正审批镜头 s11b；实际渲染时间轴将在新版素材和旁白就绪后更新。新原始素材位于 `raw/current-0.1.12/`，Word、PPT 共用的当前操作截图位于 `docs/delivery/assets/operations/03-current-workflows/`。制作与覆盖台账见 `docs/delivery/MATERIAL_ALIGNMENT.md`。文案采用正面、平实的行动与结果描述。

新版画面准备：运行 `npm run prepare:current`，从原始录屏按实际速度裁片，并从文档共用截图生成展示素材。`src/current-scenes.json` 记录来源、摘要、尺寸和剪辑时段；发牌与首尾卡片也使用本次实拍。运行 `npm run render:current -- .runtime/current-review.mp4` 导出独立的静音画面审阅，按分镜规划时长检查阅读时间。最终合成入口会核对旁白与分镜文案一致后再开始渲染。

2026-09-10 行为实拍环境补齐 V11 仓库的 `x11-utils` 7.7+6-ok1k0.1，通过真实 X11 公开演示窗口生成28秒使用记录。该工具是窗口行为统计的系统前提，安装方式同步写入部署指南。录制结束后已恢复两端助手共享设置及主机采集设置。图片裁切使用制作依赖 Pillow，裁片沿用 FFmpeg；产品源码与运行依赖此次按现有版本核对。

配音准备：`python scripts/narrate_azure.py --check-cache` 按当前文案、音色配置、音频摘要和词边界核对可复用原音。合成时从本机 `SPEECH_KEY`、`SPEECH_REGION` 读取配置，也可用 `--key-env` 指定凭据变量名、用 `--region` 指定区域；交互运行沿用隐藏输入。凭据保存在进程内存中。`prepare:timeline` 会先核对整组原音，再从匹配的音频重建170%音量PCM文件，将来源和播放文件摘要写入时间轴。当前30镜已核对11镜原音，19镜进入新版合成清单。

项目报告第6页和第12页使用同一批实拍：静音MP4按原始4fps采样制成单次播放动画，审批实录4秒后展示新会话结果3秒，跨端实录保持14秒，在麒麟V11 LibreOffice 24.2中直接随页面播放。制作依赖沿用FFmpeg和Pillow，来源与动画摘要写入演示片段清单。

以下为本次修订前的成片记录。

最终交付：`renders/演示视频-最终封面版.mp4`。用户提供的`raw/cover/approved-cover.png`替换第0帧并作为MP4内嵌封面；总计11706帧、390.2秒，完整AAC音轨直接复制。仅为替换第一帧重新编码首个250帧GOP，其余11456帧直接复制并核对解码像素一致。已验收剪辑工程保持原样。检查见`review/cover-technical.json`，复现工具为`scripts/set_video_cover.py`（`--video`、`--cover`、`--output`、`--report`），沿用FFmpeg与Python标准库，无新增依赖。

当前首尾统一版：`renders/演示视频-首尾统一版.mp4`，390.2秒；无配乐版本为`renders/演示视频-首尾统一无配乐版.mp4`。首尾共用片头完整云枫音频`audio/s01.mp3`对应的170% PCM，无重新合成、变速或变调。项目名为“PIXIU · 貔貅”，介绍为“面向麒麟操作系统智能体的去中心化记忆系统”，slogan为“让每一台设备的记忆，彼此相通”。共享画面文案定义在`src/brandCopy.ts`。

首尾均使用用户选定的 video-shotcraft `outro-group-photo-launch` 卡片聚合模板，保留片尾的卡片飞入、落定光晕、字标登场和运镜；项目名从第0秒随卡片同时登场，介绍从第41帧、slogan从第161帧对应口白依次展出。用户明确要求首尾复用同套文案和模板。两镜均257帧，片尾从11449帧开始；中间27镜的文案、字幕、起点和时长逐项保持一致。`storyboard/shots.json`的`audio_reuse: "s01"`让时间轴生成器直接引用片头素材，字幕也来自同一份词边界。OpeningReview、OutroReview、完整视频及工作台共用实际镜头组件。

音量沿用口白170%、背景音乐15%。音乐从源15秒对应视频0秒，按新总时长重新截取，在389.2–390.2秒渐退。片尾riser为5%，另外两条音效分别为0.35/3和0.18/3；“打开来源”点击音效已移除。字幕仍为60px粗体纯白、7px黑描边、向下3px/模糊2px/alpha 0.3阴影，底部57px。此前未选用的短句合成保存在`raw/audio/s30-brand-retake/`，不进入当前播放链路。

复现：`prepare:timeline`后渲染`PixiuFinalFull`静音画面与`bgm:false`的WAV；WAV补偿2048个48kHz样本并裁到18,729,600样本，与画面封装为无配乐版本；再运行`mix:bgm`（不传`--reuse-stem`）更新音乐长度与最终混音。当前检查为`review/bookends-technical.json`与`review/bookends-package.json`；原音复用、编码音轨同步与独立画面审查分别见`bookends-source-check.json`、`bookends-export-sync.json`、`bookends-independent-review.json`。最终编码峰值−1.75dBFS，首尾音轨起点偏差均小于0.3帧。沿用已有依赖，无新增运行依赖。

`src/narration-volume.json`记录1.7倍口白增益，`public/audio/narration-170/`保存由原始音频转成的立体声PCM，浏览器使用volume=1播放，避免浏览器把超过1的音量参数截断。主片、章节预览及工作台通过`narrationAudio.ts`使用相同素材；音量调整阶段无TTS调用，当前首尾直接共用片头完整口白。`scripts/rebalance_audio.py`以原1倍无配乐白字版为基准，按波形测得的原始起点加入额外0.7倍口白，保留原音效，再混入配置指定的音乐增益，并直接复制画面流。此前45%配乐版只有一个线性混音样本超过满量程（约0.04dB），配乐导出采用关闭自动增益、补偿延迟的0.98峰值限制；编码后峰值约−0.18dBFS。此前音量检查记录为`review/audio-rebalance-technical.json`。此前15%版本无需峰值限制，直接沿用170%口白无配乐音轨，以`mix:bgm`混入15%音乐，检查记录为`review/mix15-caption07-technical.json`。

历史音量调整复现入口（不包含后续片尾修订）：`npm run mix:levels -- --baseline renders/演示视频-云枫白字无配乐版.mp4 --output renders/新的音量版.mp4 --no-bgm renders/新的口白版.mp4 --report review/新的音量检查.json`。必须使用原1倍口白、无配乐基准；输出文件使用新名称。依赖沿用现有NumPy与FFmpeg。

配乐配置为 `src/bgm.json`。`scripts/mix_bgm.py` 从原音乐生成包含截取与淡出的无增益WAV，再统一应用配置中的0.15增益；Remotion和工作台使用同一WAV与增益。全片组件支持 `bgm: false` 关闭音乐。时间轴变化后必须重新执行混音脚本以更新WAV长度。脚本使用已有NumPy及FFmpeg依赖，以已审核的无配乐MP4为输入，复制视频流，仅重编码混合音轨，不自动归一化或压低音乐。

复现：`npm run mix:bgm -- --video renders/演示视频-云枫02.mp4 --output renders/新的配乐版.mp4 --report review/新的配乐检查.json`。输出与报告须选新文件名。此前白字版检查见 `review/caption-white-technical.json`，全片以静音画面重新渲染后复制已验证配乐音轨，音轨压缩数据摘要相同。配乐检查见 `review/zeni-bgm-technical.json`：全片解码通过，视频流摘要一致，混合峰值约−1.09 dBFS、编码后约−1.11 dBFS。

当前云枫版已导出：29镜、约6分30秒，沿用审核文案，按新口白重排字幕、镜尾和动作。使用 video-shotcraft 的 Ink Press 模板，原版镜头源码保存在 `reference/ink-press/` 供逐镜核对；原版其他产品截图不复制、不入片。

赛方要求唯一引用 `../../docs/DELIVERY_PLAN.md` §0：5–10 分钟、演示所有已实现核心功能、ZIP 不超过 200M、作品内容匿名。现有两层同名交付目录保持原结构，本轮只更新内层 `演示视频.zip`，其余三项作品摘要未变。

本目录按用户最新授权保存制作素材，是正式作品目录之外的工作目录。

`build/release/scripts/submission_layout.py` 只把该命名的兄弟目录排除在正式材料核对之外；正式同名目录内仍严格限制四项作品，其他额外兄弟目录仍拒绝。

- `brief/`：制作依据、颜色与功能覆盖要求。
- `storyboard/shots.json`：当前29镜中文解说，实际时间轴390.2秒；历史30镜脚本及媒体通过Git与快照保留。
- `raw/`：原始截图、录屏、配音与来源摘要；不改写原素材。
- `public/`：渲染使用的素材，裁切与取舍必须可追溯到原素材。
- `src/`、`scripts/`：视频时间线与制作工具。
- `reference/`：所选模板、镜头卡、声音许可与终检依据，仅供制作。
- `review/`：静帧、音视频检测和独立审查记录。

环境：Node.js 24、固定版本 Remotion/React/TypeScript（见 `package.json`），FFmpeg/ffprobe，Python 3 与 `requirements.txt`。采集额外复用仓库 `backend/scripts/capture_desktop.py`、libvirt、SSH 和目标机既有工具。视频依赖不加入产品运行依赖。

三端素材采集另使用 QEMU 的外部写时复制磁盘、OpenSSL 演示证书和 V11 仓库
`wl-clipboard` 2.2.1-ok1（原生 Wayland 中文输入）。这些是制作工具，不是产品新依赖。
`scripts/prepare_vms.py` 建立两个克隆；`configure_demo_guest.py` 在独立数据目录
启动已安装后端；`demo_api.py` 通过 SSH 访问本机公共 API，不保存配对令牌；
`verify_partition.py` 临时阻断第三端的同步 TCP 端口，结束时逐条移除自身规则；
`record_desktop.py` 保留实际像素与逐帧采集时间，默认每秒采样四帧，编码为三十帧。

2026-09-09：三端互发现、双向配对和首次共享查询已核实。环境为同一宿主上的三台
独立 V11 虚拟机，不是三台物理机验收。联机过程中修复了两处 Zeroconf 回调参数名，
当前是 0.1.9 安装版本加源码修复，正式交付前须重建安装包并同步源代码作品。
原用户数据库和配置未改写；演示服务占用正常后端端口，恢复时停止
`pixiu-video-backend` 并启动原 `pixiu-backend`。安装目录的 discovery.py 原件保存在
各虚拟机私有制作目录，磁盘链恢复说明在被忽略的 `.runtime/vms/RESTORE.txt`；
共享基盘仍被两个克隆依赖时，禁止合并原虚拟机写入层。证书私钥、数据库和配置
不作为媒体入库。

当前29段解说已统一切换到团队选定的云枫音色；历史原音按请求摘要保留。测量清单为
`review/narration-durations.json`，实际听感尚未经人工连续试听。

第二轮同步验证使用新的独立数据库（旧失败现场保留），已通过断连补齐、同 ID
并发收敛、遗忘重连传播及遗忘后检索为空。源文件摘要与原始 API 结果保存在
`raw/network/第二轮断连并发与遗忘传播.json`；修复提交为 `3531a75`，没有把开发
源码加载冒称为重建后的正式安装包。两台克隆的 Runtime 共享 scope 已设为
`shared:home`，修改前配置备份保留在各自私有制作目录；原虚拟机 Runtime 配置未改。

新增 `PixiuSharedChapterReview` 章节审阅合成：
`npx remotion render src/index.ts PixiuSharedChapterReview renders/集体记忆章节审阅.mp4`。
此镜沿用 Ink Press 的 PaperTitleCard/DigitRoll 实现，替换为蓝白主题和中文字体，
接入词级字幕草稿。输出 8.7 秒，只是章节引导片段。

安装：本目录运行 `npm ci`（锁文件生成后）；Python 在独立虚拟环境安装 `pip install -r requirements.txt`。音频采用 [edge-tts](https://github.com/rany2/edge-tts) 的中文语音，逐句生成原音和时间信息，仅提交公开合成演示讲稿。完整初剪使用 `npm run render:full -- <新输出.mp4>`，静帧使用 `npm run still:full -- <新输出.png> --frame=<帧号>`。不能把初剪当成最终视频。

当前已生成 `renders/开场审阅样片.mp4`：11 秒的中文配音与字幕开场，供制作检查，不能提交为比赛视频。运行 `npm run check` 做类型检查，`npm run still -- review/new-frame.png --frame=80` 出新静帧；完整重渲使用 `npx remotion render src/index.ts PixiuOpeningReview <新输出.mp4>`。默认不覆盖已有素材。`scripts/narrate.py --shots s01 s02` 生成所选讲稿的中文原音与逐词时间信息，按请求和音频摘要核对后复用；须在上述 Python 环境运行。

中文字体固定保存于 `public/fonts/`，来自系统 Noto CJK 字库的简体中文字体面，许可证随文件保存。当前无背景音乐，只有中文解说与模板转场音效。

依赖、缓存和临时运行数据库必须忽略；讲稿、采集脚本、原始素材、配音、字幕、审查关键帧及最终压缩包纳入 Git。大型二进制通过 LFS 保存。用户已授权本次制作提交后立即推送；不创建产品发布标签。

## 全片初剪（2026-09-09）

`renders/全片初剪-01.mp4` 已生成：1920×1080、30 帧、H.264/AAC，
容器时长 543.146667 秒，39129425 字节。`src/FullFilm.tsx` 接入全部 30 镜、
中文解说和字幕，真实截图经 `PageCam` 裁切展示；解释图明确标注为示意或接口观测。
当前仍有审阅标记，主要由截图和解释图构成，不能当作完整操作录像或最终交付。

`scripts/capture_memory_features.py` 通过公共 API 写入/检索四类知识、显式晋升
短期与中期记忆，另外只读查询演示数据库核对知识类型。成功证据为
`raw/network/结构化知识与记忆晋升-有效记录.json`；其他三份同前缀 JSON 是
脚本开发期间的未完成采集，见 `review/全片初剪-01.md`，不能依据文件名判定通过。
Agent 自动回合沉淀和跨端证据已保存，但客厅端随后的会话回答未通过，失败素材单独保留。模型回复声称调用记忆工具，但该条记录
来源是 CONVERSATION，不能据此认定显式工具调用成功。

下一版需接入实际操作视频、强化局部可读性、补齐 Ink Press 特征镜头及片尾，
逐段试听并核对字幕；重建安装包与正式源码保持一致后再做独立终检和正式打包。

第二版审阅稿为 `renders/全片初剪-02.mp4`（543.146667 秒、38334954 字节），
已接入两段真实检索录屏并调整遗忘页取景。整片解码与三处改动镜头抽帧通过；
具体视觉余项见 `review/全片初剪-01.md`，技术元数据绑定源码提交 `2ba42bf`。

片尾新增 `PixiuOutroReview`，可运行 `npm run render:outro -- <新输出.mp4>`。
真实页面切片组装、中文字标和三段式音效已适配，保留两版样片与实际抽帧。
`scripts/measure_audio_sync.py` 测量音轨滞后及音源峰值，依赖 NumPy；
`scripts/probe_prefetch_timing.py` 复用 Provider 测试夹具，依赖 pytest 与固定上游源码，
其默认退出 1 是已复现的首次预取为空，不是通过结果；`--ready-before-prefetch`
作为时序对照退出 0。详情见 `review/片尾与预取时序检查.md`。

第三版全片是 `renders/全片初剪-03.mp4`，9 分 3 秒、约 41 MB，已完成组装片尾、
声轨偏移补偿和整片解码。客厅 Agent 显式调用记忆工具成功复用了笔记本的家庭约定，
新取得的请求证据与操作素材已归档，随后第四版已将其接入核心共享章节；首次自动预取
为空和个别会话初始化过慢仍未修复。

共享章节新增 `PixiuSharedAgentReview`，通过 `npm run render:shared-agent -- <新输出.mp4>` 渲染。
s20 按实际配音在第 239 帧切入客厅完成后核对录屏，原速播放 300 帧后保留结果；
原始用户请求、工具完成卡和来源 ID 均取自真实画面，裁切及摘要见
`review/shared-agent-crops.json`。s21 明确切换到家庭节能约定的断连试验。
原始截图仅 1440×900，局部放大不等于高分辨率重采集；当前仍是审阅素材。

## 工作台拆分验证

`src/workbench.ts` 从同一 `timeline.json` 与 `FullFilm.tsx` 音效表生成画面、逐句字幕、
解说和音效轨道；画面单元关闭内置音轨与字幕，避免重复。标题和字幕的文案、
字号及颜色可编辑，字幕还可调整距底部位置；原有镜头运动参数保持在组件内。
工作台由 video-shotcraft 的 `workbench/scripts/open.mjs` 链接本工程后启动，
在其目录运行 `npm run parity -- --frames <绝对帧列表>` 检查导入前后的像素差异。
此检查另依赖 Python Pillow（已固定到制作依赖），不加入产品运行环境。
当前仍是初剪工作台，不能把可导入误称为最终交付；审阅结果保存在 `review/`。

工作台首轮校验在第 100、1400、10317、10537、10837、16400 帧的差异像素均为零，
原图与记录保存在 `review/workbench-parity-01/`；这是六帧抽样，不是整片或音轨全量一致性证明。
客厅演示克隆机已通过原生 `kscreen-doctor` 验证 3840×2160、scale=2，
重启演示客户端后取得清晰的新截图与工具卡展开录屏；新素材暂未入第四版。
具体模式、恢复命令及采样边界见 `review/4k-capture.json`。

当前最新全片为 `renders/全片初剪-04.mp4`：554.154667 秒、54735605 字节，
H.264/AAC、1920×1080、30 帧。整片解码通过，实际共享章节三阶段及下一镜
切换抽帧已核对。两段新解说起点相对字幕基准均约晚 0.27 帧；未主观试听。
元数据、视频摘要、原始渲染源码和抽帧见 `review/full-draft-04-*`。
这是第四版初剪，高清素材替换、功能及动效精修、独立终检、正式 ZIP 仍待完成。


## 高清与聚焦镜头（第五版初剪）

s03 接入 Gallery `spotlight-hero-card / spotlight-hero-card` 的准确示例，
保留推近、悬浮、归位和停留时序，替换为产品蓝白主题的共享记忆能力示意。
`raw/concept/` 保留可编辑 SVG；`npm run prepare:concept` 用固定 Noto 字体与
fontTools 转为 `public/concept/` 字形路径，避免截图纹理中文字缺失。
`npm run render:spotlight -- <新输出.mp4>` 生成 20 秒审阅片。

s20 已换为原生 3840×2160 素材；B/C 两台演示机在 scale=2 下重启客户端后采集。
录屏仍按实际采集间隔播放，编码 30 帧不代表采集达到了 30 帧。
高清请求和完成后核对片段的裁切记录为 `review/shared-agent-4k-crops.json`。

s07/s08 已使用同一条合成账单的录入、保存、新会话回答与证据详情。
实际 Runtime 日志确认 `pixiu_memory_search` 成功，三项金额合计 434.50 元，
知识及证据 ID 与公共 API 一致。`raw/network/高清账单实际工具检索.json`
只保存选定的工具事件与最终回答，不导出完整系统提示和模型请求。
`npm run render:bill -- <新输出.mp4>` 渲染 35 秒样片；`BillScene.tsx` 的
row-embed 来自准确参考示例，飞行体是截图裁片，片中明确标注逐行强调。
原始录屏都保留，但当前账单章节使用截图剪辑，不能称作连续实时操作录像。
书房约定的证据裁片作为中间素材保留，未接到该账单章节。

新增 public 顶层目录后，先重新运行工作台 `open.mjs <工程> --no-open`
更新静态素材链接，再执行 parity；否则新概念 SVG 会返回 404。
第五版 `renders/全片初剪-05.mp4` 已完成：554.154667秒、55101292字节，
整片解码及账单/跨端关键帧核对通过，音轨基准误差约0.27–0.29帧。
新版工作台五帧零差异，一帧字幕/审阅标记有5940像素差异，暂未通过；
独立终检和正式 ZIP 尚未完成，产品稳定性余项不变。


## 独立审查后的第六版整改

第五版独立审查不放行，完整报告和原始抽帧保存在 `review/independent-05/`，
逐项处理见 `review/独立审查整改台账.md`。字幕提高为64px，按实际词时间重分
最多24字的字幕，底部28px；四个检查静帧实际字高59px。字幕与审阅标记固定
合成层，工作台新增七帧比对均零差异，保留旧失败记录。

`scripts/prepare_evidence.py`（`npm run prepare:evidence`）将原始同步检查点与
历史评测报告提取到两个src JSON，逐行显示原始正文、版本和检查点记录时刻；
`SyncTraceScene.tsx` 明确是检查点剪辑，不是实时UI录像。`EvaluationScene.tsx`
分列历史软件值、阈值/样本量和最终V11双SDK待测状态。原始来源与SHA保留。
这些工具只用Python标准库，不新增产品依赖。

音频测量现明确区分原始声道样本峰值与单声道混合峰值，解决约3dB的口径差异；
波形时序仍用单声道相关性，未将其视作试听。第六版整片已生成（554.154667秒、56618279字节），完整解码通过；
30镜各取一个实际字幕帧，字高57–59px。其他功能镜头、
四种模板动作、主观视听审查和正式ZIP仍待完成。

第六版有限独立复查后，s21/s22又修正了阶段脚注、恢复结果措辞和检查点时间口径，
并加大表格到字幕的间距。两段解说原音新增到raw/audio，阶段时序读取字幕。
第七版已完成：554.154667秒、56615683字节，完整解码通过；快照位于
review/source-snapshots/full-draft-07。实际抽帧确认断连结句、并发阶段脚注与字幕间距
整改已进入成片，有限独立复核确认指定缺陷关闭（见review/independent-07/REPORT.md），
三行表脚注到字幕留白62px；这不是全片放行。s21/s22/s28波形相关性均大于0.9999，
相对音源声明起点偏移1.27–1.29帧；仍未进行主观听审。
已下载六个Gallery动态参考，来源/摘要保存在reference/gallery-media/sources.json；
仅用于制作比对，不作为产品视频内容。未完成项继续见独立审查整改台账。

## 偏好历史堆叠初版

s15已接入 `PreferenceHistoryScene.tsx`：当前偏好先从简洁切到详细，再以
list-stack-press的飞入、压弹和连续计数展示三个实际历史版本。飞行裁片来自
61/62两张原始产品截图，中文释义区分compact与verbose，明确标注非实时生成。
不新增依赖；使用既有render:full命令加 `--frames=7543-8202` 可渲染22秒章节。
章节解码及六帧提取已完成，原图放大偏软，高清重采和独立动效审查尚待完成。
第八版完整渲染已生成，源快照和核对记录保存在review/full-draft-08-*。

## 偏好高清实测重采

`capture_preference_history.py`使用既有SSH公共接口封装与Python标准库，在独立
演示范围中写入合成配置。`--stage 1`首次写入后显式重提取，实际形成简洁风格
版本1/2；`--stage 3`写入详细风格，管线已提取为版本3，因此不再重复提取。
原始响应逐步落盘；已有输出拒绝覆盖，前置版本不符则停止。
两张原生3840×2160截图已替换s15纹理，裁切坐标与SHA在review/preference-stack-02.json。
另保留10.336秒真实刷新与查看历史录屏及17帧原图；片中仍使用明确标记的截图动效。
高清章节及第九版整片完整解码通过；第九版554.154667秒、56091326字节。
五帧工作台比对零差异，偏好声轨相对声明起点偏移1.279帧，未视作听审；有限独立镜头复核未发现阻断项，报告在review/independent-preference-02/。
脚注字号余量、相机跟随和扫光优化建议保留，不代表整片放行。
从仓库根运行工作台检查时下载的浏览器位于/.remotion/，属于可再生工具缓存，已忽略。

搜索动效准备：同一次原生UI操作的空白、输入、结果和证据详情四张4K图已保存，
并保留14.450秒搜索操作录像；详情在录屏结束后另采，不称连续全过程录像。
再次公共API查询返回同一详细风格证据，来源与边界见review/search-source-01.json。
这批标题查询素材用于同源准备；s13最终采用下述另采的非标题查询。


## 非标题搜索与来源展开

s13接入 `SearchEvidenceScene.tsx`，实际输入“详细说明”命中标题“回答风格演示”，
再显示同一条原始证据中的配置正文。原生4K截图、公共API复核、摘要与适配边界
见 `review/search-motion-01.json`；此次非标题查询没有连续录屏，不复用此前标题查询
录像冒充本次过程。片中明确为截图动效，三通道只作机制示意。

沿用type-and-filter默认demo的3帧逐字、结果落定、双涟漪和16帧推近；真实仅一条
命中，因此不虚构非匹配网格。使用既有依赖与渲染入口，章节命令为
`npm run render:full -- renders/非标题搜索-01.mp4 --frames=6403-7002`。
TypeScript检查通过，实际章节抽帧已核对；工作台6553、6756、6830三帧零差异，
记录在 `review/workbench-parity-05/`。独立章节审阅未发现阻断项，26张抽帧及报告在 `review/independent-search-01/`；
点击强调与长静止段优化建议保留。第十版全片完整解码通过：554.154667秒、55125857字节；
实际全片6553/6830帧已核对，搜索声轨相对声明起点偏移1.279帧，相关性0.999958。
波形测量不替代主观听审；快照与技术记录保存在 `review/full-draft-10-*`。


## 洞察来源链路素材

已在隔离演示库以公共API写入“家庭月末归档步骤”，原生4K页面刷新得到候选，
选择后按标题检索，再点击来源读取同一条正文。API同源核对、四张静帧与两段
原始录屏全部保留，见 `review/insight-source-01.json`。第一段28.511秒没有录到
最后来源点击；第二段18.089秒补齐连续链路，31张原帧和两段解码检查已完成。
候选非空而采集简报为零：手动写入没有增加采集日志，不将零改成一或泛称全部
新增记忆数量。当前仅素材归档，s26文档展开尚未实现；无新增依赖或渲染配置变更。


## 洞察文档揭示与连续来源录像

s26接入 `InsightDocumentScene.tsx`，以原生截图三行候选适配文档镜头的成对节拍、
8帧遮罩、单光标和拉远；实际产品无周报或六周历史，不新增虚构侧栏。
简报在对应字幕出现时显示；后半使用第二段真实录像3秒起的片段，1.5倍速，
裁切放大跟随；最终原始正文保留4秒阅读时间。全片时间线因此增至558.1秒。

沿用既有Remotion依赖和render:full入口，章节范围为14124–14723。新增public/recordings
素材目录已重新链接工作台；14344、14424、14644三帧零差异。首次复制目录缺失导致
01章节未接入场景，02为小取景初版，均保留；当前03已完成实际抽帧核对。
有限独立复核和第十一版全片检查仍待完成，过程与适配记录见review/insight-motion-03.json。

03独立复核发现日期字身27–28px不达标，并指出来源正文晚于字幕阶段；报告与
第十一版全片保留。04将日期升至1.5倍，150帧出现；录像改为210帧起2倍速、
340–370帧移向正文、400帧转静帧。实际330帧来源已出现、390帧完整正文可读。
新工作台三帧零差异保存在workbench-parity-07；第十二版和04有限复核待完成。

04有限复查已返回：日期33px、来源330帧已出现且370帧完整，无新增阻断。
低频采样、临界字号和静帧接棒建议仍保留，详见independent-insight-04。

第十二版全片完成：558.144秒、55729660字节，完整解码通过；实际14304、14514帧
核对日期放大和正文时序已入片。s26与s28波形相关性大于0.99995，起点偏移1.29帧。
片尾含三层SFX，纯配音对混音相关性0.729；另将第十版完整混音按新增4秒移位
对照9秒，相关性0.999878，不将该检查作为主观听审。源快照和技术记录见full-draft-12-*。

行为采集补录的旧检查结论（已由下述运行中数据库复核纠正）：V11 演示机安装官方仓库 `x11-utils` 7.7+6-ok1k0.1
（附带 `libxxf86dga1`），临时启用隔离服务的行为采集与 `DISPLAY=:0`，
三次公开 X11 示例窗口均结束后，数据库仍只有四条手动配置来源，没有
`USER_BEHAVIOR` 证据。本次不作为行为自动沉淀成功或 Wayland 原生采集验收。
窗口原图、启用状态、未落库及恢复检查保存在 `raw/` 和 `raw/network/行为焦点自动采集-*`。
原隔离服务环境文件已按私有备份恢复，采集总开关与四项来源全部关闭，
重启后 `/health` 返回 ready。新增 X11 工具留在制作虚拟机；项目依赖和渲染配置
没有变化，后续成功补录仍需核实产品安装依赖及真实持久化路径。

s24 后半新增跨端遗忘检查点：284 帧显示在线端 404、断连客厅 200；378 帧
显示客厅重连后 404；428 帧显示三端墓碑状态与操作摘要一致。数据直接由
`scripts/prepare_evidence.py` 从已保存原始 JSON 校验并生成 `src/forget-trace.json`，
不将接口检查点当作实时录像、向量内部检查或物理擦除证明。原讲解、字幕与总时长
不变，沿用 `prepare:evidence`、`render:full` 和既有依赖。章节样片为
`renders/跨端遗忘检查点-01.mp4`，三个工作台关键帧零差异，独立复核待完成。

01 独立复核指出后附空查询响应缺少请求参数旁证，不能归为同一目标的完整查询
证明。02 已将最终画面改为原始事件明确的三端墓碑状态与操作摘要一致，未补写
猜测的历史请求；01 样片及报告保留。修正帧工作台零差异见 workbench-parity-09。

02 有限独立复核确认 F-M1 通过收窄结论解决，无新增遮挡。第十三版全片为
558.144 秒、55619331 字节，完整解码通过；实际入片13427/13587帧已查看。
s24 波形相关性0.999965、起点偏移1.27帧，未进行主观听审。源码快照、技术
记录和音轨测量均保存在 full-draft-13-*；不关闭其他功能与正式交付余项。

设备控制补录：`raw/video/设备暂停与恢复-4K` 与 `-4K-02` 保存两段原生4K录像，
第二段25秒内完成勾选暂停、保存、取消暂停和再次保存；`raw/network/设备暂停与恢复-*`
保留后端状态 false→true→false。网络启用值未变，最终已恢复。另保存广播记录、
配对空白入口、解除本地信任确认与取消截图；未生成令牌，也未执行解除。
本次复用 capture_desktop.py、record_desktop.py、demo_api.py 和既有依赖，无新配置。
状态回读不等于有待发数据时的传输抑制验证。素材尚未接入全片，完整配对和解除
结果仍待补证；检查清单为 `review/device-control-capture-01.json`。

本轮进一步完成笔记本解除书房本地信任、通过原生配对页恢复的操作。解除后
笔记本节点列表移除书房，书房仍保留笔记本；恢复后双方节点集合与操作前一致，
同步未暂停。18秒解除录屏、成功后无令牌的配对截图及前后节点响应保存在
`raw/video/解除本地信任-4K`、`raw/network/解除与重新配对-01.json` 和对应原图。
本次只恢复一侧信任，没有把它称为从零完成双方配对；令牌与PIN未归档。
新增 `DeviceTrustScene.tsx` 接入 s19，章节 `renders/设备信任与状态-01.mp4`
使用真实裁片解释配对结果、节点、广播与状态，独立复核待完成。沿用既有
render:full入口及Remotion依赖，总时间线不变；当前完整视频仍为第十三版。

设备01独立复核确认单侧恢复关系与素材真实，但后半原生文字30–31px须放大。
02放大至1.25倍、精简首段规则说明；页脚改为局部绝对定位及独立合成层，
工作台9717/9847两帧已由10277差异像素修正为零。两版章节与失败检查均保留，
02有限复核待完成，不把新章节源码当作已重渲染的完整影片。

02有限复核已确认原生文字32–33px，D-M1关闭，首段阅读负荷改善，无新增裁切或
遮挡阻断；七张实际抽帧及测量保存在independent-device-02。该结论只适用于本镜，
未进行主观听审，当前全片仍未纳入本镜新版本。


s23 新增 `SharingBoundaryScene.tsx`：私有范围与敏感共享拒绝使用完整公共接口
请求/响应，暂停保存使用原生录像，解除确认与单侧移除结果使用真实裁片。
`prepare:evidence` 新增目标 ID、范围、来源证据和错误码断言，生成
`src/privacy-boundary.json`；沿用现有依赖和 `render:full`，无新增运行依赖。
章节由18秒延长至21秒，后续章节顺延90帧。02独立复核指出讲稿“同步日志”
超出接口观测，04将讲稿与字幕收窄为“这条私有记忆未进入同步状态”，重新生成
对应配音及词级时间；暂停镜头延后横移以保留勾选瞬间。旧版本均保留。
一次额外数据库只读核对未确认当前服务数据库，且同步实体参数不正确，结果明确
标为无效，不作为任何视频结论。当前状态与完整终检余项以最新审查报告为准。

04有限独立复核已确认字幕/讲稿文本的S-M1闭合，暂停由337帧未选转338帧已选，
已选完整可见至355帧约0.60秒，原横移遮挡问题闭合。确认3秒、结果4.03秒，
完整辅助文案阅读偏紧建议仍保留；无新字号或遮挡阻断。四个工作台关键帧零差异
保存在workbench-parity-13。复核没有听音，不等于完整影片最终放行。

第十四版全片已整合设备信任与共享控制：561.152秒、56153345字节，整片解码通过。
实际全片9847/12737/12932/13677帧已查看，确认新设备页、范围字幕、暂停勾选
以及顺延后的遗忘画面入片。s19/s23/s24/s28波形相关性均大于0.99995，
起点偏移0.27–0.29帧；这是以时间线from+audio_from为期望值，旧报告有采用
from+14的记录，不能直接据一帧差异推断管线发生变化。未进行主观听审。
固定源码、8张实际抽帧和音频/解码报告保存在full-draft-14-*；未生成正式ZIP。
后续缺口与顺序见 `review/交付余项-20260909.md`，当前仍非稳定终稿。


s09 新增真实来源发牌章节，源码为 `DeckDealMotion.tsx`、`DeckCamera.tsx` 与
`IngestSourcesScene.tsx`。按准确 Gallery demo 保留26卡加速发牌与相机轨迹，
把材质改为浅蓝灰；五张原图各取两块内容，额外16卡复用，片中明确不是26条新记录。
`deck-assets.json` 保存原图摘要和取景位置，未改写产品截图内容。后半放大真实会话
来源，并展示新公共API实测的去重、空值移除、标题去空格、质量0.775/敏感0及同源检索。
公开合成手动输入不作为自动行为采集成功证据。原始请求/响应在
`raw/network/接入清洗与质量-01.json`，`prepare:evidence` 新增对应校验和生成。

01因卡片留白过多与来源特写残字保留为失败样片；02收紧裁片并去掉残字；03新增
模板来源的whoosh-fast音效，与已用whoosh-big组成36/68/84帧声音节点，音量递减。
该新增媒体沿用`reference/audio-attribution.md`许可记录，不新增软件依赖。
渲染继续使用`render:full --frames=4167-4706`，总时间线与原配音保持不变。
章节03独立复核及完整影片整合仍在进行，详见`review/deck-adaptation-03.json`。

03独立复核指出小卡内诊断正文对比过强且裁断，须使其明确成为动效纹理。
04将发牌卡片纹理降至0.42不透明度并轻虚化1.2px，来源及结果放大段保持清晰。
03音轨与原配音波形已测量，属于含音效混音对照，不作听审结论。

04仍存在3D共面纹理显隐，05在101帧全部落位后切换等价2D绘制，101–129帧
自检最大相邻MAE0.00666、通道差超过16的像素占比为0。独立复核确认稳定问题
闭合，但100→101底行两张卡补现；06将底板放到z=-12以免落位越界缓动穿入底板。
保持原发牌时序，06待有限复核。第15版与第16版保留为相应中间整合稿，
第17版将用于核对06最终入片；这些版本号不代表逐版完成全部功能或最终验收。


第17版已完成技术核对：561.152秒、57990031字节，整片解码通过；s09配音
起点偏移0.29帧，混音与原配音相关性0.997244，未进行主观听审。实际全片
4267/4337/4467/4587帧已查看；工作台4267/4268/4296三帧像素零差异。
06有限独立复核确认底行补现关闭，101–129帧保持稳定，无新增镜头范围阻断。
报告、实际全片抽帧及固定源码见review/full-draft-17*；15/16及失败章节全部保留。
第17版仍含“行为自动采集待补证”的历史制作文案，下次行为章节整合时一并更新。

行为采集结论更正：旧检查连接了另一份同名数据库，不能据此断言自动采集未落库。
运行中服务数据库查到公开窗口“公开行为演示”的USER_BEHAVIOR证据，焦点118秒、
质量0.715，GET /evidence/{id}返回200且内容一致。复核全程只读，采集保持关闭；
原始查询结果和接口响应在raw/network/公开行为采集落库复核-01.json。
此结果证明该公开X11窗口采集落库，不扩展为Wayland原生采集验收；原生结果画面仍待补录。
依赖与渲染入口已核对，无新增软件依赖；npm run check与prepare:evidence通过。

提交检查发现DeckCamera空白行尾随空格，已清理当前源码；固定历史源码与审查副本保持原字节以保留摘要对应关系。该变更不影响渲染。

行为结果已补录两张原生4K截图：本机个人记忆检索“公开行为演示”，点击来源后
显示USER_BEHAVIOR、user:local和focus_seconds 118。原生质量显示0.71，
公共接口精确值0.715；二者分别按原样保留。素材已查看，尚未接入完整影片。


s12行为结果已制作为BehaviorCaptureScene：原生来源、118秒焦点记录、关闭采集开关
与当前能力边界按现有配音/字幕分段展示。01存在下行残字及30–31px原生文字；02
仅收紧焦点裁片；03放大来源至1.3倍、开关至2.6倍，删除第二开关并精简说明。
03有限独立复核确认标题33px、来源36px、开关34px，残字闭合，2.23秒控制段负荷
改善，无新增镜头范围阻断。原生质量0.71按截图保留，不改写成接口精确值0.715。
s09旧“行为待补证”说明同步更新为来源均有实测。时长和原配音不变，无新增依赖；
prepare:timeline与check通过，沿用render:full。完整视频已整合为第18版，技术核对
及全片实际抽帧保存在review/full-draft-18*；这些检查不等于完整影片最终放行。

第18版技术结果：561.152秒、56850826字节、全片解码通过；s12配音起点偏移
0.29帧，波形相关性0.999953。四个工作台对照帧零差异；实际全片4337/5877/5992/6317
已查看，确认来源文案更新、118秒证据、关闭开关与能力说明入片。未进行主观听审。

新增生命周期摘要晋升实测：TURN_END短期与PRE_COMPRESS中期各晋升一条，
Agent context均返回本次晋升的同一知识ID、来源证据及完整中文摘要，未截断；
8次接口请求均200，原始记录在raw/network/生命周期摘要晋升与复用-01.json。
标题仍为manual_config，正文保留data.summary。这是公共接口合成事件测试，
不代表宿主自动生命周期钩子实拍；晋升镜头尚待制作。未修改产品代码或依赖。


s17新增MemoryFlowScene，展示公共接口实测的两类上下文创建、各晋升1条与带来源摘要复用。
prepare:evidence从原始记录生成flow-results.json，核对上下文层级、晋升输入、知识ID、
证据ID、原始摘要与返回摘要一致、未截断及max_chars预算。01有限复核未发现阻断，
02收窄摘要行宽至1250并删除一行辅助文字，复核确认完整两行、阅读负荷改善。
默认标题manual_config仍保存在原始记录，影片摘录摘要不改写标题或原生UI。
沿用现有配音、字幕与总时长；无新增依赖，prepare:evidence、prepare:timeline和check通过。
所有样片与固定源码保留；完整影片整合候选为第19版，尚未最终放行。

四类知识新增公开长正文：书房事实、归还流程、分类案例和阅读计划模板。12次写入、
Agent上下文及证据回读均200，来源ID匹配，正文与输入一致；运行中服务数据库只读
核对的类型为FACT/WORKFLOW/CASE/TEMPLATE。8张原生检索/正文4K截图保存在raw/screenshots，
四张正文均已查看，模板五项说明没有因空值清洗丢失。此批素材在第20版接入四类摘录与流程完整正文，详见下文。

第19版完成技术核对：561.152秒、57051690字节，整片解码通过；s17配音起点偏移
0.279375帧，波形相关性0.999961。三个工作台帧零差异；实际全片8833/9063/9253
已核对上下文、晋升与两行摘要入片。无主观听审，完整影片终检与其余功能缺口保留。


s14新增KnowledgeExamplesScene：前段同时展示四类真实摘录，后段放大原生四步流程正文。
prepare:evidence核对12次请求状态、知识/证据ID、类型与摘录原文，生成knowledge-examples.json。
另存四张窄栏原生正文截图及新配音；配音24.720秒，章节28秒，全片时间轴571.100秒。
01有限复核发现底部content_hash残字；02收紧裁片高度，实际f450/f815复核确认残字消失、
四步与末行完整。底部留白略紧为非阻断建议。所有原始素材、失败样片与源码快照保留。
本镜支持四类保存/检索示例与一个流程长正文阅读，不宣称四类均已展示完整原生长正文，
也不证明助手自动执行。无新增依赖；prepare:evidence、prepare:timeline与TypeScript检查通过。

第20版完成技术核对：571.157333秒、57769283字节，完整解码通过。实际全片
7103/7453/7818/9563帧已查看，确认四类摘录、完整流程及顺延后的记忆摘要入片。
s14与s17波形起点均偏移0.279375帧，相关性分别0.999965与0.999961；
三个工作台帧零差异。无主观听审，完整功能覆盖与最终交付检查仍待完成。


s16新增语义冲突公开实测：同实体书架层数3→4，六次写入/上下文/来源请求均200，
更正后检索返回新知识与新来源，GET /conflicts保留旧值、新值和NEW_WINS、中等严重度。
该接口没有scope过滤，原始记录明确只保存匹配本次旧知识ID的条目，另记省略条数。
capture_semantic_conflict.py保留完整公开请求及相关响应，prepare:evidence从其生成
semantic-conflict.json并核对ID、正文、数值与裁决。无新增依赖，证据生成、时间轴与类型检查通过。
原生审计页的“未提供关联记忆标题”保持原样；未伪造标题、旧新值UI或人工裁决按钮。
新增ConflictAuditScene分段呈现账单保存、另一公开记忆的旧版本拒绝、书架层数审计。
原生旧图放大2.6倍，审计4K裁片1.25倍；章节24秒、沿用原配音，总时间轴575.100秒。
01样片实际100/230/350/500帧已查看，独立复查另行归档；尚未导出包含本镜的下一版全片。

冲突01有限独立复核未发现必修项：关键文字33–37px，298/450帧字幕切换对应，
只读审计有原始证据支持。拒绝阶段5.07秒的两段全文阅读偏紧，保留为非阻断建议。
章节完整解码通过；配音起点偏移0.29帧、波形相关性0.999959；三个工作台帧零差异。
不代替主观听审或下一版全片验证。


完整助手任务新取证见review/完整助手任务取证-20260909.md。网关第三轮已核对终端计算、
四行文件写入/回读、真实web_search及记忆保存；原生秋季任务经两轮完成文件与保存，
独立新原生会话正确复用同一显式知识来源，三段实际录屏及全部原始PNG保留。
原生搜索多次失败且错误声称官网，不能包装为一次全部成功；成功联网搜索属于另一
网关样本。01漏传模型、02字面量换行与伪搜索输出等失败记录保留。
演示运行时新增DDGS可选搜索依赖，固定版本见scripts/requirements-agent-demo.txt；
capture_agent_task.py显式选择已核对模型，capture_agent_evidence.py只提取指定公开
会话的工具事件及指定文件，省略系统消息与无关会话。任务章节已制作为25秒样片，尚未更新全片输出。


助手任务03将网关任务与另一次原生会话明确分段，保留原生搜索失败说明；新版中文旁白
原音为s06/98036ec8f62a，实际17.952秒，总时间轴仍575.100秒。`npm run prepare:agent-scene`
核对原始工具结果、文件摘要与保存/引用对应关系，生成只读展示数据并复制原图；无需新增依赖。
`npm run render:full -- renders/助手任务-03.mp4 --frames=2367-3116`为本轮25秒章节命令。
完整解码与类型检查通过，波形起点偏移0.29帧、相关性0.999959；这不代表实际听审。
01与误重复导出的02均保留，03延长搜索结果展示至6.53秒。源码快照与技术记录在review中，
独立章节审查发现页脚实字31px，04放大后实际33px，有限复核确认闭合；全片20仍是最新完整输出，正式ZIP尚未生成。

当前有效任务章节为04；03与04除页脚34→36外无变更，04解码与波形起点复核通过。


六个旧原生界面章节已集中放大：s04服务快照、s10个人范围录入与正文核对、s11目录
授权日志与来源、s24本机便笺预览/取消/确认、s25遗忘边界、s27安装版本/退出码及
模型与更新检测。NativeSetupScenes与NativeDeliveryScenes只裁切原始截图，不重绘产品
控件或生成点击。主要原文字形32–39px，各章节有限复核通过；取消结果随后补齐且
单帧复核通过。01部署样片停留较短，02同时展示两个检测结果，均保留。
继续使用已有render:full配合--frames渲染，无新增运行依赖或发布配置变化；分镜配置
已同步实际内容。第21版575.146667秒、47925870字节，解码和任务配音起点通过；
第22版已整合取消结果，575.146667秒、47938841字节，冻结源码摘要一致，全片独立审查进行中。七个工作台对照帧像素零差异，
未包含全音轨或全帧证明；真实听觉审查、匿名与正式ZIP仍待完成。


第22版干净上下文有限终检已完成30镜335帧，报告在review/independent-full-22/report.md。
必要项集中为四类：跨端/账单答案与架构/片尾关键文字放大、两类偏好提取结果补展示、
搜索/历史/文档动效补拟音、正式版去审阅标记。主链与九卡方向无需重做；非连续观看
且未听审，不能称全片放行。全30段原音起点回测偏移≤0.29帧；s30叠音相关性0.729，
其他最低0.997，收尾混音仍需检查与听审。所有抽帧和参考样片继续保留。


终检修订 M1 第一批：跨端原生工作卡/回答以1.25倍裁切，账单金额以1.4倍、
完整知识与证据ID分行以1.3倍展示；架构正文/注记和片尾收益句放大。
实际PNG行带测量33–38px，见review/m1-readability-measurements-01.json。
账单01是命令工作目录错误后仍导出的旧画面，02暴露来源截断，03已完整分行；
全部保留。s08等同类正文检查及整片复核尚待完成，不能将M1全项标记关闭。
仍使用现有render:full/still:full配置，无新增渲染或发布依赖。

`scripts/capture_preference_categories.py --guest <演示机> --output <新JSON>`
补录已有演示范围内缺少的类别，保留原输出风格版本三；已存在的类别跳过写入。
原始证据raw/偏好三类提取-01.json保留行为事件嵌套错误导致零提取的失败；
02按USER_BEHAVIOR连接器的raw.events契约修正，实际操作习惯enabled=true、
signal_count=1、版本一；安全策略no_exfiltrate=true、版本一；输出风格verbose、
版本三未变，三项历史接口均返回成功。实际结果并非三次热键监听证明，
也不证明安全策略执行；尚未编入视频。脚本只用标准库和已有demo_api，不新增依赖。

第23版已导出575.146667秒、48136908字节，SHA-256为
`df6406688d88939449534d16d674b75f109c553a53398354528415d1043850f1`，
完整解码、渲染时冻结源码核对及任务段旁白起点检查通过（偏移0.29帧）。
跨端实际MP4抽帧已核对；未听审。随后s03注记增加独立底板并核对f917静帧，
该修正尚未进入23版，待与偏好展示/音效等剩余修订一起整合。


第24版终检修订见review/final-corrections-24.md。新增prepare:preferences生成
经过原始接口结果验证的展示JSON；原输出风格历史与字幕/旁白时间轴保持一致。
账单来源正文放大到实测37px；s13/s15/s26加入短动作拟音，工作台同步播放窗口。
音效来源和派生参数在review/action-sfx-assets-01.json；measure_action_sfx.py
依赖已有numpy/ffmpeg，逐动作回测新增音效。无新增依赖或产品发布配置变化。
独立限定范围复核通过，整片导出、渲后同步与实际听感尚待完成。

第24版已完成整片解码、30段旁白与10个新增音效时序测量，分别≤0.29帧、
≤0.44帧；原声道峰值−4.78dBFS。实际抽帧full24-check-01..04分别为
f3897/f7940/f8240/f14642。三个工作台视觉帧零差异；测量不代替听审。

新增render:final入口使用PixiuFinalFull，只移除初剪审阅标记；既有render:full
仍保留初剪标记供历史审阅。工作台默认导入无标记候选，字幕、音效及真实验证
边界均保留；package_video.py按冻结目录添加一个ZIP，校验解码报告摘要、时长、
ZIP成员/CRC及其余作品摘要。新版替换须指定旧ZIP摘要及独立保留路径，先验证暂存ZIP再原子替换。打包状态与最终听审结论分开记录。

无初剪标记候选01已生成并打包：575.146667秒，MP4为47,565,807字节，
ZIP为47,565,937字节。ZIP已放入冻结双层同名目录，内仅含演示视频.mp4；
CRC、成员摘要、目录结构与其余三项作品摘要保持检查通过。候选完整解码通过，
音轨解码SHA-256与第24版完全相同，可沿用其30段旁白及10个动作时序证据。
历史候选01视频SHA-256：6bec172afb25fc14398fcaeab05815e4fc9c9e667fd61dbe2a8337f072b2de3e。
ZIP SHA-256：e7d120274e14e7d2485aab9717cf062eb54f4dc66d38c7ac69fe1338e43fc966。
候选实际抽帧final01-check-01..04对应f917/f7940/f11222/f17103，初剪标记已移除，
真实演示边界保留。工作台已打开且候选三个视觉对照帧零差异。
以上为候选01历史记录。后续宣传修订按用户最新要求继续执行，实际听感未宣称通过。

## 本轮宣传片修订（2026-09-09）

普通镜尾按实际口白终点保留8帧、品牌镜头15帧；末句字幕延续至切点。删除单独的能力缺口/免责声明段落，14段口白重录；实际界面截图通过取景保留任务与结果，原始图像不改写。新增架构连接、金额逐项显示、使用记录计数、记忆移动、状态逐行展开、升级流程和评测报告动作。修订依据与逐镜检查见 `review/promo-revision-02/修改与验证.md`。

图片账单闭环另由 [GitHub issue #2](https://github.com/PlutoKeating/Project.PIXIU/issues/2) 请求开发修复；视频源码不修改产品实现。

验证工具使用已有Python/numpy/FFmpeg依赖；全片OCR筛查另外使用系统Tesseract及chi_sim、eng语言数据，仅用于制作检查。运行：

```sh
npm run check
python3 scripts/verify_promo.py --video renders/演示视频-候选03.mp4 --snapshot review/promo-candidate-03-source.json --output review/promo-revision-03
```

该命令保留解码报告、29段音轨相关性、每2秒及每镜中尾抽帧、OCR命中清单。OCR只用于辅助定位，须结合实际图像独立检查。原始候选01与候选02保留；正式ZIP仅含演示视频.mp4。真实听感与技术测量分开记录。

宣传修订候选03已完成导出与打包：29镜、449.813333秒，MP4为49,063,433字节，ZIP为49,063,563字节。普通镜尾8帧、品牌15帧；14段重录，去除能力缺口及免责声明宣传段落和实际截图中的诊断旁注，增加架构、计数、状态、来源及评测分步动作。

完整解码、源快照摘要、29段旁白起点、283张抽帧OCR、5帧工作台逐像素对照、ZIP单成员/CRC/摘要、冻结目录和其余作品未变核对均已完成。OCR无关键词命中，-40dBFS阈值未检测到持续1.5秒以上静音；实际主观听感未人工连续试听。独立实帧复核及技术报告见 `review/promo-revision-03/`。旧版MP4、ZIP及中间媒体保存在制作工程，Git保留每轮变化。

MP4 SHA-256：`b15d77bbbe45c0a3598b4dc7c88189ff397d9945b33551797e660e5e9f2012e7`。
ZIP SHA-256：`781d5c62e4eded9e94fde33aad29af305f702342f55c5c0295d90408f627bd78`。

## 云枫口白与时间轴（2026-09-09）

团队选定 `zh-CN-YunfengNeural`；29段已审核讲稿保持原样。
`storyboard/narration.json` 固定 Azure、默认风格、语速 -5%、24kHz/96kbps 单声道 MP3，
与入选试听参数一致，音高和音量保持服务默认。

`narrate_azure.py --region <区域>` 使用 Azure Speech SDK 1.51.2，一次生成一段，
通过隐藏终端输入读取密钥，仅在内存中使用。请求摘要、音频摘要及服务词时间
保存在 raw/audio；已有且摘要吻合的成功文件跳过，仅对临时失败最多重试两次。
鉴权错误直接停止。区域与密钥不进入制作配置。

`prepare:timeline` 严格匹配音色与全部合成参数，按新词时间重建字幕，
按解码后的实际发声终点设置镜尾，普通镜尾8帧、品牌15帧。
原生页面阶段、账单金额、记忆流转、来源展开及点击拟音改为跟随字幕语句起点；
既有动作的缓动与短动作时长保留。客厅回放保留原速，按可用窗口截取末段并展示来源定格。

验证：`npm run check`、全片渲染、`scripts/verify_promo.py`、独立实帧检查；
技术测量与主观听审分别记录。新 SDK 只属于视频制作依赖，不进入产品安装包。

云枫02为当前交付版本：`renders/演示视频-云枫02.mp4`，容器时长389.589333秒，
45,578,721字节。29段/1950字原文完整保留，全部新音频摘要核对通过；
29段起点误差最大0.29帧，成片峰值-6.58dBFS，完整解码及253张抽帧OCR检查通过。
-40dBFS阈值未检测到持续1.5秒以上静音，末句字幕持续至镜头切点。
独立复核覆盖29镜中/尾58张及16张直接抽取关键帧；未发现新增必修项，
未进行人工连续听审。s23保留72帧完整暂停操作及40帧解除确认窗口；
s20裁掉原录屏开头76帧等待，保留展开及来源结果。

证据：`review/yunfeng-narration-verification.json`、`review/yunfeng-final-02/`、
`review/yunfeng-independent-02.md`、`review/yunfeng-package-02.json`。
已更新原交付ZIP（45,578,851字节），旧ZIP保存在renders目录，其他三项作品摘要未变。

工作台已重新导入云枫时间轴，f7883/f9249/f11674三个视觉帧逐像素差异均为零；证据见review/yunfeng-workbench-parity。

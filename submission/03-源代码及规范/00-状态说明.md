# 源码归档状态说明

`Project.PIXIU-source-review.tar.gz` 是当前可审阅源码包，内含四个固定 submodule 和
供应链证据，但绑定的是上一候选提交 `0f8bcec1f81f3eb3a39c38d049e8b49e85127ddf`，不是最终
提交包。基于当前提交重新生成时，fail-closed 供应链审计正确拒绝了已失配的宿主构建
证据，因此没有把旧证据重新包装成正式 `Project.PIXIU-source.tar.gz`。

最终开发和 V11 验收完成后，必须重建同一 commit 的 Agent 宿主、Runtime、SBOM、NOTICE
及离线安装证据，再由 `build/release/submission_tools/build_source_archive.py` 生成正式
文件并替换本说明中的审阅状态。禁止仅重命名当前审阅包冒充最终源码交付物。

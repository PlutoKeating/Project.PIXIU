# PIXIU Foundation -- Python dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Data validation
pydantic>=2.6.0
PyYAML>=6.0,<7

# Async database
aiosqlite>=0.20.0

# D-Bus (desktop integration, pure-python asyncio client)
dbus-next>=0.2.3

# Directory monitoring (batch 2: DirectoryWatcher collector)
watchdog>=3.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0

# Vector search (Phase 2)
# hnswlib>=0.8.0

# Kylin SDK (Kylin OS only, not pip-installable)

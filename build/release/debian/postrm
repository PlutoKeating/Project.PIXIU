#!/bin/sh
# PIXIU 卸载后：用户数据属于各桌面用户，卸载与 purge 均不跨用户扫描或删除。
set -e

if [ "$1" = "purge" ]; then
    rm -rf /usr/lib/pixiu/venv
fi
if command -v systemctl >/dev/null 2>&1; then
    systemctl daemon-reload >/dev/null 2>&1 || true
fi
exit 0

#!/bin/sh
set -eu

runtime=/usr/lib/pixiu/venv/bin/kylin-agent-runtime
[ -x "${runtime}" ] || {
    echo "kylin-agent-runtime: packaged Runtime is unavailable" >&2
    exit 127
}
export PYTHONPATH=/usr/lib/pixiu${PYTHONPATH:+:$PYTHONPATH}
exec "${runtime}" "$@"

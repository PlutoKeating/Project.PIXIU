--- a/src/utils/thememanager.cpp
+++ b/src/utils/thememanager.cpp
@@ -343,5 +343,38 @@
             font-size: 14px;
         }
+        QTabWidget::pane { border: 0; background-color: %1; }
+        QTabBar::tab {
+            color: %4; background-color: %1;
+            border: 1px solid transparent; border-radius: 6px;
+            padding: 9px 16px;
+        }
+        QTabBar::tab:selected { color: %4; background-color: %9; border-color: %7; }
+        QTabBar::tab:hover:!selected { background-color: %3; border-color: %6; }
+        QTabBar::tab:disabled { color: %11; }
+        QCheckBox::indicator:unchecked {
+            background-color: %2; border: 1px solid %5; border-radius: 3px;
+        }
+        QCheckBox::indicator:unchecked:focus { border: 2px solid %7; }
+        QCheckBox::indicator:unchecked:disabled { background-color: %3; border-color: %11; }
+        QTabWidget#pixiuWorkspaces QListWidget, QFileDialog QAbstractItemView { color: %4; background-color: %2; outline: 0; }
+        QTabWidget#pixiuWorkspaces QListWidget::item, QFileDialog QAbstractItemView::item { padding: 6px 8px; border: 1px solid transparent; }
+        QTabWidget#pixiuWorkspaces QListWidget::item:selected, QFileDialog QAbstractItemView::item:selected {
+            color: %4; background-color: %9; background-image: none;
+            border: 1px solid %7; border-radius: 4px;
+        }
+        QTabWidget#pixiuWorkspaces QListWidget::item:hover:!selected, QFileDialog QAbstractItemView::item:hover:!selected { background-color: %3; }
+        QTabWidget#pixiuWorkspaces QListWidget::item:disabled, QFileDialog QAbstractItemView::item:disabled { color: %4; }
+        QDateEdit {
+            color: %4; background-color: %2;
+            border: 1px solid %6; border-radius: 9px; padding: 8px 11px;
+            selection-background-color: %7; selection-color: %2;
+        }
+        QDateEdit:focus { border: 2px solid %7; }
+        QDateEdit:disabled { color: %11; background-color: %3; }
+        QCalendarWidget QToolButton {
+            color: %4; background-color: %2; border: 1px solid %6; padding: 4px;
+        }
+        QCalendarWidget QToolButton:hover { background-color: %9; }
         QMainWindow#mainWindow, QWidget#appSurface, QDialog {
             background-color: %1;
         }

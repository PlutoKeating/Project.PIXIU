--- a/src/main.cpp
+++ b/src/main.cpp
@@ -90,1 +90,3 @@
-    app.setQuitOnLastWindowClosed(false);
+    // No tray is created by this host: closing its last window must not
+    // leave an invisible desktop process running.
+    app.setQuitOnLastWindowClosed(true);

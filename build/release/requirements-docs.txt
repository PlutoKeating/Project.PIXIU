# DOCX export: Markdown plus system LibreOffice Writer and python3-uno.
# Native TOC/header/footer refresh uses /usr/bin/python3 with Debian python3-uno.
# PNG paths, dimensions and digests use the Python standard library.
# Word borders, terminal blocks, plain lists and embedded assets use standard-library XML.
# These tools are not product runtime dependencies.
# System prerequisite: git-lfs; run git lfs install --local and git lfs pull
# before opening delivery binaries or rebuilding from LFS-managed screenshots.
Markdown==3.10.3
python-pptx==1.0.2
# Roadshow image sizing and validation; rendering uses WPS or LibreOffice plus Poppler.
Pillow==11.3.0

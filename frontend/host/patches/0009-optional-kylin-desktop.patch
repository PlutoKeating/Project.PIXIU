--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -29,1 +29,7 @@
-pkg_check_modules(KYSDKQTWIDGETS REQUIRED kysdk-qtwidgets)
+option(PIXIU_HAVE_KYSDK "Use official Kylin desktop widgets" ON)
+if(PIXIU_HAVE_KYSDK)
+    pkg_check_modules(KYSDKQTWIDGETS REQUIRED kysdk-qtwidgets)
+    add_definitions(-DPIXIU_HAVE_KYSDK=1)
+else()
+    message(STATUS "PIXIU desktop: portable Qt dialogs (Kylin styling unavailable)")
+endif()
--- a/src/app.cpp
+++ b/src/app.cpp
@@ -16,1 +16,3 @@
-#include <kwindowbuttonbar.h>
+#ifdef PIXIU_HAVE_KYSDK
+#include <kwindowbuttonbar.h>
+#endif
@@ -188,1 +190,2 @@
-                if (kdk::KWindowButtonBar *bar =
+#ifdef PIXIU_HAVE_KYSDK
+                if (kdk::KWindowButtonBar *bar =
@@ -201,1 +204,2 @@
-                if (!toolTip.isEmpty()) {
+#endif
+                if (!toolTip.isEmpty()) {
--- a/src/services/gatewayservice.cpp
+++ b/src/services/gatewayservice.cpp
@@ -13,1 +13,1 @@
-#include <kmessagebox.h>
+#include "utils/pixiu_desktop.h"
@@ -14,1 +14,1 @@
-#include <kdialog.h>
+
@@ -134,1 +134,1 @@
-    kdk::KMessageBox dialog(parent);
+    pixiu::desktop::MessageBox dialog(parent);
@@ -135,1 +135,1 @@
-    dialog.setIcon(kdk::KMessageBox::Question);
+    dialog.setIcon(pixiu::desktop::MessageBox::Question);
@@ -138,1 +138,1 @@
-    QPushButton *acceptButton = dialog.addButton(acceptText, kdk::KMessageBox::AcceptRole);
+    QPushButton *acceptButton = dialog.addButton(acceptText, pixiu::desktop::MessageBox::AcceptRole);
@@ -140,1 +140,1 @@
-        dialog.addButton(cancelText, kdk::KMessageBox::RejectRole);
+        dialog.addButton(cancelText, pixiu::desktop::MessageBox::RejectRole);
@@ -1675,1 +1675,1 @@
-    kdk::KDialog *progressDialog = m_progressDialog;
+    pixiu::desktop::Dialog *progressDialog = m_progressDialog;
@@ -1684,1 +1684,1 @@
-        auto *kylinProgressDialog = new kdk::KDialog(QApplication::activeWindow());
+        auto *kylinProgressDialog = new pixiu::desktop::Dialog(QApplication::activeWindow());
@@ -1861,1 +1861,1 @@
-    auto *kylinProgressDialog = new kdk::KDialog(QApplication::activeWindow());
+    auto *kylinProgressDialog = new pixiu::desktop::Dialog(QApplication::activeWindow());
@@ -1947,1 +1947,1 @@
-            QObject::connect(m_progressDialog, &kdk::KDialog::finished, &loop, &QEventLoop::quit);
+            QObject::connect(m_progressDialog, &pixiu::desktop::Dialog::finished, &loop, &QEventLoop::quit);
--- a/include/services/gatewayservice.h
+++ b/include/services/gatewayservice.h
@@ -11,1 +11,1 @@
-namespace kdk { class KDialog; }
+#include "utils/pixiu_desktop.h"
@@ -91,1 +91,1 @@
-    mutable kdk::KDialog *m_progressDialog;
+    mutable pixiu::desktop::Dialog *m_progressDialog;
--- a/src/utils/kylinfiledialog.cpp
+++ b/src/utils/kylinfiledialog.cpp
@@ -3,1 +3,1 @@
-#include <kdialog.h>
+#include "utils/pixiu_desktop.h"
@@ -19,1 +19,1 @@
-    kdk::KDialog window(parent);
+    pixiu::desktop::Dialog window(parent);
@@ -44,1 +44,1 @@
-    QObject::connect(picker, &QFileDialog::accepted, &window, &kdk::KDialog::accept);
+    QObject::connect(picker, &QFileDialog::accepted, &window, &pixiu::desktop::Dialog::accept);
@@ -45,1 +45,1 @@
-    QObject::connect(picker, &QFileDialog::rejected, &window, &kdk::KDialog::reject);
+    QObject::connect(picker, &QFileDialog::rejected, &window, &pixiu::desktop::Dialog::reject);
@@ -47,1 +47,1 @@
-    if (window.exec() != kdk::KDialog::Accepted) {
+    if (window.exec() != pixiu::desktop::Dialog::Accepted) {
--- a/include/utils/kylininputdialog.h
+++ b/include/utils/kylininputdialog.h
@@ -4,1 +4,1 @@
-#include <kinputdialog.h>
+#include "utils/pixiu_desktop.h"
@@ -16,1 +16,1 @@
-        kdk::KInputDialog dialog(parent);
+        pixiu::desktop::InputDialog dialog(parent);
@@ -18,1 +18,1 @@
-        dialog.setInputMode(kdk::KInputDialog::TextInput);
+        dialog.setInputMode(pixiu::desktop::InputDialog::TextInput);
@@ -22,1 +22,1 @@
-        const bool accepted = dialog.exec() == kdk::KDialog::Accepted;
+        const bool accepted = dialog.exec() == pixiu::desktop::Dialog::Accepted;

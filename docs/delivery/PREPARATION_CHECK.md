# 提交材料检查记录

## 执行要求

2026-09-09 按用户提供的三张赛方图片整理。唯一权威记录为 `docs/DELIVERY_PLAN.md` 第 0 节，该节保持原文。提交区仅包含规定的两层同名目录及并列的源码目录。

项目报告保存为 `项目报告.pptx`，技术方案保存为真正的 Word 二进制文件 `技术方案.doc`。手册、安装指南、效果验证和适配结论汇入技术方案。视频完成后放入材料内层目录并命名为 `演示视频.zip`；不创建占位文件。源码以 `源代码/PIXIU源代码.tar.gz` 提供，包含源码清单和校验工具。

编写源、38 张原始操作截图和图片索引保存在本目录，生成过程中的 DOCX、PDF 位于忽略的构建目录。提交目录内不放这些内部材料。旧提交目录已迁出当前仓库；发生冲突的旧记录已删除或改写。

## 内容边界

虚拟机已安装并运行 `0.1.9-1`，运行主程序摘要与安装文件一致。操作图片来自该版本的真实 V11 桌面，原始图片字节在整理中保持不变。

项目报告补充数据接入、混合检索、偏好版本、记忆流转、敏感过滤和评测设计。技术方案合并八篇源稿，明确数据集、比较方法、计算口径和已知限制。

现有量化结果来自历史 Debian 兼容测试。最终 V11 同版量化结果、三组对比、多设备及完整升级回滚实测仍未完成；停止任务后的状态显示问题仍存在。整理目录不会把这些结果改写为通过。

## 验证记录

2026-09-09 完成以下检查：

| 检查 | 结果 |
|---|---|
| 权威要求 | 第 0 节与整理前逐字一致；两份赛题文件通过官方摘要检查 |
| 文档记录 | 扫描 351 份自有文档与配置，旧交付路径、旧编号及已移除内容的表述无残留 |
| 项目报告 | 12 页逐页观察，版面完整；版本为 0.1.9 |
| 技术方案 | 最终 DOC 回读为 40 页 PDF，逐页观察；23 处插图使用 22 张独立原图 |
| 图片嵌入 | DOC 回读 DOCX 后 22 张图片均与原 PNG 字节一致，外链图片为 0 |
| 匿名内容 | Word 正文与 XML 元数据、源码文件均未检出已知学校、申报人和个人目录信息 |
| 源码 | 4,632 个文件、四个固定上游；与当前源码逐文件核对，解压校验工具通过 |
| 目录与格式 | 文件、目录、真实 DOC 格式与 PPT 容器检查通过；仅缺演示视频.zip |
| 检查工具 | 6 项目录异常与文件格式测试通过，文档来源和导出摘要通过 |
| 虚拟机 | 安装版本 0.1.9-1；后端、Runtime Gateway、系统云模型桥接服务均运行；桌面进程与已安装主程序摘要一致 |

材料格式检查已完成，视频复制到内层同名目录后仍须实看核对时长、大小、匿名内容及演示覆盖。源码文件完整性检查未替代全新环境发布构建；产品验收缺口见上文。

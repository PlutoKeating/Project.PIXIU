# PIXIU 最终交付与版本管理计划

> 状态：团队强制交付基线（2026-09-03）
>
> 本文记录四项赛事交付作品要求，并单独维护产品安装、版本与升级的工程验证计划。
> 交付作品的内容、份数、格式、命名与匿名要求，唯一权威依据为下文 §0 的赛方
> 图片完整转录（2026-09-08 冻结）。`OriginProblemDescription.md` 与
> `完整赛题要求.pptx` 的交付条款已按用户指令同步覆盖；其他赛题技术要求继续适用。
>
> 实施顺序、依赖和逐切片完成定义见
> [`IMPLEMENTATION_MASTER_PLAN.md`](IMPLEMENTATION_MASTER_PLAN.md)。

## 0. 赛方交付作品要求（唯一权威、永久冻结）

**归档日期：2026-09-08。来源：用户本次提供的三张赛方要求图片。**
§0.1～§0.3 为图片全部可见文字及目录关系的转录，仅调整换行和 Markdown 排版；
§0.4 为用户提供的团队信息，§0.5 为适用边界说明，均与赛方原文明确区分。
本节是本仓库交付作品内容要求的唯一权威记录，自本次提交起永久冻结，
禁止后续编辑、增删、重排、改写或覆盖；其他计划、模板、脚本与交付实物只能遵循、
引用本节，不得另立或反向修改要求。冻结范围是本节，不包括本文其他工程状态台账。

### 0.1 图片一：作品提交规则及方式

**2026 挑战杯揭榜挂帅麒麟赛题**

**作品提交规则及方式**

**一、提交的作品规则**

- **项目报告(ppt 格式)1 份：** PPT 应系统阐述方案架构、多源数据整合、偏好动态捕捉与版本化管理、知识结构化检索、端侧轻量化部署及敏感信息过滤，展示准确率≥85%、召回率≥85%等量化指标与真实场景案例，并明确记忆流转与适配测试结论，突出算法创新与实用价值。
- **技术方案(word、pdf 文档)1 份：** 编写完整的技术文档和用户手册（包括测试结果、软件架构、算法原理、实现方案、安装部署指南、操作说明等内容）。技术文档中附效果验证报告，包含测试数据集说明、对比实验设计、量化评测实施方案、量化指标分析等内容。
- **功能演示视频 1 份（5-10 分钟 压缩包为 zip 格式；大小不超过 200M）：** 把实现的所有核心功能进行讲解演示（建议配字幕）。
- **源代码 1 份：** 须提供项目完整源代码，含必要的技术规范要素。

### 0.2 图片二：作品提交命名及格式规范

**作品提交命名及格式规范（命名信息要和平台提交的信息相符）**

图片中的完整目录关系如下；两层同名目录均须保留：

```text
学校+作品名称+主申报人姓名/
├── 学校+作品名称+主申报人姓名/
│   ├── 项目报告.pptx
│   ├── 技术方案.doc
│   └── 演示视频.zip
└── 源代码/
    └── 源代码文件
```

### 0.3 图片三：作品内容匿名要求

> 2、提交的作品（包含项目报告 ppt、技术方案、演示视频等）内容一律不得出现学校名称、学院名称、校徽、指导教师姓名、参赛学生姓名等可识别身份信息，确保评审工作客观公正。

### 0.4 团队信息（用户确认的内部记录，非赛方原文）

| 字段 | 确认值 |
|------|--------|
| 学校名称 | 华南理工大学 |
| 作品名称 | PIXIU·貔貅：面向麒麟OS Agent的去中心化记忆系统设计与实现 |
| 主申报人姓名 | 秦基赫 |

上述信息用于平台申报信息核对及 §0.2 要求的两层目录命名；作品名称中的
`·`、`：` 和 `OS Agent` 空格按用户给定值保留，不自行缩写或更名。
按图片中的 `+` 连接三项信息，两层目录的记录用名称均为：

```text
华南理工大学+PIXIU·貔貅：面向麒麟OS Agent的去中心化记忆系统设计与实现+秦基赫
```

学校、主申报人等身份信息不得因此填入项目报告、技术方案、用户手册、视频等
提交作品内容。本节含真实团队身份，仅供内部维护与命名使用，不得整节复制进
匿名评审材料；正文、封面、页眉页脚、作者署名、致谢、截图、字幕和讲解均应遵循 §0.3。

### 0.5 生效范围（记录说明，非赛方原文）

- §0.1～§0.3 是唯一交付作品要求。发生冲突时完全以新内容覆盖，删除冲突内容，
  不保留并行口径；新要求未要求的交付物不得作为额外必交件。
- 技术方案包含完整技术文档、用户手册、安装部署指南、操作说明及效果验证报告；
  不要求把这些内容另交为独立文件，也不要求额外交付安装包、报名表或原始证据压缩包。
- 视频为 5-10 分钟，提交 zip 压缩包，大小不超过 200M；`200M` 按图片原文保留，
  不自行增加字节换算、视频编码、扩展名或分辨率要求。
- 技术方案格式保留图片一的 word、pdf 表述及图片二的 `技术方案.doc` 示例，
  不自行增加 Word 与 PDF 各交一份的要求。
- 两层目录命名与平台信息一致；提交作品内容一律匿名，内部实名审核记录不进入作品内容。
- 本次仅更新文档记录，不生成、调整或提交交付实物。

<!-- 赛方交付作品要求冻结记录结束 -->

## 1. 单一安装产物

[0.1.12 正式版](https://github.com/PlutoKeating/Project.PIXIU/releases/tag/v0.1.12)于 2026-09-10 发布，提供银河麒麟 V11 amd64 单一安装包。

| 内容 | 当前结果 |
|---|---|
| 产品 | 桌面、后台服务、Agent、文档解码及 Python 依赖随包安装 |
| 平台 | V11 原生构建与双 SDK 实际调用通过；通用构建通过 |
| 安装 | 新装启动、签名升级及自动恢复通过 |
| 数据 | 升级与恢复前后 47 条记忆摘要一致 |
| 同步 | 三台独立 V11 虚拟机共享、断连与并发更正通过 |
| 发布 | 标签绑定源码，正式包与清单摘要、签名核验通过 |

构建画像位于 `build/release/profiles/`；当前正式包使用 Python 3.12。安装时从包内安装锁定的 Python 依赖，系统组件由画像中的依赖清单管理。文档解码由包内组件完成。

首次启动创建当前用户配置并激活服务。采集通过设置页授权目录和来源，设备通过配对建立信任。测试明细见[测试报告](delivery/TEST_REPORT.md)，内部验收项见[发布记录](RELEASE_0_1_10_COMPLETION_PLAN.md)。

## 2. 版本管理唯一真相源

2026-09-06 流水线审计：远程 `v0.1.7` 仍是 `0.1.7-1` 通用画像资产，
不能代表本地 `0.1.7-3` 严格原生 Agent 包。CI 基线调整为 Ubuntu 24.04 /
Python 3.12（后端另测 3.13），与 cp312 打包目标一致。官方 Gitee 子模块继续
使用已提交 gitlink，增加按四个固定提交生成缓存键的 Git 对象缓存、有限重试和
恢复后版本校验；仅允许维护者手动在 main 上通过麒麟 runner 的只读镜像预热缓存。
PR 不进入自托管 runner。后续发布必须由同一候选提交自动重建，不能把旧包改名发布。

原生工作流提供可复用构建：在 V11/amd64/cp312 检查后，使用网络命名空间隔离
宿主构建和 Runtime 离线安装验证，先保存供应链实物，再打包严格画像；安装后运行
用户级服务、provider 激活和真实双 SDK 生命周期取证。标签发布同时要求通用 CI
与原生作业成功，再在 GitHub 托管执行器验证原生证据并生成 Ed25519 六件套。
签名密钥不传给自托管构建作业。手动 main 执行只归档候选，不创建生产 Release。
原生安装门对候选执行 `apt-get install --reinstall`：同产品版本重跑也必须替换实物，
避免 APT 以“已是最新版本”为由跳过安装后误验旧 commit；仍不自动允许版本降级。
该产品发布门与下文赛事最终三机/性能验收门分别记录，不能混称全部验收完成。

采用 SemVer `MAJOR.MINOR.PATCH`，Debian 包版本为 `MAJOR.MINOR.PATCH-REVISION`。
发布标签、CMake `applicationVersion`、Debian control、Release 资产与更新清单必须
由一个发布版本输入派生；CI 在不一致时失败。
前端根 CMake 仅构建保留的回归测试，不再生成或安装独立小窗口程序；包体中的
桌面文件和图标由统一打包脚本安装，正式应用来自 Agent 宿主导出构建。旧浮球
首击脚本已删除，其历史结果不能作为当前桌面入口验收。

最终版本清单至少包含：

- PIXIU 产品版本、Git commit、构建时间、目标架构与 V11 画像；
- 数据库 schema 版本、API 版本、Module E/provider 版本；
- 兼容的 `kylin-agent`/`agent-runtime` 固定版本范围；
- Embedding/Vector Engine SDK 及运行时版本；
- 每个资产的大小、SHA-256、独立数字签名和发布通道。

图形“关于/更新”页必须展示当前版本、可用版本、通道、组件兼容状态和发行说明。
不得只在多个源码文件手工重复版本号。当前 `0.1.8` 发布预检已同步校验根
`VERSION`、管理库宏、统一宿主的版本注入补丁及 Module E manifest；不再以旧小窗口
的 CMake/main.cpp 作为发布版本证据。预检回归在不包含旧入口的隔离源码中通过，
管理库宏或宿主应用版本被改成硬编码时必须失败。构建现生成包内只读
`/usr/share/pixiu/release-manifest.json`，从源码和构建输入记录产品/Debian、Git、
构建时间、架构/profile/KYSDK/Python ABI、API/schema/provider、Agent 上游及双 SDK
源码钉住版本与许可证；CI 会从成包反向提取并核对。上游 runtime 的 `version` 文件
与包元数据当前分别为 0.9.9/0.9.8，清单保留两项事实而不擅自归一。仓库根
`VERSION` 已成为发布脚本和前端 CMake/独立 control 的权威输入，环境变量只能作
一致性断言；Module E 源码只保留 `plugin.yaml.in`，打包/激活时从同一输入生成
`plugin.yaml`。发布工作流与 `publish.sh` 已为每个架构生成无自引用的包外
`.assets.json`，记录 `.deb`/checksum/signature 的大小、SHA-256、commit 和通道，
生成前以固定公钥验证主包签名，并为清单自身生成 checksum + Ed25519 签名；最终
commit/版本/架构从包内组件清单与 dpkg control 交叉核对，且 JSON 记录规范化生成
命令。最终候选仍须归档并复验这六件套；
还须在目标银河麒麟 V11 环境记录 Embedding/Vector SDK 的实际安装包版本、运行时
探测结果及 `/capabilities` 一致性。原生取证器已自动化绑定候选包摘要/commit、已装
manifest、dpkg 版本、Agent runtime 和三个端点，并以独立临时数据库和隔离集合直接
执行 SDK 的装载、集合、向量及断开生命周期。当前审阅候选已生成真实通过输出并绑定
候选 SHA；文档冻结后仍需对最终 commit 重跑，源码 gitlink不能代替实装证据。

三设备证据采用两级门禁：`three-device-evidence.py capture` 必须在各节点本地复用
已经通过的 strict 原生证据并读取 loopback 同步 API，只导出加盐身份/域摘要和版本、
候选包摘要、同步计数；`validate` 汇总恰好三份清单，拒绝跨 run、版本漂移、重复身份、
非完全图、离线/积压或超过 300 秒的采集窗口。它只证明三台 V11 的同版全连接拓扑，
固定 `final_device_evidence=false`；五项跨设备业务场景及最终逻辑视图收敛仍是发布硬门。
并发更新已有第二级机器门禁：三台设备各采集 baseline/diverged/converged，要求两个
暂停节点从同一版本产生不同 v+1 分支、观察节点不变，恢复后全部在线静默且逻辑视图
一致。单节点离线重连使用另一组 3×3 检查点，要求隔离端保持基线、其余两端仍可写入
并传播 v+1、恢复后三端追平。私域场景再用 6 份检查点验证只在写入端可见且同步
确认数不增长。三类工具当前 10/10 测试通过，但未输入真实三机数据，不能减少发布
阻断项。墓碑场景另以 12 检查点绑定可见性和去载荷 CRDT 状态，验证离线旧副本重连
及额外反熵后仍不复活；四类工具当前 12/12 通过。正文、查询、作用域、知识/操作 ID
和设备标识始终只进入加盐摘要。
最终 `validate-final` 还必须绑定初始拓扑、四类报告和由三份新节点清单生成的收尾
拓扑，核对同 run/版本/身份/域与时间边界；只有该总报告可以标记最终证据。工具测试
现为 14/14，但尚无真实输入。

## 3. 图形界面一键升级

升级特权边界与无特权重启脚本统一维护在 `build/release/debian/usr/lib/pixiu/`，
整包构建将其安装到同名 `/usr/lib/pixiu/` 路径。旧 `frontend/scripts/` 副本已移除，
唯一宿主继续通过原安装路径调用，无参数协议变化。独立前端包构建脚本和 Debian
元数据已删除，构建统一走 `make -C build/release build-deb`；包内旧可执行程序仍待迁移完成后删除。

目标流程：设置 → 检查更新 → 显示版本与发行说明 → 用户点击“一键升级” → 下载 →
校验签名/摘要 → 系统授权 → 原子安装/迁移 → 健康检查 → 受控重启或明确恢复。

发布门：

1. 只选择当前架构与通道的正式资产，拒绝预发布、降级和不兼容组件。
2. HTTPS 之外还要验证独立签名；同一 Release 中的 SHA-256 只证明传输完整性，
   不能单独证明发布者身份。
3. 安装前备份必要配置和 schema 状态；升级失败可恢复上一可用版本，且不丢记忆。
4. `dpkg` 阶段不可强杀；安装后执行包版本、服务/schema/provider 健康检查，全部
   通过才允许 GUI 报告成功。
5. 图形界面完整呈现下载、校验、授权、安装、重启、取消、失败和恢复状态。
6. 更新 PIXIU 自有包，不静默升级上游 Agent 或系统 SDK；兼容性不满足时明确阻断。

当前 `UpgradeController` 已实现版本比较、按架构选择 `.deb`、流式 SHA-256、强制
`.sha256.sig` 资产、重定向白名单、`pkexec` + root-only 二次校验与错误分类；发布
工作流以受保护 Secret 中的 Ed25519 私钥签名，特权 helper 用包内固定公钥在 dpkg 前
验签。helper 还会验证包名/版本/
架构、dpkg 实际安装版本、后端 `/version`、数据库 `/health`/schema 和包内 Provider
版本，健康失败以专用状态返回且不会误报成功。Module E 随包升级会保留用户显式配置，
并在桌面用户上下文重新激活。双架构签名 CI 与 Kylin V11 有效/篡改签名已通过；临时
Ed25519 密钥和两个真实 `.deb` 已完成“旧钥验过渡包、过渡包部署新公钥、新钥验下一包、
旧钥拒绝下一包”的无私钥入库轮换演练。最终仍需补完整 Module E 支持版本矩阵。
自动回滚现已实现 `dpkg-repack` 旧包重建、停服 SQLite 一致性快照、配置备份及失败后
恢复旧包/数据/服务，并区分恢复成功与恢复失败。提交 `ca35117` 的双架构 CI 和
Kylin V11 amd64 跨 revision 健康失败注入均已证明退出码 5、旧版本/配置/数据/服务
恢复；最终候选仍须随完整安装矩阵重验。GUI 成功态的“立即重启”现通过无特权 helper
等待旧客户端退出、再启动新版本；失败可重试或手动打开，安装中不会触发。源码和打包
测试已覆盖，最终 V11 图形升级/重启取证仍待完成。

## 4. 赛事交付文档台账

`submission/` 按第 0 节保留两层同名目录。内层只放 `项目报告.pptx`、
`技术方案.doc` 和制作完成后的 `演示视频.zip`；与内层并列的 `源代码/` 保存完整源码。
技术方案汇入用户手册、安装部署、测试结果和效果验证报告。编写材料、原图和内部
检查记录在 `docs/delivery/` 维护，中间导出在 `build/release/out/`，不进入提交目录。

2026-09-09 用户对视频制作另行授权：视频工程、原始截图、录屏、解说和中间素材放在
`submission/video-production/`，位于正式两层同名目录之外。该工作目录不是额外必交
作品，不改变 §0；最终仅把 `演示视频.zip` 补入既有内层目录。

文档导出、源码生成和目录检查命令见 [材料维护说明](delivery/README.md)，检查结果见
[材料检查记录](delivery/PREPARATION_CHECK.md)。各项功能和性能结论仍以真实证据判定。

| 交付作品 | 份数及格式 | 内部维护源 | 状态 |
|----------|------------|------------|------|
| 项目报告 | 1 份，ppt；目录示例 `项目报告.pptx` | `delivery/PRESENTATION_AND_VIDEO.md` | 项目报告源稿按 0.1.12 更新 |
| 技术方案 | 1 份，word、pdf；目录示例 `技术方案.doc` | `delivery/TECHNICAL_SOLUTION.md` 及同目录测试、用户手册、部署、流转、案例和适配内容 | 八篇源稿已更新，记录功能、操作与实测结果 |
| 功能演示视频 | 1 份，5-10 分钟，`演示视频.zip`，大小不超过 200M | `delivery/PRESENTATION_AND_VIDEO.md`、`submission/video-production/` | 演示增强版30镜、499.333秒：原速口白、真实鼠标操作、逐句画面和实际结果已编排，原字幕与首尾沿用指定版本；整片复核记录位于 video-production/review |
| 源代码 | 1 份，完整源代码及必要技术规范要素 | `delivery/SOURCE_AND_LICENSES.md` 与项目源码 | 按当前源码及四个固定上游生成，逐文件核对 |

提交作品匿名；团队信息只用于平台信息核对与两层目录命名。内部保留版本和审核追踪。

2026-09-11 演示增强版采用30镜、499.333秒时间轴。阶段记忆、设备配对和遗忘包含真实确认操作，图书流程与来源对应；首尾、原字幕与新增原速口白的核对记录见 `submission/video-production/review/directed-frozen-constraints.json`。

## 5. 作品材料核对与产品验证边界

作品材料核对以 §0 为准：四项作品齐全，项目报告内容完整，技术方案包含用户手册、
测试结果及效果验证报告，视频覆盖所有已实现核心功能并满足时长、zip 与大小要求，
源代码完整且含必要技术规范。目录命名与平台一致，作品内容不含可识别身份信息。

产品的 V11、双 SDK、Agent、三设备、性能、安装及升级验证继续按验收规范执行。
原始日志、数据、安装包、签名、构建记录及内部审核信息用于支撑真实测试结论，
不另列为赛事必交实物；测试结果与量化分析写入技术方案，记忆流转与适配结论写入项目报告。

日常与 CI 使用 `make -C build/release governance` 校验赛方文档摘要和 Git 工作区洁净度。
文档、源码与目录由材料生成工具核对；该检查不替代产品工程验收。

文档更新兼容记录（2026-09-08）：`final-dataset-manifest.py` 仍硬编码更新前的赛题文档摘要，
与本次文档摘要不一致，当前不能用该工具生成或验证新的数据集记录。本次限定只更新文档，
未修改工具代码；文档摘要清单已同步，不能将其核对通过写成数据集工具已兼容。

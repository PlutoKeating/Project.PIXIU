# PIXIU 最终交付与版本管理计划

> 状态：团队强制交付基线（2026-09-03）
>
> 本文记录四项赛事交付作品要求，并单独维护产品安装、版本与升级的工程验证计划。
> 交付作品的内容、份数、格式、命名与匿名要求，唯一权威依据为下文 §0 的赛方
> 图片完整转录（2026-09-08 冻结）。`OriginProblemDescription.md` 与
> `完整赛题要求.pptx` 的交付条款已按用户指令同步覆盖；其他赛题技术要求继续适用。
>
> 实施顺序、依赖和逐切片完成定义见
> [`IMPLEMENTATION_MASTER_PLAN.md`](IMPLEMENTATION_MASTER_PLAN.md)。

## 0. 赛方交付作品要求（唯一权威、永久冻结）

**归档日期：2026-09-08。来源：用户本次提供的三张赛方要求图片。**
§0.1～§0.3 为图片全部可见文字及目录关系的转录，仅调整换行和 Markdown 排版；
§0.4 为用户提供的团队信息，§0.5 为适用边界说明，均与赛方原文明确区分。
本节是本仓库交付作品内容要求的唯一权威记录，自本次提交起永久冻结，
禁止后续编辑、增删、重排、改写或覆盖；其他计划、模板、脚本与交付实物只能遵循、
引用本节，不得另立或反向修改要求。冻结范围是本节，不包括本文其他工程状态台账。

### 0.1 图片一：作品提交规则及方式

**2026 挑战杯揭榜挂帅麒麟赛题**

**作品提交规则及方式**

**一、提交的作品规则**

- **项目报告(ppt 格式)1 份：** PPT 应系统阐述方案架构、多源数据整合、偏好动态捕捉与版本化管理、知识结构化检索、端侧轻量化部署及敏感信息过滤，展示准确率≥85%、召回率≥85%等量化指标与真实场景案例，并明确记忆流转与适配测试结论，突出算法创新与实用价值。
- **技术方案(word、pdf 文档)1 份：** 编写完整的技术文档和用户手册（包括测试结果、软件架构、算法原理、实现方案、安装部署指南、操作说明等内容）。技术文档中附效果验证报告，包含测试数据集说明、对比实验设计、量化评测实施方案、量化指标分析等内容。
- **功能演示视频 1 份（5-10 分钟 压缩包为 zip 格式；大小不超过 200M）：** 把实现的所有核心功能进行讲解演示（建议配字幕）。
- **源代码 1 份：** 须提供项目完整源代码，含必要的技术规范要素。

### 0.2 图片二：作品提交命名及格式规范

**作品提交命名及格式规范（命名信息要和平台提交的信息相符）**

图片中的完整目录关系如下；两层同名目录均须保留：

```text
学校+作品名称+主申报人姓名/
├── 学校+作品名称+主申报人姓名/
│   ├── 项目报告.pptx
│   ├── 技术方案.doc
│   └── 演示视频.zip
└── 源代码/
    └── 源代码文件
```

### 0.3 图片三：作品内容匿名要求

> 2、提交的作品（包含项目报告 ppt、技术方案、演示视频等）内容一律不得出现学校名称、学院名称、校徽、指导教师姓名、参赛学生姓名等可识别身份信息，确保评审工作客观公正。

### 0.4 团队信息（用户确认的内部记录，非赛方原文）

| 字段 | 确认值 |
|------|--------|
| 学校名称 | 华南理工大学 |
| 作品名称 | PIXIU·貔貅：面向麒麟OS Agent的去中心化记忆系统设计与实现 |
| 主申报人姓名 | 秦基赫 |

上述信息用于平台申报信息核对及 §0.2 要求的两层目录命名；作品名称中的
`·`、`：` 和 `OS Agent` 空格按用户给定值保留，不自行缩写或更名。
按图片中的 `+` 连接三项信息，两层目录的记录用名称均为：

```text
华南理工大学+PIXIU·貔貅：面向麒麟OS Agent的去中心化记忆系统设计与实现+秦基赫
```

学校、主申报人等身份信息不得因此填入项目报告、技术方案、用户手册、视频等
提交作品内容。本节含真实团队身份，仅供内部维护与命名使用，不得整节复制进
匿名评审材料；正文、封面、页眉页脚、作者署名、致谢、截图、字幕和讲解均应遵循 §0.3。

### 0.5 生效范围（记录说明，非赛方原文）

- §0.1～§0.3 是唯一交付作品要求。发生冲突时完全以新内容覆盖，删除冲突内容，
  不保留并行口径；新要求未要求的交付物不得作为额外必交件。
- 技术方案包含完整技术文档、用户手册、安装部署指南、操作说明及效果验证报告；
  不要求把这些内容另交为独立文件，也不要求额外交付安装包、报名表或原始证据压缩包。
- 视频为 5-10 分钟，提交 zip 压缩包，大小不超过 200M；`200M` 按图片原文保留，
  不自行增加字节换算、视频编码、扩展名或分辨率要求。
- 技术方案格式保留图片一的 word、pdf 表述及图片二的 `技术方案.doc` 示例，
  不自行增加 Word 与 PDF 各交一份的要求。
- 两层目录命名与平台信息一致；提交作品内容一律匿名，内部实名审核记录不进入作品内容。
- 本次仅更新文档记录，不生成、调整或提交交付实物。

<!-- 赛方交付作品要求冻结记录结束 -->

## 1. 单一安装产物

本次产品发布目标为 0.1.9：单一 openKylin Agent 桌面宿主及内嵌记忆、设备、设置
工作区，旧小窗口不进入安装包。按用户最新要求，以通用 CI、严格 V11 原生构建安装、
SDK 核验和签名资产为产品发布门；完整截图、非阻塞文档整理及赛事全场景验证继续
保留，不将未完成的完整赛事交付声明为通过。版本标签必须重新验证其自身提交，
不复用其他提交的绿色结果来跳过发布门。

采集有两个不同层次的开关：包模板的 `PIXIU_MONITOR_ENABLED=1` 允许采集运行组件
启动；实际采集还受 `/monitor/config` 的总开关与来源开关共同控制，新用户默认
全部关闭。不能把“运行组件已启动”日志当成“用户已开启采集”。后端启动器先读取
用户配置文件，因此该文件同名值会覆盖调用进程传入的环境值；实际采集设置应通过
隐私界面或公共配置 API 核对，不能仅依据外层环境变量推断。

Agent 供应链记录区分 `kylin-v11` 和 `generic-debian`；审计默认仍要求麒麟 V11，
通用记录必须显式指定 `--expected-os generic-debian`。宿主与 Runtime 必须同时
匹配该目标，不允许混用或将通用记录作为原生证明。此字段区分记录目标，不单独证明
SDK 已实际运行；整包构建必须消费同画像的完整宿主/Runtime，不能只凭审计格式发布。

`prepare-agent-supply-chain.sh` 是共用构建入口：ON 对应 kylin-v11、OFF 对应
generic-debian，隔离构建显式传递 SDK 开关和 Python PATH，宿主与 Runtime 记录及
末尾审计使用同一目标。现有锁仅覆盖 amd64/CPython 3.12，不满足时在创建证据目录
前拒绝；原生工作流显式固定 ON，不因构建环境继承值而选择通用画像。

通用 CI 在 Python 3.12 环境执行上述 OFF 构建：宿主构建与 Runtime 安装分别在
网络命名空间隔离下验证，通过同目标审计后保存 `pixiu-generic-agent-<commit>`。
此产物用于核对完整分发输入，不是 Release；整包仍须实际消费这些输入并完成安装验证。

用户态激活器分别读取 `install-strict`（SDK 严格要求）和 `agent-bundled`
（包内 Agent/Runtime 所有权）。后者为 1 时使用包内程序并管理网关迁移、重启和
失败恢复；通用模式不激活遗留麒麟模型桥接文件，也不初始化麒麟模型配置。标记缺失
时兼容既有严格包行为；非法标记或严格模式搭配非包内 Runtime 在修改用户配置前拒绝。
整包输出 `agent-bundled=1`，启动器只执行包内 Agent；通用和原生都携带 Runtime
及用户网关，只有原生携带麒麟桥接。旧前端暂用于源码回归，但其可执行文件不再安装。
CI 解包检查这些文件、桌面入口和旧程序缺失；该文件检查不代表真实安装已完成。

当前三种整包画像均依赖 `python3 >= 3.12, << 3.13`、`python3-venv` 和 Qt SQLite
插件，与已审计的 cp312 Runtime 闭包保持一致。后端源码可在其他 Python 版本回归，
不代表这个二进制安装包可在那些解释器下安装。未定义画像或缺失画像依赖清单时构建
拒绝继续，不使用未经审核的通用依赖默认值。

安装脚本先核对系统 CPython 3.12，再创建或复用产品 venv，并检查 venv 的 ABI、
隔离性和 pip。任何检查失败均停止安装且不删除既有 venv；不再下载 get-pip、
在线升级 pip 或向系统 Python 回退安装。后端 wheels 改为整包必需项，构建使用
Runtime 固定版本作为共同依赖约束，下载失败直接中止；安装时两套依赖均仅从包内
安装，缺失或损坏不回退在线，最后执行 pip check。CI 新增解包后空 venv 的两套
依赖安装与一致性检查；尚待该新作业及真实 apt 安装结果，不宣称原生离线验收完成。

产品发行提供一个带版本号、架构和校验/签名信息的 PIXIU `.deb`。用户应能在
银河麒麟 V11 图形软件安装器中打开安装，或以一条 `apt install ./pixiu_*.deb`
命令完成；不得要求手工复制源码、创建虚拟环境、逐个安装 Python 包或编辑数据库。

该包必须包含团队原创的记忆服务、记忆控制台和 Module E 适配器，并完成：

- 解包前探测 V11 与架构；由包依赖/严格后端预检验证双 SDK，并在桌面用户激活时
  验证 openKylin Agent 宿主和 0.9.x runtime；
- 安装/升级/卸载脚本幂等，保留用户记忆、配置、设备身份和数据库迁移状态；
- `postinst` 部署只读默认模板，`pixiu-user-setup` 在当前用户 XDG 域首装创建配置、升级保留并补字段；用户配置不是 dpkg conffile；
- 注册服务、桌面入口、图标、日志轮转、权限和故障诊断；
- 依赖可由系统包解析或随包离线提供，安装后可直接启动；
- 严格包内含从固定上游及批准补丁重建的 Agent 宿主与 Runtime，不把上游通用能力冒充原创；
- 文件名、包元数据和运行架构一致。当前严格发行闭包仅覆盖 amd64/cp312；arm64 须先补对应 Runtime 锁与目标机验证，不能复用 amd64 包。

### 当前判定

| 能力 | 当前事实 | 发布判定 |
|------|----------|----------|
| 记忆服务 + 控制台 `.deb` | root-owned 单包 + systemd user service + XDG 私有目录；旧库迁移、覆盖安装、健康检查和 GUI 升级事务均已实现 | 命令行安装/覆盖升级通过；完整矩阵待补 |
| Module E 随包安装 | `.deb` 携带只读 Provider；用户启动时幂等安装并激活；旧用户 Runtime unit 会先备份再迁移到包内网关 | **安装与激活通过；模型 run 待补** |
| V11 双 SDK 严格画像 | 当前审阅候选的同包原生证据完成 direct SDK 和产品写查遗忘，双 SDK compliant、`contest_ready=true` | 技术门通过；最终冻结 commit 重跑归档 |
| 图形安装器双击安装 | 尚无最终版本取证 | 阻断 |
| 离线/全新机安装 | 现有 wheels 路径可用，需最终包重验 | 待重验 |

严格画像采用分阶段门禁：`preinst` 在解包前拒绝错误 OS/架构；双 SDK 是普通
`Depends`，由 apt/dpkg 正常排序安装，再由后端 strict 启动预检拒绝不可用 runtime；
桌面 Agent 与包内只读 runtime 由用户态 Provider 激活器验证；严格画像不回退到用户可变 Runtime。严格启动器
不会吞掉激活失败，generic/portable 才保留独立控制台降级。构建器强制
`KYSDK=ON`/`install_strict=1` 成对，组件清单记录最终模式。首次目标运行已证明严格失败
关闭有效；用户会话 SDK 边界现已交付化并取得同包原生证据。
推荐边界已于 2026-09-04 获负责人批准，见
[`ADR-0002`](decisions/0002-run-native-backend-in-user-session.md)：`.deb` 仍为一次
系统安装，后端改由登录用户的 systemd user manager 运行，数据进入 XDG 用户域，升级
采用用户态停启/健康检查与特权安装两阶段。在迁移/回滚矩阵完成前，现有 system
service 不删除，H-02/H-03 状态不变。

当前源码新增 `PIXIU_VECTOR_DB_PATH`，strict 启动必须实际装载应用数据库，store 按
进程复用并在退出时断开。2026-09-06 CI 在 Python 3.12/3.13 各 823 项通过；历史提交 `6f6002e` 的 revision 8
又完成 V11 同用户写入、召回、遗忘和隐藏检查。正式取证器因缺少 Agent 宿主/runtime
而拒绝出证，且该同用户运行方式尚未落实为安装包 user service，因此状态仍为阻断。
提交 `c643b1b699ba34650fdf913dd58f0cccd8168191` 随后从洁净固定源码再次完成 strict
amd64 构建、38/38 前端测试和安装；保留 portable 配置时服务健康，切到 strict 后
系统服务账户仍按预期因用户级 AI runtime 不可达而失败关闭，恢复后配置与数据库摘要
不变。这是安装安全性和根因复现实证，不是最终 user service 或 H-01～H-03 通过证据。

官方 AI 子系统是目标系统前置能力，应通过系统提供的 AI 模块管理完成安装/启用。
整套子系统体积大且包含与 PIXIU 无关的模型和引擎，不列为 PIXIU `.deb` 的整体
`Depends`；发布前以同版实测确定 Embedding、Vector Engine 及其 runtime 的最小包集，
并在安装器中对缺失能力给出可操作提示。

Agent 宿主供应链另有独立发布门：官方公开源码原状不可完整链接，仓库依据
[ADR-0003](decisions/0003-package-openkylin-agent-supply-chain.md) 保留可审计最小补丁，移除未完成且
未使用的 UI 单元、补全 Gateway/stream/cancel/model 适配，并在净化后的隔离源码副本
上完成 V11 网络隔离重建。Runtime 以入库的 CPython 3.12/amd64 运行时锁和构建工具锁
作为唯一解析输入，使用 `--require-hashes` 生成完整 wheelhouse，并在网络隔离环境安装。
`build/release/scripts/audit-agent-supply-chain.py` 已把该边界变成机器门禁：固定双上游
commit 与 Runtime 三套版本事实，扫描时只披露命中文件名；正式候选以
`--require-ready --expected-arch <包架构>` 要求 V11 宿主重建、Runtime 离线
wheelhouse、SPDX SBOM 和 NOTICE
同时有效。证据不能仅靠布尔声明：宿主产物、完整对应源码和构建日志，Runtime 全量
wheel、锁文件和离线安装日志均须提供路径与匹配摘要；目标 OS/架构/Python ABI 必须
显式记录。SPDX 2.3 覆盖固定宿主、Runtime 及 wheelhouse 全部包，NOTICE 复述固定
来源、commit 和许可证边界；当前强制报告为 `ready=true`、零 blocker。上游原始仓库
中被识别的认证式 URL 不进入发行源码或入包范围，实际发行范围复扫通过。
Runtime 离线闭包必须包含免密 `ddgs` 搜索后端；验收要求 `web_search` 返回真实结果，
不能只以工具注册或“未配置搜索 provider”的错误响应计为成功。固定上游的 setuptools
配置会遗漏 bundled plugin 清单，故发布适配必须把 `plugins/**/plugin.yaml` 纳入 wheel；
离线安装门同时验证 DDGS 清单存在、插件加载和活动 provider 解析，缺一即拒绝候选。
安装和升级必须对哈希锁闭包执行强制重装，确保上游版本号不变时也不会保留旧 wheel
内容；该动作仍禁止访问索引或退回用户可变 Runtime。
用户态集成事务在文件和配置切换后必须显式重启包管 Gateway；`enable --now` 不能替代
重启，因为已运行服务会继续持有旧模块。重启失败须恢复 Provider、配置和原用户 unit。

宿主界面依据 [ADR-0004](decisions/0004-maintain-kylin-agent-visual-accessibility-patch.md)
及 [ADR-0005](decisions/0005-use-kylin-system-cloud-models.md) 作为同一供应链的顺序
补丁应用，不修改官方 submodule。发布证据必须使用应用
双主题语义令牌、PIXIU 产品层级和无内部路径空状态后的真实二进制；旧截图立即失效。
[正式截图素材](delivery/assets/operations/截图素材索引.md)现有 38 张 0.1.9 V11 实拍，
逐张检查后记录来源提交、文件摘要和使用范围，并纳入手册与项目报告。停止后的
状态文案存在问题；多设备、图形升级回滚、工具授权及部分异常场景仍待补齐。
截图数量不能代替下面状态矩阵的完整验证。
最终候选补充 V11 浅/深主题、100%/125%/150% 缩放、键盘焦点、模型下拉、长会话、
流式/停止/错误/禁用态矩阵，并保留自动复算关键配色 ≥4.5:1 的测试结果。
宽屏消息必须保持在 920px 居中阅读列内连续排列，输入组合区不超过 960px；短消息不得
被压成窄列，末条消息不得被输入区裁切。宿主构建记录必须匹配当前 release commit 和
全部下游适配输入 SHA-256，防止新候选复用旧界面二进制。
同版宿主必须默认优先展示“麒灵系统云模型”，并通过系统 Kylin GenAI SDK 跟随
“AI 模块管理”的模型选择与授权。备用选择面只保留 DeepSeek/Anthropic/OpenAI 官方
直连服务；API Key 在 GUI 密码框导入、连接检测后由 Runtime 以本用户 `0600` 文件保存，
列表、日志和证据只显示配置状态。OpenRouter 与全部本地推理提供商必须缺席。

## 2. 版本管理唯一真相源

2026-09-06 流水线审计：远程 `v0.1.7` 仍是 `0.1.7-1` 通用画像资产，
不能代表本地 `0.1.7-3` 严格原生 Agent 包。CI 基线调整为 Ubuntu 24.04 /
Python 3.12（后端另测 3.13），与 cp312 打包目标一致。官方 Gitee 子模块继续
使用已提交 gitlink，增加按四个固定提交生成缓存键的 Git 对象缓存、有限重试和
恢复后版本校验；仅允许维护者手动在 main 上通过麒麟 runner 的只读镜像预热缓存。
PR 不进入自托管 runner。后续发布必须由同一候选提交自动重建，不能把旧包改名发布。

原生工作流提供可复用构建：在 V11/amd64/cp312 检查后，使用网络命名空间隔离
宿主构建和 Runtime 离线安装验证，先保存供应链实物，再打包严格画像；安装后运行
用户级服务、provider 激活和真实双 SDK 生命周期取证。标签发布同时要求通用 CI
与原生作业成功，再在 GitHub 托管执行器验证原生证据并生成 Ed25519 六件套。
签名密钥不传给自托管构建作业。手动 main 执行只归档候选，不创建生产 Release。
原生安装门对候选执行 `apt-get install --reinstall`：同产品版本重跑也必须替换实物，
避免 APT 以“已是最新版本”为由跳过安装后误验旧 commit；仍不自动允许版本降级。
该产品发布门与下文赛事最终三机/性能验收门分别记录，不能混称全部验收完成。

采用 SemVer `MAJOR.MINOR.PATCH`，Debian 包版本为 `MAJOR.MINOR.PATCH-REVISION`。
发布标签、CMake `applicationVersion`、Debian control、Release 资产与更新清单必须
由一个发布版本输入派生；CI 在不一致时失败。
前端根 CMake 仅构建保留的回归测试，不再生成或安装独立小窗口程序；包体中的
桌面文件和图标由统一打包脚本安装，正式应用来自 Agent 宿主导出构建。旧浮球
首击脚本已删除，其历史结果不能作为当前桌面入口验收。

最终版本清单至少包含：

- PIXIU 产品版本、Git commit、构建时间、目标架构与 V11 画像；
- 数据库 schema 版本、API 版本、Module E/provider 版本；
- 兼容的 `kylin-agent`/`agent-runtime` 固定版本范围；
- Embedding/Vector Engine SDK 及运行时版本；
- 每个资产的大小、SHA-256、独立数字签名和发布通道。

图形“关于/更新”页必须展示当前版本、可用版本、通道、组件兼容状态和发行说明。
不得只在多个源码文件手工重复版本号。当前 `0.1.8` 发布预检已同步校验根
`VERSION`、管理库宏、统一宿主的版本注入补丁及 Module E manifest；不再以旧小窗口
的 CMake/main.cpp 作为发布版本证据。预检回归在不包含旧入口的隔离源码中通过，
管理库宏或宿主应用版本被改成硬编码时必须失败。构建现生成包内只读
`/usr/share/pixiu/release-manifest.json`，从源码和构建输入记录产品/Debian、Git、
构建时间、架构/profile/KYSDK/Python ABI、API/schema/provider、Agent 上游及双 SDK
源码钉住版本与许可证；CI 会从成包反向提取并核对。上游 runtime 的 `version` 文件
与包元数据当前分别为 0.9.9/0.9.8，清单保留两项事实而不擅自归一。仓库根
`VERSION` 已成为发布脚本和前端 CMake/独立 control 的权威输入，环境变量只能作
一致性断言；Module E 源码只保留 `plugin.yaml.in`，打包/激活时从同一输入生成
`plugin.yaml`。发布工作流与 `publish.sh` 已为每个架构生成无自引用的包外
`.assets.json`，记录 `.deb`/checksum/signature 的大小、SHA-256、commit 和通道，
生成前以固定公钥验证主包签名，并为清单自身生成 checksum + Ed25519 签名；最终
commit/版本/架构从包内组件清单与 dpkg control 交叉核对，且 JSON 记录规范化生成
命令。最终候选仍须归档并复验这六件套；
还须在目标银河麒麟 V11 环境记录 Embedding/Vector SDK 的实际安装包版本、运行时
探测结果及 `/capabilities` 一致性。原生取证器已自动化绑定候选包摘要/commit、已装
manifest、dpkg 版本、Agent runtime 和三个端点，并以独立临时数据库和隔离集合直接
执行 SDK 的装载、集合、向量及断开生命周期。当前审阅候选已生成真实通过输出并绑定
候选 SHA；文档冻结后仍需对最终 commit 重跑，源码 gitlink不能代替实装证据。

三设备证据采用两级门禁：`three-device-evidence.py capture` 必须在各节点本地复用
已经通过的 strict 原生证据并读取 loopback 同步 API，只导出加盐身份/域摘要和版本、
候选包摘要、同步计数；`validate` 汇总恰好三份清单，拒绝跨 run、版本漂移、重复身份、
非完全图、离线/积压或超过 300 秒的采集窗口。它只证明三台 V11 的同版全连接拓扑，
固定 `final_device_evidence=false`；五项跨设备业务场景及最终逻辑视图收敛仍是发布硬门。
并发更新已有第二级机器门禁：三台设备各采集 baseline/diverged/converged，要求两个
暂停节点从同一版本产生不同 v+1 分支、观察节点不变，恢复后全部在线静默且逻辑视图
一致。单节点离线重连使用另一组 3×3 检查点，要求隔离端保持基线、其余两端仍可写入
并传播 v+1、恢复后三端追平。私域场景再用 6 份检查点验证只在写入端可见且同步
确认数不增长。三类工具当前 10/10 测试通过，但未输入真实三机数据，不能减少发布
阻断项。墓碑场景另以 12 检查点绑定可见性和去载荷 CRDT 状态，验证离线旧副本重连
及额外反熵后仍不复活；四类工具当前 12/12 通过。正文、查询、作用域、知识/操作 ID
和设备标识始终只进入加盐摘要。
最终 `validate-final` 还必须绑定初始拓扑、四类报告和由三份新节点清单生成的收尾
拓扑，核对同 run/版本/身份/域与时间边界；只有该总报告可以标记最终证据。工具测试
现为 14/14，但尚无真实输入。

## 3. 图形界面一键升级

升级特权边界与无特权重启脚本统一维护在 `build/release/debian/usr/lib/pixiu/`，
整包构建将其安装到同名 `/usr/lib/pixiu/` 路径。旧 `frontend/scripts/` 副本已移除，
唯一宿主继续通过原安装路径调用，无参数协议变化。独立前端包构建脚本和 Debian
元数据已删除，构建统一走 `make -C build/release build-deb`；包内旧可执行程序仍待迁移完成后删除。

目标流程：设置 → 检查更新 → 显示版本与发行说明 → 用户点击“一键升级” → 下载 →
校验签名/摘要 → 系统授权 → 原子安装/迁移 → 健康检查 → 受控重启或明确恢复。

发布门：

1. 只选择当前架构与通道的正式资产，拒绝预发布、降级和不兼容组件。
2. HTTPS 之外还要验证独立签名；同一 Release 中的 SHA-256 只证明传输完整性，
   不能单独证明发布者身份。
3. 安装前备份必要配置和 schema 状态；升级失败可恢复上一可用版本，且不丢记忆。
4. `dpkg` 阶段不可强杀；安装后执行包版本、服务/schema/provider 健康检查，全部
   通过才允许 GUI 报告成功。
5. 图形界面完整呈现下载、校验、授权、安装、重启、取消、失败和恢复状态。
6. 更新 PIXIU 自有包，不静默升级上游 Agent 或系统 SDK；兼容性不满足时明确阻断。

当前 `UpgradeController` 已实现版本比较、按架构选择 `.deb`、流式 SHA-256、强制
`.sha256.sig` 资产、重定向白名单、`pkexec` + root-only 二次校验与错误分类；发布
工作流以受保护 Secret 中的 Ed25519 私钥签名，特权 helper 用包内固定公钥在 dpkg 前
验签。helper 还会验证包名/版本/
架构、dpkg 实际安装版本、后端 `/version`、数据库 `/health`/schema 和包内 Provider
版本，健康失败以专用状态返回且不会误报成功。Module E 随包升级会保留用户显式配置，
并在桌面用户上下文重新激活。双架构签名 CI 与 Kylin V11 有效/篡改签名已通过；临时
Ed25519 密钥和两个真实 `.deb` 已完成“旧钥验过渡包、过渡包部署新公钥、新钥验下一包、
旧钥拒绝下一包”的无私钥入库轮换演练。最终仍需补完整 Module E 支持版本矩阵。
自动回滚现已实现 `dpkg-repack` 旧包重建、停服 SQLite 一致性快照、配置备份及失败后
恢复旧包/数据/服务，并区分恢复成功与恢复失败。提交 `ca35117` 的双架构 CI 和
Kylin V11 amd64 跨 revision 健康失败注入均已证明退出码 5、旧版本/配置/数据/服务
恢复；最终候选仍须随完整安装矩阵重验。GUI 成功态的“立即重启”现通过无特权 helper
等待旧客户端退出、再启动新版本；失败可重试或手动打开，安装中不会触发。源码和打包
测试已覆盖，最终 V11 图形升级/重启取证仍待完成。

## 4. 赛事交付文档台账

`submission/` 按第 0 节保留两层同名目录。内层只放 `项目报告.pptx`、
`技术方案.doc` 和制作完成后的 `演示视频.zip`；与内层并列的 `源代码/` 保存完整源码。
技术方案汇入用户手册、安装部署、测试结果和效果验证报告。编写材料、原图和内部
检查记录在 `docs/delivery/` 维护，中间导出在 `build/release/out/`，不进入提交目录。

2026-09-09 用户对视频制作另行授权：视频工程、原始截图、录屏、解说和中间素材放在
`submission/video-production/`，位于正式两层同名目录之外。该工作目录不是额外必交
作品，不改变 §0；最终仅把 `演示视频.zip` 补入既有内层目录。

文档导出、源码生成和目录检查命令见 [材料维护说明](delivery/README.md)，检查结果见
[材料检查记录](delivery/PREPARATION_CHECK.md)。各项功能和性能结论仍以真实证据判定。

| 交付作品 | 份数及格式 | 内部维护源 | 状态 |
|----------|------------|------------|------|
| 项目报告 | 1 份，ppt；目录示例 `项目报告.pptx` | `delivery/PRESENTATION_AND_VIDEO.md` | 已整理 12 页并逐页观察；同版量化证据仍待补 |
| 技术方案 | 1 份，word、pdf；目录示例 `技术方案.doc` | `delivery/TECHNICAL_SOLUTION.md` 及同目录测试、用户手册、部署、流转、案例和适配内容 | 已合并八篇内容；实际结果与限制如实列出 |
| 功能演示视频 | 1 份，5-10 分钟，`演示视频.zip`，大小不超过 200M | `delivery/PRESENTATION_AND_VIDEO.md`、`submission/video-production/` | 第十九版中文初剪已生成，9 分 21 秒、约 57.1 MB；同步检查点、历史量化表、高清偏好堆叠、非标题搜索、洞察来源链、跨端遗忘检查点、设备信任及共享边界控制已入片，整片解码通过；洞察日期字号与来源时序通过有限复核，仍有功能/模板细节/视听余项，正式 ZIP 待完成 |
| 源代码 | 1 份，完整源代码及必要技术规范要素 | `delivery/SOURCE_AND_LICENSES.md` 与项目源码 | 按当前源码及四个固定上游生成，逐文件核对 |

提交作品匿名；团队信息只用于平台信息核对与两层目录命名。内部保留版本和审核追踪。

视频行为来源已核对运行中服务的数据库与公共接口：公开X11窗口生成了焦点118秒、
质量0.715的USER_BEHAVIOR证据。此前未落库判断源于检查另一份同名数据库，
已保留原记录并更正；原生检索与来源正文画面已补录，行为来源及关闭开关已整合到第18版，不扩展为Wayland原生采集验收。
跨端遗忘已新增章节样片，按原始接口记录呈现断连保留、重连失效及三端删除标记一致；
末段证据范围修正已通过有限独立复核并接入第十三版全片，不作最终视频放行。
设备页暂停/恢复已补原生4K录屏与状态回读，一侧解除与原生配对恢复已核对结果，
双方节点集合恢复；相关章节已通过有限独立复核并接入第十四版全片，
不能据此宣称本次已验证有待发数据时的传输抑制或重新配对后的实际数据传输。
共享边界新增完整私有/敏感 API 请求记录与原生控制章节；讲稿已按复核意见收窄到
具体条目未进入同步状态，不能扩展为所有磁盘同步日志审计。

来源发牌章节已按模板制作，使用真实裁片并明确复用；新增清洗前后、质量与同来源
检索的公共接口实测记录。06章节通过有限独立复核并整合到第17版；全片解码、
新增配音起点和三帧工作台一致性检查通过，完整功能及视听终检仍待完成。

生命周期补录已核对短期/中期各一条晋升、同一知识与证据ID及完整摘要复用；
这是公共接口合成事件实测，晋升章节已通过有限复核并整合到第19版，整片解码与该段波形起点核对通过；宿主自动触发另行核对。
四类知识长正文及原生检索/来源截图已取证，类型经只读核对；第20版接入四类真实摘录及完整四步流程正文，裁片残字经有限独立复核确认修复。
第20版为571.157333秒、57769283字节，完整解码、实际抽帧及新增/顺延两段配音起点核对通过；不代表自动执行验证或全片听审通过，完整任务闭环和最终交付检查仍待完成；语义冲突审计随后已取得同实体3→4更正、来源与NEW_WINS真实记录及原生页面，24秒章节已通过有限独立复查、解码及配音起点检查，待下版整片整合与终检。

完整助手任务补录已取得网关工具闭环和原生文件任务、显式记忆保存、新原生会话同来源复用。
原生联网搜索失败与错误官网声明保留，成功搜索来自另一网关样本；已明确分段剪成25秒任务章节，完整解码、类型与波形同步检查通过，
独立章节审查的页脚尺寸问题已由04修正，有限复核通过；待下一版全片整合和最终听审。
演示机补齐DDGS可选依赖，不代表发布包及最终原生性能验收已完成。

服务、录入、目录采集、遗忘确认/取消与边界、安装/更新六个原生镜头已放大并完成
有限复核。第21版为575.146667秒、47925870字节，解码和新任务段波形起点核对通过；
包含取消预览补充的第22版已导出，575.146667秒、47938841字节，冻结源码摘要一致；
全片独立终检进行中，尚未生成正式视频ZIP。

## 5. 作品材料核对与产品验证边界

作品材料核对以 §0 为准：四项作品齐全，项目报告内容完整，技术方案包含用户手册、
测试结果及效果验证报告，视频覆盖所有已实现核心功能并满足时长、zip 与大小要求，
源代码完整且含必要技术规范。目录命名与平台一致，作品内容不含可识别身份信息。

产品的 V11、双 SDK、Agent、三设备、性能、安装及升级验证继续按验收规范执行。
原始日志、数据、安装包、签名、构建记录及内部审核信息用于支撑真实测试结论，
不另列为赛事必交实物；测试结果与量化分析写入技术方案，记忆流转与适配结论写入项目报告。

日常与 CI 使用 `make -C build/release governance` 校验赛方文档摘要和 Git 工作区洁净度。
文档、源码与目录由材料生成工具核对；该检查不替代产品工程验收。

文档更新兼容记录（2026-09-08）：`final-dataset-manifest.py` 仍硬编码更新前的赛题文档摘要，
与本次文档摘要不一致，当前不能用该工具生成或验证新的数据集记录。本次限定只更新文档，
未修改工具代码；文档摘要清单已同步，不能将其核对通过写成数据集工具已兼容。

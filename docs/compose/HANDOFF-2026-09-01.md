# PIXIU 工作交接文档

> 日期：2026-09-01 · 状态：🟡 0.1.6 本地门禁完成，等待远端双架构与麒麟真机验收
> 初始生成原因：用户要求暂停并记录状态；15:14 用户已将仓库转为公开，
> 当前由代理继续完成公开操作之外的修复、开发与本地发布验证。

## 0. 恢复后的最新状态（15:14 以后）

- ✅ 仓库已由用户转为 PUBLIC；匿名仓库 API、`releases/latest`、v0.1.5
  `.deb` 分段下载与 `.sha256` 下载均实测可达。
- ✅ 已修复安装包占位口令启动回归：核心 API 不再被无效同步配置拖垮；
  `postinst` 生成每机随机口令，旧公开口令对应的 Ed25519 私钥原地重加密，
  保留设备 ID、peer、CRDT 状态和配对关系；配置权限收紧为 0640。
- ✅ 升级器已加固：架构匹配资产、分块 SHA-256 + 文件名绑定、每跳重定向
  白名单、唯一临时文件、安装启动失败诊断、安装期间禁止强制取消/关闭。
- ✅ 版本源已统一到 0.1.6；Release 工作流准备原生 amd64 + arm64 双资产。
- ✅ 前端 OFF 路径 36/36、后端全量 681/681 通过；新增口令配置/迁移回归
  46/46 通过；后端有 3 条既有依赖/aiosqlite 资源回收 warning，不影响退出码。
- ✅ `0.1.6-1 amd64` 麒麟画像 `.deb`（含离线 cp312 wheels）构建完成；
  最终 SHA-256 为 `43ee84001a09698062525a89146d1d2b010852ffe5e98cdf124ecd3fd9d8e721`；
  Version/Architecture/Depends/Suggests、配置 0640、迁移模块与前端可执行文件
  检查通过；desktop entry 与 offscreen 启动冒烟通过。
- ✅ 本机完成最终包的应用内同路径增量升级：特权 helper 二次校验，
  `dpkg --force-confdef --force-confold` 无交互完成，设备配置逐字节保持不变，
  systemd 服务恢复且 OpenAPI 冒烟通过；独立前端包也已确认包含同一 helper。
- ✅ 最终只读复审未发现剩余发布阻断或正确性/安全回归。
- ⚠️ 麒麟测试 VM `192.168.122.197:22` 连续两次在握手阶段关闭连接，故目标机
  增量安装、systemd/API 与应用内升级仍未执行；本机无 KYSDK 开发包，不能把
  `KYSDK=OFF` 结果报告为原生 SDK 验收。
- 当前修复仍在工作区，下一步按模块形成可审查本地提交；严禁 push。

---

## 1. 当前位置

**工作区**：`/home/pluto/Project.PIXIU`（麒麟 V11 主机，真实 UKUI 会议话，main 分支）
**本地 HEAD**：`b45a7fe`（领先 origin/main **11 个提交，均未推送**）
**工作区**：🟡 有本轮 0.1.6 发布前修复与测试变更
**四分支远端同步**：`main / feat/foundation / feature/frontend / feature/engine` 全在 `18a0211`

**⚠️ 关键：本机 HEAD（`b45a7fe`）领先 origin/main 11 个提交；本轮新增修复
尚未提交，全部远端发布动作仍由 Human 执行。**

---

## 2. 未推送的 11 个提交（当前工作核心 = 一键升级）

| 提交 | 内容 | 模块 | 门禁状态 |
|---|---|---|---|
| `e4db3c6` | 一键升级设计 spec + 计划文档 | docs | ✅ 已提交 |
| `3789159` | **U-1** 版本工具 + sha256 校验（UpgradeUtils，`ui::` 命名空间） | frontend | ✅ 规格 6/6 + 质量 |
| `cc63b27` | **U-2** UpgradeController 检查/下载/校验/安装状态机 | frontend | ✅ 规格 8/8 |
| `456ef97` | U-2 修复：GitHub 302 重定向、15s 超时、源 URL 校验、FA 一致性 | frontend | ✅ 复审 |
| `67aad9c` | 计划文档命名空间回写 pixiu→ui | docs | ✅ |
| `7c6dd44` | **U-3** CheckUpdateDialog 一键升级 UI + PixiuApp 接线 | frontend | ✅ 规格 8/8 |
| `4c2bdc7` | U-3 修复：状态残留、open() 遮蔽、FailedReason 枚举 | frontend | ✅ 复审 |
| `083f009` | **U-4** i18n 收编 25 条新文案（323 条 0 unfinished） | frontend | ✅ 规格 4/4 + 质量 |
| `945ef01` | pixiu.env 口令改占位符 | release | ✅ 质量 |
| `fc1b139` | **安全 guard**：后端拒绝占位符口令 | backend | ⏳ 规格审查被中断（子代理已取消，审查未完成） |
| `b45a7fe` | 工作交接快照 | docs | ✅ 已提交 |

**U-1..U-4 四任务本身已全部通过门禁**（实现 + 规格 + 质量 + 复审）。唯一未闭环的是 `fc1b139` 的 guard 规格审查（被本次暂停打断）。

---

## 3. 一键升级已实现的功能（未发布）

完整链路已就绪（U-1..U-4），全在前端 + 后端 config：
1. **检测**：`UpgradeController` 独立 QNetworkAccessManager 拉 `GET https://api.github.com/repos/PlutoKeating/Project.PIXIU/releases/latest` → `ui::compareVersions` 对比远程 vs 本地 `applicationVersion`
2. **下载**：有界流式下载到唯一临时文件（QSaveFile 原子写）→ 分块 SHA-256 + 资产文件名绑定校验
3. **安装**：`pkexec /usr/lib/pixiu/install-update <deb> <sha256>`；特权 helper 将下载复制到 root-only 文件并二次校验，再由非交互 `dpkg` 保留设备配置完成安装 → 成功提示手动重启
4. **安全**：HTTPS + SHA-256 双校验、每跳 GitHub host 白名单、大小限制、特权边界二次校验、15s 超时、`FailedReason` 枚举解 i18n 耦合
5. **UI**：CheckUpdateDialog 一键升级（当前/远程版本、状态环、进度条、检查/一键升级/取消按钮），PixiuApp 懒创建共享 controller + `setUpgradeControllerForTest`
6. **后端安全**：同步属性拒绝公开占位符但核心 API 可降级启动；安装时以可恢复 journal 生成随机口令并无损重加密既有身份

**测试**：前端 36/36 ctest 全绿；后端相关 89 passed（config/sync/di/sync_settings 等）。

---

## 4. 发布待办（未执行）

一键升级要真正上线，还需：
1. **`fc1b139` 补门禁**：规格审查 + 质量审查（被暂停打断），通过后闭环
2. **转公开 repo**：`gh repo edit PlutoKeating/Project.PIXIU --public`（当前是 PRIVATE——一键升级前端依赖公开 GitHub Releases 匿名可达；这是用户此前明确决策「转公开 repo」，但因不可逆，**执行前应再确认**）
3. **bump 版本**：0.1.5 → 0.1.6（三处版本源：`frontend/CMakeLists.txt`、`build/release/scripts/functions.sh`、`frontend/debian/control`）——注意 V-1 预检会强制 tag 与三处同步
4. **打 tag** `v0.1.6` → 推送 → 触发 GitHub Actions release 流水线
5. **验证**：CI success、Release 非 draft、deb+sha256 资产上传；真机 `dpkg -i` 升级 + 应用内「检查更新」一键升级实测

---

## 5. 已发布的历史版本（在 origin 中）

| 版本 | 内容 | 状态 |
|---|---|---|
| `v0.1.2` | 版本管理+关于/更新/条款/隐私页面批次 | ✅ CI 发布上线 |
| `v0.1.4` | 深色主题按钮可读修复 + 问候语动态化 | ✅ CI 发布上线（SHA 0554e18d） |
| `v0.1.5` | 补丁：标题换行 + GECOS local8bit | ✅ CI 发布上线（SHA 6dd9966b） |

深色修复（acbcc78a）、问候语（ec1913a）已在 origin/main 中（被 `18a0211` 之前包含）。

---

## 6. 关键背景与约束（勿违背）

- **模块边界**：`frontend/` 只改前端，`backend/` 只改后端；批次②③④都已完成。改 backend 前需用户确认（项目规则）。
- **版本管理宗旨（用户核心规则）**：每次发布严格版本增量（CMakeLists/functions.sh/debian control 三处同步）+ .deb.sha256 一致性 + 旧版可 dpkg 增量升级（postinst venv 复用 + conffile 幂等追加，T24 已修）。
- **产品名**：PIXIU（貔貅为中文名/文化意象）；**问候语不得把用户称貔貅**（欢迎页用 `ui::displayUserName()` 动态取系统全名）。
- **禁 push**：代理未经授权不得 push；main 是权威集成分支。
- **转公开前安全前置**：已完成敏感审计（无真实凭据）；后端 guard（`fc1b139`）已加，转公开前须补门禁。

---

## 7. 任务追踪器状态

- **已完成**：T1..T79 全部 done（批次①掌控层、②目录监视、③行为偏好、④递送层、版本管理、About 页面、深色修复、问候语、一键升级 U-1..U-4 全部完成）
- **进行中**：无（所有工作子代理已停止）
- **有诡异残留**：T35「SN-1 discovery」标记 in_progress——这是历史幽灵任务（SN 批次早已完成），task DB 残留，**非当前工作**，可安全忽略/归档。

---

## 8. 建议的下一步（恢复工作时）

按顺序：①补 `fc1b139` 门禁 → ②用户确认转公开 repo → ③bump 0.1.6 → ④打 tag 发布 → ⑤真机验证一键升级。若用户决定不转公开，则一键升级需改为「经后端源」或「可配置源 URL」方案（spec S1 已记载备选）。

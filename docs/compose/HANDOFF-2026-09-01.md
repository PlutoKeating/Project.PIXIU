# PIXIU 工作交接文档

> 日期：2026-09-02 · 状态：🟢 v0.1.6 已公开发布；本机应用内升级与 KYSDK=ON 桌面路径已验收
> 初始生成原因：用户要求暂停并记录状态；15:14 用户已将仓库转为公开；
> 随后人类显式授权 push 与发布。v0.1.6 已于 2026-09-01 20:08 CST 发布。

## 0. 恢复后的最新状态（2026-09-02 本机收口）

- ✅ 仓库 PUBLIC；`releases/latest` 四资产匿名可达。
- ✅ 安装包占位口令不再拖垮核心 API；每机随机口令 + 身份重加密。
- ✅ 升级器加固并随 v0.1.6 发布（架构资产、SHA-256 文件名绑定、特权 helper）。
- ✅ **本机应用内升级实测通过（2026-09-02）**：真实 GitHub `releases/latest`，
  `CheckUpdateDialog` 发现 `0.1.6` 后一键升级；下载/校验/安装约 195s。
  本机已是 `0.1.6-1`，故把本地 `applicationVersion` 压到 `0.1.5` 才会走
  Updatable（生产 UI 对已是 latest 的客户端显示「已是最新」）。安装走与生产
  相同的 `/usr/lib/pixiu/install-update <deb> <sha256>`，无人值守用
  `sudo -n`（未弹出图形 `pkexec`）。安装后仍为 `pixiu 0.1.6-1`，
  `/etc/pixiu/pixiu.env` SHA-256
  `aacbced7408042adae56d419ad1101370537942ee55eb7efc6e4df89536ea245` 不变，
  `device_id=dev_01M0SKE5R2TA3R52EB4H3SJTM6` 不变，`pixiu-backend` active，
  OpenAPI 可达。
- ✅ **本机 `PIXIU_HAVE_KYSDK=ON` 桌面路径**：pkg-config 找到
  kysdk-shortcut/notification/qtwidgets；ctest **37/37**；offscreen 冒烟见
  UKUI 暗色主题跟随、圆角阴影、麒麟全局快捷键 `Ctrl+Alt+P`，并连上本机后端。
  这是桌面 SDK 验收，**不是** coreai embedding 召回率验收。
- ✅ 本机已构建 `_kylin_text_embedding` / `_kylin_vector_client` / `_kylin_ocr`
  原生扩展（gitignored）。`PIXIU_EMBEDDING=kylin` **fail-closed**：
  `kylin-ai-runtime` 无法建立
  `/tmp/.kylin-ai-runtime-unix/1000/core-textembedding.sock`；直接启动二进制
  报 `undefined symbol: VectorDB::ConnectParam`。`auto` 正确降级到
  `PortableTextEmbedding`。不得把 portable 结果报成麒麟 embedding 验收。
- ⚠️ 测试 VM `192.168.122.197:22` 端口可达，但 `ssh` 在握手后被对端关闭
  （`Connection closed by 192.168.122.197 port 22`）。目标机增量安装仍未执行。

---

## 1. 当前位置

**工作区**：`/home/pluto/Project.PIXIU`（麒麟 V11 主机，UKUI 会话，main）
**发布 tag^{}**：`0e5d5e2`（不要因文档或验收 harness 移动 `v0.1.6`）
**发布页**：https://github.com/PlutoKeating/Project.PIXIU/releases/tag/v0.1.6

**产品发布已上线。** 本机剩余仅：coreai 运行时 ABI/套接字、以及可登录的测试 VM。

---

## 2. 已推送并纳入 v0.1.6 的关键提交（当前工作核心 = 一键升级）

| 提交 | 内容 | 模块 | 门禁状态 |
|---|---|---|---|
| `e4db3c6` | 一键升级设计 spec + 计划文档 | docs | ✅ 已提交 |
| `3789159` | **U-1** 版本工具 + sha256 校验（UpgradeUtils，`ui::` 命名空间） | frontend | ✅ 规格 6/6 + 质量 |
| `cc63b27` | **U-2** UpgradeController 检查/下载/校验/安装状态机 | frontend | ✅ 规格 8/8 |
| `456ef97` | U-2 修复：GitHub 302 重定向、15s 超时、源 URL 校验、FA 一致性 | frontend | ✅ 复审 |
| `67aad9c` | 计划文档命名空间回写 pixiu→ui | docs | ✅ |
| `7c6dd44` | **U-3** CheckUpdateDialog 一键升级 UI + PixiuApp 接线 | frontend | ✅ 规格 8/8 |
| `4c2bdc7` | U-3 修复：状态残留、open() 遮蔽、FailedReason 枚举 | frontend | ✅ 复审 |
| `083f009` | **U-4** i18n 收编 25 条新文案（323 条 0 unfinished） | frontend | ✅ 规格 4/4 + 质量 |
| `945ef01` | pixiu.env 口令改占位符 | release | ✅ 质量 |
| `fc1b139` | **安全 guard**：后端拒绝占位符口令 | backend | ✅ 已由可降级配置与安装迁移替代 |
| `b45a7fe` | 工作交接快照 | docs | ✅ 已提交 |
| `4de9e57` | 可恢复的每机同步口令配置与身份重加密 | foundation | ✅ 46/46 |
| `5f647a7` | 架构感知、有界下载、特权二次校验的一键升级 | frontend | ✅ 36/36 + E2E |
| `14c42ca` | 0.1.6 安全安装与 amd64/arm64 发布门禁 | release | ✅ |
| `90eedc0` | 升级源拒绝用例改为架构中立 | frontend | ✅ |
| `47970ea` | 发布安装门禁使用绝对路径 | release | ✅ |
| `6191902` | 校验清单只绑定资产 basename | release | ✅ |
| `0e5d5e2` | KYSDK=OFF 发布不拉取 Gitee SDK submodule | release | ✅ |

---

## 3. 一键升级已实现并随 v0.1.6 发布的功能

完整链路已就绪（U-1..U-4），全在前端 + 后端 config：
1. **检测**：`UpgradeController` 独立 QNetworkAccessManager 拉 `GET https://api.github.com/repos/PlutoKeating/Project.PIXIU/releases/latest` → `ui::compareVersions` 对比远程 vs 本地 `applicationVersion`
2. **下载**：有界流式下载到唯一临时文件（QSaveFile 原子写）→ 分块 SHA-256 + 资产文件名绑定校验
3. **安装**：`pkexec /usr/lib/pixiu/install-update <deb> <sha256>`；特权 helper 将下载复制到 root-only 文件并二次校验，再由非交互 `dpkg` 保留设备配置完成安装 → 成功提示手动重启
4. **安全**：HTTPS + SHA-256 双校验、每跳 GitHub host 白名单、大小限制、特权边界二次校验、15s 超时、`FailedReason` 枚举解 i18n 耦合
5. **UI**：CheckUpdateDialog 一键升级（当前/远程版本、状态环、进度条、检查/一键升级/取消按钮），PixiuApp 懒创建共享 controller + `setUpgradeControllerForTest`
6. **后端安全**：同步属性拒绝公开占位符但核心 API 可降级启动；安装时以可恢复 journal 生成随机口令并无损重加密既有身份

**本机复测**：`frontend/tests/t_upgrade_live.cpp` + `frontend/scripts/live-upgrade-accept.sh`。
KYSDK=ON ctest 37/37（无 `PIXIU_LIVE_UPGRADE` 时 live 用例立即返回）。

---

## 4. 发布后续

已完成：公开仓库、v0.1.6 tag、双架构 Release、本机 GitHub 应用内升级、
本机 KYSDK=ON 桌面路径。

仍未关闭（环境，不是未实现功能）：
1. 测试 VM SSH 握手被对端关闭，无法做第二台麒麟机增量安装
2. `kylin-ai-runtime` 本机无法提供 embedding socket；严格 `PIXIU_EMBEDDING=kylin` 正确失败
3. 图形会话里点「一键升级」弹出的 `pkexec` 授权框（本机无人值守用了 `sudo -n`）

不要把 v0.1.6 tag 挪到文档/harness 提交上。需要下一产品增量时再发 **0.1.7**。

---

## 5. 已发布的历史版本（在 origin 中）

| 版本 | 内容 | 状态 |
|---|---|---|
| `v0.1.2` | 版本管理+关于/更新/条款/隐私页面批次 | ✅ CI 发布上线 |
| `v0.1.4` | 深色主题按钮可读修复 + 问候语动态化 | ✅ CI 发布上线（SHA 0554e18d） |
| `v0.1.5` | 补丁：标题换行 + GECOS local8bit | ✅ CI 发布上线（SHA 6dd9966b） |
| `v0.1.6` | 应用内安全升级 + 每机同步口令 + amd64/arm64 | ✅ CI 发布上线（`0e5d5e2`） |

---

## 6. 关键背景与约束（勿违背）

- **模块边界**：`frontend/` 只改前端，`backend/` 只改后端。
- **版本管理宗旨**：每次发布严格版本增量（CMakeLists/functions.sh/debian control）+ checksum 文件名绑定 + 旧版可 dpkg 增量升级。
- **产品名**：PIXIU；问候语不得把用户称貔貅。
- **push**：main 仅在用户最新显式授权下推送；禁止未二次确认的 force push。
- **KYSDK 汇报**：桌面 `PIXIU_HAVE_KYSDK=ON` 与 embedding `PIXIU_EMBEDDING=kylin` 必须分开写。

---

## 7. 任务追踪器状态

- **已完成**：T1..T79；一键升级 U-1..U-4；本机 GitHub 升级验收；本机 KYSDK=ON 桌面路径
- **进行中**：无
- **环境阻塞**：VM SSH；kylin-ai-runtime ABI/socket

---

## 8. 建议的下一步

修复 `192.168.122.197` SSH 后做第二台增量安装。修复或启动可用的
`kylin-ai-runtime` 后再做 embedding 召回率/P95 严格验收。图形桌面下用已装
`pixiu-frontend` 点一次 `pkexec` 授权升级（需有比 latest 更高的远程版本，
或接受「已是最新」）。

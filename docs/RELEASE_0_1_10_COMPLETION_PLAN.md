# 0.1.10 阻塞项完成与发布记录

2026-09-10，用户明确授权完成此前审查列出的全部阻塞项、发布新版本并同步文档。继承 SOURCE_MIGRATION_AND_PRODUCT_PLAN.md 的目录结构与授权边界；不新增产品范围，不改 DELIVERY_PLAN.md §0。版本仅在发布准备完成时统一更新。当前基线为 840a2e7。

| 编号 | 必须完成的结果 | 完成证据 | 当前状态 |
|---|---|---|---|
| R1 | 最新宿主、Runtime、记忆服务装入同一 V11 包；停止使用旧演示服务 | 新包清单、schema、健康与新接口、重启后正式用户服务 | 待完成；当前运行旧 schema 13 演示后端 |
| R2 | 图片、扫描 PDF、常见 Office 完整解码与当前真实模型处理；OCR 不提取知识 | 混合格式内容覆盖、真实模型图片账单保存/新会话金额/原图 | 开发中；复杂图形/备注覆盖和真实模型未验收 |
| R3 | 大附件与长文档分批让模型读取，按容量处理，不静默截断或假称全文已理解 | 超单次上下文文档首中尾事实问答与处理覆盖 | 部分完成；文本与视觉文档已接入分块工具，真实容量验证待完成 |
| R4 | Dreaming 支持受控创建、更正、合并及必要计划审批，主动报告结果 | 权限/版本/来源测试、真实计划审批到检索结果 | 部分完成；创建、更正审批与持久化已接线，合并已接线，真实模型验收未完成 |
| R5 | 回答内自然查看实际来源，不恢复已移除的全局按钮 | 当前回答引用与原文相符，普通回答无空入口 | 待完成；后台引用已有 |
| R6 | 最新同包双向配对、共享读取、离线恢复、并发收敛、遗忘传播 | 同版多设备场景结果 | 待完成；不能沿用旧版结果 |
| R7 | 同版新装、升级、签名拒绝及失败恢复，完整依赖随包 | 原生安装/升级记录、离线依赖检查 | 待完成 |
| R8 | 功能说明、使用手册与宣传描述一致；发布 0.1.10 | 文档核对、CI/V11 门、签名发布资产及版本链接 | 待完成 |

执行顺序：补齐 R2–R5 的必要源码 → 同提交候选包 R1 → 真实模型 R2/R3/R4 与多设备 R6 → 安装升级 R7 → 文档与发布 R8。构建准备和依赖核验可提前进行，避免每次 UI 变更重建完整包。来源以用户指定的 shots.json 为准，其他宣传制作文件不扩展修改。

GitHub #2 的旧 OCR 知识提取方案已被用户后续要求覆盖；验收目标仍是图片到记忆的 V11 同包链路。完成前不宣称 issue 已解决。发布授权不等同于远程发布门通过；不得绕过签名、通用或原生检查。

每项以真实业务结果判定，不能用编译、模拟模型或历史测试替代。未通过的项目保持未完成，不缩减本表范围。

R2 首批进展：完成常见位图统一转换与多页图像保留；20 项文档解码、3 项 API/MCP 检查通过，宿主补丁准备通过。尚未完成 Office 全覆盖、大文档、真实模型和安装包要求，不据此关闭 R2。

R4 进展：更正执行器、MCP 读取、冻结快照、逐方案宿主审批和带文档来源的版本化更新已实现；6 项 harness/MCP 与 8 项更新 API 检查通过。发现并修复否定旧金额的匹配错误。用户审批 UI、计划持久化已接线，合并尚未完成，R4 保持未完成。

R4 进展：schema v15 新增 dreaming_plans 私有表，`POST/GET /dreaming/plans` 提交与读取方案，`POST /dreaming/plans/{id}/decision` 执行批准或拒绝。后端复制已验证文档依据，审批前不更新，执行时检查目录授权及记忆版本；完成结果幂等。Runtime 未批准的更正进入 awaiting_approval，前端按待处理数量显示“查看整理方案”，列表比较并批准/拒绝，不自动弹窗。721 项基础设施回归通过；合并和真实模型同包验收仍未完成。

R2 PDF 进展：移除只渲染空白文字页或含位图页的判断，所有页面保留完整图像，补齐文字页中的矢量图表。21 项解码测试通过；其他复杂 Office 内容、容量处理及真实模型同包验证仍未完成。现有 wheel 锁及 CI 已覆盖所用依赖和测试路径，无新增构建输入。

R2 Office 进展：补充 Word/PPT 原生图表的完整 XML 数据输入，保留系列与分类对应关系及缓存数值，不执行公式和外链。复用主解析器并增加小型格式适配，无新依赖。23 项文档解码测试通过；复杂绘图、真实模型及最终安装包仍待完成。

R4 合并进展：完成整批版本检查的私人知识合并仓储与服务，保留证据、旧记录退出全文检索并清理生产向量；50 项服务/仓储检查通过。审批 API/MCP/前端现已接入，真实模型与同包验证仍未完成。

2026-09-10：Dreaming 合并已接入方案 API、MCP 和桌面审批。模型必须先读取全部目标，merge_ids 与冻结版本进入持久方案；桌面展示全部原记录与合并结果，用户批准后执行私人范围合并。旧记录转为 SUPERSEDED，来源保留，生产向量清理。模型工具不提供批准入口。无新增依赖、schema 或源目录变化，已有 CMake/宿主导出清单包含审批组件。真实模型、V11 同包验收尚未完成。

R5 进展：已提供绑定实际轮次引用的来源详情 API，并向已追踪上下文提供来源链接；读取时重查授权、有效状态及敏感级别。消息渲染器链接已接通，真实模型与 V11 同包点击验收未完成，R5 保持未完成。

2026-09-10：回答中的 pixiu://citation 来源链接由宿主补丁 0030 在应用内打开 CitationDialog，读取受核验的引用 API，显示原文和原图；普通网络链接沿用现有行为。已删除的全局来源按钮不恢复。新组件登记于 Management.cmake、宿主导出清单和供应链补丁清单，无新依赖。通用宿主编译通过；真实模型生成链接与 V11 同包点击验收仍待完成。

2026-09-10：超过两个文本块的纯文字附件不再一次拼入模型输入；保留文档暂存引用（既有 24 小时有效期），初始消息给出读取游标。Provider 新增只读 pixiu_document_read，按随机 document_id、原文件 version 和 cursor 读取单块文字并返回 next_cursor；不接受路径，不写记忆。模型需读至末块才可声称全文读取，后续问题可回读。短附件沿用现有直送；图片密集文档分批视觉输入、真实模型容量验证尚未完成。无新增依赖/构建文件，现有 Provider 与 Runtime 源码打包覆盖变更。26 项 Provider/附件/MCP 测试通过。

2026-09-10：分块附件扩展至多页图像/混合文档，pixiu_document_read 的图片结果复用上游 Runtime 多模态 envelope，每次仅传一块；工具读取时复核当前模型图片能力，文本预览不含 base64。非多模态模型拒绝纯图像文档，撤销其暂存引用。没有新依赖或上游核心补丁；已有 Runtime 工具执行器负责多模态内容传递及上下文管理。真实模型连续阅读和容量验证仍待完成。

候选准备：最新 Foundation/Engine/Agent 全套 954 项测试通过（通用 Python 环境）；VERSION 更新为 0.1.10 用于构建统一候选，这不是发布完成。V11 当前只有文本模型，真实多模态验收待具备模型连接。最终原生包、升级与发布门仍未通过。

V11 候选构建：宿主原生编译、Runtime wheel 重建与锁哈希一致、隔离网络下依赖安装和 headless 解码检查均通过。最终审计发现导出源码无 .git 时三处提交读取仍调用 Git，已统一使用 SOURCE-MANIFEST 的 sourceCommit，并拒绝非法提交格式；不跳过组件摘要与构建输入检查。需使用更新后源码继续候选审计/打包。

# 0.1.10 阻塞项完成与发布记录

2026-09-10，用户明确授权完成此前审查列出的全部阻塞项、发布新版本并同步文档。继承 SOURCE_MIGRATION_AND_PRODUCT_PLAN.md 的目录结构与授权边界；不新增产品范围，不改 DELIVERY_PLAN.md §0。版本仅在发布准备完成时统一更新。当前基线为 840a2e7。

| 编号 | 必须完成的结果 | 完成证据 | 当前状态 |
|---|---|---|---|
| R1 | 最新宿主、Runtime、记忆服务装入同一 V11 包；停止使用旧演示服务 | 新包清单、schema、健康与新接口、重启后正式用户服务 | 待完成；当前运行旧 schema 13 演示后端 |
| R2 | 图片、扫描 PDF、常见 Office 完整解码与当前真实模型处理；OCR 不提取知识 | 混合格式内容覆盖、真实模型图片账单保存/新会话金额/原图 | 开发中；复杂图形/备注覆盖和真实模型未验收 |
| R3 | 大附件与长文档分批让模型读取，按容量处理，不静默截断或假称全文已理解 | 超单次上下文文档首中尾事实问答与处理覆盖 | 待完成；现有附件仍将全部块拼接 |
| R4 | Dreaming 支持受控创建、更正、合并及必要计划审批，主动报告结果 | 权限/版本/来源测试、真实计划审批到检索结果 | 部分完成；创建、更正审批与持久化已接线，合并及真实模型验收未完成 |
| R5 | 回答内自然查看实际来源，不恢复已移除的全局按钮 | 当前回答引用与原文相符，普通回答无空入口 | 待完成；后台引用已有 |
| R6 | 最新同包双向配对、共享读取、离线恢复、并发收敛、遗忘传播 | 同版多设备场景结果 | 待完成；不能沿用旧版结果 |
| R7 | 同版新装、升级、签名拒绝及失败恢复，完整依赖随包 | 原生安装/升级记录、离线依赖检查 | 待完成 |
| R8 | 功能说明、使用手册与宣传描述一致；发布 0.1.10 | 文档核对、CI/V11 门、签名发布资产及版本链接 | 待完成 |

执行顺序：补齐 R2–R5 的必要源码 → 同提交候选包 R1 → 真实模型 R2/R3/R4 与多设备 R6 → 安装升级 R7 → 文档与发布 R8。构建准备和依赖核验可提前进行，避免每次 UI 变更重建完整包。来源以用户指定的 shots.json 为准，其他宣传制作文件不扩展修改。

GitHub #2 的旧 OCR 知识提取方案已被用户后续要求覆盖；验收目标仍是图片到记忆的 V11 同包链路。完成前不宣称 issue 已解决。发布授权不等同于远程发布门通过；不得绕过签名、通用或原生检查。

每项以真实业务结果判定，不能用编译、模拟模型或历史测试替代。未通过的项目保持未完成，不缩减本表范围。

R2 首批进展：完成常见位图统一转换与多页图像保留；20 项文档解码、3 项 API/MCP 检查通过，宿主补丁准备通过。尚未完成 Office 全覆盖、大文档、真实模型和安装包要求，不据此关闭 R2。

R4 进展：更正执行器、MCP 读取、冻结快照、逐方案宿主审批和带文档来源的版本化更新已实现；6 项 harness/MCP 与 8 项更新 API 检查通过。发现并修复否定旧金额的匹配错误。用户审批 UI、计划持久化已接线，合并尚未完成，R4 保持未完成。

R4 进展：schema v15 新增 dreaming_plans 私有表，`POST/GET /dreaming/plans` 提交与读取方案，`POST /dreaming/plans/{id}/decision` 执行批准或拒绝。后端复制已验证文档依据，审批前不更新，执行时检查目录授权及记忆版本；完成结果幂等。Runtime 未批准的更正进入 awaiting_approval，前端按待处理数量显示“查看整理方案”，列表比较并批准/拒绝，不自动弹窗。721 项基础设施回归通过；合并和真实模型同包验收仍未完成。

R2 PDF 进展：移除只渲染空白文字页或含位图页的判断，所有页面保留完整图像，补齐文字页中的矢量图表。21 项解码测试通过；其他复杂 Office 内容、容量处理及真实模型同包验证仍未完成。现有 wheel 锁及 CI 已覆盖所用依赖和测试路径，无新增构建输入。

R2 Office 进展：补充 Word/PPT 原生图表的完整 XML 数据输入，保留系列与分类对应关系及缓存数值，不执行公式和外链。复用主解析器并增加小型格式适配，无新依赖。23 项文档解码测试通过；复杂绘图、真实模型及最终安装包仍待完成。

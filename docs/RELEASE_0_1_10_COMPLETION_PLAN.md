# 0.1.10 阻塞项完成与发布记录

2026-09-10，用户授权完成此前审查列出的全部阻塞项、发布新版本并同步文档。继承 SOURCE_MIGRATION_AND_PRODUCT_PLAN.md 的目录结构；不新增产品范围，不改 DELIVERY_PLAN.md §0。以下为当前状态，替代此前逐次追加的进展描述。

## 验收范围与当前状态

| 编号 | 必须完成的结果 | 当前证据与剩余工作 |
|---|---|---|
| R1 | 最新宿主、Runtime、记忆服务装入同一 V11 包；停止使用旧演示服务 | 候选包已安装；正式后端与 Runtime active，桌面运行 /usr/bin/kylin-agent；旧演示后端停止。版本 0.1.10、schema 15、health ready。整机重启后验证待做。 |
| R2 | 图片、扫描 PDF、常见 Office 完整解码与当前真实模型处理；OCR 不提取知识 | headless 解码与离线依赖已随包；常见位图、多帧图像、PDF 全页图像、Office 文本与图表数据已接入。复杂 Office 图形覆盖及真实多模态图片账单保存/新会话金额/原图验收未完成。 |
| R3 | 大附件与长文档分批让模型读取，按容量处理，不静默截断或假称全文已理解 | 文本与视觉文档已接入分块工具。同包真实文本模型按游标 0/1/2 读取三个块，首中尾事实全部回答正确；超大容量及视觉连续读取仍未验收。 |
| R4 | Dreaming 受控创建、更正、合并及必要计划审批，主动报告结果 | API、MCP、持久方案、冻结版本与桌面审批已接线；真实模型提出计划、用户审批、最终检索与主动报告未完成同包验收。 |
| R5 | 回答内自然查看实际来源，不恢复全局按钮 | 引用 API、轮次来源链接及桌面来源对话框已接线；真实回答生成链接、点击后原文/原图一致性未完成同包验收。 |
| R6 | 最新同包双向配对、共享读取、离线恢复、并发收敛、遗忘传播 | 两台 V11 已安装同摘要候选包。开启同步后发现正式服务缺少 PIXIU_SYNC_CERTFILE，网络未启动；自动证书与签名配对信任已实现，真实 TLS 回归覆盖未配对拒绝、配对后通信和解绑后拒绝；须纳入候选包验证多设备场景。旧版结果不替代。 |
| R7 | 同版新装、升级、签名拒绝及失败恢复，完整依赖随包 | 0.1.9-1 → 0.1.10-1 安装成功；依赖从包内离线安装且 pip check 通过。新装、签名拒绝、失败恢复与重启后服务验证待做。 |
| R8 | 功能说明、使用手册与宣传描述一致；发布 0.1.10 | 候选已构建，尚未发布。最终文档核对、通用 CI、原生发布门与签名发布资产仍待完成。 |

GitHub 当前实际 issue 为 #2（未关闭）：图片账单到长期记忆的 V11 安装包链路。其旧 OCR 知识提取方案已被用户的多模态要求覆盖；未完成真实链路前不能关闭。宣传核对范围仅 submission/video-production/storyboard/shots.json。

每项以真实业务结果判定，不用编译、模拟模型或历史结果替代。发布授权不等同于发布门通过，不绕过签名及 CI。当前文本模型下，图片和扫描 PDF 读取接口按要求返回不可用；仍需可用多模态模型完成对应验收。

## 当前实现

- 文档：复用 Kreuzberg/PDFium、Pillow、openpyxl，无需目标机器安装 LibreOffice；OCR 不参与知识提取。所有 PDF 页保留渲染图像，Word/PPT 图表保留 XML 系列、分类与缓存值，不执行公式和外链。复杂绘图不能据此宣称完整支持。
- 长附件：超过两个块时保留既有 24 小时文档引用，pixiu_document_read 按 document_id/version/cursor 读取；图片块复用 Runtime 多模态工具结果并复核当前模型能力。未读取全文不得声称全部理解；真实模型执行情况待验证。
- Dreaming：schema 15 的 dreaming_plans 保存计划；模型先读取目标并冻结版本，更正与合并须进入审批。桌面展示原记录与建议，批准后执行；合并旧记录退出有效检索，来源保留。MCP 不提供批准入口。
- 来源：回答链接绑定已消费的上下文和实际知识条目，读取时重查授权、有效状态与敏感级别。宿主补丁 0030 打开 CitationDialog，展示原文和原图；不恢复全局来源按钮。
- 打包：源码快照审计复用固定上游版本和逐文件摘要检查；版本清单使用迁移后的 VERSION 路径，支持无 .git 的交付源码。无新增依赖或源码目录。

## 候选包与已验证结果

候选源码提交：e9f0a276b1229afac1244ffc74d7329533ae3d59。

- 包：pixiu_0.1.10-1_amd64.deb，208074472 字节。
- SHA-256：304ea59a1a4f12af5a7f57221fa499b5fc3e2c493295ef1a1123212a91cbc844。
- 画像：kylin-v11-native-x86_64，KYSDK=ON，strict=1，CPython 3.12。
- 通用环境 Foundation/Engine/Agent 954 项测试通过；供应链 14 项检查及 Git 模式版本清单回归通过。
- V11：26 项桌面回归、宿主与三项原生绑定编译、Runtime 锁哈希与断网安装检查通过。
- 已安装同包 native-sdk-smoke 通过：真实嵌入、向量库生命周期、向量写入/检索/删除、记忆写入/查询、遗忘后不可见。
- 证据保存在被忽略的 build/release/evidence/candidate-0.1.10/kylin-native-evidence.json；不将机器状态、实验数据或构建产物混入源码提交。

桌面已切换到更新后的安装包并连接 Runtime。全局快捷键修复已随包安装，日志确认 updated existing Kylin global shortcut；实际按键恢复窗口仍待同版验证。

默认模型修复已随包安装：kylin-default 按系统优先级选择模型；完整 Runtime 会话返回正常文本。省略模型参数的通用 /v1/runs 请求仍会将服务别名 pixiu 作为模型提交，需要按实际桌面调用路径判断影响范围；不能把该请求的失败归因于模型服务不可用。

三块文档真实验收：经 /api/memory/attachment 上传文本后，模型连续调用 pixiu_document_read 的 cursor 0、1、2，正确回答项目名称、会议日期和预算。工具调用与回答保存在被忽略的 candidate-0.1.10/document-transcript.json；该结果不替代超容量或视觉文档验证。

## 后续顺序

1. 补齐无需用户手工配置的同步证书与配对信任初始化；验证候选桌面真实操作、全局唤起与重启后服务。
2. 完成 R2–R5 的真实模型同包链路；多模态连接不足时继续可独立完成的文本与审批验证。
3. 完成 R6 多设备与 R7 安装升级验收。
4. 核对全部宣传用例与交付文档，通过发布门并发布 0.1.10。

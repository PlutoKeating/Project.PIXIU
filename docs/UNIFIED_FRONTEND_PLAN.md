# PIXIU 唯一桌面前端开发与交付计划

## 目标与执行约束

用户已批准以现有 openKylin Agent 为唯一桌面宿主，将小窗口中有价值的管理能力迁入同一应用，移除独立小窗口实现。架构决定见 [ADR-0006](decisions/0006-unify-agent-desktop-frontend.md)。任务编号仅用于引用，不代表优先级或时间安排。

完成状态必须由代码、构建产物、测试及真实界面共同证明；计划存在不代表实现完成。官方赛题原件保持只读。保留各既有文档的目录及章节结构，修改事实而不重排原文结构。用户数据库、设备身份、配置和会话历史不得作为旧前端残留删除。

## 代码基线与已知差距

- 当前代码核查基线为 `a77eb8c507b2ee1f12d61f6d411e1d72a48a04a4`，产品版本 `0.1.8`；该提交不是新生产 Release。
- `frontend/CMakeLists.txt` 仅构建保留的回归测试；独立 `pixiu-frontend` 目标、安装规则、旧 main、PixiuApp 装配层、FloatingBall、ChatWindow 及输入/消息展示链已删除。旧管理面板和控制器仍有专属回归，尚待逐项清理，不是第二个产品入口。
- `build/release/debian/usr/bin/pixiu` 的通用与原生包入口均执行 `kylin-agent`，不再回退旧前端。当前宿主嵌入 `frontend/management/` 的记忆、设备及设置页。
- Agent 构建使用固定 submodule 的导出副本及自有补丁；上游记忆设置源码被现有构建补丁排除，不能算已交付界面。
- 旧查询入口仅调用记忆检索，不具备 Agent 循环。管理能力已接入唯一宿主，但范围配置、视觉、事件状态及全功能验收尚未统一，不能以导航接入证明迁移完成。
- Agent 导出副本已有显式 `KYSDK=OFF` 的 Qt 适配与原生 `KYSDK=ON` 路径；固定上游源码保持只读，不恢复另一套前端作为降级路径。
- 升级辅助脚本已迁入 `build/release/debian/usr/lib/pixiu/`；管理模块复用升级控制器与对话框，不能随旧聊天删除这些有效依赖。

范围与连接配置按实际调用关系核查如下；`user:local` 与 `user:default` 是不同的硬过滤值，
不是同一身份的两个显示名称。此清单描述当前实现，不代表已经完成配置统一。

| 消费入口 | 当前事实源 | 迁移约束 |
|---|---|---|
| 正式启动器 `build/release/debian/usr/bin/pixiu` | 集成后由随包 `launch-agent.py` 只读解析当前 Agent profile，传递 scope 与一致的 endpoint，再 exec 唯一宿主 | 解析失败或显式 Qt endpoint 冲突拒绝启动；不执行 `.env`，不向 Qt 载入 profile 凭证 |
| Agent 集成脚本 `pixiu-agent-integrate` | 在当前 Agent profile 的 `.env` 中缺省写入 `PIXIU_AGENT_SCOPE=user:default`，保留已有值 | 必须保留自定义 profile、范围和 endpoint；禁止直接 source 用户 `.env` 执行其中内容 |
| `integrations/kylin_agent/pixiu/provider.py` | 显式构造参数或 `PIXIU_AGENT_SCOPE`，缺省 `user:default`；API 地址使用 `PIXIU_AGENT_ENDPOINT` | 轮次上下文、写入、更新与遗忘均绑定实际 Provider 范围；不能把模型提交的范围作为覆盖值 |
| `frontend/src/services/HttpBackendTransport.cpp` | `PIXIU_BACKEND_URL`，正式启动时由引导器与有效 Agent endpoint 对齐；直接运行宿主仍使用进程环境或缺省本机 8765 | 不能将直接宿主启动当作 profile 已解析；公共管理接口不应依赖模型可用 |
| `MemoryWorkspace`、`MemoryWriteDialog`、`MemoryAudit`、`ForgetPage` | 共用 `MemoryScopes.h`，保留 `user:local` / `shared:home` 并增加有效 Agent scope；MemoryScopeControl 支持明确输入其他范围，检索和偏好另有全部范围 | 录入只自动选择私有 Agent 域；不隐式共享或跨域遗忘；历史范围自动发现及跨入口真实数据验证仍待完成 |
| `MemoryEditDialog` | 从查询页传入范围，读取完整快照并要求目标范围匹配 | 保留版本校验和精确范围，不为找不到记录而改用无范围请求 |
| `backend/foundation/monitor/` | 目录采集默认 `user:local`；行为采集固定 `user:local` | 本机采集不应因 Agent 配置为 `shared:*` 而自动共享；与主动共享写入分别处理 |
| `backend/foundation/api/delivery.py` | 洞察固定 `user:local`；简报聚合采集日志 | 不得宣称洞察已覆盖 Agent 记忆或全部范围；范围能力变更须先更新公共契约和后端测试 |

U03/U04/U10/U12 的范围统一必须共同覆盖：读取有效配置且不泄露 profile 中的凭证；
区分 Agent 范围、本机采集范围和主动共享范围；使已有 `user:default`、`user:local`
及自定义范围的数据可以被明确选择、读取和管理；配置不一致或不可读取时显示明确错误，
不得静默换库或换范围。不能通过全局字符串替换、删除旧域数据、放宽 Provider 硬过滤、
自动合并域或将全部个人数据改为共享来实现“统一”。写入、检索、编辑、偏好、遗忘和
洞察需用同一组跨入口验收数据核对，已有数据与新数据分别验证。

## 完整开发任务

### U01：架构决定、归属与迁移清单

前端架构文档已按现行宿主和管理代码复核，保留全部 33 个章节标题与顺序，删除
退役窗口线框、尺寸及示例状态，纠正旧三入口、物理遗忘、单端配对等不实口径。
组件映射、通信和构建章节改为现有管理静态库及同一宿主，明确降级与待验收矩阵。
此项仅完成该架构文档的纠偏，不代表其余文档、旧源码清退或最终截图已完成。

前端 DEV_TASKS 已继续按当前代码复核，保留全部 23 个标题及顺序。删除旧窗口
展示状态、历史测试/翻译数量和已解除的端点阻塞；不存在的 MemoryClient/SyncClient
示例改为实际 BackendTransport 方法表，事件改为正式只读失效与刷新边界，纠正
无 SDK 后端不可用的描述。文件归属、单一包体、当前表单行为和剩余验收均重新核对；
局部原生过程记录统一引用本计划，不以旧窗口或离屏结果充当当前交付证据。

BACKEND_ISSUES 保留原三个章节，改为当前 WS 注册、导入、事件语义和复核入口，
删除已失效的故障复现建议、旧小窗口通知展示及矛盾状态。实际应用握手与采集
配置事件广播两项隔离测试通过（0.90 秒，2 条依赖弃用警告）；不扩张为心跳、
断线恢复或原生 SDK 验收，也不修改后端实现。

前端 DEVELOPMENT_PLAN 已逐段核对并更新，保持全部 36 个章节标题与顺序。
删除旧界面演示、已删除类的接线指令、累计测试/翻译数量及失效接口阻塞，正文
改为唯一宿主和管理组件、当前 HTTP/WS 语义、实际构建入口与完整待办；健康探测
不再写成 /conflicts，旧根 CMake 不再被描述为可启动产品。旧阶段名称仅保留在
标题用于结构兼容，不作完成声明；原生、通用和替身证据边界分开说明。
版本与关于页面的 compose 计划及设计文档已核对正式 SettingsWorkspace、
ProductInformation、对话框和发布代码，保留各自全部章节标题与顺序。移除旧版本
数字、旧窗口接线、过时签名边界和“永不上传”承诺，改为当前四入口、纯文本契约、
有效测试及剩余原生/语言验收；不以顶部历史声明继续保留失效操作指令。

- [x] 记录用户批准的唯一宿主决定，明确替代旧独立控制台定位。
- [x] 建立覆盖实现、删除、兼容、验证与交付的完整计划。
- [ ] 按实际调用关系登记保留、迁移、替换、删除的源文件与测试；不以文件名判断用途。
- [ ] 更新受影响的全局架构、模块职责和包体说明，区分目标与实际已实现状态。
- 验收：唯一入口、源码归属、公共接口以及上游补丁范围均可从仓库追溯。

### U02：唯一应用外壳与原生管理模块

正式设置已补自定义唤起快捷键：HostTray 持有唯一 ShortcutManager，点击应用才
替换绑定并写宿主 QSettings 的 `pixiu/activationShortcut`，不恢复旧小窗口配置层。
按官方快捷键契约约束为单组字母/数字与 1～2 个控制键，至少包含 Ctrl/Alt/Meta。
非法输入不修改绑定，损坏保存值不覆盖而本次回退默认；写入失败明确区分当前
会话应用与重启保留。托盘文案和设置状态显示实际组合及全局/应用内范围；SDK
服务变化不覆盖正在编辑的组合。未应用或保存失败的编辑接入已有退出/升级重启
保护，放弃不保存。Qt 的编辑、设置存储、测试均复用既有依赖，新增 host_shortcut
测试目标；产品导出沿用三个现有管理组件，无新窗口或 SDK 接口。
组件测试覆盖按键替换、重新创建宿主恢复、非法输入、损坏配置、写入失败和退出
草稿；SDK 替身补自定义组合在服务消失/恢复中的保留检查。完整原生冲突矩阵
仍待验收，不以替身或组件结果勾选完整 U02/U11。
管理套件 13/13 组通过（45.36 秒），宿主导出适配检查通过；新测试先复现正式设置
入口缺失，再验证上述行为。配置测试使用独立临时目录，不读写真实用户配置。

原生流水线 34199398236（cb13410）成功，V11 已安装二进制与运行进程的
SHA-256 均为 `357a91c5f4088d38274d00047ccfa99fcf1c9726300303332b7ab92924d92637`。
真实设置界面应用 Ctrl+Alt+K 后显示全局；切到终端再按组合恢复同一宿主进程。
正常退出并从系统菜单重启后，K 配置恢复且注册为全局。通过设置恢复默认
Ctrl+Alt+P 后，状态与日志均确认全局，再次跨应用按键恢复同一进程。
首次启动默认 P 曾返回 SDK `-2` 并明确降级为应用内；重新应用后恢复不代表
根因已解决，启动注册异常及跨应用冲突仍须排查。实测仅修改宿主快捷键设置，
最后恢复默认 P；采集总开关、全部来源保持关闭，目录为空，未修改用户会话或记忆。
诊断截图保留在仓库外，不计作最终交付截图库。

当前宿主已接入托盘。导出适配已改为关闭最后一个可见窗口时退出，避免普通关闭后
残留不可见进程。显式 `--quit` 先恢复窗口，再走相同关闭检查，不直接绕过管理页。
`HostCloseGuard` 属于唯一宿主：可见所属对话框先处理完；采集、设备、遗忘及偏好
审计、记忆检索、来源证据、洞察和简报请求在途时拒绝关闭；采集/同步配置与有效快照不同则默认取消退出，明确放弃
不会提交配置。Agent 通过只读回调提供全部会话在途请求表与当前未发送输入状态，
请求在途时拒绝退出，草稿默认取消放弃；确认返回后重读 Agent 及管理页请求状态。升级按钮在启动
重启 helper 前复用同一检查，只排除发起重启的对话框，检查拒绝不调度 helper。
`HostTray` 已接入唯一主窗口，使用同一图标；显示恢复原窗口及最大化状态，退出
通过现有关闭保护，无托盘平台不影响普通入口。关闭最后主窗口仍退出而非常驻。
该组件复用 Qt Widgets，无新增系统依赖；源码和 `0024` 纳入宿主导出与供应链。
组件回归覆盖恢复、在途拒绝退出与空闲关闭，真实麒麟托盘展示和交互仍待验证。
宿主复用 `ShortcutManager`，默认 Ctrl+Alt+P；ON 链接画像已有的 kysdk-shortcut，
使用官方创建/更新/删除接口及 `pixiu.activate` 名称。失败或 OFF 降级为应用内快捷键，
托盘菜单标明作用范围，激活恢复同一窗口。重复注册先释放，只有成功注册的全局
绑定才会在释放时删除。管理回归包含真实 Qt 按键和 SDK 函数替身；后者不是原生
SDK 验收。注册前通过官方删除接口清理旧 PIXIU 专用名称
`pixiu-frontend.toggle-chat`，不枚举或删除其他绑定；清理失败记录警告，仍尝试新注册。
回归覆盖清理先于注册、清理失败和已取得绑定的释放边界。
`ca7e6f8` 的 V11 作业 34172411857 通过，安装清单与运行程序哈希核对一致；真实
托盘点击和单实例命令均可恢复同一窗口，但 Ctrl+Alt+P 两次复测未恢复。
发现旧、新名称同时占用相同按键；官方 SDK 删除旧绑定返回成功，配置中旧项消失，
复测仍失败。因此旧残留不是充分原因。进一步确认官方 `/usr/bin/kdkshortcut`
会话服务未运行；按 SDK 自启动项入口启动后，同一按键恢复原窗口、主进程 PID 不变，
日志记录单实例激活。现已接入官方服务就绪处理：通过会话 D-Bus 观察
`com.kylin.kysdk.shortcut`，缺失时启动官方 `/usr/bin/kdkshortcut`；短暂有限轮询
与服务归属事件共同确认就绪，等待/失败期间使用应用内快捷键。已有服务不重启，
服务消失则降级，退出只释放自身绑定，不停止共享服务。托盘文案响应可用性变化。
ON 显式链接 Qt DBus，原生画像补列对应运行库；OFF 不依赖该服务。回归覆盖已有
服务、异步上线、启动失败、服务消失和释放后的迟到事件。`96d70e9` 原生作业
34174531957 通过；无官方服务且无残留的真实会话中，系统菜单启动自动启动服务，
实际 Ctrl+Alt+P 恢复原窗口，运行文件与已安装文件哈希一致。
另复现 SDK 服务退出后 compositor 保留自身注册而 SDK 配置为空，创建/更新均返回
已存在；只注销 SDK 组件内 `pixiu.activate` 后恢复成功。现补入这个错误条件下的
定向恢复，仅限当前及旧 PIXIU 名称，最多重试一轮，失败仍降级，不清空组件或抢占
其他名称。使用现有 Qt DBus，无新增依赖。`eb5875f` 原生作业 34175941456 通过；
真实 V11 会话复现“SDK 配置为空、compositor 保留 PIXIU 注册、SDK 服务未运行”，
系统菜单启动后软件自行恢复残留并启动服务，无手动注销辅助。实际 Ctrl+Alt+P
恢复同一主进程窗口，托盘菜单显示全局；安装及运行文件 SHA-256 均为
`7e95d1d627c6c67e45d7e483d9f41e3a23941cbe458b83d253f573ad870126b0`。
此项证明该原生恢复路径，不覆盖其他应用按键冲突、完整关闭/升级/通知矩阵，
U02/U11 保持未完成；验证截图不作为最终版本交付图库。
`55e1dac` 完整 OFF 宿主构建通过，动态依赖检查无缺失且未链接 kysdk；导出检查
核对快捷键源码字节一致、官方库链接声明存在、SDK 测试替身头文件不进入导出树。
上述结果仅证明通用构建及产品/替身隔离，不能证明 ON 注册成功或跨应用唤起。
冲突分级提醒接入同一宿主：仅 high/critical 事件请求固定文案通知，连续请求限流；
不传冲突值、命令、ID 或凭证，不弹确认框。复用 `NotifyService` 与同一托盘，ON
链接画像已有 kysdk-notification，OFF 使用托盘/日志降级，事件栏保留受影响页面。
WebSocket 回归覆盖普通/未知级别不弹、严重事件限流和无敏感正文；原生实显未验证。
偏好变化提醒已迁入同一宿主：成功读取偏好列表后以范围和记录 ID 跟踪版本；首次
读取或切换过滤范围只建立基线，重复响应、版本回退不提示，不完整/重复记录清空
跟踪基线而不推断变化。提醒只携带变化数量，系统通知使用固定文案并限流，不暴露
偏好键和值、不执行写入。接口列表存在数量上限，因此只说明列表出现新增记录或
版本变化，不宣称刚学习了新偏好或统计了全部偏好。无额外后台轮询；组件与导出
接线回归通过，真实新包通知展示仍待验证，相关任务不勾选。
旧生命周期的偏好通知函数、版本缓存及重复测试已删除，正式管理模块接管提醒与
范围/版本/损坏响应回归。旧相关主题提醒仅比较文件名和洞察标题片段，不是语义
关联；该推断函数、分词辅助、洞察缓存、每日计数、接线、两项旧测试及通知翻译
已删除，不移植“已确认相关”的结论。正式路径保留采集页面变化提示、可见日志
刷新及应用内洞察读取；WebSocket 回归验证四类采集状态均发出无正文的刷新信号，
不暴露文件名/业务标题、不请求冲突通知或发送命令。没有新增语义关联算法，
也不将页面变化提示称为相关性推荐。其他旧生命周期测试仍须逐项核对后清退。
设置页已补入关于、数据与联网、第三方组件三个只读说明入口，复用既有 InfoDialog，
归属唯一宿主并纳入关闭保护。共用正文明确共享同步、模型发送与遗忘边界；旧回归
入口和翻译资源中的“仅存本机、永不上传、任意清除”错误声明同步清除。许可证
说明指向实际随包文件，不新增法律条款。版本、只读、实例复用、窗口归属及导出
字节一致性有回归覆盖；新说明原生显示与完整语言切换尚未验收。
本项正式管理模块 12 组、保留前端回归 38 组通过；翻译源与包内 `.qm` 同步生成
为 323 条完成翻译，资源相关测试复验通过。没有新增依赖或第二个产品构建目标；
这些结果不替代最终版本的原生界面与通知验证。
源码与组件回归不证明真实模型任务已停止；托盘原生验证、真实会话关闭和升级重启矩阵
仍须补齐，不据此勾选完整生命周期。
该调整通过完整 `KYSDK=OFF` 宿主编译、导出源码断言和供应链 12 项检查；尚未
执行真实桌面关闭交互，不以构建或源码断言代替生命周期验收。
关闭保护追加后重新完成完整 OFF 宿主编译及管理/HTTP/升级五组回归。嵌套关闭用例
直接验证关闭事件被拒绝，不将 QWidget 递归 close 的返回值当作窗口已关闭；测试
回调在断言前关闭确认框，避免失败时遗留模态事件循环。

- [ ] 保留现有 Agent 会话、模型、工具过程、停止、历史及富文本显示。
- [ ] 在同一主窗口提供会话、记忆、设备、设置导航，恢复导航时不销毁进行中的会话。
- [ ] 将自有管理界面维护为可直接编译测试的 Qt 源码模块；宿主补丁仅负责接入，不复制整套管理实现。
- [ ] 统一退出、窗口激活、托盘和单实例行为；管理对话框属于唯一应用，不启动第二个进程。
- 验收：从安装入口进入同一应用即可访问全部管理功能，进程与安装文件中不存在独立小窗口产品。

### U03：公共通信与状态处理

唯一宿主新增 `BackendEventStatus`，使用 HTTP 传输相同配置的基址订阅 `/events`，
复用 WebSocket 客户端及退避重连，不接入旧 `EventRouter` 的遗忘确认对话框接线。
状态栏仅提示受影响页面；重复事件按页面合并，不显示事件正文/命令/凭证，不执行
写请求或覆盖未保存输入。`forget_confirmation` 按已执行广播处理，仅提示记忆需
重新核对。重连因无重放游标而提示重新读取，不将事件连接成功当作后端健康或数据
已最新。审计页已接入合并刷新：记忆/冲突/遗忘完成事件及连接恢复标记页面过期，
隐藏时不读，显示且无在途操作后读取；读取或提取中到达的事件延后处理。按记录 ID
恢复选择并重新读取历史，目标消失则清空详情，不重放提取或遗忘。设备页对同步、
配对广播及重连合并只读刷新：隐藏、有未保存开关或在途操作时延后；恢复原值或
保存结束后再读，保留仍存在的所选节点。撤销信任确认框也纳入忙碌门控，广播不
改变确认对象，不提交设置/配对/撤销。采集事件及重连仅刷新可见隐私页的日志首页，
在途时合并延后，历史页不跳转；不重读或保存配置，日志在途仍可编辑，返回后保留
未保存提示。组件回归覆盖隐藏、连续事件、在途编辑和历史分页，宿主导出检查覆盖
接线；真实采集事件驱动的原生界面仍待验证。其余页面自动刷新、按严重程度通知仍未完成，
不勾选 U03。
记忆检索页对记忆变更、遗忘完成、同步和重连立即清除旧结果/来源/正文，保留检索
输入与范围，不自动重放查询或写请求。旧查询按编号忽略，未带请求编号的证据读取
继续串行等待并丢弃正文，避免迟到内容重新显示；已打开的编辑对话框不被重置。
组件回归覆盖在途查询与证据失效、输入保留和零自动写入；原生事件复验仍待完成。
洞察与简报对记忆/采集/同步/冲突和重连清除过期内容，保留所选日期；在途读取
等待返回后丢弃，防止旧摘要重新显示或点击旧候选检索。不自动重放读取，用户可
重新读取原选日期。组件覆盖历史闰日、迟到摘要/洞察和无自动写入，原生复验待完成。
管理库增加 Qt WebSockets 链接，两个发布画像已包含其构建/运行依赖；新增组件、
复用客户端与 `0023` 宿主接线均纳入导出/供应链输入。Qt 5.15 起限制消息/帧为
2 MiB；旧 Qt 仅有组装后的解析长度限制，不能声称同等网络缓冲上限。测试使用
本地 WebSocket 服务核验只读提示、重复合并、断线恢复、非法地址与超大消息。
完整宿主原生界面及离线/恢复交互仍待验收。
本地验证：9 组管理回归、9 项供应链测试、宿主导出检查及完整 `KYSDK=OFF` 宿主
编译通过。该结果不作为麒麟 SDK 或原生布局验收。

`ee5da25` 原生界面复核：正常退出旧进程后从系统菜单启动，运行文件与安装文件
SHA-256 均为 `41b797c71de6878070ad2e41c2d9989a322e28656156ebcee8140ca283344bf0`。
事件栏显示连接且明确非健康判定，设备与偏好页在未点击刷新时读出已有数据；
同步开关仅在界面勾选，切页返回保留草稿及未保存提示，独立 GET 仍为 enabled=false。
撤回草稿、手动读取并清除通知后，两个开关仍关闭。首个设备点击曾切到终端，进程
未退出且无新增崩溃记录；任务栏恢复后同坐标导航成功，原因未确定，不宣称首击问题
已解决。检查结束时流水线已替换磁盘程序，旧进程哈希不变；这些图片只证明上述
ee5da25 进程行为，不作为后续新包、采集事件刷新、断线重连或最终发布图库证据。

- [ ] 复用并修正异步 HTTP、请求关联、超时、取消、错误解析与过期响应抑制。
- [ ] 从实际配置取得后端地址，核对版本及能力；管理功能不依赖模型或 Runtime 在线。
- [ ] 接入事件刷新与断线重连，区分事件通知和操作请求，禁止把执行完成事件再次当作删除授权。
- [ ] 不直接导入后端私有实现，不建立第二份业务数据真相源。
- 验收：离线、恢复、并发和迟到响应不会覆盖错误对象或误报操作成功。

### U04：记忆检索、录入与更新

检索、录入、偏好与遗忘页已共用 MemoryScopeControl 的“其他范围”入口。输入
完整范围名并明确确认后才加入当前页面选项；取消不变、重复不新增、不修剪空白，
超长输入拒绝而非截短。选项继续使用既有数据值与切换信号，偏好页可按新范围刷新
只读列表；选择本身不提交写入或遗忘。请求在途时随原选择器禁用，确认返回再次
检查可用状态。没有新配置存储、不迁移数据、不改 Agent profile，也不扩大 Provider
授权范围。新控件源文件已纳入管理模块和宿主导出，复用现有 Qt Widgets。
核心模型已接受与公开读取一致的点号范围，并以完整字符串匹配拒绝尾随换行，
HTTP 请求及查询参数也增加语法拒绝，检索范围拒绝非字符串；回归核对非法请求
不改变数据库，合法点号私有域及已配置共享域可以写入并精确读取。共享域格式
合法不代表匹配本机同步域；写入/更新已在业务变更与幂等收据占用前校验同步域
及签名身份，域错误返回 403，数据库快照回归确认无残留变更。遗忘确认也逐一
预检已审核目标的实际共享域，混合目标任一失败则删除不执行；凭证仍消费，
须重新预览。其他路径及跨组件失败恢复仍需补齐，不以该预检代替完整事务保证。
不修剪或重命名范围；同步仍拒绝私有域离开设备。模型/API/流转/同步核心回归
通过，不代表真实历史数据全链路验收。各入口长度上限统一、历史范围自动发现及
原生界面仍待完成，不把手动选择入口视为 U04 全部完成。

- [ ] 建立检索、结果、空态、失败重试与手动录入界面，保留失败输入。
- [ ] 明确个人与共享范围、敏感信息约束和异步入库状态。
- [ ] 基于当前公共更新契约提供版本校验和幂等处理，冲突时提示刷新而非覆盖。
- 验收：真实后端中写入、检索、更新及版本冲突均有界面和测试证据。

### U05：证据阅读与回溯

文件采集来源已在同一 `MemoryWorkspace` 阅读器中独立展示，使用只读纯文本区，
不混入正文或高级 raw。显示采集方法、JSON 转义路径、UTC 时间与有效性限制；
区域有高度上限并可滚动复制，不自动打开文件。缺失来源不推测，非法元数据不
展示路径；数据失效、来源/范围切换清空旧值，迟到响应不恢复内容。管理 12 组
回归与同一宿主导出检查通过，新增非法/长路径/空值/共享域等边界测试通过。
原生分栏及部分阅读交互已复测（见下）；完整布局、主题及语言验证仍待完成，
不据此勾选完整 U05 或交付图库。

`8b1d49c` 的 V11 自动作业 34191660961 成功完成严格构建、安装与 SDK 生命周期门。
旧进程正常关闭后从开始菜单启动，PID 2185034 的运行文件与安装文件 SHA-256
均为 `df3563e5764890f8626ffe21cebd63a9c522ba34d015ffa9508a17473d5c61be`；
后端报告 schema 13，采集配置仍全关且目录为空。旧合成证据可检索，实际阅读器
显示来源未记录，不猜测路径。但 1440×900 桌面下缺失来源文本框占用较多高度，
正文阅读区被挤压，需压缩缺失状态并改善空间分配。新采集元数据尚未在此版本
完成原生展示验证，检查图仅为诊断材料，不进入交付图库；未发送模型请求或修改
既有会话、采集授权与记忆内容。

针对上述布局问题，缺失/无效来源改为紧凑可选取标签，清空并隐藏独立来源文本框；
有效元数据仍用原只读区域，状态切换不残留路径。来源列表上限收紧到 72 像素且
保持滚动，正文最低 100 像素，正文/摘要剩余空间权重改为 3:1。增加 1200×600
管理区的缺失状态几何回归，检查提示高度、正文高度和空间占比。沿用现有 Qt
依赖、管理库源文件与宿主导出配置，无新增构建输入。新增几何/状态回归通过，
管理套件 12/12 组通过（45.52 秒），宿主导出适配检查通过；原生同版布局复测仍待完成。

`616c7d7` 的原生作业 34193506674 已成功完成。系统菜单启动 PID 2197272，
运行文件与安装文件 SHA-256 均为
`864bc4e0b839b288c7b64c5b1b7429a2c41fd32ae35fbb9eec58c19a3dfa460f`。
旧记录缺失来源已为紧凑标签，正文显示多行。经实际隐私页仅授权新建临时示例目录，
复制明确标注的合成文件 `pixiu-source-current.txt` 后，真实监听日志返回
`evd_01M1ZV615WFEWEX7DDTVZ1P69A` / `knw_01M1ZV616KPX7MKMC3NMSP6T5M`。
API 与阅读器一致：私有 user:local、文本采集、实际观察路径、UTC 记录时间
2026-09-08T06:27:13Z，来源与正文分离。源文件传输前后 SHA-256 均为
`6774da9fef23f8049dd0bb2b23d2e9756c57b92463470b0eb2db470601fb3ff6`。
已通过界面恢复所有采集开关关闭和空目录，公共接口再次确认原配置；未发送模型
请求、未修改既有会话或记忆。有效来源区域需滚动查看时间/限制，正文底部仍拥挤，
鼠标滚动可读到两区末尾；这说明数据可达，不代表布局验收通过。诊断 PNG 位于
仓库外，未进入正式图库。后续需改善有效来源状态的空间分配，继续覆盖主题、
键盘、长路径和 OCR 原生路径；U05/U09/U11 不勾选完整完成。

有效来源拥挤的后续修复将阅读区改为可调宽度的横向分栏：左侧摘要与来源列表，
右侧元数据、文件来源及正文。两侧不可折叠，最低宽度 200/400 像素；保留原控件、
来源校验、范围/会话失效和只读语义，无新窗口或依赖。1200×600 的有效来源几何
检查先复现正文仅 100 像素，调整后正文至少 180 像素且未越出页面；另检查左右
位置关系与极端拖动不隐藏任一侧。管理套件 12/12 组通过（45.29 秒），宿主导出
适配检查通过；沿用 Qt Widgets 与既有源文件清单，无新增打包输入。该结果仅为
组件验证，新布局的原生复验记录如下。

`d5167fe` 的 V11 自动作业 34195894299 成功完成。正常退出此前进程并从系统
菜单启动后，PID 2217175 的运行文件与安装文件 SHA-256 均为
`9cfc25115e057a5466b2ef43f731f076606242c88aedcf42253c250dde7c6965`。
在 1440×900 原生桌面检索上述合成记录，来源方法、路径、UTC 时间和限制提示
四行同时可见，右侧正文可连续阅读多行。实际鼠标拖动使分隔线从约 x=514 移至
x=620，两侧保持可见；高级 raw 与正文往返时，独立来源区不变。切到家庭共享
范围后，摘要、来源列表、文件路径及正文均清空，要求重新检索；未执行共享写入。
本轮截图仅为仓库外诊断证据，未进入交付图库；未发送模型请求或更改采集配置。
完整主题、键盘、长路径、缺失/非法来源的同版原生矩阵与 OCR 仍待验证。

会话来源接入准备：`frontend/management/AgentEvidence` 解析 Runtime 会话详情的脱敏
JSONL，只接受当前会话、当前记忆范围且具备匹配开始记录的 `pixiu_memory_search`
完成结果；模型正文、其他工具与分支事件不作为引用，重复完成事件去重。输入上限
2 MiB、最多 256 条来源；损坏或不匹配结果不返回部分引用。此模块已纳入管理库、
导出与供应链输入，使用现有 Qt 依赖。测试覆盖关联、范围、非法 ID、日志损坏与上限。
`AgentEvidenceClient` 已提供限量异步 HTTP：复用调用方传入的 Runtime 地址/认证，
只构造当前会话详情路径；拒绝重定向，2 MiB 上限同时检查声明和流式数据，超时或
切换会话取消旧请求。错误不展示服务器正文或凭据。测试覆盖会话替换、超时、取消、
超大响应、HTTP 错误与重定向。统一阅读器已有 `showAgentSources` 入口：接收已校验
来源并按证据 ID 去重，选择后读取当前数据库正文；范围变化或敏感级别不再为零时
拒绝展示。请求在途拒绝覆盖，空记录不宣称未使用记忆。`0022` 在宿主加入本会话来源
按钮，复用当前 Runtime 地址和认证，支持主动取消；切换会话、后端或离开会话页
取消读取，退出保护覆盖来源请求。会话或后端变化也清空旧会话来源及正文，迟到证据
响应不得填回；独立记忆检索不受此清理影响。成功后进入同一阅读器，失败明确提示。
完整交互、异常状态及最新补丁的原生矩阵仍待验证，不能勾选本任务。

原生补充：`fa7dfd5` 的 V11 作业 34158488644 已通过并自动安装；开始菜单启动的
进程 1871253 与已安装宿主 SHA-256 同为
`3fcb24ec727d364526c05774e9c0587071192bd5f260bd9c68b066b137b86f70`。
实际点击来源按钮可读取既有会话来源并进入统一阅读器，选择后展示当前 TOOL_RESULT
证据及其范围/质量。此验证不包含后续 `5940f08` 的上下文清理；该提交通用 CI
34158675598 已通过。按钮打断品牌标题、空白检索区占位、`body.content` 未正文优先及
来源选中态可读性仍须改进；检查截图不作为最终图库。安装前旧进程曾停留在不可见
模态对话框，正常激活/退出未立即恢复，随后自行退出且无新增 core；原因尚未确定，
不能据新进程启动成功宣称正常升级退出已验收。

针对该原生检查，来源模式已隐藏空白检索结果区，返回独立检索时恢复；阅读器支持
`body.content` 正文优先，保留其他结构化字段和完整高级原始数据。来源按钮移至品牌
标题之后、设置之前。组件测试先复现空白区占位，再检查模式恢复、正文优先与字段
保留；源码修复不代替原生布局复验。来源列表选中态已补充明确前景、背景和边框，
移除原生焦点覆盖；导出主题渲染测试覆盖浅/深/浅切换及有焦点/失焦选中态，先失败
再通过。此样式仍须在新版原生安装包复验，不将模拟冲突样式视为 V11 桌面结果。

`98259cb` 的 V11 作业 34160092232 通过；开始菜单启动进程 1890336 的运行文件与
安装文件 SHA-256 均为 `76bfde899d96e9db2e9de0332b476fb89c1462e796ec5c605b816993fe3e0057`。
浅色实际界面已确认按钮位置、无空白回答框、正文优先和清晰选中态。切换既有会话
却仍残留来源：实际 `ChatWidget::loadSession` 未发出已连接的 `sessionChanged`。
导出检查先失败，补丁改为 ID 变化后、刷新消息前通知。
`53dc32d` 的 V11 作业 34161442338 通过；菜单启动进程的运行文件与安装文件
SHA-256 均为 `713879da862f53efd2e95312c42ad4017a4db7887e95f77e46059da08029a4e1`。
实际读取来源及正文后，从会话 `88138b23` 切换到 `bb8c4cad`，返回记忆页确认旧列表
与正文清空并提示重新读取。深色选中项在有焦点/失焦时均可读，恢复浅色后仍可读。
这些结果仅覆盖该构建与操作路径；尚未验证全部错误态或完整升级流程。
原生检查另发现浅色未勾选复选框标识不可见，点击后勾选标识和原始数据正常显示。
导出主题补丁为未勾选状态补充背景、边框、焦点及禁用样式；渲染回归先复现不可见，
再验证浅/深/浅、焦点变化、空格切换与禁用态。沿用现有补丁供应链清单，无新增依赖；
此复选框修正尚待新版原生安装复验，检查截图不纳入最终交付图库。

- [ ] 从记忆结果及 Agent 相关展示进入统一证据详情。
- [ ] 正文优先，单独展示来源、范围、置信度、版本或时间等实际返回的元数据；原始数据仅作为可展开诊断信息。
- [ ] 处理缺失证据、长文、敏感内容与载入失败，不以内部 ID 作为用户理解入口。
- 验收：来源入口可到达真实证据，正文可读、可选择且没有布局截断。

### U06：偏好与冲突审计

统一宿主已接入偏好列表、证据提取、选中记录的版本历史和只读冲突列表。冲突展示
补充后端提供的关联记忆标题，处理结果及严重程度使用中文说明并保留枚举原值；
缺失字段明确显示未提供，未知结果原样保留，不猜测处理成功。详情保留冲突双方，
明确 MANUAL 为待人工确认，当前界面不提供裁决或回滚。组件回归先复现标题缺失，
再验证三种已知结果、未知/缺失值及新旧内容。沿用现有导出清单、Qt 依赖和公共
接口，无新增构建依赖；上述展示仍需原生界面及真实非空审计数据验证。

`ef7802f` 安装后经菜单启动，运行/安装文件 SHA-256 同为
`7f2158263b25dc73f4f35a24058f6866abd16bf875ae3ef733c61a0f6961598c`。
只读接口返回 1 条偏好、19 条冲突；界面实际读取偏好的两个历史版本及冲突双方。
此包不包含 `6d142d3` 的展示补充，且审计列表选中态实测为难辨认的灰底白字。
主题回归仅更换来源/审计列表名称即复现前者通过、后者失败；共享已有选中态规则后
两者浅/深/浅、有焦点/失焦测试通过。补丁仍在既有供应链输入内，不新增依赖。
实际后端记录未提供关联记忆标题，不以示例
填充，不将本组调试截图列入交付图库。

原生复验：`fd211c3` 的 V11 作业 34164563524 成功；菜单启动进程 1945502 与安装
文件 SHA-256 同为 `01a60e7be72df0f280e5a1daba41162b930218deef14a4ace9cb2992986bc0ee`。
偏好非空列表选中态及两个历史版本可读，冲突页实际显示中文结果/严重程度、缺失标题
提示和新旧内容；浅色与深色选中态均可读。设备页读取本机节点、网络关闭及零待发
记录，未勾选方框可见；没有保存设置或修改信任。这些是该构建的具体路径证据，
不等于所有主题状态、错误态或双设备传输验收，亦不覆盖后续 `1ab981d` 遗忘数量修正。

- [ ] 迁入偏好列表、提取、历史版本与刷新，避免要求用户手填内部 ID。
- [ ] 展示冲突双方、严重程度与后端实际处理结果。
- [ ] 不提供后端尚无契约的人工裁决或回滚按钮；发现契约缺口时明确记录。
- 验收：列表、详情、历史、空态与错误态对应真实返回数据。

### U07：安全遗忘

桌面预览的关联数量不再将缺失/非法数据默认为零：缺失、负数、非整数或错误类型
显示“未知”，合法零值仍显示零。该展示不改变目标范围、一次性确认与取消流程，
也不将关联预估描述为物理擦除。组件测试先复现缺失数量误显示零，再覆盖上述输入；
沿用 `ForgetPage` 的既有导出与依赖，完整原生遗忘矩阵仍待验收。

当前安全收口：Provider 工具只预览，不再生成模型可复用的确认引用或执行删除。
工具 schema 仅接受 command；伪造 token、confirm、approved 等参数均拒绝，指引
用户在正式桌面重新预览确认。Module E 测试覆盖真实后端凭证也不能使工具执行，
HTTP/D-Bus 桌面确认路径保持不变。此限制不等同于跨工具访问隔离；会话到真人审批
的自动交接、持久化失败恢复仍未完成，不勾选整体 U07。

持久化基础：Module E 新增独立 `Outbox`，使用 Python 标准库 SQLite 保存有容量/
字节上限的待发送记录，按调用方分区、幂等键去重，事务领取及超时恢复防止并发
重复领取；成功确认必须匹配当前有效领取凭据。测试覆盖重启、冲突、重试、并发、
权限及容量拒绝时保留旧记录。目录/文件仅当前用户可访问，不写入认证配置。
已接入 Provider 后台记忆写入和生命周期队列，存放于 Runtime 画像目录，按端点/
范围摘要分区；原会话及幂等键恢复不改写，网络发送保持异步，本地入盘同步。
诊断显示未确认投递，退出保留失败记录；预取仍为可丢弃的内存任务。测试覆盖后端
失败→退出→新 Provider 恢复原请求及只读队列压力。独立子进程分别在入队、领取、
确认事务完成后直接退出（不调用 SQLite.close），恢复测试确认待发送键/载荷保留、
租约到期前不可抢占、到期可重新领取及已确认记录不复活。该存储层证据不代表整机
断电或服务端去重验证；沿用现有 pytest/标准库及 CI 测试目录，无新增依赖。
真实 Runtime 崩溃/断网、完整
退出边界和用户可见失败处理仍待验收，不将存储接线视为完整可靠性通过。

- [ ] 统一显式遗忘入口和 Agent 相关确认体验，移除旧聊天前缀意图分流。
- [ ] 展示实际预览目标、影响范围与级联信息，支持取消并阻止重复提交。
- [ ] 不把模型输出当成人类确认；明确当前后端遗忘范围与软删除、墓碑语义，不宣称物理擦除。
- [ ] 验证预览变更、过期响应、执行失败和成功后刷新；必要契约变更按模块单独实现测试。
- 验收：取消不修改数据，确认只执行预期操作，完成事件不会触发第二次执行。

### U08：设备、配对与同步

- [ ] 迁入发现、配对、信任设备、状态、暂停与撤销信任。
- [ ] 核查配对请求的真实双端行为，不把本地事件或本地请求记录当作远端批准。
- [ ] 覆盖拒绝、过期、离线、恢复、无设备、撤销失败与退出网络确认。
- [ ] 展示共享范围及降级信息，不在公开截图暴露实际私有地址或令牌。
- 验收：真实设备与隔离测试分别证明交互和状态一致性；未验证项目不得标完成。

### U09：采集与隐私控制

目录 IngestBridge 已在构造时强制合法私有范围，拒绝任何 shared 域，不再仅靠
默认值和文档声明保证采集私有性。测试先复现共享/非法配置被接受，再覆盖拒绝
及合法自定义私有范围保留。复用现有核心校验器和测试入口，无新依赖/迁移。
来源元数据已新增独立的 `FileCaptureSource` 模型和 schema v13 evidence JSON 列，
与 Agent provenance、raw 和知识正文分离；只允许私有文件兼容来源，仓储重新校验
复制后的模型。旧数据保持缺失值，不推测补填路径。模型/迁移/仓储 124 项本地测试
通过，覆盖 v12 旧记录保持、重复迁移、重新打开数据库及共享域拒绝；发布清单测试
已核对代码实际 schema，不再写死 12。Foundation/Engine/Agent 集成 886 项本地
回归通过（Python 3.14，12 条依赖弃用警告），不替代 V11 新包验证。
IngestionService 已提供独立 `capture_source` 参数并直接交给 Evidence，绕过正文
清洗/归一化/指纹链路。引擎 162 项测试通过；新增 SQLite 集成证明不同来源路径
不改变知识正文、FTS 或向量化输入，非法共享/不兼容来源在 save 前拒绝。
现有 HTTP、目录监视和 Agent 集成 142 项兼容回归通过（2 条依赖弃用警告）。
目录桥接现已通过独立参数传入实际读取绝对路径、文本/OCR 方法和内容取得后的
Unix 秒时间戳，不解析符号链接。目录监视/配置/API 的 70 项本地测试通过，覆盖
真实 watchdog 文本/OCR 桩入库、敏感隔离、相对路径、链接路径、忽略及检测失败。
另有 HTTP 与引擎 241 项回归通过（2 条依赖弃用警告）。
正式界面已接入独立来源区；新目录文本的原生入库及阅读展示已核对（见 U05），
但不能将这一条文本路径及上述测试当作完整端到端交付。

`eb5875f` 的 V11 安装已通过正式设置页开启总闸与目录来源，仅监视专用合成数据
目录；新增 UTF-8 文本后，目录日志产生 `ingested`，界面在未手动刷新日志时收到
事件并更新。记忆详情与日志引用同一 evidence，范围为 `user:local`。随后通过
同一页面恢复原配置，公开接口复核总闸及四来源均关闭、目录列表为空；合成文件
及已入库测试记忆仍保留，不把关闭采集描述为清除既有数据。

来源核查确认：上述已安装版本的桥接层仅传文件名和正文，默认使用 `MANUAL_CONFIG` connector；
`Evidence.provenance` 的实际类型是 Agent 会话/工具溯源，不是通用文件元数据，
因此目录记录为 null 不应通过伪造 session/run/turn 修复。该版本日志可关联采集来源
和证据 ID，但证据本身未保存原始文件定位或采集方式，不能宣称可打开原文件或
完整文件溯源。当前源码已补独立字段及入库链路，新目录文本已完成正式界面原生核对；
旧数据不能推测补填路径。上述实证不覆盖 OCR、行为采集、权限失败及停用后的
事件竞态，不勾选整项完成。

正式 PrivacyPage 已补齐旧日志界面具备而正式页遗漏的时间与关联 ID，统一显示 UTC
和秒级精度，缺失/非法时间不回退为纪元零点或当前时间；不改变后端倒序分页。
选择后支持右键及 Ctrl+C 复制，并提示公开前核对私人信息；刷新不自动写剪贴板。
回归先复现时间缺失，再覆盖有效纪元/负秒、非法类型/分数/毫秒、缺失 ID、原样文本、
主动复制及刷新后清除选择。复用现有 Qt 与宿主导出，未新增依赖、存储或后台调用。
组件结果不替代原生长文本排版和真实日志操作验收，U09 保持未完成。
原生流水线 `34209929220`（`facd935`）已成功完成安装和 SDK 生命周期验证。
通过正常退出及系统菜单重启后，实际进程与已安装程序 SHA-256 均为
`2b53bb95da1f1127319e405f40a022bb961840670eaf470b8ca3ee317f5f2fee`。
V11/Wayland 实拍确认 system 日志的 UTC 时间、缺失 ID 提示及真实 Ctrl+C 复制；
复制结果仅粘贴到未保存草稿核对，随后清空并重新读取原配置，总闸关闭、目录为空。
发现选中日志失焦后出现浅灰背景配白字：宿主主题补丁 0019 仅覆盖来源和审计列表，
未覆盖 privacyEvents。该对比度问题须修复并复验，当前诊断图不作为交付截图，
也不能据此宣称全部日志排版、主题或目录浏览原生验收通过。
源码修复已将补丁 0019 的列表规则限定到 `pixiuWorkspaces` 下的全部 QListWidget，
覆盖日志、设备及洞察列表，而非继续维护遗漏控件名称的白名单。选中规则不依赖焦点，
明确使用主题正文/选中背景、移除原生渐变并保留边框；不改宿主外部会话列表。
导出回归检查真实应用补丁后的规则，现有明暗色板对比度检查通过；这两项静态检查
不能证明原生样式优先级和实际像素结果，须用包含该修复的安装包复验后再关闭缺陷。

正式页补充目录浏览入口，复用宿主既有 KylinFileDialog 适配，通过导出补丁 0025
接入而不修改上游 submodule。选中仅追加配置草稿，取消、重复选择、相对路径及
含前后空白/换行的路径不追加；选择期间禁止保存，结束后仍须显式保存且不会自动
开启采集。组件回归覆盖上述状态及不可用来源字段保留；原生对话框交互仍待验收。
本项正式管理回归 18/18 通过（47.30 秒），追加的选择期间退出保护用例单独通过；
宿主导出适配检查和全新目录的 KYSDK=OFF 宿主编译通过，未新增依赖。
原生 `86978a0` 安装验证后，经系统菜单正常重启，进程与安装文件 SHA-256 均为
`ee164465334be6d4c75fae612b5dcf214c731bb3c3727130c6dfc9fdfe0cefd9`。
真实 V11/Wayland 操作确认：读取前浏览禁用；打开目录对话框时主页面禁用；取消
保持空草稿；选择中文目录仅加入草稿，公开配置接口仍为总闸/四来源关闭、目录为空。
随后重新读取配置撤销本次草稿，未点击保存。目录浏览基本链路通过，但内嵌 QFileDialog
存在英文系统控件文字及选中行对比度不足。其实现是既有麒麟外壳内嵌 Qt 文件选择器，
不是系统原生文件选择服务或独立安装器；工作区 QListWidget 修复不覆盖其中的文件视图。
须在既有宿主适配链补齐翻译加载及文件视图主题并原生复验，当前画面不作为最终交付图。

- [ ] 迁入采集总开关、有效来源、目录管理、日志、暂停与敏感提示。
- [ ] 对照实际采集实现显示能力；剪贴板、截图、OCR 等缺失能力明确不可用，不用配置字段冒充实现。
- [ ] 保留有效配置，不因页面初始化覆盖用户状态；展示保存失败及实际生效状态。
- 验收：修改配置能改变真实采集行为，禁用或不可用来源不会显示虚假成功。

### U10：洞察与摘要

`66c3cbd` 经 V11 作业 34187928792 自动构建、严格 ON 验证并安装；正常关闭
旧进程后从开始菜单重启，运行文件与安装文件 SHA-256 同为
`c60cbbffa9070799d369e526f219f4bd25ec59bf7a2599bd24d8fed20456638b`。
真实桌面手动刷新洞察，显示目录采集的合成文件 `pixiu-capture-example.txt`，
标题/摘要与公共接口一致，质量分 0.775 显示为 0.78。选中候选并点击标题检索后，
同窗口转到 `user:local` 检索，选择来源展示合成正文。该入口是标题检索，不能
解释为按 knowledge ID 精确打开；证据来源仍为 MANUAL_CONFIG，文件溯源缺口
未解决。检查期间未写入或遗忘数据；过程截图不作为最终交付图库。
此前旧进程的一次导航点击后窗口隐藏，恢复后才能切页，原因仍未确定；不能以
新进程这次成功替代完整导航与错误恢复验收。

正式 DeliveryPage 保留日期选择与正文展示；旧 PixiuApp 将简报全文发送到系统通知
的接线、孤立测试数据及对应重复测试已删除，不保留第二个摘要呈现路径。
`eb5875f` 的 V11 安装实际读取本机今天及前一日，日期和空结果正文与公开接口
返回一致，切换日期会立即清除旧正文。随后 U09 的真实目录采集使所选日的公开
接口与正式界面均显示“当日新增 1 条记忆（目录 1）”，完成非空规则简报的这一
具体路径验证；输入是明确标注的合成文本，非直接伪造统计或 UI 响应。该结果
不覆盖非空洞察候选、全部来源计数或错误恢复。正式组件保留非空正文、错误、
日期不匹配与数据事件迟到响应回归；剩余原生矩阵仍待完成。

- [ ] 在唯一应用中接入已有洞察与摘要，支持空态、刷新及错误处理。
- [ ] 准确呈现现有规则聚合能力，不能将它包装成未实现的模型推理。
- 验收：界面内容可回溯至真实后端结果，不使用演示桩作为发布证据。

### U11：统一视觉与桌面行为

旧窗口的屏外恢复需求已迁入正式 `HostTray` 唤起路径：普通窗口若没有位于某块
真实屏幕可用区域内的可操作标题区域，则移回主屏；不使用多屏外接矩形把屏幕
间隙误判为可见。不改变窗口大小、不重置正常可见位置，也不退出最大化或全屏。
回归先复现远离屏幕时只 show/raise 仍不可达，再覆盖正/负屏外位置、边缘残留、
重复唤起和最大化/全屏恢复。此项沿用 Qt Gui/Widgets 与现有 HostTray 导出，无新
依赖或设置存储；多屏热拔插和 Wayland compositor 是否接受移动仍须原生验证，
不据此删除全部旧生命周期测试或宣称小屏/缩放矩阵完成。

`eb5875f` 原生安装已验证未发送合成草稿的退出保护：窗口关闭弹确认，默认回车
取消后内容保留；最小化后 `--quit` 恢复同一进程的窗口并再次确认，Esc 保留草稿。
明确放弃后进程退出，系统菜单重开仍显示原有会话列表，输入框为空，采集配置
保持全部关闭；测试未发送草稿或修改记忆。此路径不证明在途模型请求或升级重启
的完整退出矩阵。

实测确认框在中文界面显示 Yes/No，现改为可翻译的“保留编辑”“放弃并退出”，
不依赖系统 Qt 翻译包补齐动作含义。回归先复现旧按钮文案，再验证默认回车、Esc
保留以及显式放弃退出；默认/逃逸按钮和退出前在途状态复查保持不变。沿用已有
Qt Widgets 与 HostCloseGuard 导出清单，无新增依赖；新文案原生包复验仍待完成。

`d39661f` 的原生自动作业 34180052396 成功，安装/进程哈希一致后复验仍显示
Yes/No，故撤回“仅 setText 即解决原生文字”的推断。最小测试在普通 Qt 环境通过，
切入正式 UKUI/Wayland 环境则显示前为中文、显示后变回标准按钮文字；同环境改为
自定义动作后文字保留。现 HostCloseGuard 使用具名自定义保留/放弃按钮，按实际
clickedButton 判断放弃，裸 Yes 返回码不能授权退出；默认回车/Esc 与在途复查保留。
麒麟流水线新增该真实 UKUI/Wayland 控件回归，检查会话 socket 和样式，隔离测试
配置并限时；测试库用 KYSDK=OFF 只隔离快捷键/通知副作用，不把它记为 SDK 验收，
后续严格原生包构建与 SDK 门仍独立执行。新完整宿主画面和新增流水线门均待复验。
`0521664` 作业 34182160600 在构建控件测试时失败：误传 `BUILD_TESTING`，
项目忽略该变量，未生成 `t_memory_workspace`，因此尚未执行原生控件测试。
现更正为模块实际开关 `PIXIU_MANAGEMENT_TESTS=ON`，原生画像回归检查开关与
目标声明；使用新构建目录复验，不将已有测试缓存或先前包的成功作为本次门通过。
`0d3c597` 作业 34183277393 已编译测试，但原生测试因查找按钮为空而崩溃。
最小原生复现确认 UKUI 将原按钮移入实际 MessageBox，原 QMessageBox 不再包含
这些子按钮；连续打开还可能暂留旧隐藏按钮。测试现从当前活动模态窗口取动作，
不从原控件或整个宿主取第一个同名按钮，缺失时报明确失败。相同 V11/UKUI/Wayland
环境完整用例已通过中文文字、默认回车、Esc、无动作返回码及明确放弃；产品保护
未放宽。该结果为原生控件验证，新的完整自动构建/安装及宿主画面仍须复验。

- [ ] 统一主题、图标、语言、焦点、快捷键、通知与托盘，移除第二份应用设置状态。
- [ ] 检查小屏幕、缩放、长文本、深浅主题、键盘访问和错误文案可读性。
- [ ] 保留有价值的暂停和事件提示，不保留独立悬浮球应用。
- [ ] 验收正式设置页主窗口置顶及 Wayland 双向状态反馈；输入保留、离线导航和可访问名称按正式会话路径验收，不以旧聊天壳测试替代。
- 验收：各导航页属于同一视觉体系；通知与快捷键只注册一次，状态一致。

置顶已接入正式 SettingsWorkspace/HostWindowPin，不新增窗口或设置文件。V11 官方
waylandhelper 开发包与已安装运行库核对后，确认底层动作为切换而非幂等设置；
只读原生查询能唯一识别现有 PIXIU 主窗口并读取有效、未置顶状态。ON 因此仅选择
当前进程与标题唯一匹配的有效窗口，等待实际反馈，超时或目标替换不自动重试。
OFF 仅报告 Qt 兼容请求，保留隐藏/最小化/最大化/全屏状态，不声称合成器已确认。
新增 Qt 与 SDK 替身回归覆盖状态保持、外部窗口/重名拒绝、双向反馈及超时保护；
V11 真实头文件编译通过。画像新增 waylandhelper 运行/开发包与 KF5WindowSystem
开发包，源码纳入既有宿主导出及供应链清单。

f2246f0 原生作业 34207026274 已成功完成自动构建、安装和 SDK 生命周期检查。
正常关闭安装前进程后，从系统菜单启动的新进程与安装文件 SHA-256 均为
`ad723e7e0a5f4d095811126c5511d212a94aecb17a940814583c5e14ccd09f3a`。
真实设置点击置顶后，界面与 SDK 均为已置顶；终端获得焦点时 SDK active=false、
above=true，画面中 PIXIU 仍覆盖终端。取消置顶后 above=false，终端可以覆盖
PIXIU。再次置顶并最小化，SDK minimized=true 且 above=true；实际 Ctrl+Alt+P
恢复同一窗口及进程，minimized=false、above=true。最后通过界面恢复未置顶，
未发送模型请求或修改记忆。此为 V11/Wayland 的上述实际路径验证，不覆盖全部
窗口状态、多屏、超时故障及 U11 矩阵；诊断图不自动作为最终交付图库。

实测补充：同一 V11 安装包从开始菜单启动可鼠标进入记忆页，但部分容器黑底配深色
文字不可读；SSH/tty 启动样式不同，不能作为桌面入口外观证据。导出补丁 `0017`
补齐普通容器主题背景，渲染测试在冲突 palette 下先失败，修复后浅/深/浅和输入框
背景通过，已接入现有桌面 CI 与整包测试入口。`50b9a58` 原生包真实菜单启动已验证
普通背景恢复，鼠标可进入记忆/简报页；历史与当天简报日期匹配，切换日期清空旧正文，
当前仅验证无新记忆响应。仍有选中标签、聚焦日期框黑底及日历标题对比不足；`0019`
补齐状态规则，渲染测试覆盖冲突原生绘制及日历文字 palette 的浅/深/浅切换。
补丁随导出脚本与供应链清单交付，无新增依赖；尚须新版包原生复验。
既有启动阶段导航异常未证明全面解决，U11 不勾选。

`971df1f` 通用 CI（34153755937）与 V11 原生门（34153789512）通过，自动安装后
真实菜单启动验证了浅色标签、聚焦日期框及日历标题可读、历史简报日期正确。
但从设置切换深色时进程发生 SIGSEGV，不能交付：调用栈进入 ThemeManager 延迟
重绘回调；本地真实导出回归在主题更新后销毁临时控件，同样以 139 退出。
`0020` 将排队重绘的裸指针快照改为 QPointer 快照，控件销毁后不再调用 update；
相同回归恢复通过。补丁已纳入导出及供应链输入，无新增依赖。
`18d12c6` 经 V11 原生门（34154933057）自动安装，清单确认 KYSDK=ON 与对应提交。
实际菜单启动后完成深→浅→深→浅切换，设置窗口正常关闭，仍可鼠标进入记忆页；
全程同一进程，运行文件与安装文件 SHA-256 均为
`8c97549299a3936fa376e69aaf4a845c2df528e8379286dcc6ed1a2c4fb50a01`，
无新增崩溃记录。深色标签、聚焦日期框与日历月份标题可读。此证据仅覆盖上述路径，
不证明所有控件状态、DPI、并发业务、全部生命周期或最终发布已完成。
显示身份补充：`0021` 使窗口与桌面启动项复用同一 PIXIU SVG，Qt 资源嵌入避免
依赖图标主题查找；主窗口标题统一 PIXIU，KylinAgent 来源说明保留在界面副标题。
适配回归先复现图标引用不一致，再核对资源、标题及源图标字节一致性。源资源和补丁
纳入供应链输入，无新增依赖和数据迁移；尚未安装此补丁原生复验，不勾选 U11。

### U12：安全升级与用户数据迁移

- [ ] 迁入检查更新、下载、完整性及签名校验、系统授权、安装反馈和受控重启。
- [ ] 将产品级安装与重启辅助脚本从旧前端归属中解耦，更新所有打包和测试引用。
- [ ] 核查新进程退出和 Runtime 生命周期，保留现有备份、健康判定、回滚与错误区分。
- [ ] 明确旧设置向唯一应用的映射，保留用户数据、设备身份和 Agent 历史。
- 验收：成功升级与故障注入均通过；取消授权、无效签名和回滚失败不会误报成功。

### U13：同一宿主的平台适配

通用升级回归的临时文件隔离已落实到测试程序入口，不再依赖调用者提供不同 TMPDIR。
upgrade_controller 与 check_update_dialog 在 Qt 应用创建前各自建立 QTemporaryDir，
确认 Qt 使用该私有路径后才执行用例；创建或路径确认失败即拒绝运行。临时目录
存活至控制器和应用销毁后，不改变产品下载/安装路径，不新增系统依赖。
根回归和正式管理 CMake 均加入两项哨兵检查：在外层目录放置模拟其他进程的安装包，
执行最小用例后必须原样保留。修复前两项均稳定复现错误删除，修复后均通过。
两个套件使用同一外层 TMPDIR 并发执行，根回归 38/38 组通过（27.30 秒，真实
升级未启用），正式管理 16/16 组通过（45.80 秒）。这修复测试清理的跨进程越界，
不代表原生升级验收完成；无需修改生产代码或依赖。

- [ ] 在 SDK 适配层支持通用 Debian 系构建，禁止改为另一套前端回退。
- [ ] 保留麒麟 V11 的 SDK 优先路径，明确无 SDK 时桌面能力差异。
- [ ] 检查宿主、Runtime、离线依赖与架构画像，不只证明单个 Qt 窗口可启动。
- 验收：通用 `KYSDK=OFF` 与 V11 `KYSDK=ON` 各自构建并完成真实基础操作，报告分开。

### U14：旧应用删除与残留清理

旧 MessageList、ThinkingSkeleton、EvidenceCard 和 frontend/src/models/ChatMessage.h
及两项专属测试已删除；根 CMake、版本门及翻译资源同步清退。上游宿主同名消息
类型和正式 AgentEvidence/MemoryWorkspace 来源阅读器未改动。
旧卡片只是外部数据和证据 ID 的展示/信号转发，不是来源验证；旧置信度文案、
固定气泡宽度及占位结束清空整个列表的行为不迁移。正式会话历史、流式输出和
来源读取沿当前宿主路径验收，长文、错误重试和键盘可达性仍列 U05/U11。
删除仅涉及源码与资源，不清空用户真实消息、证据、数据库或会话。
资源生成、重新配置和构建通过；根回归 32/32 组（26.77 秒，真实升级未启用）、
正式管理 16/16 组（45.83 秒）及宿主导出适配通过。旧展示链的源码引用仅剩
退役检查，四份更新文档的章节标题和顺序保持不变。

旧 InputBar 实现、专属测试和翻译上下文已删除；UiIcons 已无其他调用者，一并
清退，正式宿主图标不受影响。根 CMake 移除目标，版本门拒绝旧输入栏及图标库恢复。
Enter/Shift+Enter、输入保留、离线导航、窄屏入口可达性与可访问名称继续按 U11
正式会话路径验收；旧“发出信号立即清空”和溢出菜单未同步禁用状态不迁移。
记忆/设备/设置采用主导航，采集状态使用实际配置与服务反馈，不保留第二套输入
状态徽标。未修改上游会话输入实现，不读取或删除用户草稿、会话和配置。
资源生成、重新配置和构建通过，根回归 34/34 组（27.22 秒，真实升级未启用）、
正式管理 16/16 组（46.49 秒）及宿主导出适配通过。源码引用仅剩退役检查，
四份更新文档的章节标题与顺序保持不变。

ChatWindow 窗口壳、专属测试、翻译上下文和旧窗口几何配置键定义已删除；
根构建清单移除装配目标，版本门防止回归；其消息展示子链也已清退，其他旧管理
控件和控制器继续按调用关系清退，不把旧聊天壳删除等同所有旧前端源码均已清空。
正式会话归属上游宿主与 Runtime，记忆/设备/设置导航在同一主窗口，简报由
DeliveryPage 提供；不迁移静态建议或“关闭即隐藏”的旧生命周期。窗口置顶由正式
HostWindowPin 接管，原生证据见 U11；输入保留、离线导航和无障碍继续用正式路径验收。
不删除用户窗口配置、会话或记忆，也不修改只读上游代码。
资源生成、重新配置和构建通过，根回归 36/36 组通过（27.68 秒，真实升级未启用），
正式管理 16/16 组通过（45.84 秒），宿主导出适配通过。代码中旧 ChatWindow 引用
仅剩退役门禁；四份更新文档的原章节标题与顺序保持不变。

欢迎页专属 UserIdentity 账户姓名读取器及唯一测试已删除，根 CMake 同步移除
目标，版本门拒绝其恢复；不读取或修改真实账户。UiIcons 中无调用者的图钉绘制
重载一并清退，其他仍被引用的图标保留。UpgradeUtils 的注释改为当前正式升级
调用关系，不再声称 pixiu 命名空间只属于旧窗口装饰或依赖已删除的姓名工具。
正式窗口置顶已由同一宿主实现并取得 U11 所列原生证据，不保留无调用者图标。
重新构建后根回归 35/35 组通过（27.62 秒，真实升级未启用），正式管理
16/16 组通过（45.73 秒）；旧姓名读取和图钉函数在产品代码中已无引用，
版本门保留退役检查，三份更新文档的原章节标题与顺序不变。

FloatingBall 实现、专属回归、翻译上下文与位置键定义已删除；根构建清单与版本门
同步清退并防止旧目标回归。AppSettings 的缺省值测试改用明确测试键，不以旧球
位置字段维持测试。已有用户配置文件和其中数据不删除。
替代关系：HostTray/ShortcutManager 唤起同一窗口，HostCloseGuard 保护退出，
正式记忆与设置页面承接导航，PrivacyPage 以实际保存配置控制采集。旧角标仅为
外部赋值的本地事件计数，不迁作可靠业务未读数量；保留正式事件失效提示及通知。
拖动、呼吸动画与独立窗口位置不迁移；完整事件、采集和多屏矩阵仍须验收。
重新配置、构建及资源生成通过，根回归 37/37 组通过（27.36 秒，真实升级未启用），
正式管理 16/16 组通过（46.21 秒），宿主导出适配通过。前端/发布代码中剩余
FloatingBall 引用仅为退役检查；四份受影响文档的全部章节标题与顺序保持不变。

旧 SettingsDialog 的头文件、实现和专属测试已移除，快捷键由正式设置与 HostTray
接管，采集入口由 PrivacyPage 接管。仍被正式宿主使用的 InfoDialog 与
CheckUpdateDialog 不删除：原混合测试中的只读正文、关闭不影响宿主、版本显示及
无控制器禁用升级检查拆为 product_dialogs，同时纳入根回归和正式管理库测试。
正文测试使用纯测试文本并检查 HTML 字面量原样显示，不沿用失实隐私承诺。
删除 SettingsDialog 专属翻译上下文与翻译断言，重新生成资源；版本门禁止旧
设置源码和构建目标回归。语言选择不迁入空壳控件，正式管理的完整英文资源与
语言切换仍列 U11 待办；不删除用户已有设置文件。其他旧窗口和历史文档仍待清理。
本次重新配置及构建通过：根回归 36/36 组（27.64 秒，真实升级未启用），
正式管理 14/14 组（45.25 秒，独立临时目录），宿主导出适配检查通过。
首次并发运行两个套件时，管理升级页的临时包清理断言失败；测试均扫描共享临时
目录，隔离目录后全套通过。此处记录测试隔离风险，不宣称产品升级已完成真机验收。

PixiuApp 装配层、仅由它引用的 SingleInstanceGuard/TrayIcon/EvidenceDetailDialog
及 app_navigation/window_restore 两项旧测试已删除，根 CMake 同步移除两个目标。
正式管理库与导出清单不消费这些文件；共用通信、升级、快捷键、通知及其测试保留。
版本门新增旧装配源码缺失及旧测试目标禁止回归检查。替代关系为：HostTray 的同窗
唤起与屏外恢复、HostCloseGuard 的在途/草稿保护、MemoryWorkspace 的来源阅读、
PrivacyPage 的显式采集授权、DevicePage/PairingDialog 的信任操作及 SettingsWorkspace
的升级入口。自定义快捷键设置与持久化现已迁入正式 HostTray（见 U02），
其原生矩阵、完整语言、多屏及真实双端配对仍须继续验收；不把删除旧测试当作这些能力已通过。
其余旧控件、控制器、专属资源及文档引用仍在清理范围；不删除用户配置或数据。
删除后重新配置及构建通过，剩余回归 36/36 组通过（27.82 秒，真实升级未启用），
正式管理 12/12 组通过（45.70 秒），宿主导出适配检查通过；旧四类定义和 include
在前端/发布代码中已无引用。三份本轮更新文档的章节标题及顺序保持一致。

旧 PixiuApp/EventRouter/ForgetController 中遗忘广播直达执行的专用接线、命令缓存、
confirmRemote 方法及两项旧语义测试已移除，不迁移该无预览凭证流程。旧事件路由
新增遗忘广播忽略检查；正式 BackendEventStatus 的广播只读提示、无弹窗/命令回归
与 ForgetPage 的凭证测试继续保留。此项只清退该残留路径，不代表其他旧生命周期
或普通遗忘控制器已经全部删除；无真实遗忘请求，无新增构建输入或依赖。
保留回归 38/38 组通过（真实升级用例未启用），正式管理事件/页面 2/2 组与宿主
导出检查通过；旧 API 名称和缓存字段在 frontend/src、frontend/tests 中已无引用。

- [ ] 在替代能力验证后删除旧可执行目标、main、PixiuApp、聊天外壳、悬浮球及重复生命周期。
- [ ] 删除仅服务旧界面的资源、脚本、打包目录与测试；保留并迁移有效业务测试。
- [ ] 更新所有旧程序启动引用、安装清单和卸载行为，不保留隐藏的第二产品入口。
- [ ] 删除失效的小窗口截图及引用，不增加旧版展示归档；用户数据不在删除范围。
- 验收：源码引用检查、安装包文件清单和真实进程检查共同证明旧前端已退役。

发布版本预检与清单生成器已解除对旧 CMake/main.cpp 的依赖，改查实际管理库的
根 VERSION 宏派生及宿主版本导出/注入。隔离回归不包含旧入口，并分别注入管理库
和宿主版本漂移验证拒绝发布。独立产品目标、main 及安装规则已删除，旧 CMake
只保留测试；版本回归要求旧目标不可构建。旧浮球首击脚本已删除，regression.sh
改为管理回归加同一宿主按 OFF/ON 选择构建，不再启动旧程序。剩余旧类和专用测试
仍待清理，不能据此宣布旧应用源码已完全删除。
旧 DeliveryController 的两个源文件、两处构建清单、PixiuApp 启动/显示时自动
洞察请求及重复导航用例已删除；正式 DeliveryPage 承担候选展示、标题检索、按日
简报、空态与错误处理。共享 HTTP transport、升级控制器和有效管理回归保留。
正式页面不启动旧欢迎页轮询，不用静态建议掩盖失败；原生非空洞察的一条真实
路径已在 U10 记录，完整候选/异常矩阵仍待完成。
旧聊天壳内已经没有调用方的 `setInsights`、`InsightCard`、动态成员和两项
专用渲染测试已删除；正式 DeliveryPage 的非空内容、空态及标题检索回归仍保留。
无新增依赖或目标，沿用既有回归构建；这不代表 PixiuApp 与整个旧聊天壳已删除。
共用 HTTP 契约测试已纳入管理库 CMake，不再只由旧应用构建承载。遗忘替身删除了
仅凭 confirm=true 成功的旧协议，按后端一次性凭证绑定命令/范围；错域消耗凭证、
重新预览后才能执行，重复确认返回冲突。测试只证明传输与替身契约，不替代真实
遗忘、同步副作用或人机确认验收；其余旧界面专用测试仍待逐项迁移或删除。

### U15：构建、CI 与自动发布

- [ ] 更新 CMake、版本派生、依赖、包体、桌面入口、升级和供应链证据生成。
- [ ] 通用 CI 覆盖唯一宿主及管理功能；原生门覆盖完整安装和 SDK 路径。
- [ ] 检查 Release 版本、签名资产、源码归档与已测试提交一致，不绕过原生门手工上传包。
- [ ] 在明确的推送与发布授权范围内提交、推送并通过自动化发布安装包；记录实际作业及产物证据。
- 验收：通过的工作流对应实际交付内容，所有安装入口启动唯一应用。

### U16：回归、文档与截图交付

- [ ] 汇总单元、接口、宿主构建、真实桌面、安装升级与数据保留验证，不以测试数量代替覆盖说明。
- [ ] 保持既有目录与章节，更新受影响全局及模块文档、操作说明、提交材料与生成文档；官方原件不变。
- [ ] 按原有效构图重拍安装、启动截图；补全会话、记忆、证据、偏好、冲突、设备、隐私、主题与升级示例。
- [ ] 截图只来自真实软件，演示数据明确为合成数据；更新索引、画廊与清单，核对无旧版素材残留。
- [ ] 每个逻辑变更独立提交，检查忽略规则、无意外产物、工作区干净及远程同步结果。
- 验收：逐任务附可复核证据；任何未验证平台、界面或发布门保持未完成状态。

## 执行依赖与完成审计

唯一宿主接入和公共通信支撑各管理任务；通用宿主适配及管理功能验证是删除旧应用的前提。升级迁移与包体变更必须共同验证。截图与交付说明基于已运行的最终界面，而不是规划中的页面。

完成审计须对 U01—U16 的每个未勾选项提供实际证据并更新状态。构建成功不能代替运行，模拟测试不能代替原生验证，绿色 CI 不能代替安装包唯一入口检查，计划或 ADR 已提交不能代替实现完成。

## 实施记录

- U11 产品显示名称：修正启动入口覆盖主窗口 PIXIU 标题的问题，显示名称改为 PIXIU，保留主窗口既有 PIXIU/openKylin 来源标识。0016 不修改内部应用/组织身份和存储路径；后续 0018 将 desktop 身份修正为实际随包安装的 `com.kylin.pixiu`，而非未安装的 `kylin-agent`。回归先复现 desktop 标识不匹配，再核对导出声明、启动项源文件和打包路径。此改动不迁移配置/历史或单实例身份，不代表导航故障已修复；图标、任务栏关联与完整主题仍须实际验收，不勾选 U11 完成。
- U03 设置诊断入口：唯一设置页新增服务与能力快照，通过专用共用 HTTP transport 依次读取健康、版本和能力，展示实际适配器与配置区别、混装/接口范围提示；不把 SDK 报告称为完整验收。读取失败、矛盾数据和刷新会清除旧结果，在途拒绝重复请求并纳入关闭保护。组件、HTTP 请求顺序/失败中止及宿主导出/供应链检查覆盖此接线；全局共享状态、版本兼容操作门和事件刷新仍待实现，真实新包界面未验，不勾选 U03 完成。
- U03 健康探测与断开保护：共用 HTTP transport 显式连接后使用 `/health`，核对组件、ready 和数据库 ok；普通 HTTP 可达不覆盖健康判定。断开取消在途探测，并以连接代次阻止旧业务响应改变连接状态，同时保留业务结果信号。五类异常健康响应先在旧实现下复现误判，再完成修复；既有 HTTP 回归接入管理 CMake，六组测试通过。管理页统一状态订阅、版本/能力握手、事件重连仍待实现，不勾选 U03 完成。
- U05 结构化证据阅读：默认纯文本区同时保留正文和附加字段，以层级及列表呈现非文本内容，布尔/空值不丢失，HTML 保持字面文本；高级 raw JSON 继续独立且默认收起。普通展开有节点、深度和字符上限，超出明确提示，原始视图仍保留末尾字段。先用组件测试复现结构字段缺失，再实现并通过管理/HTTP/升级五组回归；未新增依赖，沿用同一宿主导出文件。真实新包长文和主题、Agent 来源跳转尚未验证，不勾选整体 U05。
- U03/U11/U13 安装后界面实证：`8a7772b` 的 V11 作业 34147789543 全部通过；旧进程正常关闭后经 `/usr/bin/pixiu` 启动，运行文件与已安装 `/usr/bin/kylin-agent` 的 SHA-256 同为 `9b2d78a74ec8cb4163feafaae538c098a22159babc5d5cabd5157db7e2d1d85b`，不再指向 deleted 文件。实际标题显示 `PIXIU · KylinAgent — PIXIU`，产品名称已生效，但重复品牌文字和上游机器人图标仍需统一。通过定向 X11 键盘事件进入服务诊断页并实际读取：桌面/后端 0.1.8、HTTP API 0.5.0、Agent Memory API 1、schema 12、kylin 11、Embedding/向量存储 configured/runtime 均 kylin；页面显示读取完成和非完整验收提示。QMP 点击和普通键盘事件曾未切页，X11 指针坐标与截图指针位置不一致，原因未确定；定向键盘成功不能证明鼠标或完整导航验收通过。旧进程临时截图已删除，新检查图未纳入最终交付图库。`a77eb8c` 通用 CI 34148822450 通过，其日期控件尚未在此安装版本中验收。
- U03/U04/U13/U15 安装实证：`e4fe281` 的通用 CI 34141401672 与 V11 原生作业 34142095393 均通过；后者实际构建并安装严格 ON 包，完成官方 Embedding、Vector Engine 生命周期及公共记忆写入/检索/遗忘隐藏检查。候选 deb 为 114581312 字节，SHA-256 `4dceae97ff024ce887e446c5fec5692e041b01d10074c00e7d20a8e266f06f04`，下载后校验通过。环境为 V11 虚拟机，不称物理硬件验收，探针字段 `contest_ready` 不代表完整赛事验收或新 Release。
- 此项真实启动验证：旧宿主经窗口关闭入口正常退出后，以随包 `/usr/bin/pixiu` 启动；新进程唯一，安装 manifest 对应 `e4fe281`，引导器哈希与源码一致，进程 scope 和两个 endpoint 均匹配只读解析的当前 profile。真实检索页可选择 Agent 域，空白录入对话框默认 `user:default`；未提交数据。鼠标会话→记忆切换经实际截图 OCR 连续三次通过，设备页可点击进入；启动后曾出现点击记忆仍停留会话的现象，尚未稳定复现，不宣称生命周期或导航矩阵全部通过。临时界面检查不纳入最终交付截图，其他自定义域、共享默认值与 profile 热切换仍待验证。
- U14/U16 素材撤下：逐张核对交付图片目录后删除全部失效 PNG，保留目录、索引章节和画廊入口；清除对应清单、旧版本数据与成功展示说明，不建立展示归档。当前清单为空，唯一前端的新实拍、其他目录素材及生成文档仍须继续核验，不能据此勾选完整截图交付。
- U14/U16 前端图库撤下：核查七组截图来源与代表画面，删除 `frontend/docs/screenshots` 中 77 张旧小窗口/面板/悬浮球 PNG，七个子目录以 `.gitkeep` 保留，不创建旧图归档。更新 UI/UX 清单全部既有章节，清除开发任务和模块计划中的旧图库引用及相关展示完成声明；README 未修改。实际唯一宿主的新图、其他文档和生成材料仍须验收，不勾选完整交付。
- U03/U04 配置接线：正式入口使用产品 venv 的只读 dotenv 引导器，保留 profile 中有效范围与 endpoint；不修改数据或用户配置，不传出 profile 凭证。共用 Qt 范围选项覆盖当前 Agent 域，保留本机采集与家庭共享域；共享 Agent 不自动改变录入默认范围。新增引导器与跨管理入口请求测试并接入治理、整包安装及宿主供应链；真实新包安装、历史自定义域发现、运行中 profile 切换和洞察范围扩展尚未验证或实现，不勾选整体完成。
- U08 配对二维码：新配对对话框接入已有 libqrencode，QR 令牌可同时查看图形和复制文本，不新增生命周期；到期、方式切换、重新生成和关闭同步清除图形。超长编码失败明确使用文本，不宣称摄像头扫码；组件测试覆盖绘制、静区、清除及超长回退，真实扫码和双端传输仍待验证。
- U16 测试资源收尾：将退出挂起缩小到行为采集 DI 接线用例，真实 SQLite 连接被直接清空引用而未关闭。先新增子进程退出回归并确认超时失败，再复用 stop_db 清理；原组合 67 项正常通过并退出，无需人工中断。未修改生产采集逻辑，仍需远程 CI 及真实桌面验收。
- U09 采集状态复核：确认运行组件启动开关与持久化采集授权不同，默认配置总闸/来源均关闭；行为读取焦点前以及落库前均检查授权，目录事件处理也受 effective 门控。澄清安装配置注释与交付说明，不把启动日志误报为采集开启，不通过关闭组件破坏界面热生效。真实桌面开启/暂停与权限矩阵仍待验证。
- U05 证据原始数据：嵌入记忆页增加默认收起的高级原始数据切换，正文仍为默认；非文本结构可查看，切换来源/范围时清空并禁用旧数据。组件测试覆盖正文往返、结构化字段展示和旧内容清除。Agent 会话来源跳转与真实桌面可读性仍待补齐，不勾选整体 U05。
- U13/U15 离线后端闭包：整包拒绝关闭后端 wheels，统一解析后端与同步依赖并受 Runtime 固定版本约束；下载失败中止构建。postinst 删除在线回退，Runtime wheelhouse 缺失拒绝，安装后 pip check。CI 开启后端打包并验证解包后的空 venv 依赖共存。Shell、引导及配置回归已验证；真实新闭包构建和安装待 CI，不据源码声明完成。
- U12/U13 安装环境引导：移除 postinst 的 get-pip 下载、pip 在线升级与系统 Python 安装回退；创建或复用产品 venv 前后校验 cp312/隔离性，pip 缺失明确失败且保留既有环境。新增执行实际引导片段的隔离测试，覆盖新建、复用、系统 ABI 错误、创建失败、旧 venv ABI 错误与 pip 缺失，并接入治理。测试不代表实际 apt 安装；后端依赖在线回退仍待收口。
- U13/U15 安装依赖一致性：全部三个画像声明 cp312 对应的 Python 下限/上限及 venv；补齐麒麟兼容画像 Qt SQLite 插件和预置 curl。构建拒绝未知画像和缺失依赖清单，移除旧 Python 3.10 默认依赖回退。画像回归检查通过；源码 Python 3.13 测试与 cp312 整包适用范围分开，不声明未验证解释器可安装。
- U13/U15 同宿主整包接线：通用 CI 34126241898 的完整隔离宿主与 Runtime 离线构建、审计均通过。整包现强制审计对应目标、携带宿主/Runtime/网关及所有权标记；取消旧二进制安装和启动回退，旧源码回归暂留。通用依赖约束 cp312 并补 SQLite Qt 插件，CI 增加实际解包唯一入口断言。新包构建、安装和原生验证尚待执行，不勾选最终交付。
- U12/U13 Runtime 激活归属：新增独立 agent-bundled 标记读取，将网关迁移、启动、重启与事务恢复从 SDK 严格模式中解耦；通用随包模式不启用残留麒麟桥接或种入模型配置。集成测试覆盖通用成功、重启失败恢复、非法标记与严格/非随包矛盾拒绝，既有原生路径回归通过。整包尚未输出此标记或切换通用启动入口，仍须继续安装验证。
- U13/U15 通用实际构建接入：CI 用共用 OFF 脚本替代重复普通宿主构建，执行隔离宿主编译与 Runtime 离线安装、目标审计，并保存同提交完整供应链 Artifact。尚待远程执行结果，不能以工作流定义代替通用包安装成功。
- U13/U15 共用供应链准备：原原生专用脚本迁为 prepare-agent-supply-chain.sh，显式 ON/OFF 选择目标并透传隔离构建，宿主/Runtime/审计同目标；架构与 Python 锁前检在创建目录前执行。原生画像、供应链 12 项测试通过，本机 Python 3.14 实跑前检按预期拒绝。尚未以 CPython 3.12 执行新的完整通用离线构建，不宣称通用整包已就绪。
- U13/U15 通用供应链前置：记录器允许显式 generic-debian 目标，审计默认仍为 kylin-v11，新增 expected-os 并要求宿主/Runtime 同目标。供应链 12 项测试通过，覆盖通用记录不能通过默认原生审计及混合平台拒绝。尚未生成真实通用离线供应链记录或切换整包入口；字段标签不代替 SDK 编译/运行证据。
- U14 独立包目标移除：删除 frontend/debian 的 control/rules/postinst 和旧 build-deb.sh，前端回归打包步骤委托整包入口；版本测试检查旧目标不存在，受影响文档保留章节并删除旧包体积/产物宣称。单版本来源、旧前端 CMake 配置及其版本测试、升级辅助检查通过。旧可执行程序和生命周期尚未删除，不据此勾选唯一前端交付。
- U12/U14 升级辅助归属：install-update 与 restart-client 移入整包 Debian 安装树，删除旧前端源码副本，更新整包/暂存旧打包入口与测试引用。除归属注释外逐行一致，安装路径及安装后执行权限不变；源码 install-update 从 0644 统一为可执行脚本。Shell 语法、升级辅助及 Agent 安装集成测试通过；真实升级矩阵和旧应用包目标删除仍未完成。
- U04 真实后端联调：新增隔离服务编排与 Qt 编辑器实测，未替换业务服务，验证完整读取、界面保存进入 SQLite、正文/附加字段保留、版本递增，以及另一调用抢先提交后的草稿保留和陈旧保存拒绝。本地实跑通过，已接入通用 CI（远程执行尚未发生）。首次运行的仓库路径定位错误已修正并复验；临时测试数据及进程已清理。此结果不替代原生 SDK、真实桌面视觉或安装包验收。
- U04 编辑易用性：字符串 text 正文默认直接编辑，保留其他字段；高级 JSON 切换保留当前结构，非法内容禁止切换丢失。未保存关闭默认取消，已确认保存关闭不再提示。五组前端测试通过，新增字段保留、模式转换与关闭保护断言；复杂业务字段和真实桌面联调仍未完成。
- U04 编辑界面：明确范围检索后可编辑主要命中记忆，所属窗口模态对话框读取完整快照，提交变化字段及版本/幂等键；标题单独修改不重写正文，冲突保留输入并要求重新读取，未知结果原样重试复用键。源码适配及供应链 11 项通过。结构化 JSON 编辑已接入，普通用户字段控件、真实桌面/后端联调仍待完善，U04 不整体勾选。
- 此项验证：完整 OFF 宿主构建与五组管理/升级/HTTP 测试通过。新增组件测试验证冲突门控及输入保留、未知结果不报成功、标题单独提交和完整嵌套内容读取。HTTP 夹具首次因未解码 scope 的断言失败，改为按 URL 解码后核对复验通过；未修改实际请求编码。
- U04 完整编辑快照：新增按 ID 和显式 scope 读取 ACTIVE 记忆的只读接口，返回完整正文和版本，不存在/错范围/非 ACTIVE 统一 404。API 68 项回归通过，新增完整结构、无数据库写入、范围隔离与读取后并发更新拒绝测试。桌面编辑器及原生联调尚未完成。
- U04 更新接入基础：公共 HTTP transport 新增版本化召回与乐观锁更新，载荷保留范围、版本、结构化正文和幂等键，冲突不自动重试。管理及升级五测试组通过，其中新增本地 TCP 服务验证路径、载荷和冲突信号。编辑界面、完整正文读取及真实后端联调尚未完成，不能将召回的截断 context 回填为正文。
- U02/U11 导航收拢：宿主同级会话/记忆/设备/设置，SettingsWorkspace 接管隐私与升级，旧宿主齿轮切换设置页，模型配置仍使用同一宿主既有对话框。记忆页移除临时嵌套的设备/隐私/升级，不创建新应用生命周期。原生和真实缩放导航仍未验收。
- 此项验证：完整 OFF 宿主构建通过，管理与升级四测试组通过，源码适配及供应链 11 项检查通过。新增源码断言验证四个顶级入口、设置按钮导航及打开会话返回会话页；组件测试验证设置页嵌入性、配置请求信号和记忆页无重复设置控件。尚未验证真实桌面操作、缩放和新安装包入口。
- U07/U15 原生探针迁移：正常遗忘及失败清理均先取得后端预览凭证，强制 `user:acceptance` 范围和精确探针标题，主路径核对唯一检索 ID；非自有目标拒绝确认，结果集合必须匹配。检索也限定探针范围。原生探针单元测试 11 项通过，未重跑 V11 SDK 真机探针。
- 本轮完整通用回归：Python 3.14 隔离环境执行 Foundation/Engine/Module E 共 832 项通过，12 条来自测试依赖的弃用警告。此结果不覆盖 Qt 桌面、Runtime 实际模型、安装升级和原生双 SDK，不勾选整体交付项。
- U03/U07 兼容声明：HTTP API 升为 0.5.0 标识强制预览凭证的不兼容确认契约，Provider 只接受 0.5.x，拒绝旧 0.3/0.4 与未经验证后续 minor；产品版本和 Agent Memory v1 保持独立。发布探针夹具同步 API 字段，不改其独立产品版本测试数据。
- 此项验证：Module E 与 HTTP API 组合 100 项通过，发布模型接口探针夹具 10 项通过；旧 minor 拒绝断言和新版本报告断言均通过。尚未执行新版本真实 Runtime/安装包验收或自动发布。
- U07 Agent 调用方：Provider 遗忘固定 scope，校验预览字段/范围/有效期；已移除模型确认引用、内存令牌缓存及执行分支，工具仅提供预览并指引桌面重新确认。该限制不证明宿主完整真人审批已闭环。
- 此项验证：Module E 测试 35 项通过，新增后端凭证转交、模型预览隐藏凭证、跨会话/到期拒绝及缺失凭证拒绝断言；未修改上游工作树，也未以假客户端测试证明真实 Agent 人工审批。
- U07 D-Bus：新增 `ReviewedForget` JSON 方法，复用 HTTP 遗忘编排，显式注入知识仓储与同步服务；旧 `Forget(..., true)` 拒绝执行，避免绕过预览。D-Bus 11 项测试通过，覆盖凭证重放拒绝、旧确认拒绝、共享墓碑与方法签名；未连接真实总线，Agent 工具/脚本等调用方仍待迁移。
- U07 唯一窗口：新增 `ForgetPage` 与 HTTP `reviewedForget`，显式 scope 预览目标/版本/范围，取消无写入，编辑或到期失效；确认消费凭证，失败不允许重用，成功必须匹配预览 ID 集合。未采用旧前缀识别或事件自动确认；真实界面联调、旧调用方迁移与跨组件失败恢复仍未完成。
- 此项验证：管理与升级四组测试、完整 OFF 宿主构建、源码适配和供应链 11 项通过；新增交互验证范围、编辑/到期失效、取消、凭证传递、失败重预览与成功信号。不以界面替身测试证明真实副作用完成。
- U07 HTTP 凭证：`/forget` 强制确认携带 command/scope/目标版本绑定的一次性预览 token，执行前消费，过期/缺失/重放/不匹配拒绝；进程内有效期与容量受限，重启后重新预览。旧前端、Agent 工具、D-Bus、API 版本兼容声明及跨组件失败恢复尚待统一迁移，不具备发布条件。
- 此项验证：预览凭证、HTTP API 与 Engine 安全组合 89 项通过；覆盖容量、快照复制、到期、范围不匹配、缺失与重复凭证，以及真实 HTTP 预览后确认和共享墓碑。两条测试依赖弃用警告不影响通过；未验证进程重启后的用户界面交互。
- U07 原子存储边界：新增 `KnowledgeRepository.forget_if_versions`，SQLite 用单条条件更新校验整批 ACTIVE ID/版本并标记 FORGOTTEN、递增版本；引擎确认快照路径调用该契约，任一漂移整批拒绝。HTTP 凭证、向量失败恢复与同步墓碑可靠投递仍未闭环，不能视为安全遗忘整体完成。
- 此项验证：知识仓储 26 项、安全与 SQLite 集成 23 项通过；覆盖跨连接版本变化、缺失目标、整批拒绝、版本递增和旧版本编辑失效。无 schema 或依赖新增。
- U07 安全前置：代码核对发现当前确认会重新匹配自然语言且不绑定预览。Engine 新增可选 `expected_targets` ID/版本集合检查，预览补充版本与范围，集合或版本变化在删除前拒绝。尚未接入 API 凭证与存储原子 CAS，因此旧入口仍未安全迁移，不新增未经保护的确认界面；级联数量只是关联统计，非物理删除承诺。
- 此项验证：独立环境按现有 backend/requirements.txt 安装依赖，安全模块 22 项测试通过，包含新增匹配拒绝、版本变化拒绝、重新预览版本可执行及无删除副作用断言；没有新增项目依赖。此证据不覆盖 HTTP 或并发事务安全。
- U10：新增内嵌 `DeliveryPage`，读取实际个人域洞察与当日采集简报，支持按候选标题跳转个人域检索；空态说明候选/冲突抑制边界，明确简报为后端规则汇总。HTTP 洞察缺失列表字段改报错误，不再静默转为空数组。真实数据与桌面联调尚未完成。
- U10：简报补充日历日期选择，初始为本机今天，所选日按后端本地时区解释；沿用现有 `date` 查询参数，不改后端契约。切换日期与重读清空旧摘要，在途锁定选择，返回日期须与请求及当前选择一致。增加历史闰日、错日响应、失败后迟到响应及 HTTP 查询路径回归；真实桌面和采集日志统计仍须联调验收。
- 此项验证：管理与升级四组测试通过，新增洞察空态、标题检索信号、网络错误、非法日期与摘要显示断言；完整 OFF 宿主构建及供应链 11 项通过。未以替身测试替代真实简报数据验收。
- U08 退出网络：设备页读取实际节点后默认取消地请求确认，逐一核对解除响应，再关闭网络并读取剩余信任节点；中途失败明确报告可能部分完成并停止，不删除记忆、不自动重试或回滚。配对对话框改为所属窗口模态，避免同页操作竞争。真实双端和桌面验收仍未完成。
- 此项验证：管理与升级四测试组通过；新增取消无写入、串行解除、故障停止与最终节点核对测试；完整 OFF 宿主构建及供应链 11 项通过。配置关闭响应仅证明后端配置结果，不替代运行时网络停机验收。

- U08 配对入口：新增内嵌设备页所属的 `PairingDialog`，通过现有公共接口生成/交换 PIN 或 QR 格式文本令牌，方法匹配、在途防重、过期清除、失败保留及本地信任成功边界均落实；关闭仅清除界面，不宣称令牌已撤销。双端真实配对/同步、扫码交互及退出全部网络仍待完成。
- 此项验证：管理与升级四测试组通过；完整 OFF 宿主构建、源码适配及供应链 11 项通过。新增测试覆盖前导零 PIN、QR 不携带 PIN、响应方法不匹配、到期清除、失败输入保留、重复操作阻止、异常成功响应与本地成功信号。不以测试替身证明真实网络或 V11 验收。

- U08：新增内嵌 `DevicePage`，实现状态/节点读取、发现广播、启用/暂停保存及人工确认解除单设备本地信任；本机不可解绑，响应 ID 必须匹配，保存失败要求刷新确认，明确不把广播和本地记录当成同步完成。双端配对、全部退出、真实传输及桌面验收仍未完成，U08 保持未勾选。
- 此项验证：管理与升级测试组 4/4 通过，管理新增配置门控、默认取消、响应匹配、保存超时与设备发现状态断言；完整 OFF 宿主构建、源码适配、供应链 11 项与对比度检查通过。首次编译因 `/tmp` 配额耗尽失败，指定可写 TMPDIR 后通过；测试替身不证明双端网络结果。

- U12/U15：唯一宿主接入现有升级控制器/对话框，重启事件退出 Agent 应用；applicationVersion 改为根产品 VERSION，避免与上游版本混淆。原安全下载、验签、授权安装与回滚逻辑未重写。辅助脚本归属迁移及新宿主真实升级矩阵仍未完成。
- 此项验证：完整 OFF 宿主构建、源码适配/版本来源断言、供应链 11 项与对比度通过；管理与三个既有升级测试组 4/4 通过。首次测试遇到临时文件写入失败和升级测试共用临时文件的并发干扰；改用可写 TMPDIR、检查测试文件完整写入并为共享临时文件增加测试互斥后复验通过。未执行真实下载安装。

- U09：同窗口新增 `PrivacyPage`，接入采集配置读取/保存和分页日志；目录/行为为可编辑来源，剪贴板/截图明确未实现并保留其既有配置值。保存前校验配置及绝对目录，编辑未保存和操作失败分别提示。实际采集热生效与原生桌面验证尚未完成。
- 此项验证：管理测试覆盖加载前门控、非法目录拒绝、目录去重、不可用来源保留、保存失败编辑保留与日志显示；完整 OFF 宿主编译、源码适配、供应链 11 项和对比度检查通过。

- U06：`MemoryAudit` 嵌入记忆页，接线偏好列表、按列表选择历史、从现有证据提取偏好和只读冲突审计。加载、错误、空态分别展示，历史核对 ID；冲突无范围过滤或人工裁决接口，不提供虚假操作。真实后端与桌面验收仍未完成，U06 保持未勾选。
- 此项验证：管理交互测试覆盖列表选择、历史正文、提取载荷去重、冲突错误恢复和只读详情；完整 OFF 宿主编译、源码适配、供应链 11 项及主题对比度检查通过。

- U04：嵌入记忆页新增文本录入，按 `MANUAL_CONFIG` 与 `raw.body.text` 公共契约提交；失败保留输入，同载荷重试复用幂等键，在途防重，合法 accepted 才清空。证据页补齐对象正文读取。仍未完成记忆更新、真实后端联调与跨设备验证。
- 此项验证：管理模块测试新增重复提交、失败输入保留、幂等重试、未知响应和成功清空断言；完整 OFF 宿主重新编译通过，源码准备/适配及供应链 11 项检查通过。测试替身不能证明真实写入与同步完成。

- U02/U04/U05：新增可独立测试的 `frontend/management` 静态模块，宿主以同窗口页签嵌入记忆检索与正文优先证据查看；复用 HTTP transport，查询响应按请求 ID 隔离，范围变更清除旧结果。单元测试覆盖上述交互与失败重试；写入、更新、Agent 来源入口和其他管理页尚未迁入，相关任务保持未完成。
- 此切片验证：管理模块测试、包含模块的完整 OFF 宿主编译与隔离 XDG 的 `--hide` 启动、宿主补丁适配、供应链 11 项检查和主题对比度通过。证据加载串行化，失败可重选；不将测试替身或无窗口启动当作真实后端联调、原生桌面和视觉验收。

- U13：新增同宿主桌面对话框适配，默认保留官方 SDK，显式 OFF 使用 Qt；通用 CI 增加宿主构建与对话框行为测试。包体与启动入口尚未切换，U13 不整体勾选。
- U13 验证：最终补丁导出树完成 `PIXIU_HAVE_KYSDK=OFF` Release 编译；隔离 XDG 配置下 `QT_QPA_PLATFORM=offscreen kylin-agent --hide` 正常退出；动态依赖无缺失且未链接 kysdk。对话框行为、源码适配、供应链审计/记录和主题对比度检查通过。此处仅证明宿主编译与无窗口启动路径，不证明 Agent 对话、Runtime 安装、管理迁移或 V11 SDK 路径。

- 已完成独立代码审查并记录完整计划与批准决定；当前仍有两套前端，尚未切换启动入口或删除实现。
- U02/U15 构建接入基础：宿主构建与适配测试改为调用同一个源码准备脚本，纳入供应链摘要；管理模块和平台适配仍未完成。
- 此项验证：Shell 语法、宿主适配（包括拒绝覆盖非空目录和符号链接）、供应链审计 9 项、供应链记录 2 项、主题对比度检查通过。尚未以此变更完成宿主编译或原生安装，不能据此勾选 U02/U13/U15。

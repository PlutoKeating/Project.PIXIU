# 6.3.6 关键进程监控

封装 DBus 接口设置关键进程异常退出监控
DBus信息
dbus 服务名称：com.kylin.processMonitor
路径名称：/com/kylin/processMonitor
Interfaces：com.kylin.processMonitor
信号：
关键进程异常退出广播信号：signal_processExit
子模块信息

> 📌 发送关键进程异常退出广播信号(自1.2.0.9-0k7.0版本启用)
> **子模块**: 关键进程监控 | **接口类型**: DBus

**原型**:
```c
processExit (String path, String str) ↦ (Int32 ret)
```

**描述**: 发送关键进程异常退出广播信号 signal_processExit

**参数**:

| 参数 | 说明 |
|------|------|
| `path` | 关键进程程序路径 |
| `str` | 预留参数 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | - |
| `成功` | 其他 失败 |


**备注**: 无

---


> 📌 关键进程异常退出广播信号(自1.2.0.9-0k7.0版本启用)
> **子模块**: 关键进程监控 | **接口类型**: DBus

**原型**:
```c
signal_processExit(String path, String str)
```

**描述**: 关键进程异常退出广播信号

**参数**:

| 参数 | 说明 |
|------|------|
| `path` | 关键进程程序路径 |
| `str` | 预留参数 |


**备注**: 无

---


> 📌 设置应急响应类型(自1.2.0.9-0k7.0版本启用)
> **子模块**: 关键进程监控 | **接口类型**: DBus

**原型**:
```c
emergencyResponse (Int32 type) ↦ (Int32 ret)
```

**描述**: 设置应急响应类型

**参数**:

| 参数 | 说明 |
|------|------|
| `type` | 应急响应类型(1 锁屏；2 重启) |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | - |
| `成功` | 其他 失败 |


**备注**: 无

---

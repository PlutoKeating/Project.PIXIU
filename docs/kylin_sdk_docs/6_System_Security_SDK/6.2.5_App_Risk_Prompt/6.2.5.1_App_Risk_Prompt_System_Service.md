# 6.2.5.1 应用风险提示system服务


**dbus信息**

System Bus 接口
dbus 服务名称：com.kylin.secriskbox
路径名称：/service
Interfaces：com.kylin.secriskbox
信号：
返回信号：signalResult

**子模块信息：**


> 📌 获取当前系统语言环境(自1.2.0.9-0k6.1版本启用)
> **子模块**: 应用风险提示system服务 | **接口类型**: dbus

**原型**:
```c
QString getLanguage()
```

**描述**: 获取当前系统语言环境

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QString` | 当前系统语言 |


**备注**: 返回语言：中文:"zh_CN"；英文:"en_CN"；藏文:"bo_CN";
返回""，表示门禁管控禁止访问或没有调用方法的权限

---


> 📌 创建提示(自1.2.0.9-0k6.1版本启用)
> **子模块**: 应用风险提示system服务 | **接口类型**: dbus

**原型**:
```c
int create(int type, const QString &icon, const QString &title, int &uid)
```

**描述**: 创建提示

**参数**:

| 参数 | 说明 |
|------|------|
| `type` | 提示类型（0:弹窗 1:通知） |
| `icon` | 系统图标名称 |
| `title` | 提示标题 |
| `uid` | 当前进程的用户uid |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `>0` | 成功：返回提示的唯一标识 |
| `-1` | - |
| `创建失败` | -3 失败：设置的标题、图标至少有一项为空 -4 失败：级别类型错误 -6 失败：system获取session结果失败 -7 失败：system连接session失败 -8 失败：没有调用方法的权限 -5000 失败：门禁管控禁止访问 |


**备注**: 无

---


> 📌 设置弹窗阻屏(自1.2.0.9-0k6.1版本启用)
> **子模块**: 应用风险提示system服务 | **接口类型**: dbus

**原型**:
```c
int setDialogFull(int id)
```

**描述**: 设置弹窗阻屏

**参数**:

| 参数 | 说明 |
|------|------|
| `id` | 弹窗的唯一标识 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | - |
| `成功` | - |
| `-1` | 失败 |
| `-6` | 失败：system获取session结果失败 -7 失败：system连接session失败 -8 失败：没有调用方法的权限 -5000 门禁管控禁止访问 |


**备注**: 仅弹窗可用。默认不是阻屏，设置该接口后阻屏。

---


> 📌 隐藏关闭按钮(自1.2.0.9-0k6.1版本启用)
> **子模块**: 应用风险提示system服务 | **接口类型**: dbus

**原型**:
```c
int setDialogHideClose(int id)
```

**描述**: 隐藏关闭按钮

**参数**:

| 参数 | 说明 |
|------|------|
| `id` | 弹窗的唯一标识 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | - |
| `成功` | - |
| `-1` | 失败 |
| `-6` | 失败：system获取session结果失败 -7 失败：system连接session失败 -8 失败：没有调用方法的权限 -5000 门禁管控禁止访问 |


**备注**: 仅弹窗可用。默认不隐藏，设置该接口后隐藏关闭按钮。

---


> 📌 添加内容图标(自1.2.0.9-0k6.1版本启用)
> **子模块**: 应用风险提示system服务 | **接口类型**: dbus

**原型**:
```c
int addContentIcon(int id,const QString &icon)
```

**描述**: 添加内容图标

**参数**:

| 参数 | 说明 |
|------|------|
| `id` | 弹窗的唯一标识 |
| `icon` | 系统图标名称 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | - |
| `成功` | - |
| `-1` | 失败 |
| `-6` | 失败：system获取session结果失败 -7 失败：system连接session失败 -8 失败：没有调用方法的权限 -5000 门禁管控禁止访问 |


**备注**: 仅弹窗可用

---


> 📌 添加内容(自1.2.0.9-0k6.1版本启用)
> **子模块**: 应用风险提示system服务 | **接口类型**: dbus

**原型**:
```c
int addContent(int id, const QString &content)
```

**描述**: 添加内容

**参数**:

| 参数 | 说明 |
|------|------|
| `id` | 提示的唯一标识 |
| `content` | 内容 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | - |
| `成功` | - |
| `-1` | 失败 |
| `-6` | 失败：system获取session结果失败 -7 失败：system连接session失败 -8 失败：没有调用方法的权限 -5000 门禁管控禁止访问 |


**备注**: 添加的第一条内容为摘要，剩余的几条为正文。
添加按钮(自1.2.0.9-0k6.1版本启用)
子
模
块
应用风险提示system服务
接
口
类
型
dbus
原
型
int addButton(int id, const QString &name, const QString &strActionCmd)
描
述
添加按钮
参
数
id
提示的唯一标识
name
界面显示的按钮名称
strActionCmd
点击按钮执行的命令
返
回
值
0
成功
-1
失败
-6
失败：system获取session结果失败
-7
失败：system连接session失败
-8
失败：没有调用方法的权限
-5000
门禁管控禁止访问
备
注
通知：添加界面显示按钮时，name为按钮名称，strActionCmd为点击按钮执行的命令；添加点击
窗体响应时，name为点击窗体执行的命令，strActionCmd为"default"。包含default按钮，按钮数
量最多三个。界面最多显示两个按钮，default按钮不显示。
弹窗：添加的按钮全部为界面要显示的按钮。按钮数量最多三个。按钮添加后将从右到左添加到弹
框界面中。

---


> 📌 设置超时(自1.2.0.9-0k6.1版本启用)
> **子模块**: 应用风险提示system服务 | **接口类型**: dbus

**原型**:
```c
int setTimeout(int id, int second)
```

**描述**: 设置提示自动关闭时间

**参数**:

| 参数 | 说明 |
|------|------|
| `id` | 提示的唯一标识 |
| `second` | 超时时间（单位为秒） |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | - |
| `成功` | - |
| `-1` | 失败 |
| `-6` | 失败：system获取session结果失败 -7 失败：system连接session失败 -8 失败：没有调用方法的权限 -5000 门禁管控禁止访问 |


**备注**: 弹窗：默认为30秒。>0：弹窗关闭时间；-1：表示弹窗没有超时时间，一直显示。
通知：默认为6秒。>0：表示通知显示持续时间；0：表示通知将直接进入通知中心，不会显
示；-1：表示通知常驻，不会收纳进通知中心

---


> 📌 设置弹窗鉴权用户名(自1.2.0.9-0k6.1版本启用)
> **子模块**: 应用风险提示system服务 | **接口类型**: dbus

**原型**:
```c
int setDialogAuth(int id, const QStringList &authList)
```

**描述**: 设置弹窗鉴权用户名

**参数**:

| 参数 | 说明 |
|------|------|
| `id` | 弹窗的唯一标识 |
| `authList` | 管理员用户名列表 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | - |
| `成功` | - |
| `-1` | 失败 |
| `-6` | 失败：system获取session结果失败 -7 失败：system连接session失败 -8 失败：没有调用方法的权限 -5000 门禁管控禁止访问 |


**备注**: 仅弹窗可用

---


> 📌 设置鉴权信息(自1.2.0.9-0k6.1版本启用)
> **子模块**: 应用风险提示system服务 | **接口类型**: dbus

**原型**:
```c
int setDialogAuthInfo(int id, const QString &info)
```

**描述**: 设置鉴权信息

**参数**:

| 参数 | 说明 |
|------|------|
| `id` | 弹窗的唯一标识 |
| `info` | 鉴权信息 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | - |
| `成功` | - |
| `-1` | 失败 |
| `-6` | 失败：system获取session结果失败 -7 失败：system连接session失败 -8 失败：没有调用方法的权限 -5000 门禁管控禁止访问 |


**备注**: 仅弹窗可用。设置鉴权控件的信息，如“允许或禁止操作，需要输入您的管理员密码”。

---


> 📌 更新鉴权控件(自1.2.0.9-0k6.1版本启用)
> **子模块**: 应用风险提示system服务 | **接口类型**: dbus

**原型**:
```c
int updateDialogAuth(int id, const QString &auth)
```

**描述**: 更新鉴权控件

**参数**:

| 参数 | 说明 |
|------|------|
| `id` | 弹窗的唯一标识 |
| `auth` | 鉴权提示信息 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | - |
| `成功` | - |
| `-1` | 失败 |
| `-6` | 失败：system获取session结果失败 -7 失败：system连接session失败 -8 失败：没有调用方法的权限 -5000 门禁管控禁止访问 |


**备注**: 仅弹窗可用。密码输入错误时，通过此接口设置要显示的提示。如“认证失败，连续输错 4 次
将锁定账户”

---


> 📌 设置折叠(自1.2.0.9-0k6.1版本启用)
> **子模块**: 应用风险提示system服务 | **接口类型**: dbus

**原型**:
```c
int setNotifyFold(int id, int state)
```

**描述**: 设置折叠

**参数**:

| 参数 | 说明 |
|------|------|
| `id` | 通知的唯一标识 |
| `state` | 折叠状态（0为折叠，1为不折叠） |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | - |
| `成功` | - |
| `-1` | 失败 |
| `-6` | 失败：system获取session结果失败 -7 失败：system连接session失败 -8 失败：没有调用方法的权限 -5000 门禁管控禁止访问 |


**备注**: 仅通知可用。默认折叠。

---


> 📌 设置交互模式(自1.2.0.9-0k6.1版本启用)
> **子模块**: 应用风险提示system服务 | **接口类型**: dbus

**原型**:
```c
int setInteractive(int id,int mode)
```

**描述**: 设置交互模式

**参数**:

| 参数 | 说明 |
|------|------|
| `id` | 弹窗的唯一标识 |
| `mode` | 交互模式（0：单次交互；1：多次交互） |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | - |
| `成功` | - |
| `-1` | 失败 |
| `-6` | 失败：system获取session结果失败 -7 失败：system连接session失败 -8 失败：没有调用方法的权限 -5000 门禁管控禁止访问 |


**备注**: 仅弹窗可用，默认为单次交互。

---


> 📌 展示提示(自1.2.0.9-0k6.1版本启用)
> **子模块**: 应用风险提示system服务 | **接口类型**: dbus

**原型**:
```c
int show(int id)
```

**描述**: 展示提示

**参数**:

| 参数 | 说明 |
|------|------|
| `id` | 提示的唯一标识 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | - |
| `成功` | - |
| `-1` | 失败 |
| `-2` | 失败：添加按钮错误（通知可显示按钮超过两个，弹窗按钮超过三个或小于1个） -5 失败：没有设置内容 -6 失败：system获取session结果失败 -7 失败：system连接session失败 -8 失败：没有调用方法的权限 -5000 门禁管控禁止访问 |


**备注**: 无

---


> 📌 关闭提示(自1.2.0.9-0k6.1版本启用)
> **子模块**: 应用风险提示system服务 | **接口类型**: dbus

**原型**:
```c
int close(int id)
```

**描述**: 关闭提示

**参数**:

| 参数 | 说明 |
|------|------|
| `id` | 提示的唯一标识 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | - |
| `成功` | - |
| `-1` | 失败 |
| `-6` | 失败：system获取session结果失败 -7 失败：system连接session失败 -8 失败：没有调用方法的权限 -5000 门禁管控禁止访问 |


**备注**: 无

---


> 📌 返回信号(自1.2.0.9-0k6.1版本启用)
> **子模块**: 应用风险提示system服务 | **接口类型**: dbus

**原型**:
```c
int signalResult(int id, int state, const QString &reserved)
```

**描述**: 弹窗关闭时发出的信号

**参数**:

| 参数 | 说明 |
|------|------|
| `id` | 提示的唯一标识 |
| `state` | 关闭状态 |
| `reserved` | 返回信息 |


**备注**: state -1:超时退出 0:点击窗体（用在通知）123:某个位置按钮被点击（弹窗按钮顺序从右到左，顺
序）；-2:点击右上角关闭按钮。
reserved格式为： { "type": 0， //type为0时候，为用户鉴权信息，user表示账户名，passwd为
对应密码； "authentication": { "user": "xxx"， "passwd": "xxx" } }

---

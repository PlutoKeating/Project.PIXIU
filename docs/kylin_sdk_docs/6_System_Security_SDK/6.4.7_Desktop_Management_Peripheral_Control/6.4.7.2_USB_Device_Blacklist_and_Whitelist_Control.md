# 6.4.7.2 USB设备黑白名单管控

封装 DBus 接口设置USB设备黑白名单管控（该功能需麒麟授权，如果需要使用，请联系麒麟技术开发人员）
服务说明： 自libkysdk-desktopctrl 1.2.0.9-0k1.2版本起，取消了桌管外设管控服务自启动，若需使用
相关功能请启动以下服务：
# 桌面管控设备插拔监控服务：

```bash
$ sudo systemctl enable kylin-mdm-monitor.service
```


```bash
$ sudo systemctl start kylin-mdm-monitor.service
```

# USB设备黑白名单管控DBus服务：

```bash
$ sudo systemctl enable dbus-com.kylin.mdm.listcontrol.service
```


```bash
$ sudo systemctl start dbus-com.kylin.mdm.listcontrol.service
```

DBus信息
dbus 服务名称：com.kylin.kysdk.ListControl
路径名称：/com/kylin/kysdk/ListControl
Interfaces：com.kylin.kysdk.ListControl
信号：无
子模块信息

> 📌 设置USB设备黑名单(自1.2.0.8版本启用)
> **子模块**: USB设备黑白名单管控 | **接口类型**: DBus

**原型**:
```c
SetBlackList (String pid, String vid) ↦ (Int32 arg_2)
```

**描述**: 设置USB设备黑名单

**参数**:

| 参数 | 说明 |
|------|------|
| `pid` | 设备产品ID(Product ID) |
| `vid` | 设备生产厂商ID(Vendor ID) |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | - |
| `成功` | 其他 失败 |


**备注**: 无

---


> 📌 设置USB设备白名单(自1.2.0.8版本启用)
> **子模块**: USB设备黑白名单管控 | **接口类型**: DBus

**原型**:
```c
SetWhiteList (String pid, String vid) ↦ (Int32 arg_2)
```

**描述**: 设置USB设备白名单

**参数**:

| 参数 | 说明 |
|------|------|
| `pid` | 设备产品ID(Product ID) |
| `vid` | 设备生产厂商ID(Vendor ID) |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | - |
| `成功` | 其他 失败 |


**备注**: 无

---


> 📌 删除USB设备黑名单(自1.2.0.8版本启用)
> **子模块**: USB设备黑白名单管控 | **接口类型**: DBus

**原型**:
```c
DelBlackList (String pid, String vid) ↦ (Int32 arg_2)
```

**描述**: 删除USB设备黑名单

**参数**:

| 参数 | 说明 |
|------|------|
| `pid` | 设备产品ID(Product ID) |
| `vid` | 设备生产厂商ID(Vendor ID) |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | - |
| `成功` | 其他 失败 |


**备注**: 无

---


> 📌 删除USB设备白名单(自1.2.0.8版本启用)
> **子模块**: USB设备黑白名单管控 | **接口类型**: DBus

**原型**:
```c
DelWhiteList (String pid, String vid) ↦ (Int32 arg_2)
```

**描述**: 删除USB设备白名单

**参数**:

| 参数 | 说明 |
|------|------|
| `pid` | 设备产品ID(Product ID) |
| `vid` | 设备生产厂商ID(Vendor ID) |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | - |
| `成功` | 其他 失败 |


**备注**: 无

---


> 📌 获取USB设备黑名单列表(自1.2.0.8版本启用)
> **子模块**: USB设备黑白名单管控 | **接口类型**: DBus

**原型**:
```c
GetBlackList () ↦ (Array of [String] list)
```

**描述**: 获取USB设备黑名单列表

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `list` | 黑名单列表 |


**备注**: 无

---


> 📌 获取USB设备白名单列表(自1.2.0.8版本启用)
> **子模块**: USB设备黑白名单管控 | **接口类型**: DBus

**原型**:
```c
GetWhiteList () ↦ (Array of [String] list)
```

**描述**: 获取USB设备白名单列表

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `list` | 白名单列表 |


**备注**: 无

---


> 📌 清空USB设备黑名单(自1.2.0.8版本启用)
> **子模块**: USB设备黑白名单管控 | **接口类型**: DBus

**原型**:
```c
ClearBlackList () ↦ (Int32 arg_0)
```

**描述**: 清空USB设备黑名单

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | - |
| `成功` | 其他 失败 |


**备注**: 无

---


> 📌 清空USB设备白名单(自1.2.0.8版本启用)
> **子模块**: USB设备黑白名单管控 | **接口类型**: DBus

**原型**:
```c
ClearWhiteList () ↦ (Int32 arg_0)
```

**描述**: 清空USB设备白名单

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | - |
| `成功` | 其他 失败 |


**备注**: 无

---

# 6.1.10 水印管控

D-BUS接口：
名称：com.kylin.kysdk.systemsettins
路径：com/kylin/kysdk/watermark
接口：com.kylin.kysdk.watermark

> 📌 创建水印(自1.2.0版本启用)
> **子模块**: 创建水印 | **接口类型**: C

**原型**:
```c
int kdk_watermark_create(const char *name);
```

**描述**: 创建水印

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 水印名称 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 0-成功，非0-失败 |


**备注**: 无

---


> 📌 删除水印(自1.2.0版本启用)
> **子模块**: 删除水印 | **接口类型**: C

**原型**:
```c
int kdk_watermark_delete(const char *name);
```

**描述**: 删除水印

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 水印名称 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 0-成功，非0-失败 |


**备注**: 无

---


> 📌 设置水印是否可视(自1.2.0版本启用)
> **子模块**: 设置水印是否可视 | **接口类型**: C

**原型**:
```c
int kdk_watermark_set_visibe(const char *name, bool visibel);
```

**描述**: 设置水印是否可视

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 水印名称 |
| `visibel` | true-显示，false-隐藏 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 0-成功，非0-失败 |


**备注**: 无

---


> 📌 获取水印是否可视(自1.2.0版本启用)
> **子模块**: 获取水印是否可视 | **接口类型**: C

**原型**:
```c
bool kdk_watermark_get_visibe(const char *name);
```

**描述**: 获取水印是否可视

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 水印名称 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `bool` | true-显示，false-隐藏 |


**备注**: 无

---


> 📌 设置水印用户名是否显示(自1.2.0版本启用)
> **子模块**: 设置水印用户名是否显示 | **接口类型**: C

**原型**:
```c
int kdk_watermark_set_username_visible(const char *name, bool visible);
```

**描述**: 设置水印用户名是否显示

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 水印名称 |
| `visible` | true-显示，false-隐藏 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 0-成功，非0-失败 |


**备注**: 无

---


> 📌 获取水印用户名是否显示(自1.2.0版本启用)
> **子模块**: 获取水印用户名是否显示 | **接口类型**: C

**原型**:
```c
bool kdk_watermark_get_username_visible(const char *name);
```

**描述**: 获取水印用户名是否显示

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 水印名称 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `bool` | true-显示，false-隐藏 |


**备注**: 无

---


> 📌 设置水印时间戳是否显示(自1.2.0版本启用)
> **子模块**: 设置水印时间戳是否显示 | **接口类型**: C

**原型**:
```c
int kdk_watermark_set_timestamp_visible(const char *name, bool visible);
```

**描述**: 设置水印时间戳是否显示

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 水印名称 |
| `visible` | true-显示，false-隐藏 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 0-成功，非0-失败 |


**备注**: 无

---


> 📌 获取水印时间戳是否显示(自1.2.0版本启用)
> **子模块**: 获取水印时间戳是否显示 | **接口类型**: C

**原型**:
```c
bool kdk_watermark_get_timestamp_visible(const char *name);
```

**描述**: 获取水印时间戳是否显示

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 水印名称 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `bool` | true-显示，false-隐藏 |


**备注**: 无

---


> 📌 设置水印内容是否显示(自1.2.0版本启用)
> **子模块**: 设置水印内容是否显示 | **接口类型**: C

**原型**:
```c
int kdk_watermark_set_context_visible(const char *name, bool visible);
```

**描述**: 设置水印内容是否显示

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 水印名称 |
| `visible` | true-显示，false-隐藏 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 0-成功，非0-失败 |


**备注**: 无

---


> 📌 获取水印内容是否可视(自1.2.0版本启用)
> **子模块**: 获取水印内容是否可视 | **接口类型**: C

**原型**:
```c
bool kdk_watermark_get_context_visible(const char *name);
```

**描述**: 获取水印内容是否可视

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 水印名称 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `bool` | true-显示，false-隐藏 |


**备注**: 无

---


> 📌 设置水印透明度(自1.2.0版本启用)
> **子模块**: 设置水印透明度 | **接口类型**: C

**原型**:
```c
int kdk_watermark_set_opacity(const char *name, unsigned int trans);
```

**描述**: 设置水印透明度

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 水印名称 |
| `trans` | 0-100的透明度 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 0-成功，非0-失败 |


**备注**: 无

---


> 📌 获取水印透明度(自1.2.0版本启用)
> **子模块**: 获取水印透明度 | **接口类型**: C

**原型**:
```c
unsigned int kdk_watermark_get_opacity(const char *name);
```

**描述**: 获取水印透明度

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 水印名称 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 0-100透明度 |


**备注**: 无

---


> 📌 设置水印时间格式(自1.2.0版本启用)
> **子模块**: 设置水印时间格式 | **接口类型**: C

**原型**:
```c
int kdk_watermark_set_timeformat(const char *name, const char *fmt);
```

**描述**: 设置水印时间格式

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 水印名称 |
| `fmt` | 时间格式，符合QDateTime时间格式 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 0-成功，非0-失败 |


**备注**: 无

---


> 📌 获取水印时间格式(自1.2.0版本启用)
> **子模块**: 获取水印时间格式 | **接口类型**: C

**原型**:
```c
int kdk_watermark_get_timeformat(const char *name, char *fmt);
```

**描述**: 获取水印时间格式

**参数**:

| 参数 | 说明 |
|------|------|
| `[in] name` | 水印名称 |
| `[out]fmt` | 时间格式 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 0-成功，非0-失败 |


**备注**: 无

---


> 📌 设置水印内容(自1.2.0版本启用)
> **子模块**: 设置水印内容 | **接口类型**: C

**原型**:
```c
int kdk_watermark_set_context(const char *name, const char *context);
```

**描述**: 设置水印内容

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 水印名称 |
| `context` | 水印内容 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 0-成功，非0-失败 |


**备注**: 无

---


> 📌 获取水印内容(自1.2.0版本启用)
> **子模块**: 获取水印内容 | **接口类型**: C

**原型**:
```c
int kdk_watermark_get_context(const char *name, char *context);
```

**描述**: 获取水印内容

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 水印名称 |
| `context` | 水印内容 |
| `版本号` | - |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 0-成功，非0-失败 |


**备注**: 无

---


> 📌 设置水印字体大小(自1.2.0版本启用)
> **子模块**: 设置水印字体大小 | **接口类型**: C

**原型**:
```c
int kdk_watermark_set_fontsize(const char *name, int size);
```

**描述**: 设置水印字体大小

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 水印名称 |
| `size` | 字体大小 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 0-成功，非0-失败 |


**备注**: 这里的数字指的是相对大小

---


> 📌 获取水印字体大小(自1.2.0版本启用)
> **子模块**: 获取水印字体大小 | **接口类型**: C

**原型**:
```c
int kdk_watermark_get_fontsize(const char *name);
```

**描述**: 获取水印字体大小

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 水印名称 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 字体大小 |


**备注**: 无

---


> 📌 设置水印位置(自1.2.0版本启用)
> **子模块**: 设置水印位置 | **接口类型**: C

**原型**:
```c
int kdk_watermark_set_position(const char *name, unsigned int left, unsigned int top,
unsigned int horizontal, unsigned int vertical);
```

**描述**: 设置水印位置

**参数**:

| 参数 | 说明 |
|------|------|
| `[in] name` | 水印名称 |
| `[in] left` | 中心点左边距离 |
| `[in] right` | 中心点右边距离 |
| `[in] horizontal` | 横向大小 |
| `[in] vertical` | 纵向大小 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 0-成功，非0-失败 |


**备注**: [in]代表入参，[out]出参

---


> 📌 获取水印位置(自1.2.0版本启用)
> **子模块**: 获取水印位置 | **接口类型**: C

**原型**:
```c
int kdk_watermark_get_position(const char *name, unsigned int *left, unsigned int *top,
unsigned int *horizontal, unsigned int *vertical);
```

**描述**: 获取水印位置

**参数**:

| 参数 | 说明 |
|------|------|
| `[in] name` | 水印名称 |
| `[out] left` | 中心点左边距离 |
| `[out] right` | 中心点右边距离 |
| `[out] horizontal` | 横向大小 |
| `[out] vertical` | 纵向大小 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 0-成功，非0-失败 |


**备注**: [in]代表入参，[out]出参

---


> 📌 设置水印间距(自1.2.0版本启用)
> **子模块**: 设置水印间距 | **接口类型**: C

**原型**:
```c
int kdk_watermark_set_density(const char *name, int horizontal, int vertical);
```

**描述**: 设置水印间距

**参数**:

| 参数 | 说明 |
|------|------|
| `[in] name` | 水印名称 |
| `[in] horizontal` | 水平间距 |
| `[in] vertical` | 垂直间距 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 0-成功，非0-失败 |


**备注**: 如果是[-1,-1]则表示只显示一个(默认只显示一个)单位是pt

---


> 📌 获取水印间距(自1.2.0版本启用)
> **子模块**: 获取水印间距 | **接口类型**: C

**原型**:
```c
int kdk_watermark_get_density(const char *name, int *horizontal, int *vertical);
```

**描述**: 获取水印密集程度

**参数**:

| 参数 | 说明 |
|------|------|
| `[in] name` | 水印名称 |
| `[out] horizontal` | 水平间距 |
| `[out] vertical` | 垂直间距 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 字体大小 |


**备注**: 如果是[-1,-1]则表示只显示一个(默认只显示一个)单位是pt

---


> 📌 设置水印旋转角度(自1.2.0版本启用)
> **子模块**: 设置水印旋转角度 | **接口类型**: C

**原型**:
```c
int kdk_watermark_set_rotate(const char *name, int rotate);
```

**描述**: 设置水印旋转角度

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 水印名称 |
| `rotate` | 角度,默认0,可在(-360~360之间设置） |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 0-成功，非0-失败 |


**备注**: 无

---


> 📌 获取水印旋转角度(自1.2.0版本启用)
> **子模块**: 获取水印旋转角度 | **接口类型**: C

**原型**:
```c
int kdk_watermark_get_rotate(const char *name);
```

**描述**: 获取水印旋转角度

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 水印名称 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 旋转角度 |


**备注**: 无

---


> 📌 设置水印字体颜色(自1.2.0版本启用)
> **子模块**: 设置水印字体颜色 | **接口类型**: C

**原型**:
```c
int kdk_watermark_set_fontcolor(const char *name, unsigned int r, unsigned int g,
unsigned int b);
```

**描述**: 设置水印字体颜色

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 水印名称 |
| `r` | RGB中R色值 |
| `g` | RGB中G色值 |
| `b` | RGB中B色值 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 0-成功，非0-失败 |


**备注**: 无

---


> 📌 获取水印字体颜色(自1.2.0版本启用)
> **子模块**: 获取水印字体颜色 | **接口类型**: C

**原型**:
```c
int kdk_watermark_get_fontcolor(const char *name, unsigned int *r, unsigned int *g,
unsigned int *b);
```

**描述**: 获取水印字体颜色

**参数**:

| 参数 | 说明 |
|------|------|
| `[in] name` | 水印名称 |
| `[out] r` | RGB中R色值 |
| `[out] g` | RGB中G色值 |
| `[out] b` | RGB中B色值 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 0-成功，非0-失败 |


**备注**: 无

---

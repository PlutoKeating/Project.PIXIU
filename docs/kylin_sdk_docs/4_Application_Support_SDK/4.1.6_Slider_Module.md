# 4.1.6 滑动条模块


#### 4.1.6.1 滑动条

功能描述：非步数关系：步数为 1，可以在任意位置点击和拖拽；步数关系：步数为固定值，可根据步
数值点击和拖拽；步数为节点间隔，可根据节点间隔点击和拖拽。
枚举类型
枚举类型
enum
KSliderType{SmoothSlider,StepSlider,NodeSlider,SingleSelectSlider}
`};`

> 📌 设置节点间隔（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setTickInterval(int interval);
```

**描述**: 设置节点间隔

**参数**:

| 参数 | 说明 |
|------|------|
| `interval` | 节点间隔 |


**备注**: 无

---


> 📌 设置滑动条类型（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setSliderType(KSliderType type);
```

**描述**: 设置滑动条类型

**参数**:

| 参数 | 说明 |
|------|------|
| `type` | 设置滑动条类型 |


**备注**: 无

---


> 📌 获取滑动条类型（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
KSliderType sliderType();
```

**描述**: 获取滑动条类型

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `KSliderType` | 获取滑动条类型 |


**备注**: 无

---


> 📌 获取节点间隔（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
int tickInterval() const;
```

**描述**: 获取节点间隔

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 获取节点间隔 |


**备注**: 无

---


> 📌 设置值（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setValue(int);
```

**描述**: 设置值

**参数**:

| 参数 | 说明 |
|------|------|
| `int` | 设置值 |


**备注**: 无

---


> 📌 设置是否显示节点（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setNodeVisible(bool flag);
```

**描述**: 设置是否显示节点

**参数**:

| 参数 | 说明 |
|------|------|
| `true` | 显示节点 |
| `fasle` | 隐藏节点 |


**备注**: 无

---


> 📌 获取是否显示节点（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
bool nodeVisible();
```

**描述**: 获取是否显示节点

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 显示节点 fasle 隐藏节点 |


**备注**: 无

---


> 📌 设置tooltip（自1.2.0.7启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setToolTip(const QString&);
```

**描述**: 设置tooltip（自1.2.0.7启用）

**参数**:

| 参数 | 说明 |
|------|------|
| `QString` | tooltip提示信息 |


**备注**: 无

---


> 📌 获取toolTip（自1.2.0.7启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QString toolTip() const;
```

**描述**: 获取toolTip（自1.2.0.7启用）

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QString` | 获取tooltip提示信息 |


**备注**: 无

---


> 📌 设置是否启用半透明效果（自1.2.0.10启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setTranslucent(bool flag);
```

**描述**: 设置是否启用半透明效果（自1.2.0.10启用）

**参数**:

| 参数 | 说明 |
|------|------|
| `true` | 启用半透明效果 |
| `false` | 禁用半透明效果 |


**备注**: 无

---


> 📌 获取是否启用半透明效果（自1.2.0.10启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
bool isTranslucent();
```

**描述**: 获取是否启用半透明效果（自1.2.0.10启用）

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 启用半透明效果 false 禁用半透明效果 |


**备注**: 无

---

# 4.1.1.1 基础窗体

功能描述：KWidget，继承自 QWidget,支持响应主题背景切换，响应图标主题切换，标题颜色响应窗口
激活状态，窗口按钮样式符合 ukui3.1 的设计风格，分为四个组成部分
iconBar,windowButtonBar,sideBar,baseBar 。iconBar,windowButtonBar 默认高度为 40px，
sideBar 默认宽度为 200px。
枚举类型
```c
enum LayoutType{VerticalType, HorizontalType,
MixedType };
```

> 📌 获取左边栏widget,通过setlayout添加自定义内容(自1.2.0版本启用)
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QWidget* sideBar();
```

**描述**: 获取左边栏widget,通过setlayout添加自定义内容

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QWidget*` | 返回侧边widget |


**备注**: 无

---


> 📌 获取主内容区widget,通过setlayout添加自定义内容（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QWidget* baseBar();
```

**描述**: 获取主内容区widget,通过setlayout添加自定义内容。

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QWidget*` | 返回主内容区widget |


**备注**: 无

---


> 📌 通过icon添加窗体图标（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setIcon(const QIcon& icon);
```

**描述**: 通过icon添加窗体图标

**参数**:

| 参数 | 说明 |
|------|------|
| `QIcon` | 传入图片 |


**备注**: 无

---


> 📌 设置窗体图标，iconName 需要直接指定系统目录中的图标名称，如"kylin-music"（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setIcon(const QString& iconName);
```

**描述**: 设置窗体图标，iconName 需要直接指定系统目录中的图标名称，如"kylin-music"

**参数**:

| 参数 | 说明 |
|------|------|
| `QString` | 传入图片名称 |


**备注**: 无

---


> 📌 设置窗体名称。（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setWidgetName(const QString& widgetName);
```

**描述**: 设置窗体名称。

**参数**:

| 参数 | 说明 |
|------|------|
| `QString` | 传入需要设置的名称 |


**备注**: 无

---


> 📌 获取窗口三联组合控件，以控制是否显示最大化、最小化按钮和下拉菜单按钮。（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
KWindowButtonBar* windowButtonBar();
```

**描述**: 获取窗口三联组合控件，以控制是否显示最大化、最小化按钮和下拉菜单按钮。

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `KWindowButtonBar*` | 返回界面右上角的windowbuttonbar |


**备注**: 无

---


> 📌 获取窗口标题、图标组合控件，以控制相关样式。（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
KIconBar* iconBar();
```

**描述**: 获取窗口标题、图标组合控件，以控制相关样式。

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `KIconBar*` | 返回界面左上角的iconbar |


**备注**: 无

---


> 📌 设置布局结构类型。（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setLayoutType(LayoutType type);
```

**描述**: 设置布局结构类型。

**参数**:

| 参数 | 说明 |
|------|------|
| `LayoutType` | 设置窗体的布局结构，见上方枚举值 |


**备注**: 无

---


> 📌 设置窗口标志（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setWindowFlags(Qt::WindowFlags type);
```

**描述**: 设置窗口标志，1.2版本添加

**参数**:

| 参数 | 说明 |
|------|------|
| `Qt::WindowType` | Qt::WindowFlags枚举值 |


**备注**: 无

---


> 📌 设置窗口标志（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setWindowFlag(Qt::WindowType flag,bool on =true);
```

**描述**: 设置窗口标志，1.2版本添加

**参数**:

| 参数 | 说明 |
|------|------|
| `Qt::WindowType` | Qt::WindowType枚举值 |
| `true` | 启用设置的窗口标志 |
| `false` | 禁用设置的窗口标志 |


**备注**: 无

---


> 📌 设置sidebar是否遵循系统更改宽度（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setSidebarFollowMode(bool flag);
```

**描述**: 设置sidebar是否遵循系统更改宽度，1.2版本添加

**参数**:

| 参数 | 说明 |
|------|------|
| `true` | 遵循默认策略 |
| `false` | 遵循自主策略 |


**备注**: 无

---


> 📌 获取sidebar是否遵循系统更改宽度（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
bool sidebarFollowMode();
```

**描述**: 获取sidebar是否遵循系统更改宽度，1.2版本添加

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | kwidget跟随默认策略更改sidebar宽度 false kwidget跟随自主策略更改sidebar宽度 |


**备注**: 无

---

# 4.1.4.3 下拉菜单按钮

功能描述：KMenuButton，继承自 QPushButon，默认 ICon 为"open-menu-symbolic"，一级菜单中
包含 5 个选项，分别是："设置"，"主题"，"帮助"，"关于"，"退出"，主题中包括 3 个二级选项，分别
是："跟随主题"，"浅色主题"，"深色主题"。
QPushButton 的各个接口均适用。

> 📌 获取主菜单（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QMenu* menu();
```

**描述**: 获取主菜单

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QMenu*` | 返回主菜单menu |


**备注**: 无

---


> 📌 获取主题菜单（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QMenu* themeMenu();
```

**描述**: 获取主题菜单

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QMenu*` | 主题菜单menu |


**备注**: 无

---


> 📌 获取设置action（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QAction* settingAction();
```

**描述**: 获取设置action

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QAction*` | 获取设置action |


**备注**: 无

---


> 📌 获取主题Action（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QAction* themeAction();
```

**描述**: 获取主题Action

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QAction*` | 获取主题Action |


**备注**: 无

---


> 📌 获取帮助Action（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QAction* assistAction();
```

**描述**: 获取帮助Action

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QAction*` | 获取帮助Action |


**备注**: 无

---


> 📌 获取关于Action（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QAction* aboutAction()
```

**描述**: 获取关于Action

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QAction*` | 获取关于Action |


**备注**: 无

---


> 📌 获取离开Action（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QAction* quitAction();
```

**描述**: 获取离开Action

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QAction*` | 获取离开Action |


**备注**: 无

---


> 📌 获取跟随主题Action（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QAction* autoAction();
```

**描述**: 获取跟随主题Action

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QAction*` | 获取跟随主题Action |


**备注**: 无

---


> 📌 获取浅色主题Action（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QAction* lightAction();
```

**描述**: 获取浅色主题Action

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QAction*` | 获取浅色主题Action |


**备注**: 无

---


> 📌 获取深色主题Action（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QAction* darkAction();
```

**描述**: 获取深色主题Action

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QAction*` | 获取深色主题Action |


**备注**: 无

---

# 4.1.12.2 KLabel

功能描述：会显示省略文本的lable以及存在一些特定样式，自2.4启用
枚举类型
枚举类型
```c
enum KLabelType {NormalType,DataType};
```

> 📌 设置label类型（自2.4版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setLabelType(KLabelType type = KLabelType::DataType)
```

**描述**: 设置label类型

**参数**:

| 参数 | 说明 |
|------|------|
| `type` | 设置label的类型 |


**备注**: 无

---


> 📌 设置是否启用文本/图标高亮色（自2.4版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setDateHightColor（bool flag）
```

**描述**: 设置是否启用文本/图标高亮色

**参数**:

| 参数 | 说明 |
|------|------|
| `true` | 启用高亮色 |
| `false` | 禁用高亮色 |


**备注**: 无

---


> 📌 设置图标pixmap（自2.4版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setPixmap（const QPixmap &pixmap）
```

**描述**: 设置图标pixmap

**参数**:

| 参数 | 说明 |
|------|------|
| `pixmap）` | label设置pixmap |


**备注**: 无

---


> 📌 获取图标（自2.4版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
const QPixmap *pixmap() const
```

**描述**: 获取图标

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QPixmap` | 获取label的图标 |


**备注**: 无

---


> 📌 是否启用自定义背景并设置自定义背景色（自2.4版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setBackgroundColor(bool flag,QColor color = Qt::white)
```

**描述**: 是否启用自定义背景并设置自定义背景色

**参数**:

| 参数 | 说明 |
|------|------|
| `true` | 启用自定义背景色 |
| `false` | 禁用自定义背景色 |
| `color` | 自定义背景色的色值 |


**备注**: 无

---


> 📌 设置圆角（自2.4版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setBorderRadius(int bottomLeft,int topLeft,int topRight,int bottomRight);
```

**描述**: 设置圆角

**参数**:

| 参数 | 说明 |
|------|------|
| `bottomLeft` | 左下角圆角半径 |
| `topLeft` | 左上角圆角半径 |
| `topRight` | 右上角圆角半径 |
| `bottomRight` | 右下角圆角半径 |


**备注**: 无

---


> 📌 设置圆角（自2.4版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setBorderRadius(int radius);
```

**描述**: 设置圆角

**参数**:

| 参数 | 说明 |
|------|------|
| `radius` | 设置圆角半径 |


**备注**: 无

---


> 📌 设置字体颜色colorRole（V11自3.0.1.0启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setFontColorRole(QPalette::ColorRole role)
```

**描述**: 设置字体颜色colorRole

**参数**:

| 参数 | 说明 |
|------|------|
| `role` | QPalette::ColorRole |


**备注**: 无

---


> 📌 设置三态是否开启（V11自3.0.1.0启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setTristate(bool flag)
```

**描述**: 设置三态是否开启

**参数**:

| 参数 | 说明 |
|------|------|
| `flag` | 设置三态是否开启 |


**备注**: 无

---


> 📌 获取三态是否开启（V11自3.0.1.0启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
bool isTristate()
```

**描述**: 获取三态是否开启

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 开启 false 未开启 |


**备注**: 无
4.1.13 KTranslucentFloor
KTranslucentFloor，继承自 QFrame，提供一个毛玻璃底板，可以设置圆角以及是否添加阴影效果。
自 1.2.0.12 版本启用。

---


> 📌 设置圆角半径（自1.2.0.12版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setBorderRadious(int radious);
```

**描述**: 设置圆角半径大小

**参数**:

| 参数 | 说明 |
|------|------|
| `radious` | 设置圆角半径大小 |


**备注**: 无

---


> 📌 返回圆角半径大小（自1.2.0.12版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
int borderRadius();
```

**描述**: 返回圆角半径大小

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 获取圆角半径大小 |


**备注**: 无

---


> 📌 设置是否显示阴影（自1.2.0.12版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setShadow(bool flag);
```

**描述**: 设置是否显示阴影

**参数**:

| 参数 | 说明 |
|------|------|
| `true` | 启用阴影 |
| `false` | 禁用阴影 |


**备注**: 无

---


> 📌 获取是否显示阴影（自1.2.0.12版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
bool shadow();
```

**描述**: 获取是否显示阴影

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 启用阴影 false 禁用阴影 |


**备注**: 无

---


> 📌 设置是否启用毛玻璃效果（自2.0.0.0启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setEnableBlur(bool flag);
```

**描述**: 设置是否启用毛玻璃效果（自2.0.0.0启用）

**参数**:

| 参数 | 说明 |
|------|------|
| `true` | 启用毛玻璃效果 |
| `false` | 禁用毛玻璃效果 |


**备注**: 无

---


> 📌 获取是否已启用毛玻璃效果（自2.0.0.0启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
bool enableBlur();
```

**描述**: 获取是否已启用毛玻璃效果（自2.0.0.0启用）

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 启用毛玻璃效果 false 禁用毛玻璃效果 |


**备注**: 无

---


> 📌 设置透明度（自2.0.0.0启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setOpacity(qreal opacity);
```

**描述**: 设置透明度（自2.0.0.0启用）

**参数**:

| 参数 | 说明 |
|------|------|
| `opacity` | 设置控件透明度的值 |


**备注**: 无

---


> 📌 获取透明度（自2.0.0.0启用)
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
qreal opacity();
```

**描述**: 获取透明度（自2.0.0.0启用)

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `qreal` | 获取当前透明度的值 |


**备注**: 无
4.1.14 KDragWidget
功能描述：KDragWidget,继承自QWidget，可以获取拖动进入控件或者选中的文件夹或文件的路径，提
供获取设置图片的按钮，获取设置文本的label，获取filedialog等接口,自2.3.0.0启用

---


> 📌 返回设置图标的button（自2.3.0.0启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
KPushbutton *iconButton()；
```

**描述**: 返回设置图标的button,自2.3.0.0启用

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `KPushbutton*` | 获取设置图标的button |


**备注**: 无

---


> 📌 返回设置文本的label （自2.3.0.0启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QLabel *textlabel();
```

**描述**: 返回设置文本的label,自2.3.0.0启用

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QLabel*` | 返回设置文本的label |


**备注**: 无

---


> 📌 返回打开的文件对话框 （自2.3.0.0启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QFileDialog *fileDialog()
```

**描述**: 返回打开的文件对话框 ,自2.3.0.0启用

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QFileDialog*` | 返回打开的文件对话框 |


**备注**: 无

---


> 📌 将文件对话框中使用的过滤器设置为给定的过滤器（自2.3.0.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setNameFilter(const QString &filter)
```

**描述**: 将文件对话框中使用的过滤器设置为给定的过滤器,自2.3.0.0启用

**参数**:

| 参数 | 说明 |
|------|------|
| `filter` | 将文件对话框中使用的过滤器设置为给定的过滤器 |


**备注**: 无

---


> 📌 设置文件对话框中使用的过滤器（自2.3.0.0启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setNameFilters(const QString &filters)
```

**描述**: 设置文件对话框中使用的过滤器,自2.3.0.0启用

**参数**:

| 参数 | 说明 |
|------|------|
| `filters` | 设置文件对话框中使用的过滤器 |


**备注**: 无
4.1.15 KFileWidget
图片+两行文本的组合（自2.4启用）

---


> 📌 设置主文本（自2.4版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setMainText(QString str)
```

**描述**: 设置主文本

**参数**:

| 参数 | 说明 |
|------|------|
| `str` | 设置主文本 |


**备注**: 无

---


> 📌 设置次文本（自2.4版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setSubText（QString str）
```

**描述**: 设置次文本

**参数**:

| 参数 | 说明 |
|------|------|
| `str` | 设置次级文本 |


**备注**: 无

---


> 📌 设置图片（自2.4版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setIcon（QIcon icon）
```

**描述**: 设置图片

**参数**:

| 参数 | 说明 |
|------|------|
| `icon` | 设置图片 |


**备注**: 无

---


> 📌 获取左边栏widget（自2.4版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setBorderRadius（int radius）
```

**描述**: 获取左边栏widget

**参数**: 无

**备注**: 无

---


> 📌 设置圆角（自2.4版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setBorderRadius(int bottomLeft,int topLeft,int topRight,int bottomRight);
```

**描述**: 设置圆角

**参数**:

| 参数 | 说明 |
|------|------|
| `bottomLeft` | 左下角圆角半径 |
| `topLeft` | 左上角圆角半径 |
| `topRight` | 右上角圆角半径 |
| `bottomRight` | 右下角圆角半径 |


**备注**: 无
4.1.16 KAddButton
功能描述: 添加按钮（V11自3.0.1.0启用）

---


> 📌 设置圆角模式
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setRadiusType(KBaseFrame::BorderRadiusStyle style)
```

**描述**: 设置圆角模式

**参数**:

| 参数 | 说明 |
|------|------|
| `style` | 需要设置的圆角模式 |


**备注**: 无

---


> 📌 获取平板模式
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
bool getTableMode()
```

**描述**: 获取平板模式

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 平板模式 false 非平板模式 |


**备注**: 无

---


> 📌 设置高度是否自适应
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setHeightAdaptive(bool flag)
```

**描述**: 设置高度是否自适应

**参数**:

| 参数 | 说明 |
|------|------|
| `flag` | 设置高度是否自适应 |


**备注**: 无
4.1.17 KFLowLayout
功能描述: 存在水平间距、垂直间距的布局（V11自3.0.1.0启用）

---


> 📌 创建一个父对象parent，边距为margin，水平间距为hSpace，垂直间距为vSpace的布局
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
explicit KFlowLayout(QWidget *parent, bool home = false, int margin = -1, int hSpacing =
-1, int vSpacing = -1 )
```

**描述**: 创建一个父对象parent，边距为margin，水平间距为hSpace，垂直间距为vSpace的布局

**参数**:

| 参数 | 说明 |
|------|------|
| `parent` | 父指针 |
| `home` | 是否是首页布局 |
| `margin` | 边距 |
| `hspacing` | 水平间距 |
| `vspacing` | 垂直间距 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `KFlowLayout` | - |


**备注**: 无

---


> 📌 创建一个边距为margin，水平间距为hSpacing，垂直间距为vSpacing的布局。
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
explicit KFlowLayout(QWidget *parent, bool home = false, int margin = -1, int hSpacing =
-1, int vSpacing = -1 )
```

**描述**: 创建一个边距为margin，水平间距为hSpacing，垂直间距为vSpacing的布局。

**参数**:

| 参数 | 说明 |
|------|------|
| `margin` | 边距 |
| `hspacing` | 水平间距 |
| `vspacing` | 垂直间距 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `KFlowLayout` | - |


**备注**: 无

---


> 📌 获取水平间距
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
int horizontalSpacing() const
```

**描述**: 获取水平间距

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 水平间距 |


**备注**: 无

---


> 📌 获取垂直间距
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
int verticalSpacing()const
```

**描述**: 获取垂直间距

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 获取垂直间距 |


**备注**: 无
4.2 Wayland-helper
该模块主要负责提供在 X 平台和 Wayland 平台下均能生效的兼容接口，使应用无需考虑显示平台的差异。该
模块安装方式如下：
sudo apt install libkysdk-waylandhelper libkysdk-waylandhelper-dev
根据不同项目类型，可参考以下 demo 构建项目： （1） .pro 文件构建项目： qt 项目.pro 文件中增加：
CONFIG += link_pkgconfig
PKGCONFIG += kysdk-waylandhelper
（2） CMakeLists.txt 构建项目
cmake_minimum_required(VERSION 3.5)
find_package(Qt5 COMPONENTS Widgets REQUIRED)
find_package(PkgConfig REQUIRED)
pkg_check_modules(KYSDKWAYLANDHELPER_PKG kysdk-waylandhelper)
target_include_directories(demo PRIVATE ${KYSDKWAYLANDHELPER_PKG_INCLUDE_DIRS})
target_link_directories(demo PRIVATE ${KYSDKWAYLANDHELPER_PKG_LIBRARY_DIRS})
target_link_libraries(demo Qt5::Widgets ${KYSDKWAYLANDHELPER_PKG_LIBRARIES})
4.2.1 WindowManager

---


> 📌 获取单例对象（自1.2版本启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static WindowManager* self();
```

**描述**: 获取单例对象

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `WindowManager*` | 获取单例对象 |


**备注**: 无

---


> 📌 获取窗口信息（自1.2版本启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static WindowInfo getwindowInfo(const WindowId& windowId);
```

**描述**: 获取窗口信息

**参数**:

| 参数 | 说明 |
|------|------|
| `windowId` | 传入window id |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `WindowInfo` | 通过winid返回windowinfo |


**备注**: 无

---


> 📌 获取当前活动窗口（自1.2版本启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static WindowId currentActiveWindow();
```

**描述**: 获取当前活动窗口

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `WindowId` | 获取当前窗口的windowid |


**备注**: 无

---


> 📌 置顶窗口（自1.1版本启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static void keepWindowAbove(const WindowId& windowId);
```

**描述**: 置顶窗口

**参数**:

| 参数 | 说明 |
|------|------|
| `windowId` | 传入window id |


**备注**: 无

---


> 📌 获取窗口标题（自1.2版本启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static QString getWindowTitle(const WindowId& windowId);
```

**描述**: 获取窗口标题

**参数**:

| 参数 | 说明 |
|------|------|
| `windowId` | 传入window id |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QString` | 通过window id 获取窗口标题 |


**备注**: 无

---


> 📌 获取窗口图标（自1.2版本启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static QIcon getWindowIcon(const WindowId& windowId);
```

**描述**: 获取窗口图标

**参数**:

| 参数 | 说明 |
|------|------|
| `获取窗口图标` | 传入windowid |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QIcon` | 获取传入windowid的窗口图标 |


**备注**: 无

---


> 📌 获取窗口所在组的组名（自1.2版本启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static QString getWindowGroup(const WindowId& windowId);
```

**描述**: 获取窗口所在组的组名

**参数**:

| 参数 | 说明 |
|------|------|
| `windowId` | 传入的window id |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QString无` | 获取传入window id所在组的组名 |


**备注**: 无

---


> 📌 关闭窗口（自1.2版本启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static void closeWindow(const WindowId& windowId);
```

**描述**: 关闭窗口

**参数**:

| 参数 | 说明 |
|------|------|
| `windowId` | 传入window id |


**备注**: 无

---


> 📌 激活窗口（自1.2版本启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static void activateWindow(const WindowId& windowId);
```

**描述**: 激活窗口

**参数**:

| 参数 | 说明 |
|------|------|
| `windowId` | 传入window id |


**备注**: 无

---


> 📌 最大化窗口（自1.2版本启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static void maximizeWindow(const WindowId& windowId);
```

**描述**: 最大化窗口

**参数**:

| 参数 | 说明 |
|------|------|
| `windowId` | 传入的window id |


**备注**: 无

---


> 📌 最小化窗口（自1.2版本启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static void minimizeWindow(const WindowId& windowId);
```

**描述**: 最小化窗口

**参数**:

| 参数 | 说明 |
|------|------|
| `windowId` | 传入的window id |


**备注**: 无

---


> 📌 获取窗口进程pid（自1.2版本启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static quint32 getPid(const WindowId& windowId);
```

**描述**: 获取窗口进程pid

**参数**:

| 参数 | 说明 |
|------|------|
| `windowId` | 传入的window id |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `quint32` | 获取到对应窗口进程的pid |


**备注**: 无

---


> 📌 显示当前桌面（自1.2版本启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static void showDesktop();
```

**描述**: 显示当前桌面

**参数**: 无

**备注**: 无

---


> 📌 取消显示当前桌面（自1.2版本启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static void hideDesktop();
```

**描述**: 取消显示当前桌面

**参数**: 无

**备注**: 无

---


> 📌 获取当前桌面的名称（自1.2版本启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static QString currentDesktop();
```

**描述**: 获取当前桌面的名称

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QString` | 获取当前桌面的名称 |


**备注**: 无

---


> 📌 获取当前窗口列表（自1.2版本启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static QList windows();
```

**描述**: 获取当前窗口列表

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QList` | 获取当前窗口列表 |


**备注**: 无

---


> 📌 获取窗口类型，仅适用于X环境下，wayland下统一返回normal（自1.2版本启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static NET::WindowType getWindowType(const WindowId& windowId);
```

**描述**: 获取窗口类型，仅适用于X环境下，wayland下统一返回normal

**参数**:

| 参数 | 说明 |
|------|------|
| `windowId` | 传入的window id |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `NET::WindowType` | 获取窗口类型，仅适用于X环境下，wayland下统一返回normal |


**备注**: 无

---


> 📌 设置窗口位置（自1.2版本启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static void setGeometry(QWindow *window,const QRect &rect);
```

**描述**: 设置窗口位置

**参数**:

| 参数 | 说明 |
|------|------|
| `window` | 传入的window id |
| `rect` | 需要将窗体移动到的位置rect |


**备注**: 无

---


> 📌 设置是否跳过任务栏（自2.0.0.0启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static void setSkipTaskBar(QWindow *window,bool skip);
```

**描述**: 设置是否跳过任务栏（自2.0.0.0启用）

**参数**:

| 参数 | 说明 |
|------|------|
| `window` | 要跳过任务栏的窗口 |
| `布尔值` | true 跳过任务兰，false 不跳过任务栏 |


**备注**: 无

---


> 📌 设置是否跳过窗口选择（自2.0.0.0启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static void setSkipSwitcher(QWindow *window,bool skip);
```

**描述**: 设置是否跳过窗口选择（自2.0.0.0启用）

**参数**:

| 参数 | 说明 |
|------|------|
| `window` | 要跳过窗口选择的窗口 |
| `true` | 跳过窗口选择 |
| `fasle` | 不跳过窗口选择 |


**备注**: 无

---


> 📌 获取窗体是否跳过任务栏（自2.0.0.0启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static bool skipTaskBar(const WindowId& windowId);
```

**描述**: 获取窗体是否跳过任务栏（自2.0.0.0启用）

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 跳过任务兰 false 不跳过任务栏 |


**备注**: 无

---


> 📌 获取窗体是否跳过窗口选择（自2.0.0.0启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static bool skipSwitcher(const WindowId& windowId);
```

**描述**: 获取窗体是否跳过窗口选择（自2.0.0.0启用）

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `fasle` | 不跳过窗口选择 |


**备注**: 无

---


> 📌 获取桌面是否处于显示状态（自2.0.0.0启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static bool isShowingDesktop();
```

**描述**: 获取桌面是否处于显示状态（自2.0.0.0启用）

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 出于显示状态 false 处于隐藏状态 |


**备注**: 无

---


> 📌 设置窗口在所有桌面中显示（自2.0.0.0启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static void setOnAllDesktops(const WindowId &windowId);
```

**描述**: 设置窗口在所有桌面中显示（自2.0.0.0启用）

**参数**:

| 参数 | 说明 |
|------|------|
| `windowId` | 显示在所有桌面窗口的window id |


**备注**: 无

---


> 📌 获取窗口是否在所有桌面中显示（自2.0.0.0启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static bool isOnAllDesktops(const WindowId &windowId);
```

**描述**: 获取窗口是否在所有桌面中显示（自2.0.0.0启用）

**参数**:

| 参数 | 说明 |
|------|------|
| `windowId` | 传入窗口的window id |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `false` | 当前窗口不在所有桌面中显示 |


**备注**: 无

---


> 📌 获取窗口是否在当前桌面(自2.1启用)
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static bool isOnCurrentDesktop(const WindowId& id);
```

**描述**: 获取窗口是否在当前桌面(自2.1启用)

**参数**:

| 参数 | 说明 |
|------|------|
| `id` | 传入窗口的window id |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `false` | 窗口不在当前桌面 |


**备注**: 无

---


> 📌 获取窗口是否在指定桌面(自2.1启用)
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static bool isOnDesktop(const WindowId &id , int desktop);
```

**描述**: 获取窗口是否在指定桌面(自2.1启用)

**参数**:

| 参数 | 说明 |
|------|------|
| `id` | 窗口的window id |
| `desktop` | 指定桌面 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `false` | 窗口不在指定桌面 |


**备注**: 无

---


> 📌 设置panel属性窗体是否获取焦点，仅在wayland环境下生效(自2.3启用)
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static void setPanelTakefocus(QWindow *window ,bool flag)
```

**描述**: 设置panel属性窗体是否获取焦点，仅在wayland环境下生效(自2.3启用)

**参数**:

| 参数 | 说明 |
|------|------|
| `window` | 传入的窗口 window |
| `true` | 获取焦点 |
| `false` | 未获取焦点 |


**备注**: 无

---


> 📌 发送demanAttention状态(自2.3启用)
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static void demandAttention(const WindowId &wid)
```

**描述**: 发送demanAttention状态(自2.3启用)

**参数**:

| 参数 | 说明 |
|------|------|
| `wid` | 传入的window id |


**备注**: 无

---


> 📌 获取窗口所属的进程名称(自2.3启用)
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static QString getProcessName(const WindowId& windowId)
```

**描述**: 获取窗口所属的进程名称(自2.3启用)

**参数**:

| 参数 | 说明 |
|------|------|
| `windowId` | 传入的window id |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QString` | 获取窗口所属的进程名称 |


**备注**: 无
4.2.2 WindowInfo

---


> 📌 返回窗口是否是最大化状态（自1.2版本启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
inline bool isMaximized() const;
```

**描述**: 返回窗口是否是最大化状态

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 窗口在最大化状态 false 窗口不在最大化状态 |


**备注**: 无

---


> 📌 返回窗口是否是最小化状态（自1.2版本启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
inline bool isMinimized() const;
```

**描述**: 返回窗口是否是最小化状态

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 窗口在最小化状态 false 窗口不在最小化状态 |


**备注**: 无

---


> 📌 返回窗口是否可以最大化（自1.2版本启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
inline bool isMaximizable() const;
```

**描述**: 返回窗口是否可以最大化

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 窗口可最大化 false 窗口不可最大化 |


**备注**: 无

---


> 📌 返回窗口是否可以最小化（自1.2版本启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
inline bool isMinimizable() const;
```

**描述**: 返回窗口是否可以最小化

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 窗口可最小化 false 窗口不可最小化 |


**备注**: 无

---


> 📌 返回窗口是否是置顶状态（自1.2版本启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
inline bool isKeepAbove() const;
```

**描述**: 返回窗口是否是置顶状态

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 置顶状态 false 不在置顶状态 |


**备注**: 无

---


> 📌 返回窗口是否跳过任务栏（自1.2版本启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
inline bool hasSkipTaskbar() const;
```

**描述**: 返回窗口是否跳过任务栏

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 窗口跳过任务栏 false 窗口未跳过任务栏 |


**备注**: 无

---


> 📌 返回窗口是否是全屏状态（在1.2版本启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
inline bool isFullscreen() const;
```

**描述**: 返回窗口是否是全屏状态

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 窗口在全屏状态 false 窗口不在全屏状态 |


**备注**: 无

---


> 📌 返回窗口是否在所有桌面中显示（自1.2版本启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
inline bool isOnAllDesktops() const； noexcept;
```

**描述**: 返回窗口是否在所有桌面中显示

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 窗口在所有桌面中显示 false 窗口不在所有桌面中显示 |


**备注**: 无

---


> 📌 判断窗口id是否有效（自1.2版本启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
inline bool isValid() const;
```

**描述**: 判断窗口id是否有效

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 窗口id有效 false 窗口id无效 |


**备注**: 无

---


> 📌 返回窗口是否是激活状态(自1.2版本启用)
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
inline bool isActive() const noexcept;
```

**描述**: 返回窗口是否是激活状态

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 窗口是激活状态 false 窗口不是激活状态 |


**备注**: 无
4.2.3 UkuiStyleHelper

---


> 📌 获取单例对象（自1.2版本启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
static UkuiStyleHelper *self();
```

**描述**: 获取单例对象

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `UkuiStyleHelper*` | 获取单例对象 |


**备注**: 无

---


> 📌 移除窗管标题栏（自1.2版本启用）
> **子模块**: 窗口管理模块 | **接口类型**: C++

**原型**:
```c
void removeHeader(QWidget* widget);
```

**描述**: 移除窗管标题栏

**参数**:

| 参数 | 说明 |
|------|------|
| `widget` | 将给定widget的窗口标题栏移除 |


**备注**: 无
4.3 应用通用功能模块
4.3.1 日志模块

---


> 📌 用于Qt注册日志函数，不应单独调用(自1.2版本启用)
> **子模块**: 日志模块 | **接口类型**: C++

**原型**:
```c
static void logOutput(QtMsgType type , const QMessageLogContext &context , const
QString &msg);
```

**描述**: 用于Qt注册日志函数，不应单独调用.

**参数**:

| 参数 | 说明 |
|------|------|
| `type` | 日志类型 |
| `context` | 调用打印日志接口文件信息 |
| `msg` | 日志信息 |


**备注**: 无
4.3.2 系统相关模块

---


> 📌 添加窗管协议（自1.2版本启用）
> **子模块**: 窗管模块 | **接口类型**: C++

**原型**:
```c
static bool setWindowMotifHint(int winId);
```

**描述**: 添加窗管协议

**参数**:

| 参数 | 说明 |
|------|------|
| `winId` | 窗口 id |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `布尔值` | true 成功，false 失败 |


**备注**: 无

---


> 📌 设置窗口缩放属性（自1.2版本启用）
> **子模块**: 窗管模块 | **接口类型**: C++

**原型**:
```c
static bool setScalingProperties(void);
```

**描述**: 设置窗口缩放属性

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `布尔值` | true 成功，false 失败 |


**备注**: 无

---


> 📌 禁止系统锁屏（自1.2版本启用）
> **子模块**: session模块 | **接口类型**: C++

**原型**:
```c
quint32 setInhibitLockScreen(AppName appName , QString reason);
```

**描述**: 禁止系统锁屏

**参数**:

| 参数 | 说明 |
|------|------|
| `appName` | 应用名 |
| `reason` | 禁止锁屏的原因 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `布尔值` | 成功 : 非 0 的正整数，失败 : 0 |


**备注**: 无

---


> 📌 取消禁止系统锁屏（自1.2版本启用）
> **子模块**: session模块 | **接口类型**: C++

**原型**:
```c
bool unInhibitLockScreen(quint32 flag)
```

**描述**: 取消禁止系统锁屏

**参数**:

| 参数 | 说明 |
|------|------|
| `flag` | 禁止锁屏标识 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | - |
| `成功` | false 失败 |


**备注**: 无
4.3.3 d-bus 模块------即将废弃

---


> 📌 获取左边栏widget（自1.2版本启用）
> **子模块**: d-bus模块 | **接口类型**: C++

**原型**:
```c
static QList callMethod(QString serviceName, QString objectPath, QString interfaceName,
QString methodName, QList args = QList());
```

**描述**: 获取左边栏widget

**参数**:

| 参数 | 说明 |
|------|------|
| `serviceName` | 服务名 |
| `objectPath` | 对象路径 |
| `interfaceName` | 接口名 |
| `methodName` | 函数名 |
| `args` | 参数列表 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QList` | 函数返回值 |


**备注**: 无
4.3.4 系统信息模块

---


> 📌 根据 lsb-release 文件的 key 值 获取信息（自1.2版本启用）
> **子模块**: 系统信息模块 | **接口类型**: C++

**原型**:
```c
QString getLsbReleaseInformation(QString key);
```

**描述**: 根据 lsb-release 文件的 key 值 获取信息

**参数**:

| 参数 | 说明 |
|------|------|
| `key` | 键值 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QString` | 获取到的信息 , 为空时可能确实为空也可能失败 |


**备注**: 无

---


> 📌 根据 os-release 文件的 key 值 获取信息（自1.2版本启用）
> **子模块**: 系统信息模块 | **接口类型**: C++

**原型**:
```c
QString getOsReleaseInformation(QString key);
```

**描述**: 根据 os-release 文件的 key 值 获取信息

**参数**:

| 参数 | 说明 |
|------|------|
| `key` | 键值 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QString` | 获取到的信息 , 为空时可能确实为空也可能失败 |


**备注**: 无

---


> 📌 获取 PROJECT_CODENAME 字段的值（自1.2版本启用）
> **子模块**: 系统信息模块 | **接口类型**: C++

**原型**:
```c
QString getProjectCodeName(void)
```

**描述**: 获取 PROJECT_CODENAME 字段的值

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QString` | 非空 : 获取到的值 , 空 : 失败 |


**备注**: 无

---


> 📌 获取 CPU 型号（自1.2版本启用）
> **子模块**: 系统信息模块 | **接口类型**: C++

**原型**:
```c
QString getCpuModelName(void)
```

**描述**: 获取 CPU 型号

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QString` | 非空 : 获取到的值 , 空 : 失败 |


**备注**: 无

---


> 📌 获取硬件平台信息（自1.2版本启用）
> **子模块**: 系统信息模块 | **接口类型**: C++

**原型**:
```c
QString getHdPlatform(void);
```

**描述**: 获取硬件平台信息

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QString` | 非空 : 获取到的值 , 空 : 失败 |


**备注**: 无
5 基础开发 SDK
该层设计主要为应用开发提供与操作系统无关的、高通用性、基础性的功能集合。减少不同应用在实现相同
功能式的差异性和复杂性。该层设计主要为应用开发提供与操作系统无关的、高通用性、基础性的功能集
合。减少不同应用在实现相同功能式的差异性和复杂性。
安装命令
$ sudo apt install libkysdk-base libkysdk-base-dev
5.1 日志模块
日志模块，提供日志记录功能
安装命令
$ sudo apt-get install libkysdk-log libkysdk-log-dev libkysdk-logrotate
构建示例
（1）.pro 文件构建项目
CONFIG += link_pkgconfig
PKGCONFIG += kysdk-log
（2）CMakeLists.txt 构建项目
cmake_minimum_required(VERSION 3.5)
find_package(PkgConfig REQUIRED)
pkg_check_modules(KYSDKTIMER kysdk-log)
target_include_directories(demo PRIVATE ${KYSDKTIMER_INCLUDE_DIRS})
target_link_directories(demo PRIVATE ${KYSDKTIMER_LIBRARY_DIRS})
target_link_libraries(demo PRIVATE ${KYSDKTIMER_LIBRARIES})
5.1.1 日志记录功能
头文件路径
#include "kysdk/kysdk-base/libkylog.h"
so库路径
/usr/lib/*/libkylog.so
'*'代表不同架构的架构目录名称，例如x86_64-linux-gnu
子模块信息

---


> 📌 输出日志(自1.2.0版本启用)
> **子模块**: 日志模块 | **接口类型**: C

**原型**:
```c
#define klog_debug(fmt , ...)
#define klog_info(fmt , ...)
#define klog_notice(fmt , ...)
#define klog_warning(fmt , ...)
#define klog_err(fmt , ...)
#define klog_crit(fmt , ...)
#define klog_alert(fmt , ...)
#define klog_emerg(fmt , ...)
```

**描述**: 输出不同等级的日志，宏名即日志等级

**参数**:

| 参数 | 说明 |
|------|------|
| `fmt` | 格式化字符串 |


**返回值**: 无

**备注**: 无

---


> 📌 初始化日志记录(自1.2.0版本启用)
> **子模块**: 日志模块 | **接口类型**: C

**原型**:
```c
int kdk_logger_init(const char *ini)
```

**描述**: 初始化日志记录，也可以不调用该函数直接使用上方日志记录的宏，若以此方式运行，则程序会
使用默认的日志配置文件

**参数**:

| 参数 | 说明 |
|------|------|
| `ini` | 日志配置文件路径，若传入NULL则会使用默认的日志配置文件 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | - |
| `成功 int(非0)` | - |
| `失败` | - |


**备注**: 无

---


> 📌 缓存区日志写入(自1.2.0版本启用)
> **子模块**: 日志模块 | **接口类型**: C

**原型**:
```c
void kdk_logger_flush()
```

**描述**: 在异步写入的方式下，可以调用该函数手动将缓存区中的日志写入文件中

**参数**: 无

**返回值**: 无

**备注**: 无

---


> 📌 设置日志输出自动换行(自1.2.0版本启用)
> **子模块**: 日志模块 | **接口类型**: C

**原型**:
```c
void kdk_logger_set_autowrap(int autowarp)
```

**描述**: 设置日志输出自动换行

**参数**:

| 参数 | 说明 |
|------|------|
| `autowarp` | 1 启用 |
| `0 禁用` | - |


**返回值**: 无

**备注**: 无
5.1.2 日志转储功能
本模块提供日志转储功能，安装此包则可以转存，未安装不可以转存。
安装命令
$ sudo apt install libkysdk-logrotate
子模块信息
是否转存：是否安装libkysdk-logrotate包（此包在system层）；安装此包则可以转存，未安装不可以转存。
转存规则：
1. 日志文件按天轮训；
2. 一次将存储 5 个归档日志，对于第六个归档，时间最久的归档将被删除。
3. 在轮循任务完成后，已轮循的归档将使用 gzip 进行压缩。
4. 归档文件文件以创建日期命名；如xxx.log-20131216。
5. 在日志轮循期间，任何错误将被忽略，例如 “文件无法找到” 之类的错误。
6. 如果日志文件为空，轮循不会进行
7. 还在打开中的日志文件，把当前日志备份并截断。
转存目录：非 root 程序记录在~/.log目录中，root 程序记录在/var/log 下。
转存后压缩文件名称：程序名.log-打包日期.gz；例如：kylin-photo-viewer.log-20240220.gz
5.2 定时器
C 语言定时器模块，给 C/C++程序提供定时器功能接口。
安装命令
$ sudo apt-get install libkysdk-timer libkysdk-timer-dev
构建示例
（1）.pro 文件构建项目
CONFIG += link_pkgconfig
PKGCONFIG += kysdk-timer

---

# 4.1.3.1 密码输入框

功能描述：KPasswordEdit，支持切换输入内容明文/暗文的切换，支持设置密码校验结果（正常输入为
蓝色边框，设置密码正确为绿色边框，密码错误为红色边框），支持 loading 状态，支持设置是否启用
clearButton。
枚举类型
枚举类型
```c
enum LoginState{Ordinary,LoginSuccess,LoginFailed};
```

> 📌 设置登录状态（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setState(LoginState state);
```

**描述**: 设置登录状态

**参数**:

| 参数 | 说明 |
|------|------|
| `state` | LoginState类枚举，显示登录状态 |


**备注**: 无

---


> 📌 返回登录状态（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
LoginState state();
```

**描述**: 返回登录状态

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `LoginState` | 获取搜索框的登录状态 |


**备注**: 无

---


> 📌 设置是否启用加载状态。（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setLoading(bool flag);
```

**描述**: 设置是否启用加载状态。

**参数**:

| 参数 | 说明 |
|------|------|
| `true` | 启用加载状态 |
| `false` | 不启用加载状态 |


**备注**: 无

---


> 📌 获取是否处于加载状态（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
bool isLoading();
```

**描述**: 获取是否处于加载状态

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 启用加载状态 false 不启用加载状态 |


**备注**: 无

---


> 📌 返回placeholderText的文本内容(自1.2版本启用)
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QString placeholderText();
```

**描述**: 返回placeholderText的文本内容

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QString` | placeholder的文本内容 |


**备注**: 无

---


> 📌 设置PlaceholderText的文本内容（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setPlaceholderText(QString&text);
```

**描述**: 设置PlaceholderText的文本内容

**参数**:

| 参数 | 说明 |
|------|------|
| `text` | 设置placeholder的文本内容 |


**备注**: 无

---


> 📌 设置是否启用ClearButton（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setClearButtonEnabled(bool enable);
```

**描述**: 设置是否启用ClearButton

**参数**:

| 参数 | 说明 |
|------|------|
| `true` | 启用清除按钮 |
| `false` | 禁用清除按钮 |


**备注**: 无

---


> 📌 获取是否启用了ClearButton（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
bool isClearButtonEnabled() const;
```

**描述**: 获取是否启用了ClearButton

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 启用清除按钮 false 禁用清除按钮 |


**备注**: 无

---


> 📌 设置EchoModeBtn是否可见（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setEchoModeBtnVisible(bool enable);
```

**描述**: 设置EchoModeBtn是否可见,1.2版本添加

**参数**:

| 参数 | 说明 |
|------|------|
| `true` | 文本样式按钮可见 |
| `false` | 文本样式按钮不可见 |


**备注**: 无

---


> 📌 获取EchoModeBtn是否可见（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
bool echoModeBtnVisible();
```

**描述**: 获取EchoModeBtn是否可见,1.2版本添加

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 文本样式按钮可见 false 文本样式按钮不可见 |


**备注**: 无

---


> 📌 设置ClearBtn是否可见（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setClearBtnVisible(bool enable);
```

**描述**: 设置ClearBtn是否可见,1.2版本添加

**参数**:

| 参数 | 说明 |
|------|------|
| `true` | 清除按钮可见 |
| `false` | 清楚按钮不可见 |


**备注**: 无

---


> 📌 获取ClearBtn是否可见（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
bool clearBtnVisible();
```

**描述**: 获取ClearBtn是否可见,1.2版本添加

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 清除按钮可见 false 清楚按钮不可见 |


**备注**: 无

---


> 📌 设置KPasswordEdit是否可用（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setEnabled(bool);
```

**描述**: 设置KPasswordEdit是否可用

**参数**:

| 参数 | 说明 |
|------|------|
| `true` | 控件可用 |
| `false` | 控件不可用 |


**备注**: 无

---


> 📌 设置文本的显示模式（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setEchoMode(EchoMode mode)
```

**描述**: 设置文本的显示模式，1.2版本添加

**参数**:

| 参数 | 说明 |
|------|------|
| `mode` | 决定文本显示样式 |


**备注**: 无

---


> 📌 设置是否走默认palette（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setUseCustomPalette(bool flag)
```

**描述**: 设置是否走默认palette,1.2版本添加

**参数**:

| 参数 | 说明 |
|------|------|
| `true` | 跟随默认palette |
| `false` | 跟随自定义palette |


**备注**: 无

---

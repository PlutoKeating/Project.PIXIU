# 4.1.8.2 KButtonBox

功能描述：提供了一个按钮类容器

> 📌 获取KButtonBox的布局类型（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
Qt::Orientation orientation()
```

**描述**: 获取KButtonBox的布局类型

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `Qt::Orientation` | 获取KButtonBox的布局类型 |


**备注**: 无

---


> 📌 设置KButtonBox的布局类型，包括水平类型和垂直类型（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setOrientation(Qt::Orientation orientation)
```

**描述**: 设置KButtonBox的布局类型，包括水平类型和垂直类型

**参数**:

| 参数 | 说明 |
|------|------|
| `orientation` | 设置KButtonBox的布局类型，包括水平类型和垂直类型 |


**备注**: 无

---


> 📌 添加按钮（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void addButton(KPushButton *button,int i =-1)
```

**描述**: 添加按钮

**参数**:

| 参数 | 说明 |
|------|------|
| `button` | 需要添加到buttonbox的button |
| `i` | 添加button的位置，默认按顺序添加 |


**备注**: 无

---


> 📌 删除按钮（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void removeButton(KPushButton *button)
```

**描述**: 删除按钮

**参数**:

| 参数 | 说明 |
|------|------|
| `button` | 从buttonbox中删除按钮 |


**备注**: 无

---


> 📌 按id删除指定按钮（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void removeButton(int i)
```

**描述**: 按id删除指定按钮

**参数**:

| 参数 | 说明 |
|------|------|
| `i` | 删除指定id的button |


**备注**: 无

---


> 📌 以列表形式向KButtonBox中添加按钮（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setButtonList(const QList &list)
```

**描述**: 以列表形式向KButtonBox中添加按钮

**参数**:

| 参数 | 说明 |
|------|------|
| `list` | 以列表形式向KButtonBox中添加按钮 |


**备注**: 无

---


> 📌 获取KButtonBox中的按钮列表（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QList buttonList()
```

**描述**: 获取KButtonBox中的按钮列表

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QList` | 获取KButtonBox中的按钮列表 |


**备注**: 无

---


> 📌 设置KButtonBox首尾部分按钮的圆角（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setBorderRadius(int radius)
```

**描述**: 设置KButtonBox首尾部分按钮的圆角

**参数**:

| 参数 | 说明 |
|------|------|
| `radius` | 设置buttonbox的四角圆角半径 |


**备注**: 无

---


> 📌 获取KButtonBox首尾部分按钮的圆角（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
int borderRadius()
```

**描述**: 获取KButtonBox首尾部分按钮的圆角

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 获取KButtonBox四个圆角半径 |


**备注**: 无

---


> 📌 设置按钮id（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setId(KPushButton *button,int id)
```

**描述**: 设置按钮id

**参数**:

| 参数 | 说明 |
|------|------|
| `button` | 指定要设置id的button |
| `id` | 将指定button设置为指定id |


**备注**: 无

---


> 📌 获取按钮id（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
int id(KPushButton *button)
```

**描述**: 获取按钮id

**参数**:

| 参数 | 说明 |
|------|------|
| `button` | 指定按钮button |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 获取指定按钮的id |


**备注**: 无

---


> 📌 返回已选中的按钮（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
KPushButton* checkedButton()
```

**描述**: 返回已选中的按钮

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `KPushButton*` | 获取当前选中的按钮 |


**备注**: 无

---


> 📌 通过按钮id获取按钮（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
KPushButton* button(int id)
```

**描述**: 通过按钮id获取按钮

**参数**:

| 参数 | 说明 |
|------|------|
| `id` | 指定id |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `KPushButton*` | 获取指定id的button |


**备注**: 无

---


> 📌 返回已选中按钮的id（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
int checkedId()
```

**描述**: 返回已选中按钮的id

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 获取当前选中按钮的id |


**备注**: 无

---


> 📌 设置KButtonBox按钮间是否互斥（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setExclusive(bool )
```

**描述**: 设置KButtonBox按钮间是否互斥

**参数**:

| 参数 | 说明 |
|------|------|
| `true` | 互斥 |
| `false` | 不互斥 |


**备注**: 无

---


> 📌 获取KButtonBox按钮间是否互斥（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
bool exclusiv()
```

**描述**: 获取KButtonBox按钮间是否互斥

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `false` | 不互斥 |


**备注**: 无

---


> 📌 设置KButtonBox中的按钮是否可以选中（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setCheckable(bool flag)
```

**描述**: 设置KButtonBox中的按钮是否可以选中

**参数**:

| 参数 | 说明 |
|------|------|
| `true` | 可选中 |
| `false` | 不可选中 |


**备注**: 无

---


> 📌 获取KButtonBox中的按钮是否可以选中（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
bool isCheckable()
```

**描述**: 获取KButtonBox中的按钮是否可以选中

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 可选中 false 不可选中 |


**备注**: 无

---


> 📌 设置是否启动阴影效果（自2.4启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setShadow(bool flag)
```

**描述**: 设置是否启动阴影效果（自2.4启用）

**参数**:

| 参数 | 说明 |
|------|------|
| `true` | 启动阴影效果 |
| `false` | 未启动阴影效果 |


**备注**: 无

---


> 📌 获取是否启用阴影效果（自2.4启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
bool shadow()
```

**描述**: 获取是否启用阴影效果（自2.4启用）

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 启动阴影效果 false 未启动阴影效果 |


**备注**: 无

---

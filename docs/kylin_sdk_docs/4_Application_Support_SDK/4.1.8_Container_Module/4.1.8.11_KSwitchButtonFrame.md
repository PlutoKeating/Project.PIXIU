# 4.1.8.11 KSwitchButtonFrame

功能描述: 创建一个带KSwitchButton的frame（V11自3.0.1.0启用）

> 📌 设置提示文案
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setHintText(QString &hintText)
```

**描述**: 设置提示文案

**参数**:

| 参数 | 说明 |
|------|------|
| `hintText` | 需要设置的文本 |


**备注**: 无

---


> 📌 获取switchbutton
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
KSwitchButton *switchbutton()
```

**描述**: 获取kswitchbutton

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `KSwitchButton *` | KSwitchButton指针 |


**备注**: 无

---


> 📌 获取label
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
KLabel *titleLabel()
```

**描述**: 获取label

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `KLabel *` | KLabel指针 |


**备注**: 无

---


> 📌 获取layout
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QHBoxLayout *mainHLayout()
```

**描述**: 获取layout

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QHBoxLayout *mainHLayout()` | 获取layout |


**备注**: 无
4.1.9 面包屑 KBreadCrumb
功能描述：无边框的标签栏，可以添加文字和图标，支持点击和 hover 高亮。
枚举类型
枚举类型
enum KBreadCrumbType { FlatBreadCrumb,
CubeBreadCrumb };

---


> 📌 设置图标（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setIcon(const QIcon &icon);
```

**描述**: 设置图标

**参数**:

| 参数 | 说明 |
|------|------|
| `icon` | 需要设置给部件的icon |


**备注**: 无

---


> 📌 获取图标（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QIcon icon() const;
```

**描述**: 获取图标

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QIcon` | 获取控件的图标 |


**备注**: 无

---


> 📌 获取KBreadCrumb是否为flat类型。（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
bool isFlat() const;
```

**描述**: 获取KBreadCrumb是否为flat类型。

**参数**:

| 参数 | 说明 |
|------|------|
| `>返回值` | true |
| `flat类型` | false |
| `不是flat类型` | - |


**返回值**: 无

**备注**: 无

---


> 📌 设置KBreadCrumb是否为flat类型。（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setFlat(bool flat);
```

**描述**: 设置KBreadCrumb是否为flat类型。

**参数**:

| 参数 | 说明 |
|------|------|
| `true` | flag类型 |
| `false` | 不是flat类型 |


**备注**: 无
4.1.10 KCommentPanel
枚举类型
枚举类型
enum StarLevel {LevelZero =0, LevelOne, LevelTwo,
LevelThree, LevelFour,LevelFive};

---


> 📌 设置图标（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setIcon(const QIcon&);
```

**描述**: 设置图标

**参数**:

| 参数 | 说明 |
|------|------|
| `QIcon` | 设置图标 |


**备注**: 无

---


> 📌 设置评论时间（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setTime(const QString&);
```

**描述**: 设置评论时间

**参数**:

| 参数 | 说明 |
|------|------|
| `QString` | 设置评论的时间 |


**备注**: 无

---


> 📌 设置评论名称（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setName(const QString&);
```

**描述**: 设置评论名称

**参数**:

| 参数 | 说明 |
|------|------|
| `QString` | 设置评论者的名称 |


**备注**: 无

---


> 📌 设置评论内容（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setText(const QString&);
```

**描述**: 设置评论内容

**参数**:

| 参数 | 说明 |
|------|------|
| `QString` | 设置评论的内容 |


**备注**: 无

---


> 📌 设置评论等级（自1.2版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setGrade(StarLevel level);
```

**描述**: 设置评论等级

**参数**:

| 参数 | 说明 |
|------|------|
| `level` | 设置评论的等级 |


**备注**: 无
4.1.11 KListView
功能描述：提供一个图片两行文字的显示效果，若只有一行文字，则对于 icon 居中显示 ，已弃用。
4.1.12 标签模块

---

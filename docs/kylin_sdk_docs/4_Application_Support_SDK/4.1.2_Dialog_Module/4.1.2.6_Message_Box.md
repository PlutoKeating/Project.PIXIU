# 4.1.2.6 消息框

功能描述：KMessageBox，继承自KDialog，提供一个可自定义设置按钮和图标的对话框，类似
QMessageBox
KMessageBox* messageBox = new KMessageBox(this);
枚举类型
枚
举
类
型
```c
enum Icon{ TNoIcon ,Information,Warning ,Critical ,Question };
enum ButtonRole{ InvalidRole，AcceptRole，RejectRole,DestructiveRole,ActionRole,HelpRole,YesRole,NoRole,ResetR
enum StandardButton{
NoButton,Ok,Save,SaveAll,Open,Yes,YesToAll,No,NoToAll,Abort,Retry,Ignore,Close,Cancel,Discard,Help,Apply,Reset,Re
= Ok,LastButton = RestoreDefaults,YesAll = YesToAll, NoAll = NoToAll, Default = 0x00000100,Escape = 0x00000200,FlagM
ButtonMask = ~FlagMask};
```

> 📌 自定义KMessageBox的提示图标（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setCustomIcon(const QIcon&icon)
```

**描述**: 自定义KMessageBox的提示图标

**参数**:

| 参数 | 说明 |
|------|------|
| `QIcon` | 设置的图标 |


**备注**: 无

---


> 📌 添加一个自定义按钮（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void addButton(QAbstractButton *button,ButtonRole role)
```

**描述**: 添加一个自定义按钮

**参数**:

| 参数 | 说明 |
|------|------|
| `button` | 自定义按钮 |
| `role` | 按钮角色 |


**备注**: 无

---


> 📌 添加设置好文本的按钮(自2.0版本启用)
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QPushButton* addButton(const QString &text,ButtonRole role)
```

**描述**: 添加设置好文本的按钮

**参数**:

| 参数 | 说明 |
|------|------|
| `text` | 按钮文本 |
| `role` | 按钮角色 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QPushButton*` | 给定文本和角色的按钮 |


**备注**: 无

---


> 📌 添加一个标准按钮并且返回这个按钮（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QPushButton* addButton(StandardButton button)
```

**描述**: 添加一个标准按钮并且返回这个按钮

**参数**:

| 参数 | 说明 |
|------|------|
| `button` | 添加的标准按钮 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QPushButton*` | 需要添加的标准按钮 |


**备注**: 无

---


> 📌 移除一个按钮（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void removeButton(QAbstractButton *button)
```

**描述**: 移除一个按钮

**参数**:

| 参数 | 说明 |
|------|------|
| `button` | 需要移除的按钮 |


**备注**: 无

---


> 📌 返回与标准按钮对应的指针，如果此消息框中不存在标准按钮，则返回0（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QAbstractButton *button(StandardButton which) const
```

**描述**: 返回与标准按钮对应的指针，如果此消息框中不存在标准按钮，则返回0

**参数**:

| 参数 | 说明 |
|------|------|
| `which` | 标准按钮 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QAbstractButton*` | 标准按钮的指针，不存在返回0 |


**备注**: 无

---


> 📌 返回已添加到消息框中的所有按钮的列表（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QList buttons() const
```

**描述**: 返回已添加到消息框中的所有按钮的列表

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QList` | 所有按钮的列表 |


**备注**: 无
返回指定按钮的按钮角色，如果按钮为0或尚未添加到消息框中，此函数将返回InvalidRole（自2.0版本

---


> 📌 启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
KMessageBox::ButtonRole buttonRole(QAbstractButton *button) const
```

**描述**: 返回指定按钮的按钮角色，如果按钮为0或尚未添加到消息框中，此函数将返回InvalidRole

**参数**:

| 参数 | 说明 |
|------|------|
| `button` | 指定按钮 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `ButtonRole` | 指定按钮的角色属性 |


**备注**: 无

---


> 📌 返回KMessageBox中显示的复选框（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QCheckBox checkBox() const
```

**描述**: 返回KMessageBox中显示的复选框

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QCheckBox` | MessageBox中显示的复选框 |


**备注**: 无

---


> 📌 设置KMessageBox显示的复选框，未设置则为0（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setCheckBox(QCheckBox *cb)
```

**描述**: 设置KMessageBox显示的复选框，未设置则为0

**参数**:

| 参数 | 说明 |
|------|------|
| `cb` | 设置KMessageBox显示的复选框，未设置则为0 |


**备注**: 无

---


> 📌 获取KMessageBox的文本（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QString text() const
```

**描述**: 获取KMessageBox的文本

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QString` | messagebox文本 |


**备注**: 无

---


> 📌 设置KMessageBox的文本（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setText（const QString& text）
```

**描述**: 设置KMessageBox的文本

**参数**:

| 参数 | 说明 |
|------|------|
| `text` | 需要设置的文本 |


**备注**: 无

---


> 📌 获取KMessageBox信息性文本的描述（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QString informativeText() const
```

**描述**: 获取KMessageBox信息性文本的描述

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QString` | 获取KMessageBox信息性文本的描述 |


**备注**: 无

---


> 📌 设置KMessageBox信息性文本的描述（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setInformativeText(const QString &text)
```

**描述**: 设置KMessageBox信息性文本的描述

**参数**:

| 参数 | 说明 |
|------|------|
| `text` | 设置KMessageBox信息性文本的描述 |


**备注**: 无

---


> 📌 获取KMessageBox的图标（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
ICon icon() cosnt
```

**描述**: 获取KMessageBox的图标

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `ICon` | 获取KMessageBox的图标 |


**备注**: 无

---


> 📌 设置KMessageBox的图标（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setIcon(Icon icon )
```

**描述**: 设置KMessageBox的图标

**参数**:

| 参数 | 说明 |
|------|------|
| `icon` | 设置KMessageBox的图标 |


**备注**: 无

---


> 📌 返回当前KMessageBox的icon（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QPixmap iconPixmap() const
```

**描述**: 返回当前KMessageBox的icon

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QPixmap` | 返回当前KMessageBox的icon |


**备注**: 无

---


> 📌 设置当前KMessageBox的icon（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setIconPixmap(const QPixmap &pixmap)
```

**描述**: 设置当前KMessageBox的icon

**参数**:

| 参数 | 说明 |
|------|------|
| `pixmap` | 设置当前KMessageBox的icon |


**备注**: 无

---


> 📌 KMessageBox中标准按钮的集合（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
KMessageBox::StandardButtons standardButtons() const
```

**描述**: KMessageBox中标准按钮的集合

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `KMessageBox::StandardButtons` | KMessageBox中标准按钮的集合 |


**备注**: 无

---


> 📌 设置多个标准按钮（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setStandardButtons(KMessageBox::StandardButtons buttons)
```

**描述**: 设置多个标准按钮

**参数**:

| 参数 | 说明 |
|------|------|
| `buttons` | 设置多个标准按钮 |


**备注**: 无
返回与给定按钮对应的标准按钮枚举值，如果给定按钮不是标准按钮，则返回NoButton（自2.0版本启

---


> 📌 用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
KMessageBox::StandardButton standardButton(QAbstractButton *button) const
```

**描述**: 返回与给定按钮对应的标准按钮枚举值，如果给定按钮不是标准按钮，则返回NoButton

**参数**:

| 参数 | 说明 |
|------|------|
| `button` | 传入按钮 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `KMessageBox::StandardButton` | 返回与给定按钮对应的标准按钮枚举值，如果给定按钮不是标 准按钮，则返回NoButton |


**备注**: 无

---


> 📌 返回KMessageBox的默认按钮（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QPushButton* defaultButton() const
```

**描述**: 返回KMessageBox的默认按钮

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QPushButton*` | 返回KMessageBox的默认按钮 |


**备注**: 无

---


> 📌 设置KMessageBox的默认按钮（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setDefaultButton(QPushButton *button )
```

**描述**: 设置KMessageBox的默认按钮

**参数**:

| 参数 | 说明 |
|------|------|
| `button` | 设置KMessageBox的默认按钮 |


**备注**: 无

---


> 📌 设置KMessageBox的默认按钮（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setDefaultButton(KMessageBox::StandardButton button)
```

**描述**: 设置KMessageBox的默认按钮

**参数**:

| 参数 | 说明 |
|------|------|
| `button` | 设置KMessageBox的默认按钮 |


**备注**: 无

---


> 📌 返回被点击的按钮（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QAbstractButton* clickedButton() const
```

**描述**: 返回被点击的按钮

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QAbstractButton*` | 被点击的按钮 |


**备注**: 无

---


> 📌 返回用于标准图标的pixmap（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
static QPixmap standardIcon(Icon icon)
```

**描述**: 返回用于标准图标的pixmap

**参数**:

| 参数 | 说明 |
|------|------|
| `icon` | 传入的图标 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QPixmap` | 返回用于标准图标的pixmap |


**备注**: 无

---


> 📌 打开带有给定标题和文本的信息消息框，对话框最多可以有三个按钮（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
static StandardButton information(QWidget *parent,const QString &title,const QString
&text,StandardButton buttons =OK ,StandardButton defaultButton = NoButton)
```

**描述**: 打开带有给定标题和文本的信息消息框，对话框最多可以有三个按钮

**参数**:

| 参数 | 说明 |
|------|------|
| `parent` | 父类 |
| `title` | 窗口标题 |
| `text` | 提示文本 |
| `buttons` | 添加按钮 |
| `defaultButton` | NoButton |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `StandardButton` | 返回被单击的标准按钮的标识。如果按了Esc键，则返回escape键 |


**备注**: 无

---


> 📌 打开一个带有给定标题和文本的问题消息框（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
static StandardButton qusetion(QWidget *parent,const QString &title,const QString
*text,StandardButtons buttons =StandardButton(Yes \| No),StandardButton defaultButton =
NoButton)
```

**描述**: 打开一个带有给定标题和文本的问题消息框

**参数**:

| 参数 | 说明 |
|------|------|
| `parent` | 父类 |
| `title` | 窗口标题 |
| `text` | 提示文本 |
| `buttons` | 添加按钮 |
| `defaultButton` | NoButton |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `StandardButton` | 返回被单击的标准按钮的标识。如果按了Esc键，则返回escape键 |


**备注**: 无

---


> 📌 打开一个带有给定标题和文本的警告消息框（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
static StandardButton warning(QWidget *parent,const QString &title,const QString
&text,StandardButtons buttons =Ok,StandardButton defaultButton = NoButton)
```

**描述**: 打开一个带有给定标题和文本的警告消息框

**参数**:

| 参数 | 说明 |
|------|------|
| `parent` | 父类 |
| `title` | 窗口标题 |
| `text` | 提示文本 |
| `buttons` | 添加按钮 |
| `defaultButton` | NoButton |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `StandardButton` | 返回被单击的标准按钮的标识。如果按了Esc键，则返回escape键 |


**备注**: 无

---


> 📌 用给定的标题和文本打开一个关键信息框（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
static StandardButton critical(QWidget *parent,const QString *title,const QString
&text,StandardButtons buttons = Ok,StandardButton defaultButton = NoButton)
```

**描述**: 用给定的标题和文本打开一个关键信息框

**参数**:

| 参数 | 说明 |
|------|------|
| `parent` | 父类 |
| `title` | 窗口标题 |
| `text` | 提示文本 |
| `buttons` | 添加按钮 |
| `defaultButton` | NoButton |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `StandardButton` | 返回被单击的标准按钮的标识。如果按了Esc键，则返回escape键 |


**备注**: 无

---


> 📌 用给定的标题和文本打开一个成功消息框（自2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
static StandardButton success(QWidget *parent, const QString &title,const QString &text,
StandardButtons buttons = Ok,StandardButton defaultButton = NoButton);
```

**描述**: 用给定的标题和文本打开一个成功消息框

**参数**:

| 参数 | 说明 |
|------|------|
| `parent` | 父类 |
| `title` | 窗口标题 |
| `text` | 提示文本 |
| `buttons` | 添加按钮 |
| `defaultButton` | NoButton |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `StandardButton` | 返回被单击的标准按钮的标识。如果按了Esc键，则返回escape键 |


**备注**: 无

---

# 4.1.2.3 输入对话框

功能描述：输入对话框 KInputDialog，继承自 QDialog，参考 QInputDialog源码，对子控件布局以及
样式进行了调整，功能同 QInputDialog。
(1) 文本输入对话框，同 QInputDialog
QString KInputDialog::getText(QWidget \*parent,const QString &label,QLineEdit::EchoMode mode, const
QString &text,bool \*ok,Qt::WindowFlags flags, Qt::InputMethodHints inputMethodHints)
(2) 多行文本输入框，同 QInputDialog
QString KInputDialog::getMultiLineText(QWidget \*parent,constQString&label, const QString &text, bool
\*ok, Qt::WindowFlags flags,Qt::InputMethodHints inputMethodHints)
(3) 整型数字输入对话框，同 QInputDialog
```c
int KInputDialog::getInt(QWidget *parent,const QString &label, int value,int min, int max, int step,
bool *ok, Qt::WindowFlags flags)
```
(4) 浮点型数字输入对话框，同 QInputDialog
`double KInputDialog::getDouble(QWidget *parent,const QString &label,double value, double minValue,`
`double maxValue, int decimals, bool *ok, Qt::WindowFlags flags)`
枚举类型
枚举类型
```c
enum InputDialogOption { NoButtons,
UseListViewForComboBoxItems,
UsePlainTextEditForTextInput };
```
```c
enum InputMode { TextInput, IntInput, DoubleInput };
```

> 📌 设置输入模式（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setInputMode(InputMode mode);
```

**描述**: 设置输入模式

**参数**:

| 参数 | 说明 |
|------|------|
| `InputMode` | 输入模式 |


**备注**: 无

---


> 📌 设置标签文本（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setLabelText(const QString &text);
```

**描述**: 设置标签文本

**参数**:

| 参数 | 说明 |
|------|------|
| `QString` | 设置文本 |


**备注**: 无

---


> 📌 获取标签文本（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QString labelText() const;
```

**描述**: 获取标签文本

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QString` | 返回标签文本 |


**备注**: 无

---


> 📌 设置选项（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setOption(InputDialogOption option, bool on = true);
```

**描述**: 设置选项

**参数**:

| 参数 | 说明 |
|------|------|
| `InputDialogOption` | 输入类型 |
| `true` | 选项启用 |
| `false` | 选项禁用 |


**备注**: 无

---


> 📌 测试选项（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
bool testOption(InputDialogOption option) const;
```

**描述**: 测试选项

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 选项启用 false 选项未启用 |


**备注**: 无

---


> 📌 设置选项（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setOptions(InputDialogOptions options);
```

**描述**: 设置选项

**参数**:

| 参数 | 说明 |
|------|------|
| `InputDialogOptions` | 可传入多个选项 |


**备注**: 无

---


> 📌 获取输入框选项（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
InputDialogOptions options() const;
```

**描述**: 获取输入框选项

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `InputDialogOptions` | 获取选项模式 |


**备注**: 无

---


> 📌 设置文本值(自1.2.0版本启用)
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setTextValue(const QString &text);
```

**描述**: 设置文本值

**参数**:

| 参数 | 说明 |
|------|------|
| `QString` | 设置文本值 |


**备注**: 无

---


> 📌 获取文本值（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QString textValue() const;
```

**描述**: 获取文本值

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QString` | 文本值内容 |


**备注**: 无

---


> 📌 设置文本模式（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setTextEchoMode(QLineEdit::EchoMode mode);
```

**描述**: 设置文本模式

**参数**:

| 参数 | 说明 |
|------|------|
| `QLineEdit::EchoMode` | 文本模式 |


**备注**: 无

---


> 📌 获取文本模式（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QLineEdit::EchoMode textEchoMode() const;
```

**描述**: 获取文本模式

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QLineEdit::EchoMode` | 文本模式 |


**备注**: 无

---


> 📌 设置组合框可编辑（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setComboBoxEditable(bool editable);
```

**描述**: 设置组合框可编辑

**参数**:

| 参数 | 说明 |
|------|------|
| `true` | 可编辑 |
| `false` | 不可编辑 |


**备注**: 无

---


> 📌 返回组合框是否可以编辑（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
bool isComboBoxEditable() const;
```

**描述**: 返回组合框是否可以编辑

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 可编辑 false 不可编辑 |


**备注**: 无

---


> 📌 设置组合框项目(自1.2.0版本启用)
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setComboBoxItems(const QStringList &items);
```

**描述**: 设置组合框项目

**参数**:

| 参数 | 说明 |
|------|------|
| `QStringList` | 组合框项目列表 |


**备注**: 无

---


> 📌 返回组合框列表（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QStringList comboBoxItems() const;
```

**描述**: 返回组合框列表

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QStringList` | 组合框项目列表 |


**备注**: 无

---


> 📌 设置int类型的值（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setIntValue(int value);
```

**描述**: 设置int类型的值

**参数**:

| 参数 | 说明 |
|------|------|
| `int` | 值 |


**备注**: 无

---


> 📌 获取int类型的值（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
int intValue() const;
```

**描述**: 获取int类型的值

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int int类型的值` | - |


**备注**: 无

---


> 📌 设置int类型的最低值（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setIntMinimum(int min);
```

**描述**: 设置int类型的最低值

**参数**:

| 参数 | 说明 |
|------|------|
| `min` | int类型最小的值 |


**备注**: 无

---


> 📌 获取int最低值（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
int intMinimum() const;
```

**描述**: 获取int最低值

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 获取int类型的最小值 |


**备注**: 无

---


> 📌 设置int类型最大值（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setIntMaximum(int max);
```

**描述**: 设置int类型最大值

**参数**:

| 参数 | 说明 |
|------|------|
| `max` | int类型最大值 |


**备注**: 无

---


> 📌 获取int最大值（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
int intMaximum() const;
```

**描述**: 获取int最大值

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int int类型最大值` | - |


**备注**: 无

---


> 📌 设置int范围（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setIntRange(int min, int max);
```

**描述**: 设置int范围

**参数**:

| 参数 | 说明 |
|------|------|
| `min` | int类型最小值 |
| `max` | int类型最大值 |


**备注**: 无

---


> 📌 设置int步数（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setIntStep(int step);
```

**描述**: 设置int步数

**参数**:

| 参数 | 说明 |
|------|------|
| `step` | int类型步长 |


**备注**: 无

---


> 📌 获取int步数（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
int intStep() const;
```

**描述**: 获取int步数

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 获取int类型步长 |


**备注**: 无

---


> 📌 设置double值（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setDoubleValue(double value);
```

**描述**: 设置double值

**参数**:

| 参数 | 说明 |
|------|------|
| `value` | 设置double类型值 |


**备注**: 无

---


> 📌 获取double值（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
double doubleValue() const;
```

**描述**: 获取double值

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `double` | 获取double类型值 |


**备注**: 无

---


> 📌 设置double最小值（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setDoubleMinimum(double min);
```

**描述**: 设置double最小值

**参数**:

| 参数 | 说明 |
|------|------|
| `min` | 设置double类型最小值 |


**备注**: 无

---


> 📌 获取double最小值（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
double doubleMinimum() const;
```

**描述**: 获取double最小值

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `double` | 获取double类型最小值 |


**备注**: 无

---


> 📌 设置double最大值（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setDoubleMaximum(double max);
```

**描述**: 设置double最大值

**参数**:

| 参数 | 说明 |
|------|------|
| `max` | 设置double类型最大值 |


**备注**: 无

---


> 📌 获取double最大值（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
double doubleMaximum() const;
```

**描述**: 获取double最大值

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `double` | 获取double类型最大值 |


**备注**: 无

---


> 📌 设置double范围（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setDoubleRange(double min, double max);
```

**描述**: 设置double范围

**参数**:

| 参数 | 说明 |
|------|------|
| `min` | double范围最小值 |
| `max` | double范围最大值 |


**备注**: 无

---


> 📌 设置两个小数（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setDoubleDecimals(int decimals);
```

**描述**: 设置两个小数

**参数**:

| 参数 | 说明 |
|------|------|
| `decimal` | 小数位数 |


**备注**: 无

---


> 📌 返回小数位数（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
int doubleDecimals() const;
```

**描述**: 返回小数位数

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 小数位数 |


**备注**: 无

---


> 📌 设置确认按钮文本（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setOkButtonText(const QString &text);
```

**描述**: 设置确认按钮文本

**参数**:

| 参数 | 说明 |
|------|------|
| `text` | 确认按钮的文本内容 |


**备注**: 无

---


> 📌 获取确认按钮文本（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QString okButtonText() const;
```

**描述**: 获取确认按钮文本

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QString` | 确认按钮的文本 |


**备注**: 无

---


> 📌 设置取消按钮文本（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setCancelButtonText(const QString &text);
```

**描述**: 设置取消按钮文本

**参数**:

| 参数 | 说明 |
|------|------|
| `text` | 取消按钮的文本内容 |


**备注**: 无

---


> 📌 获取取消按钮文本(自1.2.0版本启用)
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QString cancelButtonText() const;
```

**描述**: 获取取消按钮文本

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QString` | 取消按钮的文本内容 |


**备注**: 无
此函数将其信号之一连接到接收器和成员指定的插槽。特定信号取决于在成员中指定的参数。（自1.2.0

---


> 📌 版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void open(QObject *receiver, const char *member);
```

**描述**: 此函数将其信号之一连接到接收器和成员指定的插槽。特定信号取决于在成员中指定的参数。

**参数**:

| 参数 | 说明 |
|------|------|
| `receiver` | 信号 |
| `member` | 获取信号执行操作 |


**备注**: 无

---


> 📌 设置是否可见（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setVisible(bool visible) override;
```

**描述**: 设置是否可见

**参数**:

| 参数 | 说明 |
|------|------|
| `true` | 可见 |
| `false` | 不可见 |


**备注**: 无

---


> 📌 最小尺寸提示（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QSize minimumSizeHint() const override;
```

**描述**: 最小尺寸提示

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QSize` | 最小尺寸 |


**备注**: 无

---


> 📌 尺寸提示（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QSize sizeHint() const override;
```

**描述**: 尺寸提示

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QSize` | 尺寸大小 |


**备注**: 无
获取文本（自1.2.0版本启用）
子
模
块
qt控件扩展模块
接
口
类
型
C++
原
型
static QString getText(QWidget *parent,const QString &label,QLineEdit::EchoMode echo =
QLineEdit::Normal,const QString &text = String(), bool *ok = nullptr,Qt::WindowFlags flags =
Qt::WindowFlags(),Qt::InputMethodHints inputMethodHints = Qt::ImhNone);
描
述
获取文本
参
数
parent
父类
label
显示给用户的文本
echo
行编辑器将使用的回声模式
text
行编辑中的默认文本
ok
ok为非空*如果用户按下ok ok将被设置为true，如果用户按下Cancel
ok将被设置为false
flags
Qt::WindowFlags选项
inputMethodHints
输入法提示
返
回
值
文本内容
备
注
无
获取多行文本（自1.2.0版本启用）
子
模
块
qt控件扩展模块
接
口
类
型
C++
原
型
static QString getMultiLineText(QWidget *parent,const QString &label,const QString &text =
QString(), bool *ok = nullptr,Qt::WindowFlags flags = Qt::WindowFlags(),Qt::InputMethodHints
inputMethodHints = Qt::ImhNone);
描
述
获取多行文本
参
数
parent
父类
label
显示给用户的文本
text
行编辑中的默认文本
ok
ok为非空*如果用户按下ok ok将被设置为true，如果用户按下Cancel
ok将被设置为false
flags
Qt::WindowFlags选项
inputMethodHints
输入法提示
返
回
值
QString
文本内容
备
注
无
获取项目（自1.2.0版本启用）
子
模
块
qt控件扩展模块
接
口
类
型
C++
原
型
static QString getItem(QWidget *parent,const QString &label,const QStringList &items, int
current = 0, bool editable = true,bool *ok = nullptr, Qt::WindowFlags flags =
Qt::WindowFlags(),Qt::InputMethodHints inputMethodHints = Qt::ImhNone);
描
述
获取项目
参
数
parent
父类
label
显示给用户的文本
items
插入到组合框中的字符串列表
current
当前项的编号
editable
edit为true，用户可以输入自己的文本;否则，用户可能只选择现有项
目中的一个
ok
ok为非空*如果用户按下ok ok将被设置为true，如果用户按下Cancel
ok将被设置为false
flags
Qt::WindowFlags选项
inputMethodHints
输入法提示
返
回
值
QString
返回选中的项目文本
备
注
无

---


> 📌 获取int型文本（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
static int getInt(QWidget *parent,const QString &label, int value = 0,int minValue =
-2147483647, int maxValue = 2147483647,int step = 1, bool *ok = nullptr, Qt::WindowFlags
flags = Qt::WindowFlags()
```

**描述**: 获取int型文本

**参数**:

| 参数 | 说明 |
|------|------|
| `parent` | 父类 |
| `label` | 显示给用户的文本 |
| `value` | spinbox将被设置为的默认整数 |
| `minValue` | 选择的最小值 |
| `maxValue` | 选择的最大值 |
| `step` | 按箭头按钮增加或减少值时值的变化量 |
| `ok` | ok为非空*如果用户按下ok ok将被设置为true，如果用户按下Cancel ok将被设 |
| `置为false` | flags |
| `Qt::WindowFlags选项` | - |


**备注**: 无
获取double类型文本（自1.2.0版本启用）
子
模
块
qt控件扩展模块
接
口
类
型
C++
原
型
static double getDouble(QWidget *parent,const QString &label,double value = 0, double
minValue = -2147483647,double maxValue = 2147483647, int decimals = 1, bool *ok =
nullptr,Qt::WindowFlags flags = Qt::WindowFlags());
描
述
获取double类型文本
参
数
parent
父类
label
显示给用户的文本
value
编辑将设置为的默认浮点数
minValue
选择的最小值
maxValue
选择的最大值
decimals
数字可能具有的最大小数位数
ok
ok为非空*如果用户按下ok ok将被设置为true，如果用户按下Cancel ok将被设
置为false
flags
Qt::WindowFlags选项
返
回
值
无
无
备
注
无

---


> 📌 设置double步数（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setDoubleStep(double step);
```

**描述**: 设置double步数

**参数**:

| 参数 | 说明 |
|------|------|
| `double` | double类型步数 |


**备注**: 无

---


> 📌 获取double步数（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
double doubleStep() const;
```

**描述**: 获取double步数

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `double double值` | - |


**备注**: 无

---


> 📌 设置PlaceholderText的文本内容（自1.2.0.12启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setPlaceholderText(const QString &);
```

**描述**: 设置PlaceholderText的文本内容（自1.2.0.12启用）

**参数**:

| 参数 | 说明 |
|------|------|
| `QString` | 需要设置的文本 |


**备注**: 无

---


> 📌 获取PlaceholderText的文本内容（自1.2.0.12启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QString placeholderText() const;
```

**描述**: 获取PlaceholderText的文本内容（自1.2.0.12启用）

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QString` | 获取文本内容 |


**备注**: 无

---

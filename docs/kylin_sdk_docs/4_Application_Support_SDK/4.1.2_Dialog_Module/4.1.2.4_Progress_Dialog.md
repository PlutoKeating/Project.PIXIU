# 4.1.2.4 进度对话框

功能描述：KProgressDialog，继承自 QDialog，参考 QProgressDialog 源码，对子控件 ProgressBar
的样式进行了调整。可以设置具体下载信息是否需要显示，设置进度值的后缀等。
KProgressDialog *progress2 = new KProgressDialog(tr("下载"),tr("取消"),0,100,this);
progress2->setSubContent("下载中...");
progress2->setSuffix("MB");
progress2->setWindowTitle("进度对话框");
progress2->setWindowIcon("kylin-music");
progress2->setValue(50);
progress2->setShowDetail(false);
将标签设置给label。进度对话框会调整大小以适应。标签成为进度对话框的所有权，必要时将被删除，

> 📌 所以不要在堆栈中传递对象的地址。（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setLabel(QLabel *label);
```

**描述**: 将标签设置给label。进度对话框会调整大小以适应。标签成为进度对话框的所有权，必要时将被
删除，所以不要在堆栈中传递对象的地址。

**参数**:

| 参数 | 说明 |
|------|------|
| `QLabel*` | 标签设置给label |


**备注**: 无
将取消按钮设置给按钮，cancelButton。进度对话框拥有这个按钮的所有权，这个按钮在必要时将被删
除，所以不要传递堆栈中的对象的地址，用new()来创建按钮。如果传递nullptr，将不会显示取消按

---


> 📌 钮。（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setCancelButton(QPushButton *button);
```

**描述**: 将取消按钮设置给按钮，cancelButton。进度对话框拥有这个按钮的所有权，这个按钮在必要时将
被删除，所以不要传递堆栈中的对象的地址，用new()来创建按钮。如果传递nullptr，将不会显示
取消按钮。

**参数**:

| 参数 | 说明 |
|------|------|
| `QPushButton*` | 将取消按钮设置给按钮 |


**备注**: 无
将进度条部件设置给bar。进度对话框会调整大小以适应。进度条对话框拥有进度条的所有权，该进度

---


> 📌 条将在必要时被删除，所以不要使用分配在堆栈中的进度条。（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setBar(QProgressBar *bar);
```

**描述**: 将进度条部件设置给bar。进度对话框会调整大小以适应。进度条对话框拥有进度条的所有权，该
进度条将在必要时被删除，所以不要使用分配在堆栈中的进度条。

**参数**:

| 参数 | 说明 |
|------|------|
| `QProgressBar*` | 进度条部件设置给bar |


**备注**: 无

---


> 📌 设置detail的后缀（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setSuffix(const QString& suffix);
```

**描述**: 设置detail的后缀

**参数**:

| 参数 | 说明 |
|------|------|
| `suffix` | detail的后缀 |


**备注**: 无

---


> 📌 设置是否显示详细信息（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setShowDetail(bool flag);
```

**描述**: 设置是否显示详细信息

**参数**:

| 参数 | 说明 |
|------|------|
| `flag` | true 显示信息，false 隐藏信息 |


**备注**: 无

---


> 📌 返回最小值（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
int minimum() const;
```

**描述**: 返回最小值

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 最小值 |


**备注**: 无

---


> 📌 返回最大值（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
int maximum() const;
```

**描述**: 返回最大值

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 最大值 |


**备注**: 无

---


> 📌 返回当前值（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
int value() const
```

**描述**: 返回当前值

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 当前值 |


**备注**: 无

---


> 📌 返回提示内容（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QString labelText() const;
```

**描述**: 返回提示内容

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QString` | 返回提示内容 |


**备注**: 无

---


> 📌 设置进度对话框是否在value()等于maximum()时立即调用reset()，默认为true。（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setAutoReset(bool reset);
```

**描述**: 设置进度对话框是否在value()等于maximum()时立即调用reset()，默认为true。

**参数**:

| 参数 | 说明 |
|------|------|
| `true` | 调用reset |
| `false` | 不调用reset |


**备注**: 无

---


> 📌 获取是否自动重置（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
bool autoReset() const;
```

**描述**: 获取是否自动重置

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `false` | 不自动重置 |


**备注**: 无

---


> 📌 设置是否自动关闭对话框（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setAutoClose(bool close);
```

**描述**: 设置是否自动关闭对话框

**参数**:

| 参数 | 说明 |
|------|------|
| `true` | 自动关闭对话框 |
| `false` | 不自动关闭对话框 |


**备注**: 无

---


> 📌 获取是否自动关闭对话框（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
bool autoClose() const;
```

**描述**: 获取是否自动关闭对话框

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 自动关闭对话框 false 不自动关闭对话框 |


**备注**: 无

---


> 📌 获取进度条（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
QProgressBar* progressBar();
```

**描述**: 获取进度条

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `QProgressBar*` | 进度条控件 |


**备注**: 无

---


> 📌 取消进度条。（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void cancel()
```

**描述**: 取消进度条。

**参数**: 无

**备注**: 无

---


> 📌 重置进度条。（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void reset()
```

**描述**: 重置进度条。

**参数**: 无

**备注**: 无

---


> 📌 设置进度条所代表的最高值，默认值是100。（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setMaximum(int maximum)
```

**描述**: 设置进度条所代表的最高值，默认值是100。

**参数**:

| 参数 | 说明 |
|------|------|
| `maximum` | 进度条最大值 |


**备注**: 无

---


> 📌 设置进度条所代表的最小值，默认值是0。（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setMinimum(int minimum)
```

**描述**: 设置进度条所代表的最小值，默认值是0。

**参数**:

| 参数 | 说明 |
|------|------|
| `minimum` | 进度条最小值 |


**备注**: 无
设置进度条范围，如果最大值小于最小值，则最小值成为唯一的合法值。如果当前值超出了新的范围，

---


> 📌 则用reset()重置进度框。（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setRange(int minimum, int maximum)
```

**描述**: 设置进度条范围，如果最大值小于最小值，则最小值成为唯一的合法值。如果当前值超出了
新的范围，则用reset()重置进度框。

**参数**:

| 参数 | 说明 |
|------|------|
| `minimum` | 进度条最小值 |
| `maximum` | 进度条最大值 |


**备注**: 无

---


> 📌 设置当前进度值。（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setValue(int progress)
```

**描述**: 设置当前进度值。

**参数**:

| 参数 | 说明 |
|------|------|
| `progress` | 当前进度条的值 |


**备注**: 无

---


> 📌 设置提示标签文本。（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setLabelText(const QString &text)
```

**描述**: 设置提示标签文本。

**参数**:

| 参数 | 说明 |
|------|------|
| `text` | 提示文本 |


**备注**: 无

---


> 📌 设置取消按钮文本。（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setCancelButtonText(const QString &text)
```

**描述**: 设置取消按钮文本。

**参数**:

| 参数 | 说明 |
|------|------|
| `text` | 取消按钮文本 |


**备注**: 无

---


> 📌 设置次级内容。（自1.2.0版本启用）
> **子模块**: qt控件扩展模块 | **接口类型**: C++

**原型**:
```c
void setSubContent(const QString &text)
```

**描述**: 设置次级内容。

**参数**:

| 参数 | 说明 |
|------|------|
| `text` | 取消按钮文本 |


**备注**: 无

---

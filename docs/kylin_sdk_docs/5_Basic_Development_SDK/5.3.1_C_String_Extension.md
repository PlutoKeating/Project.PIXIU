# 5.3.1 C字符串功能扩展

该模块扩展c字符串操作函数
头文件路径
`#include "kysdk/kysdk-base/cstring-extension.h"`
so库路径

```
/usr/lib/*/libkyutils.so '*'代表不同架构的架构目录名称，例如x86_64-linux-gnu
```

子模块信息

> 📌 分割字符串(自1.2.0版本启用)
> **子模块**: C语言字符串扩展 | **接口类型**: C

**原型**:
```c
static inline char** strsplit(char *str, char delim)
```

**描述**: 对原字符串以给定的分隔符进行分割，注意该函数会修改原字符串

**参数**:

| 参数 | 说明 |
|------|------|
| `str` | 需要分割的字符串指针 |
| `delim` | 分隔符 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char**` | 分割后的字符串列表，以NULL结尾 |


**备注**: 存储分割后所有字符串的字符串列表本身是由alloc申请的内存，因此当使用完成后应当被
free；
而分割出来的各个字符串不是申请的内存，而是分别指向了原字符串中的特定位置，因此他们不
需要被分别free

---


> 📌 小写字符串(自1.2.0版本启用)
> **子模块**: C语言字符串扩展 | **接口类型**: C

**原型**:
```c
static inline void str2lower(char *str)
```

**描述**: 将字符串中的所有大写字母转化为小写字母

**参数**:

| 参数 | 说明 |
|------|------|
| `str` | 需要操作的字符串指针 |


**返回值**: 无

**备注**: 无

---


> 📌 大写字符串(自1.2.0版本启用)
> **子模块**: C语言字符串扩展 | **接口类型**: C

**原型**:
```c
static inline void str2upper(char *str)
```

**描述**: 将字符串中的所有小写字母转化为大写字母

**参数**:

| 参数 | 说明 |
|------|------|
| `str` | 需要操作的字符串指针 |


**返回值**: 无

**备注**: 无

---


> 📌 统计出现次数(自1.2.0版本启用)
> **子模块**: C语言字符串扩展 | **接口类型**: C

**原型**:
```c
static inline size_t strcounts(const char *str, char ch)
```

**描述**: 统计给定字符在字符串中出现的次数

**参数**:

| 参数 | 说明 |
|------|------|
| `str` | 原字符串 |
| `ch` | 需要统计的字符 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `size_t` | 字符出现的次数 |


**备注**: 无

---


> 📌 后缀判断(自1.2.0版本启用)
> **子模块**: C语言字符串扩展 | **接口类型**: C

**原型**:
```c
static inline int strendwith(const char *str, const char *postfix)
```

**描述**: 判断str是否以postfix结尾需要区分大小写

**参数**:

| 参数 | 说明 |
|------|------|
| `str` | 原字符串 |
| `postfix` | 需要匹配的字符串后缀 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | str以postfix结尾 1 str不以postfix结尾 |


**备注**: 无

---


> 📌 查找子字符串的首次出现位置(自1.2.0版本启用)
> **子模块**: C语言字符串扩展 | **接口类型**: C

**原型**:
```c
static inline int strfirstof(const char* str, char ch)
```

**描述**: 在给定的字符串中查找给定字符第一次出现的位置；计数从0开始

**参数**:

| 参数 | 说明 |
|------|------|
| `str` | 原字符串 |
| `ch` | 需要查找的字符 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 第一次出现的位置，若未找到给定的字符，则返回-1 |


**备注**: 无

---


> 📌 查找子字符串的末次出现位置(自1.2.0版本启用)
> **子模块**: C语言字符串扩展 | **接口类型**: C

**原型**:
```c
static inline int strlastof(const char* str, char ch)
```

**描述**: 在给定的字符串中查找给定字符最后一次出现的位置；计数从0开始

**参数**:

| 参数 | 说明 |
|------|------|
| `str` | 原字符串 |
| `ch` | 需要查找的字符 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 最后一次出现的位置，若未找到给定的字符，则返回-1 |


**备注**: 无

---


> 📌 删除空格和制表符(自1.2.0版本启用)
> **子模块**: C语言字符串扩展 | **接口类型**: C

**原型**:
```c
static inline void strstripblank(char *str)
```

**描述**: 删除给定字符串前后的空格和水平制表符（tab），注意该操作会修改原字符串

**参数**:

| 参数 | 说明 |
|------|------|
| `str` | 需要修改的字符串指针 |


**返回值**: 无

**备注**: 无

---


> 📌 跳过开始的所有空格、制表符、换行符(自1.2.0版本启用)
> **子模块**: C语言字符串扩展 | **接口类型**: C

**原型**:
```c
static inline const char *strskipspace(const char *p)
```

**描述**: 跳过字符串前的所有空格、制表符、换行符；该操作不会修改原字符串

**参数**:

| 参数 | 说明 |
|------|------|
| `p` | 指向原字符串的指针 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `const char*` | 指向跳过space后的字符串指针 |


**备注**: 无

---


> 📌 前缀判断（区分大小写）(自1.2.0版本启用)
> **子模块**: C语言字符串扩展 | **接口类型**: C

**原型**:
```c
static inline int strstartswith(const char *str, const char *prefix)
```

**描述**: 判断str是否以prefix开头；区分大小写

**参数**:

| 参数 | 说明 |
|------|------|
| `str` | 原字符串 |
| `prefix` | 需要匹配的字符串前缀 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | str是以prefix开头 1 str不是以prefix开头 |


**备注**: 无

---


> 📌 前缀判断（不区分大小写）(自1.2.0版本启用)
> **子模块**: C语言字符串扩展 | **接口类型**: C

**原型**:
```c
static inline int strstartswith_nocase(const char *str, const char *prefix)
```

**描述**: 判断str是否以prefix开头；不区分大小写

**参数**:

| 参数 | 说明 |
|------|------|
| `str` | 原字符串 |
| `prefix` | 需要匹配的字符串前缀 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | str是以prefix开头 1 str不是以prefix开头 |


**备注**: 无

---


> 📌 删减字符串前后的指定字符(自1.2.0版本启用)
> **子模块**: C语言字符串扩展 | **接口类型**: C

**原型**:
```c
static inline void strstrip(char *str, char ch)
```

**描述**: 对给定的字符串进行strip操作，删减字符串前后的指定字符；注意该操作会修改原字符串

**参数**:

| 参数 | 说明 |
|------|------|
| `str` | 需要进行strip的字符串指针 |
| `ch` | 需要删除的字符 |


**返回值**: 无

**备注**: 无

---


> 📌 跳过开始的所有空格、水平制表符(自1.2.0版本启用)
> **子模块**: C语言字符串扩展 | **接口类型**: C

**原型**:
```c
static inline const char *strskipblank(const char *p)
```

**描述**: 跳过字符串前的所有空格和水平制表符（tab）；该操作不会修改原字符串

**参数**:

| 参数 | 说明 |
|------|------|
| `p` | 指向原字符串的指针 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `const char*` | 指向跳过space后的字符串指针 |


**备注**: 无

---


> 📌 删除前后的空格、制表符、换行符(自1.2.0版本启用)
> **子模块**: C语言字符串扩展 | **接口类型**: C

**原型**:
```c
static inline void strstripspace(char *str)
```

**描述**: 删除给定字符串前后的空格、制表符、换行符，注意该操作会修改原字符串

**参数**:

| 参数 | 说明 |
|------|------|
| `str` | 需要进行strip操作的字符串指针 |


**返回值**: 无

**备注**: 无

---


> 📌 判断字符串是否是数字(自3.0.1.0启用)
> **子模块**: C语言字符串扩展模块 | **接口类型**: C

**原型**:
```c
bool strisdigit(char *str)
```

**描述**: 判断字符串 `str` 是否全部由数字字符组成。

**参数**:

| 参数 | 说明 |
|------|------|
| `str` | 待检查的字符串 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 是 false 不是 |


**备注**: 无

---


> 📌 判断字符串str是否是空格符(自3.0.1.0启用)
> **子模块**: C语言字符串扩展模块 | **接口类型**: C

**原型**:
```c
bool strisspace(char *str)
```

**描述**: 判断字符串 `str` 是否全部由空格符组成。

**参数**:

| 参数 | 说明 |
|------|------|
| `str` | 待检查的字符串 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 是 false 不是 |


**备注**: 无

---


> 📌 判断字符串str是否是空白符(自3.0.1.0启用)
> **子模块**: C语言字符串扩展模块 | **接口类型**: C

**原型**:
```c
bool strisspace(char *str)
```

**描述**: 判断字符串 `str` 是否全部由空白符组成。

**参数**:

| 参数 | 说明 |
|------|------|
| `str` | 待检查的字符串 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 是 false 不是 |


**备注**: 无

---


> 📌 判断字符串str是否是字母(自3.0.1.0启用)
> **子模块**: C语言字符串扩展模块 | **接口类型**: C

**原型**:
```c
bool strisalpha(char *str)
```

**描述**: 判断字符串str是否是字母

**参数**:

| 参数 | 说明 |
|------|------|
| `str` | 待检查的字符串 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 是 false 不是 |


**备注**: 无

---


> 📌 判断字符串str是否是表意文字(自3.0.1.0启用)
> **子模块**: C语言字符串扩展模块 | **接口类型**: C

**原型**:
```c
bool strisideograph(char *str)
```

**描述**: 判断字符串 `str` 是否是表意文字

**参数**:

| 参数 | 说明 |
|------|------|
| `str` | 待检查的字符串 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 是 false 不是 |


**备注**: 无

---


> 📌 判断字符串str是否是从右到左语言的字符(自3.0.1.0启用)
> **子模块**: C语言字符串扩展模块 | **接口类型**: C

**原型**:
```c
bool strisrtl(char *str)
```

**描述**: 判断字符串 `str` 是否属于从右到左语言的字符。

**参数**:

| 参数 | 说明 |
|------|------|
| `str` | 待检查的字符串 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 是 false 不是 |


**备注**: 无

---

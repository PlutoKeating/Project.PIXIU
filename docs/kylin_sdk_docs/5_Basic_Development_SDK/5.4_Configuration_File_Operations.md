# 5.4 配置文件操作

KYSDK 配置文件处理库，支持标准格式、XML(未实现)、JSON(未实现)的 配置文件处理，包括配置文件的读
与写操作。
安装命令

```bash
$ sudo apt-get install libkysdk-config libkysdk-config-dev
```

构建示例
（1）.pro 文件构建项目
CONFIG += link_pkgconfig
PKGCONFIG += kysdk-config
（2）CMakeLists.txt 构建项目
`cmake_minimum_required(VERSION 3.5)`
find_package(PkgConfig REQUIRED)
pkg_check_modules(KYSDKCONFIG kysdk-config)
target_include_directories(demo PRIVATE ${KYSDKCONFIG_INCLUDE_DIRS})
target_link_directories(demo PRIVATE ${KYSDKCONFIG_LIBRARY_DIRS})
target_link_libraries(demo PRIVATE ${KYSDKCONFIG_LIBRARIES})

#### 5.4.1 配置文件操作功能

头文件路径
`#include "kysdk/kysdk-base/libkyconf.h"`
so库路径

```
/usr/lib/*/libkyconf.so '*'代表不同架构的架构目录名称，例如x86_64-linux-gnu
```

子模块信息

> 📌 初始化配置文件(自1.2.0版本启用)
> **子模块**: 配置文件操作 | **接口类型**: C

**原型**:
```c
int kdk_conf_init(const char* confpath)
```

**描述**: 初始化配置文件

**参数**:

| 参数 | 说明 |
|------|------|
| `confpath` | 配置文件的路径 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int(非负值)` | - |
| `成功 int(负值)` | 错误码 |


**备注**: 无

---


> 📌 销毁指定的配置文件句柄(自1.2.0版本启用)
> **子模块**: 配置文件操作 | **接口类型**: C

**原型**:
```c
void kdk_conf_destroy(int id)
```

**描述**: 销毁指定的配置文件句柄

**参数**:

| 参数 | 说明 |
|------|------|
| `id` | 由kdk_conf_init返回的句柄值 |


**返回值**: 无

**备注**: 无

---


> 📌 重新载入配置文件(自1.2.0版本启用)
> **子模块**: 配置文件操作 | **接口类型**: C

**原型**:
```c
int kdk_conf_reload(int id)
```

**描述**: 重新载入配置文件

**参数**:

| 参数 | 说明 |
|------|------|
| `id` | 由kdk_conf_init返回的句柄值 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | - |
| `成功 int` | 错误码 |


**备注**: 无

---


> 📌 获取指定配置项的值(自1.2.0版本启用)
> **子模块**: 配置文件操作 | **接口类型**: C

**原型**:
```c
const char* kdk_conf_get_value(int id, const char* group, const char* key)
```

**描述**: 获取指定配置项的值

**参数**:

| 参数 | 说明 |
|------|------|
| `id` | 由kdk_conf_init返回的句柄值 |
| `group` | 组名称 |
| `key` | 配置项名称 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `const char*` | 配置项所拥有的值,若key不存在，则返回一个空字符串 |


**备注**: 无

---


> 📌 枚举 key 值(自1.2.0版本启用)
> **子模块**: 配置文件操作 | **接口类型**: C

**原型**:
```c
char** const kdk_conf_list_key(int id, const char* group)
```

**描述**: 列举id对应配置文件的指定Group下的key值，结尾以NULL指针表示

**参数**:

| 参数 | 说明 |
|------|------|
| `id` | 由kdk_conf_init返回的句柄值 |
| `group` | 需要列举的Group名称 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `const char** const` | 以NULL结尾的字符串列表，每个字符串都是一个key名称 |


**备注**: 字符串列表本身是由alloc分配的内存，需要被free释放；字符串不需要释放

---


> 📌 枚举配置文件的 Group(自1.2.0版本启用)
> **子模块**: 配置文件操作 | **接口类型**: C

**原型**:
```c
char** const kdk_conf_list_group(int id)
```

**描述**: 列举id对应配置文件的所有Group，结尾以NULL指针表示

**参数**:

| 参数 | 说明 |
|------|------|
| `id` | 由kdk_conf_init返回的句柄值 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `const char** const` | 以NULL结尾的字符串列表，每个字符串都是一个组名称 |


**备注**: 字符串列表本身是由alloc分配的内存，需要被free释放；字符串不需要释放

---

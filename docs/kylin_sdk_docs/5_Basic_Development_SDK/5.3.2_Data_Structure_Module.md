# 5.3.2 数据结构模块

该模块实现了 C 语言链表操作相关接口。
头文件路径
`#include "kysdk/kysdk-base/skip_linklist.h"`
so库路径

```
/usr/lib/*/libkydatastruct.so '*'代表不同架构的架构目录名称，例如x86_64-linux-gnu
```

子模块信息

> 📌 创建跳表(自1.2.0版本启用)
> **子模块**: 数据结构模块 | **接口类型**: C

**原型**:
```c
kysdk_skiplist* kysdk_create_skiplist()
```

**描述**: 创建跳表

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `kysdk_skiplist *` | 跳表 |


**备注**: 无

---


> 📌 销毁跳表(自1.2.0版本启用)
> **子模块**: 数据结构模块 | **接口类型**: C

**原型**:
```c
void kysdk_destroy_skiplist(kysdk_skiplist *list)
```

**描述**: 销毁跳表，并回收所有分配的内存；注意data.ptr指向的内存（若存在）不会被释放

**参数**:

| 参数 | 说明 |
|------|------|
| `list` | 需要操作的跳表 |


**返回值**: 无

**备注**: 无

---


> 📌 删除 key 值对应的节点(自1.2.0版本启用)
> **子模块**: 数据结构模块 | **接口类型**: C

**原型**:
```c
int kysdk_skiplist_delete(kysdk_skiplist *list, int key)
```

**描述**: 删除key值对应的节点

**参数**:

| 参数 | 说明 |
|------|------|
| `list` | 需要操作的跳表 |
| `key` | 键 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | - |
| `成功` | - |
| `-1` | 失败 |


**备注**: 无

---


> 📌 插入节点(自1.2.0版本启用)
> **子模块**: 数据结构模块 | **接口类型**: C

**原型**:
```c
int kysdk_skiplist_insert(kysdk_skiplist *list, int key, kysdk_listdata data)
```

**描述**: 插入节点

**参数**:

| 参数 | 说明 |
|------|------|
| `list` | 需要操作的跳表 |
| `key` | 键 |
| `data` | 数据 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | - |
| `成功` | - |
| `-1` | 失败 |


**备注**: 无

---


> 📌 根据给定的 key 搜索 data 内容(自1.2.0版本启用)
> **子模块**: 数据结构模块 | **接口类型**: C

**原型**:
```c
kysdk_listdata kysdk_skiplist_search(kysdk_skiplist *list, int key)
```

**描述**: 根据给定的key搜索data内容

**参数**:

| 参数 | 说明 |
|------|------|
| `list` | 需要操作的跳表 |
| `key` | 键 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `kysdk_listdata` | 节点数据,当搜索的key不存在时，data.num值为-1 |


**备注**: 无

---


> 📌 设置跳表最高层数(自1.2.0版本启用)
> **子模块**: 数据结构模块 | **接口类型**: C

**原型**:
```c
int kysdk_skiplist_setmaxlevels(kysdk_skiplist *list, unsigned int maxlevels)
```

**描述**: 设置跳表最高层数，该选项必须在跳表为空时使用

**参数**:

| 参数 | 说明 |
|------|------|
| `list` | 需要操作的跳表 |
| `maxlevels` | 层数 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | - |
| `成功` | - |
| `-1` | 失败 |


**备注**: 无

---

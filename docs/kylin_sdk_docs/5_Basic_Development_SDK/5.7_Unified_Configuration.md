# 5.7 统一配置

该模块提供了统一配置读写功能
安装命令

```bash
$ sudo apt-get install libkysdk-conf2 libkysdk-conf2-dev
```

构建示例
（1）.pro 文件构建项目
CONFIG += link_pkgconfig
PKGCONFIG += kysdk-conf2
（2）CMakeLists.txt 构建项目
`cmake_minimum_required(VERSION 3.5)`
find_package(PkgConfig REQUIRED)
pkg_check_modules(CONF2 kysdk-conf2)
target_include_directories(demo PRIVATE ${CONF2_INCLUDE_DIRS})
target_link_directories(demo PRIVATE ${CONF2_LIBRARY_DIRS})
target_link_libraries(demo PRIVATE ${CONF2_LIBRARIES})

#### 5.7.1 统一配置模块

头文件路径
`#include "kysdk/kysdk-base/libkysettings.h"`
so库路径

```
/usr/lib/*/libkyconf2.so '*'代表不同架构的架构目录名称，例如x86_64-linux-gnu
```

子模块信息

> 📌 获取配置句柄(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
KSettings *kdk_conf2_new(const char *id, const char *version)
```

**描述**: 获取指定id指定版本的句柄

**参数**:

| 参数 | 说明 |
|------|------|
| `id` | 包含了所有父组件名以'.'分割的字符串。不包括版本号 |
| `version` | 版本号，传NULL使用默认版本 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `KSettings *` | 配置句柄 |


**备注**: 无

---


> 📌 释放配置句柄(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
void kdk_conf2_ksettings_destroy(KSettings *ksettings)
```

**描述**: 释放句柄

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |


**返回值**: 无

**备注**: 无

---


> 📌 获取配置句柄对应的配置id(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
char *kdk_conf2_get_id(KSettings *ksettings)
```

**描述**: 获取配置句柄对应的配置id

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char *` | 句柄对应的id |


**备注**: 返回值需要释放

---


> 📌 获取配置句柄对应的配置版本(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
char *kdk_conf2_get_version(KSettings *ksettings)
```

**描述**: 获取配置句柄对应的配置版本

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char *` | 句柄对应的配置版本 |


**备注**: 返回值需要释放

---


> 📌 获取子组件句柄(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
KSettings *kdk_conf2_get_child(KSettings *ksettings, const char *name)
```

**描述**: 获取子组件句柄

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `name` | 子组件名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `KSettings *` | 子组件句柄 |


**备注**: 无

---


> 📌 获取所有子组件名(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
char **kdk_conf2_list_children(KSettings *ksettings)
```

**描述**: 获取所有子组件名

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char **` | 子组件名列表 |


**备注**: 返回值需要释放

---


> 📌 获取所有键名(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
char **kdk_conf2_list_keys(KSettings *ksettings)
```

**描述**: 获取所有键名

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char **` | 键名列表 |


**备注**: 返回值需要释放

---


> 📌 获取键值取值范围(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
char *kdk_conf2_get_range(KSettings *ksettings, const char *key)
```

**描述**: 获取键值取值范围

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char *` | 取值范围 取值区间 "min,max" 枚举"{nick:value}" |


**备注**: 返回值需要释放

---


> 📌 检查要设置值是否在取值区间(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
int kdk_conf2_range_check(KSettings *ksettings, const char *key, const char *value)
```

**描述**: 检查要设置值是否在取值区间

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |
| `value` | 要设置的值 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | false 1 true |


**备注**: 无

---


> 📌 设置键值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
int kdk_conf2_set_value(KSettings *ksettings, const char *key, const char *value)
```

**描述**: 设置键的值

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |
| `value` | 要设置的值 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | false 1 true |


**备注**: 无

---


> 📌 读取键值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
char *kdk_conf2_get_value(KSettings *ksettings, const char *key)
```

**描述**: 读取键的键值

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char *` | 键值 |


**备注**: 返回值需要释放

---


> 📌 读取键的默认值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
char *kdk_conf2_get_defalut_value(KSettings *ksettings, const char *key)
```

**描述**: 读取键的默认值

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char *` | 键值 |


**备注**: 返回值需要释放

---


> 📌 设置 boolean类型的键值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
int kdk_conf2_set_boolean(KSettings *ksettings, const char *key, int value)
```

**描述**: 设置boolean类型的键值
需确保键存在且类型正确

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |
| `value` | 要设置的值 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | false 1 true |


**备注**: 无

---


> 📌 读取 boolean类型的键值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
int kdk_conf2_get_boolean(KSettings *ksettings, const char *key)
```

**描述**: 读取boolean类型的键值
需确保键存在且类型正确

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | false 1 true |


**备注**: 无

---


> 📌 读取 boolean类型的默认值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
int kdk_conf2_get_default_boolean(KSettings *ksettings, const char *key)
```

**描述**: 读取boolean类型的默认值
需确保键存在且类型正确

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | false 1 true |


**备注**: 无

---


> 📌 设置 double类型的键值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
int kdk_conf2_set_double(KSettings *ksettings, const char *key, double value)
```

**描述**: 设置double类型的键值
需确保键存在且类型正确

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |
| `value` | 要设置的值 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | false 1 true |


**备注**: 无

---


> 📌 读取 double类型的键值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
double kdk_conf2_get_double(KSettings *ksettings, const char *key)
```

**描述**: 读取double类型的键值
需确保键存在且类型正确

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `double` | 键值 |


**备注**: 无

---


> 📌 读取 double类型的默认值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
double kdk_conf2_get_defalut_double(KSettings *ksettings, const char *key)
```

**描述**: 读取double类型的默认值
需确保键存在且类型正确

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `double` | 键值 |


**备注**: 无

---


> 📌 设置枚举类型的键值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
int kdk_conf2_set_enum(KSettings *ksettings, const char *key, int value)
```

**描述**: 设置枚举类型的键值
需确保键存在且类型正确

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |
| `value` | 要设置的值 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | false 1 true |


**备注**: 无

---


> 📌 读取枚举类型的键值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
int kdk_conf2_get_enum(KSettings *ksettings, const char *key)
```

**描述**: 读取枚举类型的键值
需确保键存在且类型正确

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 枚举值 |


**备注**: 无

---


> 📌 读取枚举类型的默认值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
int kdk_conf2_get_default_enum(KSettings *ksettings, const char *key)
```

**描述**: 读取枚举类型的默认值
需确保键存在且类型正确

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 枚举值 |


**备注**: 无

---


> 📌 设置 int类型的键值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
int kdk_conf2_set_int(KSettings *ksettings, const char *key, int value)
```

**描述**: 设置int类型的键值
需确保键存在且类型正确

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |
| `value` | 要设置的值 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | false 1 true |


**备注**: 无

---


> 📌 读取 int类型的键值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
int kdk_conf2_get_int(KSettings *ksettings, const char *key)
```

**描述**: 读取int类型的键值
需确保键存在且类型正确

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 键值 |


**备注**: 无

---


> 📌 读取 int类型的默认值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
int kdk_conf2_get_default_int(KSettings *ksettings, const char *key)
```

**描述**: 读取int类型的默认值
需确保键存在且类型正确

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 键值 |


**备注**: 无

---


> 📌 设置 int64类型的键值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
int kdk_conf2_set_int64(KSettings *ksettings, const char *key, long value)
```

**描述**: 设置long类型的键值
需确保键存在且类型正确

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |
| `value` | 要设置的值 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | false 1 true |


**备注**: 无

---


> 📌 读取 int64类型的键值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
long kdk_conf2_get_int64(KSettings *ksettings, const char *key)
```

**描述**: 读取long类型的键值
需确保键存在且类型正确

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `long` | 键值 |


**备注**: 无

---


> 📌 读取 int64类型的默认值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
long kdk_conf2_get_default_int64(KSettings *ksettings, const char *key)
```

**描述**: 读取long类型的默认值
需确保键存在且类型正确

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `long` | 键值 |


**备注**: 无

---


> 📌 设置 uint类型的键值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
int kdk_conf2_set_uint(KSettings *ksettings, const char *key, unsigned int value)
```

**描述**: 设置unsigned int类型的键值
需确保键存在且类型正确

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |
| `value` | 要设置的值 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | false 1 true |


**备注**: 无

---


> 📌 读取 uint类型的键值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
unsigned int kdk_conf2_get_uint(KSettings *ksettings, const char *key)
```

**描述**: 读取unsigned int类型的键值
需确保键存在且类型正确

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `unsigned int` | 键值 |


**备注**: 无

---


> 📌 读取 uint类型的默认值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
unsigned int kdk_conf2_get_default_uint(KSettings *ksettings, const char *key)
```

**描述**: 读取unsigned int类型的默认值
需确保键存在且类型正确

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `unsigned int` | 键值 |


**备注**: 无

---


> 📌 设置 uint64类型的键值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
int kdk_conf2_set_uint64(KSettings *ksettings, const char *key, unsigned long value)
```

**描述**: 设置unsigned long类型的键值
需确保键存在且类型正确

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |
| `value` | 要设置的值 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | false 1 true |


**备注**: 无

---


> 📌 读取 uint64类型的键值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
unsigned long kdk_conf2_get_uint64(KSettings *ksettings, const char *key)
```

**描述**: 读取unsigned long类型的值
需确保键存在且类型正确

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `unsigned long` | 键值 |


**备注**: 无

---


> 📌 读取 uint64类型的默认值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
unsigned long kdk_conf2_get_default_uint64(KSettings *ksettings, const char *key)
```

**描述**: 读取unsigned long类型的默认值
需确保键存在且类型正确

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `unsigned long` | 键值 |


**备注**: 无

---


> 📌 设置字符串类型的键值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
int kdk_conf2_set_string(KSettings *ksettings, const char *key, const char *value)
```

**描述**: 设置字符串类型的键值
需确保键存在且类型正确

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |
| `value` | 要设置的值 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | false 1 true |


**备注**: 无

---


> 📌 读取字符串类型的键值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
char *kdk_conf2_get_string(KSettings *ksettings, const char *key)
```

**描述**: 读取字符串类型的键值
需确保键存在且类型正确

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char *` | 键值 |


**备注**: 返回值需要释放

---


> 📌 读取字符串类型的默认值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
char *kdk_conf2_get_default_string(KSettings *ksettings, const char *key)
```

**描述**: 读取字符串类型的默认值
需确保键存在且类型正确

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char *` | 键值 |


**备注**: 返回值需要释放

---


> 📌 设置字符串列表的键值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
int kdk_conf2_set_strv(KSettings *ksettings, const char *key, const char *const *value)
```

**描述**: 设置字符串列表的键值
需确保键存在且类型正确

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |
| `value` | 要设置的值，以NULL结尾的字符串列表 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | false 1 true |


**备注**: 无

---


> 📌 读取字符串列表的键值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
char **kdk_conf2_get_strv(KSettings *ksettings, const char *key)
```

**描述**: 读取字符串列表的键值
需确保键存在且类型正确

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char **` | 键值 |


**备注**: 返回值需要释放

---


> 📌 读取字符串列表的默认值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
char **kdk_conf2_get_default_strv(KSettings *ksettings, const char *key)
```

**描述**: 读取字符串列表的默认值
需确保键存在且类型正确

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char **` | 键值 |


**备注**: 返回值需要释放

---


> 📌 读取键的描述(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
char *kdk_conf2_get_descrition(KSettings *ksettings, const char *key)
```

**描述**: 读取键的描述

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char *` | 键的描述 |


**备注**: 返回值需要释放

---


> 📌 读取键的简述(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
char *kdk_conf2_get_summary(KSettings *ksettings, const char *key)
```

**描述**: 读取键的简述

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char *` | 键的简述 |


**备注**: 返回值需要释放

---


> 📌 读取键的类型字符串(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
char *kdk_conf2_get_type(KSettings *ksettings, const char *key)
```

**描述**: 读取键的类型字符串

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char *` | 键的类型 |


**备注**: 返回值需要释放

---


> 📌 重置键值为默认值(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
void kdk_conf2_reset(KSettings *ksettings, const char *key)
```

**描述**: 重置键值为默认值

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |


**返回值**: 无

**备注**: 无

---


> 📌 检测键值是否可设置(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
int kdk_conf2_is_writable(KSettings *ksettings, const char *key)
```

**描述**: 检测键值是否可设置

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | false 1 true |


**备注**: 无

---


> 📌 检测组件配置中是否存在某个key(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
int kdk_conf2_has_key(KSettings *ksettings, const char *key)
```

**描述**: 检测组件配置中是否存在某个key

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `key` | 键名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | false 1 true |


**备注**: 无

---


> 📌 注册回调函数响应信号(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
unsigned long kdk_conf2_connect_signal(KSettings *ksettings, const char *signal_name,
KCallBack handler, void *user_data)
```

**描述**: 注册回调函数响应信号

**参数**:

| 参数 | 说明 |
|------|------|
| `ksettings` | 配置句柄 |
| `signal_name` | 信号名 |
| `handler` | 回调函数指针 |
| `user_date` | 用户数据 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `unsigned long` | 信号连接id |


**备注**: 无

---


> 📌 信号详情
> **子模块**: 统一配置模块 | **接口类型**: C
信号
changed::detailed
void changed(KSettings *setting, const char *key, void *user_data)
reload
void reload(void)


**参数**: 无

**返回值**: 无

**备注**: 无

---


> 📌 重新读取配置文件(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
void kdk_conf2_reload(void)
```

**描述**: 重新读取配置文件

**参数**: 无

**返回值**: 无

**备注**: 无

---


> 📌 读取指定应用所有id(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
char **kdk_conf2_list_schemas(const char *app, const char *version)
```

**描述**: 查看指定应用拥有的所有schema id。可指定版本，不指定则读取默认版本

**参数**:

| 参数 | 说明 |
|------|------|
| `app` | 应用名 |
| `version` | 版本号 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char **` | id列表 |


**备注**: 无

---


> 📌 重定位(自2.4.1.0版本启用)
> **子模块**: 统一配置模块 | **接口类型**: C

**原型**:
```c
KSettings *kdk_conf2_new_extends_id(const char *old_id, const char *new_id, const char
*version)
```

**描述**: 在统一视图中创建一个新的id，内容继承自old_id。并返回句柄

**参数**: 无

**返回值**: 无

---

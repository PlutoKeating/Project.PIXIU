# 9.4.2.3 音频配置

添加头文件
`#include <coreai/speech/audioconfig.h>`

> 📌 配置输入音频数据系统默认麦克风获取，适用于流式语音识别
> **子模块**: 音频配置
编程语
言
C

**原型**:
```c
AudioConfig
*audio_config_create_continuous_audio_input_from_default_microphone()
```

**描述**: 创建音频配置，输入音频数据从默认麦克风中获取

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `AudioConfig *` | 音频配置实例指针 |


---


> 📌 配置输入音频数据从数据流中获取，适用于流式语音识别
> **子模块**: 音频配置
编程语
言
C

**原型**:
```c
AudioConfig *audio_config_create_continuous_audio_input_from_audio_data_stream(
AudioDataStream *stream)
```

**描述**: 创建音频配置，使用音频数据流作为输入音频
stream
音频数据流

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `AudioConfig *` | 音频配置实例指针 |


---


> 📌 配置输入音频从 pcm 数据中获取，适用于一次性语音识别
> **子模块**: 音频配置 | **编程语言**: C

**原型**:
```c
AudioConfig *audio_config_create_once_audio_input_from_pcm_data( const uint8_t
*audio_data, uint32_t data_length)
```

**描述**: 创建音频配置，使用 pcm 音频数据作为输入音频

**参数**:

| 参数 | 说明 |
|------|------|
| `audio_data` | 音频数据指针 |
| `data_length` | 音频数据长度 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `AudioConfig *` | 音频配置实例指针 |


---


> 📌 配置输入音频从 pcm 文件中获取数据，适用于一次性语音识别
> **子模块**: 音频配置
编程语
言
C

**原型**:
```c
AudioConfig *audio_config_create_once_audio_input_from_pcm_file(const char
*pcm_file)
```

**描述**: 创建音频配置，使用 pcm 文件作为输入音频

**参数**:

| 参数 | 说明 |
|------|------|
| `pcm_file` | pcm 文件 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `AudioConfig *` | 音频配置实例指针 |


---


> 📌 配置语音以原始数据输出
> **子模块**: 音频配置 | **编程语言**: C

**原型**:
```c
AudioConfig *audio_config_create_audio_output_from_pcm_data()
```

**描述**: 创建音频配置，将合成的音频以原始数据形式输出

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `AudioConfig *` | 音频配置实例指针 |


---


> 📌 配置语音输出到 pcm 文件
> **子模块**: 音频配置
编程语
言
C

**原型**:
```c
AudioConfig *audio_config_create_audio_output_from_pcm_file_name(const char
*pcm_file)
```

**描述**: 创建音频配置，将合成的音频输出到 pcm 文件

**参数**:

| 参数 | 说明 |
|------|------|
| `pcm_file` | 输出保存的 pcm 文件 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `AudioConfig *` | 音频配置实例指针 |


---


> 📌 置语音输出到系统默认扬声器
> **子模块**: 音频配置 | **编程语言**: C

**原型**:
```c
AudioConfig *audio_config_create_audio_output_from_default_speaker()
```

**描述**: 创建音频配置，使用系统默认的扬声器作为音频输出

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `AudioConfig *` | 音频配置实例指针 |


---

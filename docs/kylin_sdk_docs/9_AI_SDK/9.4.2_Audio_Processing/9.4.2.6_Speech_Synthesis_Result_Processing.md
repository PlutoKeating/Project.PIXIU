# 9.4.2.6 语音合成结果处理

添加头文件
`#include <coreai/speech/result.h>`

> 📌 语音合成结果回调函数
> **子模块**: 语音合成
编程语
言
C

**原型**:
```c
typedef void (*SpeechSynthesisResultCallback)(SpeechSynthesisResult *result, void
*user_data)
```

**描述**: 语音合成结果回调函数类型

**参数**:

| 参数 | 说明 |
|------|------|
| `result` | SpeechSynthesisResult类型的指针 |
| `user_data` | 用户数据 |


**返回值**: 无

---


> 📌 获取语音合成结果的状态
> **子模块**: 语音合成
编程语
言
C

**原型**:
```c
SpeechResultReason speech_synthesis_result_get_reason(SpeechSynthesisResult
*result)
```

**描述**: 获取语音合成结果的状态

**参数**:

| 参数 | 说明 |
|------|------|
| `result` | 语音合成结果的指针 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `SpeechResultReason` | 语音合成结果的状态 |


---


> 📌 获取语音合成的数据
> **子模块**: 语音合成
编程语
言
C

**原型**:
```c
const uint8_t *speech_synthesis_result_get_data(SpeechSynthesisResult *result, uint8_t
*data_length)
```

**描述**: 获取语音合成的数据

**参数**:

| 参数 | 说明 |
|------|------|
| `result` | 语音合成结果的指针 |
| `data_length` | 输出参数，音频数据的长度 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `const uint8_t*` | 语音合成的音频数据的指针 |


---


> 📌 获取语音合成结果的音频数据格式
> **子模块**: 语音合成 | **编程语言**: C

**原型**:
```c
int speech_synthesis_result_get_audio_format(SpeechSynthesisResult *result)
```

**描述**: 获取语音合成结果的音频数据格式

**参数**:

| 参数 | 说明 |
|------|------|
| `result` | 语音合成结果实例的指针 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 具体的音频数据格式 |


---


> 📌 获取语音合成结果的音频数据采样率
> **子模块**: 语音合成 | **编程语言**: C

**原型**:
```c
int speech_synthesis_result_get_audio_channel(SpeechSynthesis *result)
```

**描述**: 获取语音合成结果的音频数据通道数

**参数**:

| 参数 | 说明 |
|------|------|
| `result` | 语音合成结果的指针 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 具体的音频数据通道数 |


---


> 📌 获取语音合成结果的错误码
> **子模块**: 语音合成 | **编程语言**: C

**原型**:
```c
int speech_synthesis_result_get_error_code(SpeechSynthesisResult *result)
```

**描述**: 获取语音合成结果的错误码

**参数**:

| 参数 | 说明 |
|------|------|
| `result` | 语音合成结果的指针 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 具体的错误码 |


---


> 📌 获取语音合成结果的错误信息
> **子模块**: 语音合成 | **编程语言**: C

**原型**:
```c
const char *speech_synthesis_result_get_error_message(SpeechSynthesisResult *result)
```

**描述**: 获取语音合成结果的具体错误信息

**参数**:

| 参数 | 说明 |
|------|------|
| `result` | 语音合成结果的指针 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `const char*` | 具体的错误信息 |


---

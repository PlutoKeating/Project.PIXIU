# 9.4.2.1 语音识别

添加头文件
`#include <coreai/speech/recognizer.h>`

> 📌 创建语音识别会话
> **子模块**: 语音识别 | **编程语言**: C

**原型**:
```c
SpeechRecognitionSession *speech_recognizer_create_session()
```

**描述**: 创建语音识别会话

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `SpeechRecognitionSession*` | 语音识别会话指针 |


---


> 📌 初始化语音识别会话
> **子模块**: 语音识别 | **编程语言**: C

**原型**:
```c
int speech_recognizer_init_session(SpeechRecognitionSession *session)
```

**描述**: 初始化语音识别会话

**参数**:

| 参数 | 说明 |
|------|------|
| `session` | 语音识别会话的指针 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 返回初始化的结果，0表示成功，非0表示错误码 |


---


> 📌 销毁语音识别会话
> **子模块**: 语音识别 | **编程语言**: C

**原型**:
```c
void speech_recognizer_destroy_session(SpeechRecognitionSession **session)
```

**描述**: 销毁语音识别会话

**参数**:

| 参数 | 说明 |
|------|------|
| `session` | 语音识别会话的指针地址 |


**返回值**: 无

---


> 📌 设置语音识别结果回调函数
> **子模块**: 语音识别 | **编程语言**: C

**原型**:
```c
void speech_recognizer_result_set_callback(SpeechRecognitionSession *session,
SpeechRecognitionResultCallback callback, void *user_data)
```

**描述**: 设置语音识别结果的回调函数

**参数**:

| 参数 | 说明 |
|------|------|
| `session` | 语音识别会话的指针 |
| `callback` | SpeechRecognitionResultCallback类型的结果回调函数 |
| `user_data` | 调用者自定义的数据 |


**返回值**: 无

---


> 📌 设置音频配置信息
> **子模块**: 语音识别 | **编程语言**: C

**原型**:
```c
void speech_recognizer_set_audio_config(SpeechRecognitionSession *session,
AudioConfig *audio_config)
```

**描述**: 设置音频相关的配置

**参数**:

| 参数 | 说明 |
|------|------|
| `session` | 语音识别会话的指针 |
| `audio_config` | 语音相关的配置 |


**返回值**: 无

---


> 📌 设置模型配置信息
> **子模块**: 语音识别 | **编程语言**: C

**原型**:
```c
void speech_recognizer_set_model_config(SpeechRecognitionSession *session,
SpeechModelConfig *config)
```

**描述**: 设置模型配置信息

**参数**:

| 参数 | 说明 |
|------|------|
| `session` | 语音识别会话的指针 |
| `config` | 模型配置 |


**返回值**: 无

---


> 📌 开始异步流式语音识别
> **子模块**: 语音识别 | **编程语言**: C

**原型**:
```c
void speech_recognizer_start_continuous_recognition_async(SpeechRecognitionSession
*session)
```

**描述**: 开始流式异步语音识别

**参数**:

| 参数 | 说明 |
|------|------|
| `session` | 语音识别会话的指针 |


**返回值**: 无

---


> 📌 停止异步流式语音识别
> **子模块**: 语音识别 | **编程语言**: C

**原型**:
```c
void speech_recognizer_stop_continuous_recognition_async(SpeechRecognitionSession
*session)
```

**描述**: 停止异步流式语音识别

**参数**:

| 参数 | 说明 |
|------|------|
| `session` | 语音识别会话的指针 |


**返回值**: 无

---


> 📌 进行一次性语音识别
> **子模块**: 语音识别 | **编程语言**: C

**原型**:
```c
void speech_recognizer_recognize_once_async(SpeechRecognitionSession *session)
```

**描述**: 进行一次性语音识别

**参数**:

| 参数 | 说明 |
|------|------|
| `session` | 语音识别会话的指针 |


**返回值**: 无

---

# 9.5.2.2 图像生成配置

添加头文件
`#include <genai/vision/imageconfig.h>`

> 📌 图片大小
> **子模块**: 图像生成配置 | **编程语言**: C

**原型**:
```c
typedef struct _ImageSize { int width; int height; } ImageSize
```

**描述**: 图片大小的相关结构体
成员
width
图片的宽度
成员
height
图片的高度

**参数**: 无

**返回值**: 无

---


> 📌 创建图片配置实例
> **子模块**: 图像生成配置 | **编程语言**: C

**原型**:
```c
ImageConfig *image_config_create()
```

**描述**: 创建图片配置相关的结构体实例

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `图片配置结构体指针` | - |


---


> 📌 销毁图片配置实例
> **子模块**: 图像生成配置 | **编程语言**: C

**原型**:
```c
void image_config_destroy(ImageConfig **config)
```

**描述**: 销毁图片配置结构体实例

**参数**:

| 参数 | 说明 |
|------|------|
| `config` | 图片配置结构体指针的地址 |


**返回值**: 无

---


> 📌 设置生成的图片的数量
> **子模块**: 图像生成配置 | **编程语言**: C

**原型**:
```c
void image_config_set_generation_number(ImageConfig *config, int number)
```

**描述**: 设置要生成的图片数量

**参数**:

| 参数 | 说明 |
|------|------|
| `number` | 图片数量 |


**返回值**: 无

---


> 📌 设置生成的图片的风格
> **子模块**: 图像生成配置 | **编程语言**: C

**原型**:
```c
void image_config_set_style(ImageConfig *config, const char *style)
```

**描述**: 设置要生成的图片风格

**参数**:

| 参数 | 说明 |
|------|------|
| `style` | 图片风格 |


**返回值**: 无

---


> 📌 设置生成的图片的大小
> **子模块**: 图像生成配置 | **编程语言**: C

**原型**:
```c
void image_config_set_size(ImageConfig *config, ImageSize image_size)
```

**描述**: 设置要生成的图片大小

**参数**:

| 参数 | 说明 |
|------|------|
| `image_size` | 图片大小 |


**返回值**: 无

---


#### 9.3.2.3 图像生成结果

添加头文件
`#include <genai/vision/imageresult.h>`

> 📌 获取生成的图片的格式
> **子模块**: 图像生成结果 | **编程语言**: C

**原型**:
```c
ImageFormat image_result_get_format(VisionImageResult *image_result)
```

**描述**: 获取图片格式（jpg、png 等）

**参数**:

| 参数 | 说明 |
|------|------|
| `image_result` | 图片配结果结构体指针 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `ImageFormat` | 图片格式（jpg、png 等） |


---


> 📌 获取生成的图片的大小
> **子模块**: 图像生成结果 | **编程语言**: C

**原型**:
```c
ImageSize image_result_get_size(VisionImageResult *image_result)
```

**描述**: 获取图片尺寸

**参数**:

| 参数 | 说明 |
|------|------|
| `image_result` | 图片配结果结构体指针 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `ImageSize` | 图片尺寸结构体 |


---


> 📌 获取生成的图片的数量
> **子模块**: 图像生成结果 | **编程语言**: C

**原型**:
```c
int image_result_get_total(VisionImageResult *image_result)
```

**描述**: 获取生成图片总数量

**参数**:

| 参数 | 说明 |
|------|------|
| `image_result` | 图片配结果结构体指针 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 生成图片总数量 |


---


> 📌 获取当前生成图片的序号
> **子模块**: 图像生成结果 | **编程语言**: C

**原型**:
```c
int image_result_get_index(VisionImageResult *image_result)
```

**描述**: 获取生成图片序号

**参数**:

| 参数 | 说明 |
|------|------|
| `image_result` | 图片配结果结构体指针 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 当前图片的序号 |


---


> 📌 获取生成的图片的数据
> **子模块**: 图像生成结果
编程语
言
C

**原型**:
```c
const uint8_t *image_result_get_data(VisionImageResult *image_result, int
*data_length)
```

**描述**: 获取生成的图片数据

**参数**:

| 参数 | 说明 |
|------|------|
| `data_length` | 图片数据的长度 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `const uint8_t *` | 图片数据的首地址 |


---


> 📌 获取生成结果的错误码
> **子模块**: 图像生成结果 | **编程语言**: C

**原型**:
```c
int image_result_get_error_code(VisionImageResult *image_result)
```

**描述**: 获取图片尺寸获取生成图片结果中的错误码

**参数**:

| 参数 | 说明 |
|------|------|
| `image_result` | 图片配结果结构体指针 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 具体的错误码 |


---


> 📌 获取生成结果的错误信息
> **子模块**: 图像生成结果 | **编程语言**: C

**原型**:
```c
const char *image_result_get_error_message(VisionImageResult *image_result)
```

**描述**: 获取图片尺寸获取生成图片结果中的错误信息

**参数**:

| 参数 | 说明 |
|------|------|
| `image_result` | 图片配结果结构体指针 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `const char *` | 具体的错误信息 |


---

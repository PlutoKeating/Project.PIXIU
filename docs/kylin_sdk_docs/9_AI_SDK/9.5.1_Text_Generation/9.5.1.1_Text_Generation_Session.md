# 9.5.1.1 文本生成会话

添加头文件
`#include <genai/text/chat.h>`

> 📌 创建文本生成会话
> **子模块**: 文本生成 | **编程语言**: C

**原型**:
```c
GenAiTextSession *genai_text_create_session()
```

**描述**: 创建文本生成会话

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `GenAiTextSession*` | 文本生成会话指针 |


---


> 📌 初始化文本生成会话
> **子模块**: 文本生成 | **编程语言**: C

**原型**:
```c
int genai_text_init_session(GenAiTextSession *session)
```

**描述**: 初始化文本生成会话

**参数**:

| 参数 | 说明 |
|------|------|
| `session` | 文本生成会话的指针 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 成功时返回 0，否则返回具体的错误码 |


---


> 📌 销毁文本生成会话
> **子模块**: 文本生成 | **编程语言**: C

**原型**:
```c
void genai_text_destroy_session(GenAiTextSession **session)
```

**描述**: 销毁文本生成会话

**参数**:

| 参数 | 说明 |
|------|------|
| `session` | 文生生成会话指针的地址 |


**返回值**: 无

---


> 📌 设置文本生成的结果回调函数
> **子模块**: 文本生成
编程语
言
C

**原型**:
```c
void genai_text_result_set_callback(GenAiTextSession *session, ChatResultCallback
callback, void *user_data)
```

**描述**: 设置文本生成的结果回调函数

**参数**:

| 参数 | 说明 |
|------|------|
| `session` | 文本生成会话的指针 |
| `callback` | 文本生成结果的回函函数 |
| `user_data` | 用户数据 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `GenAiTextSession*` | 文本生成会话指针 |


---


> 📌 设置模型的配置
> **子模块**: 文本生成 | **编程语言**: C

**原型**:
```c
void genai_text_set_model_config(GenAiTextSession *session, ChatModelConfig *config)
```

**描述**: 设置模型的配置

**参数**:

| 参数 | 说明 |
|------|------|
| `session` | 文本生成会话的指针 |
| `config` | 模型配置的指针 |


**返回值**: 无

---


> 📌 进行文本生成
> **子模块**: 文本生成
编程语
言
C

**原型**:
```c
void genai_text_generate_content_async(GenAiTextSession *session, const char
*prompt)
```

**描述**: 进行文本生成

**参数**:

| 参数 | 说明 |
|------|------|
| `session` | 文本生成会话的指针 |
| `prompt` | 输入的提示词文本 |


**返回值**: 无

---


> 📌 进行文本对话，支持缓存历史消息
> **子模块**: 文本生成 | **编程语言**: C

**原型**:
```c
void genai_text_chat_async(GenAiTextSession *session, const char *question)
```

**描述**: 进行文本对话，支持缓存历史消息

**参数**:

| 参数 | 说明 |
|------|------|
| `session` | 文本生成会话的指针 |
| `question` | 用户输入的具体问题 |


**返回值**: 无

---


> 📌 进行文本对话，需要传入历史消息
> **子模块**: 文本生成 | **编程语言**: C

**原型**:
```c
void genai_text_chat_with_history_messages_async(GenAiTextSession *session,
ChatMessage *history_messages)
```

**描述**: 进行文本对话，需要传入历史消息

**参数**:

| 参数 | 说明 |
|------|------|
| `session` | 文本生成会话的指针 |
| `history_messages` | 历史消息 |


**返回值**: 无

---


> 📌 设置系统提示词
> **子模块**: 文本生成
编程语
言
C

**原型**:
```c
void genai_text_set_chat_system_prompt(GenAiTextSession *session, const char
*prompt)
```

**描述**: 设置系统提示词

**参数**:

| 参数 | 说明 |
|------|------|
| `session` | 文本生成会话的指针 |
| `prompt` | 具体的系统提示词 |


**返回值**: 无

---


> 📌 设置系统提示词 id，使用内置提示词
> **子模块**: 文本生成
编程语
言
C

**原型**:
```c
void genai_text_set_chat_system_prompt_id(GenAiTextSession *session, PromptId
prompt_id)
```

**描述**: 设置系统提示词 id，使用内置提示词

**参数**:

| 参数 | 说明 |
|------|------|
| `session` | 文本生成会话的指针 |
| `prompt_id` | 提示词 id |


**返回值**: 无

---


> 📌 停止对话
> **子模块**: 文本生成 | **编程语言**: C

**原型**:
```c
void genai_text_stop_chat(GenaiTextSession *session)
```

**描述**: 停止对话

**参数**:

| 参数 | 说明 |
|------|------|
| `session` | 文本生成会话的指针 |


**返回值**: 无

---

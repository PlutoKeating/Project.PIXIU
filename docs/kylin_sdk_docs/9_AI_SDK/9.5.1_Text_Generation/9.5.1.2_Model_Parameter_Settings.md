# 9.5.1.2 模型参数设置

添加头文件
`#include <genai/text/config.h>`

> 📌 创建模型配置实例
> **子模块**: 模型参数配置 | **编程语言**: C

**原型**:
```c
ChatModelConfig *chat_model_config_create()
```

**描述**: 创建模型配置实例

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `ChatModelConfig *` | 模型配置实例指针 |


---


> 📌 销毁模型配置实例
> **子模块**: 模型参数配置 | **编程语言**: C

**原型**:
```c
void chat_model_config_destroy(ChatModelConfig **config)
```

**描述**: 销毁模型配置实例

**参数**:

| 参数 | 说明 |
|------|------|
| `config` | 模型配置的二级指针 |


**返回值**: 无

---


> 📌 设置模型的名称
> **子模块**: 模型参数配置 | **编程语言**: C

**原型**:
```c
void chat_model_config_set_name(ChatModelConfig *config, const char *model_name)
```

**描述**: 设置模型的名称

**参数**:

| 参数 | 说明 |
|------|------|
| `model_name` | 模型的名称，"设置"->"AI模块管理"中显示的名称 |


**返回值**: 无

---


> 📌 设置模型的 top_k 参数
> **子模块**: 模型参数配置 | **编程语言**: C

**原型**:
```c
void chat_model_config_set_top_k(ChatModelConfig *config, double top_k)
```

**描述**: 设置模型的 top_k 参数

**参数**:

| 参数 | 说明 |
|------|------|
| `top_k` | 参数的数值 |


**返回值**: 无

---


> 📌 设置模型的部署类型
> **子模块**: 模型参数配置
编程语
言
C

**原型**:
```c
void chat_model_config_set_deploy_type(ChatModelConfig *config, ModelDeployType
type)
```

**描述**: 设置模型的部署类型

**参数**:

| 参数 | 说明 |
|------|------|
| `type` | 模型的部署类型。如果未指定部署类型，则会根据“设置->AI 模块管理”中配置的优先 |
| `级进行选择` | - |


**返回值**: 无

---


> 📌 设置模型的名称
> **子模块**: 模型参数配置
编程语
言
C

**原型**:
```c
void chat_model_config_set_name(ChatModelConfig *config, const char *name)
```

**描述**: 设置模型的名称

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 模型的名称。如果同时指定了部署类型和名称，会优先匹配名称，如果无法匹配，则匹 |
| `配对应的部署类型` | - |


**返回值**: 无

---

# 9.4.1.2 设置配置信息

添加头文件
`#include <coreai/vision/config.h>`

> 📌 设置模型配置信息
> **子模块**: 文字识别 | **编程语言**: C

**原型**:
```c
TextRecognitionModelConfig *text_recognition_model_config_create()
```

**描述**: 创建模型配置实例

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `TextRecognitionModelConfig*` | 模型配置实例指针 |


---


> 📌 销毁模型配置
> **子模块**: 文字识别 | **编程语言**: C

**原型**:
```c
void text_recognition_model_config_destroy(TextRecognitionModelConfig **config)
```

**描述**: 销毁模型配置实例

**参数**:

| 参数 | 说明 |
|------|------|
| `config` | 模型配置实例指针的地址 |


**返回值**: 无

---


> 📌 设置模型名称
> **子模块**: 文字识别 | **编程语言**: C

**原型**:
```c
void text_recognition_model_config_set_name(TextRecognitionModelConfig *config, const
char *name)
```

**描述**: 设置要使用的模型名称

**参数**:

| 参数 | 说明 |
|------|------|
| `config` | 模型配置实例的指针 |
| `name` | 设置的模型名字 |


**返回值**: 无

---


> 📌 设置模型部署类型
> **子模块**: 文字识别 | **编程语言**: C

**原型**:
```c
void text_recognition_model_config_set_deploy_type(TextRecognitionModelConfig *config,
ModelDeployType type)
```

**描述**: 设置使用的模型的部署类型

**参数**:

| 参数 | 说明 |
|------|------|
| `config` | 模型配置实例的指针 |
| `type` | 指定的模型部署类型 |


**返回值**: 无

---

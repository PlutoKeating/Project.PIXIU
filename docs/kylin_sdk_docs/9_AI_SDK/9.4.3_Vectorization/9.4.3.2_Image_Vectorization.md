# 9.4.3.2 图像向量化

添加头文件
`#include <coreai/embedding/embedding.h>`

> 📌 创建会话
> **子模块**: 图像向量化 | **编程语言**: C

**原型**:
```c
ImageEmbeddingSession *image_embedding_create_session()
```

**描述**: 创建图像向量化会话

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `ImageEmbeddingSession *` | 图像向量化会话指针 |


---


> 📌 初始化会话
> **子模块**: 图像向量化 | **编程语言**: C

**原型**:
```c
int image_embedding_init_session(ImageEmbeddingSession *session)
```

**描述**: 初始化会话

**参数**:

| 参数 | 说明 |
|------|------|
| `session` | 图像向量化会话指针 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 成功时返回 0，否则返回具体的错误码 |


---


> 📌 销毁会话
> **子模块**: 图像向量化 | **编程语言**: C

**原型**:
```c
void image_embedding_destroy_session(ImageEmbeddingSession **session)
```

**描述**: 销毁会话

**参数**:

| 参数 | 说明 |
|------|------|
| `session` | 图像向量化会话指针的地址 |


**返回值**: 无

---


> 📌 获取图像向量化模型信息
> **子模块**: 图像向量化
编程语
言
C

**原型**:
```c
bool image_embedding_get_model_info(ImageEmbeddingSession *session, char
**model_info)
```

**描述**: 获取图像向量化模型信息，需要调用embedding_model_info_destroy销毁资源

**参数**:

| 参数 | 说明 |
|------|------|
| `session` | 图像向量化会话指针 |
| `model_info` | 模型信息指针的地址 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `bool` | 成功时返回true，否则返回false 使用图像向量化模型向量化文本（异步） 子 模 块 图像向量化 编 程 语 言 C 原 型 |
| `void text_embedding_by_image_model_async(ImageEmbeddingSession *session, const char` | *text, ImageEmbeddingResultCallback callback, void *callback_user_data) 描 述 通过异步的方式图像向量化模型向量化文本 参 数 session 图像向量化会话指针 text 文本 callback 结果回调函数 callback_user_data 用户数据 返 回 值 |


---


> 📌 通过传入图片路径向量化图片（同步）
> **子模块**: 图像向量化 | **编程语言**: C

**原型**:
```c
bool image_embedding_by_image_file(ImageEmbeddingSession *session, const char
*image_file, EmbeddingResult **result)
```

**描述**: 通过同步的方式向量化图片，需要调用embedding_result_destroy销毁资源

**参数**:

| 参数 | 说明 |
|------|------|
| `session` | 图像向量化会话指针 |
| `image_file` | 图片路径 |
| `result` | EmbeddingResult类型指针的地址 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `bool` | 成功时返回true，否则返回false 通过传入图片路径向量化图片（异步） 子 模 块 图像向量化 编 程 语 言 C 原 型 |
| `void image_embedding_from_by_file_async(ImageEmbeddingSession *session, const char` | *file_path, ImageEmbeddingResultCallback callback, void *callback_user_data) 描 述 通过异步的方式向量化图片 参 数 session 图像向量化会话指针 image_file 图片路径 callback 结果回调函数 callback_user_data 用户数据 返 回 值 通过传入base64图片数据向量化图片（同步） 子 模 块 图像向量化 编 程 语 言 C 原 型 |
| `bool image_embedding_by_base64_image_data(ImageEmbeddingSession *session, const unsigned char *image_data, unsigned int image_data_length, EmbeddingResult **result)` | 描 述 通过同步的方式向量化图片，需要调用embedding_result_destroy销毁资源 参 数 session 图像向量化会话指针 image_data base64图片数据 image_data_length base64图片数据的长度 result EmbeddingResult类型指针的地址 返 回 值 |
| `bool` | 成功时返回true，否则返回false 通过传入base64图片数据向量化图片（异步） 子 模 块 图像向量化 编 程 语 言 C 原 型 |
| `void image_embedding_by_base64_image_data_async(ImageEmbeddingSession *session, const unsigned char *image_data, unsigned int image_data_length,` | ImageEmbeddingResultCallback callback, void *callback_user_data) 描 述 通过异步的方式向量化图片 参 数 session 图像向量化会话指针 image_data base64图片数据 image_data_length base64图片数据的长度 callback 结果回调函数 callback_user_data 用户数据 返 回 值 |


---

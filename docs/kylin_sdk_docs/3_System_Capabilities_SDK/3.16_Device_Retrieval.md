# 3.16 设备检索

提供与驱动通讯控制设备，安装新设备，控制CD-ROM与DVD驱动器功能的接口
安装命令

```bash
$ sudo apt-get install libkysdk-devicemanager libkysdk-devicemanager-dev
```

构建示例
1. pro文件构建示例
CONFIG += link_pkgconfig
PKGCONFIG += kysdk-devicemanager
2. CMakeLists文件构建示例
`cmake_minimum_required(VERSION 3.5)`
find_package(PkgConfig REQUIRED)
pkg_check_modules(KYSDK kysdk-devicemanager)
target_include_directories(demo PRIVATE ${KYSDK_INCLUDE_DIRS})
target_link_directories(demo PRIVATE ${KYSDK_LIBRARY_DIRS})
target_link_libraries(demo PRIVATE ${KYSDK_LIBRARIES})

#### 3.16.1 设备检索

头文件路径
` #include "kysdk/kysdk-system/libkydevicemanager.h"`
so库路径

```
/usr/lib/*/libkydevicemanager.so
```

'*'代表不同架构的架构目录名称，例如x86_64-linux-gnu
子模块信息
数据结构
```c
typedef struct _kdk_device_number
{
    char *device_subsystem; // 设备的类型
    char *device_devtype;   // 设备在其子系统中的具体角色
    char *device_syspath;   // 设备在内核空间中的路径
    char *device_sysname;   // 内核分配的设备名称
    int device_sysnum;      // 设备的编号
} kdk_device_number;
typedef enum _kdk_device_type
{
    DEVICE_DEVTYPE_UNKNOWN,      // 未知设备类型
    DEVICE_DEVTYPE_DISK,              // 硬盘
    DEVICE_DEVTYPE_PARTITION,         // 分区
    DEVICE_DEVTYPE_NET,               // 网络设备
    DEVICE_DEVTYPE_USB,               // USB 设备
    DEVICE_DEVTYPE_SERIAL,            // 串口设备
    DEVICE_DEVTYPE_SCSI,              // 串口设备
    DEVICE_DEVTYPE_INPUT,             // 输入设备
    DEVICE_DEVTYPE_PCI_CARD,          // PCI 卡
    DEVICE_DEVTYPE_SOUND_CARD,        // 声卡
    DEVICE_DEVTYPE_GRAPHIC_CARD,      // 显卡
    DEVICE_DEVTYPE_I2C_SENSOR,        // I²C 传感器
    DEVICE_DEVTYPE_SPI_SENSOR,        // SPI 传感器
    DEVICE_DEVTYPE_BATTERY,           // 电池
    DEVICE_DEVTYPE_BLUETOOTH_ADAPTER, // 蓝牙适配器
    DEVICE_DEVTYPE_HID_DEVICE,        // 人机接口设备
    DEVICE_DEVTYPE_EMBEDDED,          // 嵌入式设备
    DEVICE_DEVTYPE_THERMAL_SENSOR,    // 温度传感器
    DEVICE_DEVTYPE_ACPI_DEVICE,       // ACPI 设备
    DEVICE_DEVTYPE_MISC_DEVICE        // ACPI 设备
} kdk_device_type;
```

> 📌 获取系统中所有已连接的设备(自3.0.1.0版本启用)
> **子模块**: 设备检索 | **接口类型**: C

**原型**:
```c
extern char **kdk_dm_get_device_list();
```

**描述**: 获取系统中所有已连接的设备

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char**` | 已连接设备的名称列表 |
| `NULL` | 失败 |


**备注**: 无

---


> 📌 检查指定设备是否支持访问(自3.0.1.0版本启用)
> **子模块**: 设备检索 | **接口类型**: C

**原型**:
```c
extern int kdk_dm_check_device_available(char *devicename);
```

**描述**: 检查指定设备是否支持访问

**参数**:

| 参数 | 说明 |
|------|------|
| `devicename` | 设备名称，例如 /dev/sda |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | 支持访问 |
| `-1` | - |
| `不支持访问` | - |


**备注**: 无

---


> 📌 获取指定设备的设备信息(自3.0.1.0版本启用)
> **子模块**: 设备检索 | **接口类型**: C

**原型**:
```c
extern kdk_device_number *kdk_dm_get_device_number(char *devicepath);
```

**描述**: 获取指定设备的设备信息

**参数**:

| 参数 | 说明 |
|------|------|
| `devicepath` | kdk_dm_get_device_list 获取的设备名称 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `kdk_device_number*` | 设备类型、设备编号及相关的结构体 |
| `NULL` | 失败 |


**备注**: 无

---


> 📌 获取指定设备的热插拔支持情况(自3.0.1.0版本启用)
> **子模块**: 设备检索 | **接口类型**: C

**原型**:
```c
extern int kdk_dm_get_device_hotplug_info(char *devicepath);
```

**描述**: 获取指定设备的热插拔支持情况

**参数**:

| 参数 | 说明 |
|------|------|
| `devicepath` | kdk_dm_get_device_list 获取的设备名称 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `1` | 支持 |
| `0` | - |
| `未知` | - |
| `-1` | - |
| `不支持` | - |


**备注**: 无

---


> 📌 获取系统上已连接的，指定类型的，设备列表(自3.0.1.0版本启用)
> **子模块**: 设备检索 | **接口类型**: C

**原型**:
```c
extern char **kdk_dm_get_device_list_by_type(kdk_device_type type);
```

**描述**: 获取系统上已连接的，指定类型的，设备列表

**参数**:

| 参数 | 说明 |
|------|------|
| `type` | 设备类型 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char**` | 设备的名称列表 |
| `NULL` | 失败 |


**备注**: 无

---


> 📌 获取系统上已连接的，指定厂商的，设备列表(自3.0.1.0版本启用)
> **子模块**: 设备检索 | **接口类型**: C

**原型**:
```c
extern char **kdk_dm_get_device_list_by_vendor(char *vendor);
```

**描述**: 获取系统上已连接的，指定厂商的，设备列表

**参数**:

| 参数 | 说明 |
|------|------|
| `vendor` | 设备厂商 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char**` | 设备的名称列表 |
| `NULL` | 失败 |


**备注**: 无

---


> 📌 刷新系统上连接的设备列表(自3.0.1.0版本启用)
> **子模块**: 设备检索 | **接口类型**: C

**原型**:
```c
extern void kdk_dm_refresh_device_list();
```

**描述**: 刷新系统上连接的设备列表

**参数**: 无

**返回值**: 无

**备注**: 无

---

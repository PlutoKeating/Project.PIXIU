# 3.1.4 获取包列表信息


**安装命令：**

sudo apt-get install libkysdk-package libkysdk-package-dev

**构建示例：**

（1）.pro 文件构建项目
`qt 项目.pro 文件中增加：`
CONFIG += link_pkgconfig
PKGCONFIG += kysdk-package
（2）CMakeLists.txt 构建项目
`cmake_minimum_required(VERSION 3.5)`
find_package(PkgConfig REQUIRED)
pkg_check_modules(KYSDKPACKAGE kysdk-package)
target_include_directories(demo PRIVATE ${KYSDKPACKAGE_INCLUDE_DIRS})
target_link_directories(demo PRIVATE ${KYSDKPACKAGE_LIBRARY_DIRS})
target_link_libraries(demo PRIVATE ${KYSDKPACKAGE_LIBRARIES})
3.1.4.1获取包列表信息
封装 C 接口获取系统中所有的包信息

**头文件路径：**

` #include "kysdk/kysdk-system/libkypackages.h"`

**so库路径：**


```
/usr/lib/aarch64-linux-gnu/libkypackage.so
```


**子模块信息：**

数据结构
/**
```c
 * @brief 安装指定包回调函数
*/
typedef void (*InstallCallBack)(int progress, int state, char *loginfo);
```
/**
```c
 * @brief 卸载指定包回调函数
*/
typedef void (*RemoveCallBack)(int progress, int state, char *loginfo);
//定义回调函数
typedef void (*FinishedCallBack)(int state, const char *standard_output, const char *standard_err);
typedef void (*UpdateCallBack)(const char *output);
typedef struct _kdk_cmd_node
{
```
char cmd[512];                      // cmd
FinishedCallBack finished;          // 回调函数-结束
UpdateCallBack update;              // 回调函数-更新
```c
    int stat;                           // 1为debian安装，0为kare安装
}kdk_cmd_node;
typedef enum _KDK_PACKAGE_LIST_MODE
{
```
KDK_PACKAGE_AUTO = 0,
KDK_PACKAGE_KARE,
KDK_PACKAGE_KAIMING,
KDK_PACKAGE_DEBIAN
`} KDK_PACKAGE_LIST_MODE;`

> 📌 获取系统中所有包列表(自1.2.0版本启用, 自2.5.1.0版本兼容kare和kaiming)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern kdk_package_list* kdk_package_get_packagelist()
```

**描述**: 获取系统中所有包列表(所有状态)

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `kdk_package_list*` | 包描述结构体列表；返回的结构体需要由 kdk_package_free_packagelist()回收。 |
| `NULL` | 获取失败 |


**备注**: 成员：name(char *)，描述：包名
成员：version(char *)，描述：版本号
成员：section(char *)，描述：包类型
成员：status(char *)，描述：状态
成员：size_kb(char *)，描述：包大小

---


> 📌 获取系统中指定包的版本号(自1.2.0版本启用, 自2.5.1.0版本兼容kare和kaiming)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_package_get_version(const char *name)
```

**描述**: 获取系统中指定包的版本号

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 软件包名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回版本号，由alloc生成，需要被free |
| `NULL` | - |
| `获取失败或包不存在` | - |


**备注**: 无

---


> 📌 检测指定包名的软件包是否正确安装(自1.2.0版本启用, 自2.5.1.0版本兼容kare和kaiming)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern int kdk_package_is_installed(const char *name, const char *version);
```

**描述**: 检测指定包名的软件包是否正确安装

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 软件包名 |
| `version` | 版本号，大部分情况下为NULL；输入版本号可检测此版本的指定包是否安装 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | 成功安装 errno 失败返回错误码 |


**备注**: 无

---


> 📌 回收由kdk_package_get_packagelist()返回的结构体(自1.2.0版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern void kdk_package_free_packagelist(kdk_package_list *list);
```

**描述**: 回收由kdk_package_get_packagelist()返回的结构体

**参数**:

| 参数 | 说明 |
|------|------|
| `list` | 由kdk_package_get_packagelist()返回的结构体 |


**返回值**: 无

**备注**: 无

---


> 📌 获取指定应用的描述(自2.4.1.0版本启用, 自2.5.1.0版本兼容kare)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern char *kdk_package_get_description(const char *name);
```

**描述**: 获取指定应用的描述

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 应用名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 应用的描述信息 |
| `NULL` | 获取失败 |


**备注**: 无

---


> 📌 获取指定应用的安装目录路径(自2.4.1.0版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern char **kdk_package_get_code_path(const char *name);
```

**描述**: 获取指定应用的安装目录路径

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 应用名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char**` | 应用的安装目录路径列表 |
| `NULL` | 获取失败 |


**备注**: 无

---


> 📌 获取指定应用的安装文件总数(自2.4.1.0版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern int kdk_package_get_file_count(const char *name);
```

**描述**: 应用的安装文件总数

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 应用名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 应用的安装文件总数 |
| `0` | 获取失败 |


**备注**: 无

---


> 📌 判断指定应用是否可以被移除(自2.4.1.0版本启用)(未启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern bool kdk_package_is_removable(const char *pkg_name);
```

**描述**: 判断指定应用是否可以被移除

**参数**:

| 参数 | 说明 |
|------|------|
| `pkg_name` | 应用名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 可卸载 false 不可卸载 |


**备注**: 无

---


> 📌 判断安装deb包是否有足够的磁盘空间(自2.4.1.0版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern int kdk_package_verify_disk_space(const char *path);
```

**描述**: 判断安装deb包是否有足够的磁盘空间

**参数**:

| 参数 | 说明 |
|------|------|
| `path` | deb包路径 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `1` | true |
| `0` | - |
| `false` | - |


**备注**: 无

---


> 📌 获取默认浏览器名称(自2.4.1.0版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_package_get_default_browser(void);
```

**描述**: 获取默认浏览器名称

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回默认浏览器名称，返回的字符串需要被 free 释放 |
| `NULL` | 获取失败 |


**备注**: 无

---


> 📌 获取默认图片查看器名称(自2.4.1.0版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_package_get_default_image_viewer(void);
```

**描述**: 获取默认图片查看器名称

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回默认图片查看器名称，返回的字符串需要被 free 释放 |
| `NULL` | 获取失败 |


**备注**: 无

---


> 📌 获取默认音频播放器名称(自2.4.1.0版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_package_get_default_audio_player(void);
```

**描述**: 获取默认音频播放器名称

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回默认音频播放器名称，返回的字符串需要被 free 释放 |
| `NULL` | 获取失败 |


**备注**: 无

---


> 📌 获取默认视频播放器名称(自2.4.1.0版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_package_get_default_video_player(void);
```

**描述**: 获取默认视频播放器名称

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回默认视频播放器名称，返回的字符串需要被 free 释放 |
| `NULL` | 获取失败 |


**备注**: 无

---


> 📌 获取默认PDF文档查看器名称(自2.4.1.0版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_package_get_default_pdf_viewer(void);
```

**描述**: 获取默认PDF文档查看器名称

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回默认PDF文档查看器名称，返回的字符串需要被 free 释放 |
| `NULL` | 获取失败 |


**备注**: 无

---


> 📌 获取默认WORD文档查看器名称(自2.4.1.0版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_package_get_default_word_viewer(void);
```

**描述**: 获取默认WORD文档查看器名称

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回默认WORD文档查看器名称，返回的字符串需要被 free 释放 |
| `NULL` | 获取失败 |


**备注**: 无

---


> 📌 获取默认EXCEL文档查看器名称(自2.4.1.0版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_package_get_default_excel_viewer(void);
```

**描述**: 获取默认EXCEL文档查看器名称

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回默认EXCEL文档查看器名称，返回的字符串需要被 free 释放 |
| `NULL` | 获取失败 |


**备注**: 无

---


> 📌 获取默认PPT文档查看器名称(自2.4.1.0版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_package_get_default_ppt_viewer(void);
```

**描述**: 获取默认PPT文档查看器名称

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回默认PPT文档查看器名称，返回的字符串需要被 free 释放 |
| `NULL` | 获取失败 |


**备注**: 无

---


> 📌 获取开始菜单中的所有应用信息(自2.4.1.0版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern kdk_app_info* kdk_package_get_startmenu_list(void);
```

**描述**: 获取开始菜单中的所有应用信息(内容读取自desktop文件)

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `kdk_app_info*` | 成功返回开始菜单中的所有应用信息，由kdk_package_free_startmenu_list 释放 |
| `NULL` | 获取失败 |


**备注**: typedef struct _kdk_app_t
{
char *name; // 程序名称
char *version; // 版本号
char *company; // 所属公司
char *cmd; // 可执行文件路径
char *param; // 启动参数
char *icon; // 图标信息
} kdk_app_t;
typedef struct _kdk_app_info
{
unsigned int nums; //list的成员个数
kdk_app_t **list; //应用信息列表
} kdk_app_info;

---


> 📌 释放kdk_package_get_startmenu_list的返回(自2.4.1.0版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern void kdk_package_free_startmenu_list(kdk_app_info *list);
```

**描述**: 释放kdk_package_get_startmenu_list的返回

**参数**:

| 参数 | 说明 |
|------|------|
| `list` | kdk_package_get_startmenu_list返回的指针 |


**返回值**: 无

**备注**: 无

---


> 📌 判断指定包的安装方式(自2.5.1.0版本启用)
> **子模块**: 判断指定包的安装方式 | **接口类型**: C

**原型**:
```c
InstallMethod kdk_package_get_installation_method(const char *name, const char
*version)
```

**描述**: 判断指定包的安装方式

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 包名 |
| `version` | 版本号 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `InstallMethod` | 安装类型枚举值 |


**备注**: typedef enum _InstallMethod
{
NONE_METHOD,
DPKG_METHOD,
KM_METHOD,
KARE_METHOD,
KMRE_METHOD,
KWRE_METHOD
} InstallMethod;

---


> 📌 获取所有应用信息(自2.5.1.0版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern kdk_app_info* kdk_package_get_application_list(void)
```

**描述**: 获取所有应用信息(内容读取自desktop文件)

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `kdk_app_info*` | 成功返回所有应用信息，由kdk_package_free_app_info释放 |
| `NULL` | 获取失败 |


**备注**: typedef struct _kdk_app_t
{
char *name; // 程序名称
char *version; // 版本号
char *company; // 所属公司
char *cmd; // 可执行文件路径
char *param; // 启动参数
char *icon; // 图标信息
} kdk_app_t;
typedef struct _kdk_app_info
{
unsigned int nums; //list的成员个数
kdk_app_t **list; //应用信息列表
} kdk_app_info;

---


> 📌 释放kdk_app_info链表(自2.5.1.0版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern void kdk_package_free_app_info(kdk_app_info *list);
```

**描述**: 释放kdk_app_info链表

**参数**:

| 参数 | 说明 |
|------|------|
| `list` | kdk_app_info链表 |


**返回值**: 无

**备注**: 无

---


> 📌 离线安装指定的软件包(自2.5.1.2版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern int kdk_package_install_package_offline(const char *uri, int list_mode,
InstallCallBack call_back);
```

**描述**: 离线安装指定的软件包

**参数**:

| 参数 | 说明 |
|------|------|
| `uri` | 软件包路径 |
| `list_mode` | 指定软件包安装方式，当不确定具体安装途径时，传0，则按自动推导安装途径安 |
| `装` | call_back |
| `回调函数，用于获取软件包安装进度和中间状态` | - |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 成功返回1，失败返回0 |


**备注**: 无

---


> 📌 在线安装指定的软件包(自2.5.1.2版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern int kdk_package_install_package_online(const char *pkg_name, int list_mode,
InstallCallBack call_back);
```

**描述**: 在线安装指定的软件包

**参数**:

| 参数 | 说明 |
|------|------|
| `pkg_name` | 软件包名 |
| `list_mode` | 指定软件包安装方式，当不确定具体安装途径时，传0，则按自动推导安装途径 |
| `安装` | call_back |
| `回调函数，用于获取软件包安装进度和中间状态` | - |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 成功返回1，失败返回0 |


**备注**: 无

---


> 📌 通用安装指定的软件包(自2.5.1.2版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern int kdk_package_install_package(const char *uri, int list_mode, InstallCallBack
call_back);
```

**描述**: 通用安装指定的软件包

**参数**:

| 参数 | 说明 |
|------|------|
| `pkg_name` | 传入软件包名则进行在线安装，传入本地软件包路径则进行离线安装 |
| `list_mode` | 指定软件包安装方式，当不确定具体安装途径时，传0，则按自动推导安装途径 |
| `安装` | call_back |
| `回调函数，用于获取软件包安装进度和中间状态` | - |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 成功返回1，失败返回0 |


**备注**: 无

---


> 📌 注销获取软件包安装进度和中间状态的回调函数(自2.5.1.2版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern void kdk_package_unregister_install_package_handle();
```

**描述**: 注销获取软件包安装进度和中间状态的回调函数

**参数**: 无

**返回值**: 无

**备注**: 无

---


> 📌 删除指定的包(自2.5.1.2版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern int kdk_package_remove_package(const char *pkg_name, RemoveCallBack
call_back);
```

**描述**: 删除指定的包

**参数**:

| 参数 | 说明 |
|------|------|
| `pkg_name` | 包名 |
| `call_back` | 回调函数，用于获取软件包卸载进度和中间状态 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 成功返回1，失败返回0 |


**备注**: 无

---


> 📌 注销获取软件包卸载进度和中间状态的回调函数(自2.5.1.2版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern void kdk_package_unregister_remove_package_handle();
```

**描述**: 注销获取软件包卸载进度和中间状态的回调函数

**参数**: 无

**返回值**: 无

**备注**: 无

---


> 📌 获取软件包名(自2.5.1.2版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_package_get_name(char *desktop);
```

**描述**: 获取软件包名

**参数**:

| 参数 | 说明 |
|------|------|
| `desktop` | desktop文件路径 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char *` | 软件包名 |
| `NULL` | 失败 |


**备注**: 无

---


> 📌 判断是否可卸载(自2.5.1.2版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern bool kdk_package_is_removable_by_desktop(const char *desktop);
```

**描述**: 判断是否可卸载

**参数**:

| 参数 | 说明 |
|------|------|
| `desktop` | desktop文件路径 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `true` | 可卸载 false 不可卸载 |


**备注**: 无

---


> 📌 初始化(自2.5.1.2版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern kdk_cmd_node* kdk_package_cmd_init(const char *cmd);
```

**描述**: 初始化

**参数**:

| 参数 | 说明 |
|------|------|
| `cmd` | 命令 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `kdk_cmd_node` | 结构体 |
| `NULL` | 失败 |


**备注**: 无

---


> 📌 执行命令（异步执行，不阻塞，通过回调函数finished知晓是否同步完成）(自2.5.1.2版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern int kdk_package_launch_cmd(kdk_cmd_node* node);
```

**描述**: 执行命令（异步执行，不阻塞，通过回调函数finished知晓是否同步完成）

**参数**:

| 参数 | 说明 |
|------|------|
| `kdk_cmd_node` | 结构体 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `0` | - |
| `成功` | - |
| `-1` | 失败 |


**备注**: 无

---


> 📌 释放kdk_cmd_node 结构体(自2.5.1.2版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern void kdk_package_free_cmd(kdk_cmd_node* node);
```

**描述**: 释放kdk_cmd_node 结构体

**参数**:

| 参数 | 说明 |
|------|------|
| `kdk_cmd_node` | 结构体 |


**返回值**: 无

**备注**: 无

---


> 📌 关闭进程(自2.5.1.2版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern void kdk_package_cmd_close(kdk_cmd_node* node);
```

**描述**: 关闭进程

**参数**:

| 参数 | 说明 |
|------|------|
| `kdk_cmd_node` | 结构体 |


**返回值**: 无

**备注**: 无

---


> 📌 获取安装文件路径(自3.0.1.0版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern char **kdk_package_get_standard_path(const char *name);
```

**描述**: 获取安装文件路径

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 路径名称字符串，如“KYLIN_USER_GUIDE_FILE_PATH” |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char **` | 成功返回配置文件中的路径列表，需要释放 |
| `NULL` | 失败 |


**备注**: 无

---


> 📌 获取安装文件路径名列表(自3.0.1.0版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern char **kdk_package_get_standard_path_lists();
```

**描述**: 获取安装文件路径名列表

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char **` | 成功返回配置文件中的路径名列表，需要释放 |
| `NULL` | 失败 |


**备注**: 无

---


> 📌 获取软件包的已安装文件接口(自3.0.1.0版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern char **kdk_package_list_files(const char *pkg_name);
```

**描述**: 获取软件包的已安装文件接口

**参数**:

| 参数 | 说明 |
|------|------|
| `pkg_name` | 待查询的软件包名，如“ukui-control-center” |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char **` | 成功返回软件包的资源文件，需要释放 |
| `NULL` | 失败 |


**备注**: 无

---


> 📌 文件到包的转换接口(自3.0.1.0版本启用)
> **子模块**: 获取包列表信息 | **接口类型**: C

**原型**:
```c
extern char *kdk_package_search_by_file(const char *file_name);
```

**描述**: 文件到包的转换接口

**参数**:

| 参数 | 说明 |
|------|------|
| `file_name` | 待查询的文件名，如“/usr/share/applications/ukui-control-center.desktop”； |
| `为文件绝对路径` | - |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char *` | 成功返回存在文件名的软件包名，需要释放 |
| `NULL` | 失败 |


**备注**: 无

---

其他接口类型接口：
引用方法
（1）dbus服务名
服务名称： com.kylin.kysdk.service
路径名称： /com/kylin/kysdk/packageinfo
接口名称： com.kylin.kysdk.packageinfo
（2）python导入方法
from kysdk import Packageinfo
（3）websocket调用
<script src="/usr/lib/kysdk-dbus-websocket/html/js/qwebchannel.js"></script>
（4）Java导入方法
import kylin.kysdk.java.PackageInfoMethod;
调用子模块：
功能描述
接口类型
接口
入参
返回值
获取系统
中所有包
列表(所
有状态)
dbus
getPackageList () ↦ (Array of [String] arg_0)
无
返回值为
系统中所
有包列表
python
getPackageList()->list
无
返回值为
系统中所
有包列表
websocket
packageinfo.getPackageList()
无
返回值为
系统中所
有包列表
http
http://127.0.0.1:8888/packageinfo/getPackageList
无
Json 返回值为系统
中所有包
列表
java
`List getPackageList()`
无
List 返回值为系统
中所有包
列表
功
能
描
述
接口类型
接口
入参
返回值
获
取
系
统
中
指
定
包
的
版
本
号
dbus
getPackageVersion (String packageName) ↦ (String
arg_0)
packageName
软件包名
String
返回值
为系统
中指定
包的版
本号
python
getPackageVersion(package_name)->str
package_name
软件包名
返回值
为系统
中指定
包的版
本号
websocket
packageinfo.getPackageVersion(pkgname)
pkgname 软件
包名
返回值
为系统
中指定
包的版
本号
http
http://127.0.0.1:8888/packageinfo/getPackageVersion?
packagename=parameter
parameter为软
件包名
Json
返回值
为系统
中指定
包的版
本号
java
`String getPackageVersion(String packageName)`
packageName
软件包名
String
返回值
为系统
中指定
包的版
本号
功
能
描
述
接口类型
接口
入参
返回值
检
测
指
定
包
名
的
软
件
包
是
否
正
确
安
装
dbus
getPackageInstalled (String packageName, String
version) ↦ (Int32 arg_0)
packageName
软件包名
version 版本
号，大部分情况
下为NULL
Int32
返回值为
软件
包是
否正
确安
装；
成功
(1)/失
败(错
误码)
python
getPackageInstalled(package_name, version=None)-
>int
package_name
软件包名
version 版本
号，默认为
None
返回值为
软件
包是
否正
确安
装；
成功
(1)/失
败(错
误码)
websocket
packageinfo.getPackageInstalled(pkgname, vsname)
pkgname 软件
包名
vsname 版本
号，大部分情况
下为NULL
返回值为
软件
包是
否正
确安
装；
成功
(1)/失
败(错
误码)
http
http://127.0.0.1:8888/packageinfo/getPackageInstalled?
packagename=parameter&packageversion=version
parameter 为软
件包名
version为版本
号，大部分情况
下为NULL
Json
返回值为
软件
包是
否正
确安
装；
成功
(1)/失
败(错
误码)
java
`int getPackageInstalled(String packageName, String`
version)
packageName
软件包名
version 版本
号，大部分情况
下为NULL
`int 返`
回值
为软
件包
是否
正确
安
装；
成功
(1)/失
败(错
误码)
示例代码：
#-----------------------------C语言示例-----------------------------
```c
#include "libkypackages.h"
#include <stdio.h>
```
static void print_package(kdk_package_t *package)
```c
{
    if (NULL == package)
```
return;
```c
    printf("包名：%s\t", package->name);
    printf("版本号：%s\t", package->version);
    printf("包类型：%s\t", package->section);
    printf("包状态：%s\t", package->status);
    printf("包大小：%lu\n", package->size_kb);
}
int main()
{
```
kdk_package_list *list = kdk_package_get_packagelist();
```c
    printf("系统中一共%u个软件包\n", list->nums);
    for (size_t i = 0; i < list->nums; i ++)
    {
```
print_package(list->list[i]);
`    }`
kdk_package_free_packagelist(list);
`    printf("Evolution是否安装：%s\n", kdk_package_is_installed("evolution", NULL) == 1 ? "是" : "否");`
char *version = kdk_package_get_version("evolution");
`    printf("Evolution版本号：%s\n", version);`
free(version);
char *description = kdk_package_get_description("aapt");
```c
    if(description != NULL)
    {
        printf("%s\n", description);
```
free(description);
`    }`
char **dir_list = kdk_package_get_code_path("aapt");
```c
    if(NULL != dir_list)
    {
        int i = 0;
        while (dir_list[i])
        {
            printf("%s\n", dir_list[i++]);
        }
        for(int i = 0; dir_list[i]; i++)
        {
```
free(dir_list[i]);
`        }`
free(dir_list);
`    }`
```c
    printf("%d\n", kdk_package_get_file_count("aapt"));
    printf("%d\n", kdk_package_verify_disk_space("/home/kylin/systemd_245.4-
```
4kylin3.20k0.17oemhwy0.4.u_arm64.deb"));
`    printf("%s\n", kdk_package_get_default_browser() ? kdk_package_get_default_browser() : "get browser`
failed");
`    printf("%s\n", kdk_package_get_default_image_viewer() ? kdk_package_get_default_image_viewer() :`
"get image failed");
`    printf("%s\n", kdk_package_get_default_audio_player() ? kdk_package_get_default_audio_player() :`
"get audio failed");
`    printf("%s\n", kdk_package_get_default_video_player() ? kdk_package_get_default_video_player() :`
"get video failed");
`    printf("%s\n", kdk_package_get_default_pdf_viewer() ? kdk_package_get_default_pdf_viewer() : "get`
pdf failed");
`    printf("%s\n", kdk_package_get_default_word_viewer() ? kdk_package_get_default_word_viewer() : "get`
word failed");
`    printf("%s\n", kdk_package_get_default_excel_viewer() ? kdk_package_get_default_excel_viewer() :`
"get execl failed");
`    printf("%s\n", kdk_package_get_default_ppt_viewer() ? kdk_package_get_default_ppt_viewer() : "get`
ppt failed");
kdk_startmenu_list *start_menu_list = kdk_package_get_startmenu_list();
```c
    if (NULL != start_menu_list)
    {
        for (int i = 0; i < start_menu_list->nums; i++)
        {
```
kdk_startmenu_t *tmp = start_menu_list->list[i];
```c
            printf("%s\n", tmp->cmd);
            printf("%s\n", tmp->company);
            printf("%s\n", tmp->icon);
            printf("%s\n", tmp->name);
            printf("%s\n", tmp->param);
            printf("%s\n", tmp->version);
            printf("\n");
        }
```
kdk_package_free_startmenu_list(start_menu_list);
```c
    }
    return 0;
}
```
#-----------------------------python语言示例-----------------------------
from kysdk import Packageinfo
package = Packageinfo()
# getPackageList接口
package.getPackageList()
# getPackageVersion接口
package.getPackageVersion(package_name)
# getPackageInstalled接口
package.getPackageInstalled(package_name, version)
```c
//-----------------------------Java语言示例-----------------------------
import kylin.kysdk.java.PackageInfoMethod;
```
PackageInfoMethod obj = new PackageInfoMethod();
List<String> list = obj.getPackageList();
System.out.println(list);
System.out.println("Version:" + obj.getPackageVersion(list.get(1)));
System.out.println("state: " + obj.getPackageInstalled(list.get(1),
obj.getPackageVersion(list.get(1))));
```c
//-----------------------------websocket语言示例-----------------------------
    // pkgname 为软件包名,vsname为软件包对应版本号
```
var websocket_url = 'ws://localhost:12345';
var websocket = null;
`    if (websocket === null) {`
websocket = new WebSocket(websocket_url);
websocket.onopen = function () {
console.log("connect websocketserver success");
```c
        }
    } else {
```
websocket.close();
websocket = null;
```c
    }
    function xxx() {
```
pkgname = $("#pkgname").val()
vsname = $("#vsname").val()
`        new QWebChannel(websocket,function(channel){`
var packageinfo = channel.objects.packageinfo;
`                //返回信息接收`
packageinfo.sendText.connect(function(message) {
...
```c
                });
                //获得系统中所有包列表
```
packageinfo.getPackageList();
`                //获得系统中指定包的版本号`
packageinfo.getPackageVersion(pkgname);
`                //软件包是否正确安装`
packageinfo.getPackageInstalled(pkgname, vsname);
`            }`
);
```c
    }
//-----------------------------http语言示例-----------------------------
// parameter 为软件包名,vertsion为软件包对应版本号
```
1.http://127.0.0.1:8888/packageinfo/getPackageList
返回值: json
`    {`
Result:连接dbus服务是否成功(0 成功 -1 失败),
ResultMessage:返回dbus方法 getPackageList()信息
`    }`
2.http://127.0.0.1:8888/packageinfo/getPackageVersion?packagename=parameter
返回值: json
`    {`
Result:连接dbus服务是否成功(0 成功 -1 失败),
ResultMessage:返回dbus方法 getPackageVersion(parameter)信息
`    }`
3.http://127.0.0.1:8888/packageinfo/getPackageInstalled?packagename=parameter&packageversion=version
返回值: json
`    {`
Result:连接dbus服务是否成功(0 成功 -1 失败),
ResultMessage:返回dbus方法 getPackageInstalled(parameter,version)信息
`    }`
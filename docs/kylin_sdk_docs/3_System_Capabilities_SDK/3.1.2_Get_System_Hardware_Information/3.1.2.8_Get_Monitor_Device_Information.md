# 3.1.2.8 获取显示器设备信息

封装 C 接口获取到显示器设备硬件信息。

**头文件路径：**

` #include "kysdk/kysdk-system/libkyedid.h"`

**so库路径：**


```
/usr/lib/aarch64-linux-gnu/libkyedid.so
```


**子模块信息：**


> 📌 获取显示器的当前接口(自2.2.3.5版本启用)
> **子模块**: 获取显示器信息 | **接口类型**: C

**原型**:
```c
extern char** kdk_edid_get_interface();
```

**描述**: 获取显示器的当前接口

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char**` | 成功返回显示器的当前接口，由NULL字符串表示结尾；由alloc生成，需要被 kdk_edid_freeall回收 |
| `NULL` | 获取失败 |


---


> 📌 获取显示器的伽马值(自2.2.3.5版本启用)
> **子模块**: 获取显示器信息 | **接口类型**: C

**原型**:
```c
extern float kdk_edid_get_gamma(char *name);
```

**描述**: 获取显示器的伽马值

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 显示器的当前接口 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `float` | 成功返回显示器的伽马值 0.00 获取失败 |


---


> 📌 获取显示器的屏幕尺寸（英寸）(自2.2.3.5版本启用)
> **子模块**: 获取显示器信息 | **接口类型**: C

**原型**:
```c
extern float kdk_edid_get_size(char *name);
```

**描述**: 获取显示器的屏幕尺寸（英寸）

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 显示器的当前接口 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `float` | 成功返回显示器的屏幕尺寸（英寸） 0.00 获取失败 |


---


> 📌 获取显示器的最大分辨率(自2.2.3.5版本启用)
> **子模块**: 获取显示器信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_edid_get_max_resolution(char *name);
```

**描述**: 获取显示器的最大分辨率

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 显示器的当前接口 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回显示器的最大分辨率，返回的字符串需要被 free 释放。 |
| `NULL` | 获取失败 |


---


> 📌 获取显示器的显示器型号(自2.2.3.5版本启用)
> **子模块**: 获取显示器信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_edid_get_model(char *name);
```

**描述**: 获取显示器的显示器型号

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 显示器的当前接口 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回显示器的显示器型号，返回的字符串需要被 free 释放。 |
| `NULL` | 获取失败 |


---


> 📌 获取显示器的可视面积(自2.2.3.5版本启用)
> **子模块**: 获取显示器信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_edid_get_visible_area(char *name);
```

**描述**: 获取显示器的可视面积

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 显示器的当前接口 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回显示器的可视面积，返回的字符串需要被 free 释放。 |
| `NULL` | 获取失败 |


---


> 📌 获取显示器的厂商(自2.2.3.5版本启用)
> **子模块**: 获取显示器信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_edid_get_manufacturer(char *name);
```

**描述**: 获取显示器的厂商

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 显示器的当前接口 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回显示器的厂商，返回的字符串需要被 free 释放。 |
| `NULL` | 获取失败 |


---


> 📌 获取显示器的生产日期（周）(自2.2.3.5版本启用)
> **子模块**: 获取显示器信息 | **接口类型**: C

**原型**:
```c
extern int kdk_edid_get_week(char *name);
```

**描述**: 获取显示器的生产日期（周）

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 显示器的当前接口 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 成功返回显示器的生产日期（周）；例如：13。 |
| `-1` | 获取失败 |


---


> 📌 获取显示器的生产日期(年)(自2.2.3.5版本启用)
> **子模块**: 获取显示器信息 | **接口类型**: C

**原型**:
```c
extern int kdk_edid_get_year(char *name);
```

**描述**: 获取显示器的生产日期(年)

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 显示器的当前接口 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 成功返回显示器的生产日期(年)；例如：2017。 |
| `-1` | 获取失败 |


---


> 📌 获取是否是主显示器（是/否）(自2.2.3.5版本启用)
> **子模块**: 获取显示器信息 | **接口类型**: C

**原型**:
```c
extern int kdk_edid_get_primary(char *name);
```

**描述**: 获取是否是主显示器（是/否）

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 显示器的当前接口 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `1` | 是主显示器 |
| `0` | - |
| `不是主显示器` | - |


---


> 📌 获取当前使用分辨率(自2.2.3.5版本启用)
> **子模块**: 获取显示器信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_edid_get_resolution(char *name);
```

**描述**: 获取使用分辨率

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 显示器的当前接口 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回分辨率，返回的字符串需要被 free 释放 |
| `NULL` | 获取失败 |


---


> 📌 获取图像高宽比(自2.2.3.5版本启用)
> **子模块**: 获取显示器信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_edid_get_ratio(char *name);
```

**描述**: 获取图像高宽比

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 显示器的当前接口 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回图像高宽比，返回的字符串需要被 free 释放 |
| `NULL` | 获取失败 |


---


> 📌 获取显示器edid未解析的字符串(自2.2.3.5版本启用)
> **子模块**: 获取显示器信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_edid_get_character(char *name);
```

**描述**: 获取显示器edid未解析的字符串

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 显示器的当前接口 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回edid未解析的字符串，返回的字符串需要被 free 释放 |
| `NULL` | 获取失败 |


---


> 📌 用于回收字符串列表(自2.2.3.5版本启用)
> **子模块**: 获取显示器信息 | **接口类型**: C

**原型**:
```c
extern inline void kdk_edid_freeall(char **ptr);
```

**描述**: 用于回收字符串列表

**参数**:

| 参数 | 说明 |
|------|------|
| `ptr` | 字符串列表 |


**返回值**: 无

**备注**: 无

---


> 📌 获取显示器的最大亮度(自2.4.1.0版本启用)
> **子模块**: 获取显示器信息 | **接口类型**: C

**原型**:
```c
extern BrightnessInfo* kdk_edid_get_max_brightness(char *name);
```

**描述**: 获取显示器的最大亮度

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 显示器的当前接口 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `BrightnessInfo*` | 显示器的最大亮度结构体。返回的结构体需要由kdk_free_brightnessInfo() 回收。 |
| `NULL` | 获取失败 |


**备注**: 成员：brightness_percentage(int)，描述：当前/最大亮度百分比
成员：brightness_value(int)，描述：当前/最大亮度值

---


> 📌 获取显示器的当前亮度(自2.4.1.0版本启用)
> **子模块**: 获取显示器信息 | **接口类型**: C

**原型**:
```c
extern BrightnessInfo* kdk_edid_get_current_brightness(char *name);
```

**描述**: 获取显示器的当前亮度

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 显示器的当前接口 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `BrightnessInfo*` | 显示器的当前亮度结构体。返回的结构体需要由kdk_free_brightnessInfo() 回收。 |
| `NULL` | 获取失败 |


**备注**: 成员：brightness_percentage(int)，描述：当前/最大亮度百分比
成员：brightness_value(int)，描述：当前/最大亮度值

---


> 📌 获取显示器红色的色度坐标值(自2.4.1.0版本启用)
> **子模块**: 获取显示器信息 | **接口类型**: C

**原型**:
```c
extern ChromaticityCoordinates* kdk_edid_get_red_primary(char *name);
```

**描述**: 获取显示器红色的色度坐标值

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 显示器的当前接口 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `ChromaticityCoordinates*` | 显示器色度坐标值结构体。返回的结构体需要由 kdk_free_chromaticityCoordinates()回收。 |
| `NULL` | 获取失败 |


**备注**: 成员：xCoordinate(char *)，描述：色度坐标x轴
成员：yCoordinate(char *)，描述：色度坐标y轴

---


> 📌 获取显示器绿色的色度坐标值(自2.4.1.0版本启用)
> **子模块**: 获取显示器信息 | **接口类型**: C

**原型**:
```c
extern ChromaticityCoordinates* kdk_edid_get_green_primary(char *name);
```

**描述**: 获取显示器绿色的色度坐标值

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 显示器的当前接口 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `ChromaticityCoordinates*` | 显示器色度坐标值结构体。返回的结构体需要由 kdk_free_chromaticityCoordinates()回收。 |
| `NULL` | 获取失败 |


**备注**: 成员：xCoordinate(char *)，描述：色度坐标x轴
成员：yCoordinate(char *)，描述：色度坐标y轴

---


> 📌 获取显示器蓝色的色度坐标值(自2.4.1.0版本启用)
> **子模块**: 获取显示器信息 | **接口类型**: C

**原型**:
```c
extern ChromaticityCoordinates* kdk_edid_get_blue_primary(char *name);
```

**描述**: 获取显示器蓝色的色度坐标值

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 显示器的当前接口 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `ChromaticityCoordinates*` | 显示器色度坐标值结构体。返回的结构体需要由 kdk_free_chromaticityCoordinates()回收。 |
| `NULL` | 获取失败 |


**备注**: 成员：xCoordinate(char *)，描述：色度坐标x轴
成员：yCoordinate(char *)，描述：色度坐标y轴

---


> 📌 获取显示器白色的色度坐标值(自2.4.1.0版本启用)
> **子模块**: 获取显示器信息 | **接口类型**: C

**原型**:
```c
extern ChromaticityCoordinates* kdk_edid_get_white_primary(char *name);
```

**描述**: 获取显示器白色的色度坐标值

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 显示器的当前接口 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `ChromaticityCoordinates*` | 显示器色度坐标值结构体。返回的结构体需要由 kdk_free_chromaticityCoordinates()回收。 |
| `NULL` | 获取失败 |


**备注**: 成员：xCoordinate(char *)，描述：色度坐标x轴
成员：yCoordinate(char *)，描述：色度坐标y轴

---


> 📌 获取显示器物理水平DPI(自2.4.1.0版本启用)
> **子模块**: 获取显示器信息 | **接口类型**: C

**原型**:
```c
extern float kdk_edid_get_rawDpiX(char *name);
```

**描述**: 获取显示器物理水平DPI

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 显示器的当前接口 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `float` | 显示器物理水平DPI 0.0 获取失败 |


**备注**: 无

---


> 📌 获取显示器物理垂直DPI(自2.4.1.0版本启用)
> **子模块**: 获取显示器信息 | **接口类型**: C

**原型**:
```c
extern float kdk_edid_get_rawDpiY(char *name);
```

**描述**: 获取显示器物理垂直DPI

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 显示器的当前接口 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `float` | 显示器物理垂直DPI 0.0 获取失败 |


**备注**: 无

---


> 📌 获取显示器当前使用的刷新率(自2.4.1.0版本启用)
> **子模块**: 获取显示器信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_edid_get_refreshRate(char *name);
```

**描述**: 获取显示器当前使用的刷新率

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 显示器的当前接口 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回显示器刷新率，返回的字符串需要被 free 释放 |
| `NULL` | 获取失败 |


**备注**: 无

---


> 📌 获取显示器的方向(自2.4.1.0版本启用)
> **子模块**: 获取显示器信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_edid_get_rotation(char *name);
```

**描述**: 获取显示器的方向

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 显示器的当前接口 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回显示器的方向，返回的字符串需要被 free 释放 |
| `NULL` | 获取失败 |


**备注**: 无

---


> 📌 获取显示器的序列号(自2.4.1.0版本启用)
> **子模块**: 获取显示器信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_edid_get_serialNumber(char *name);
```

**描述**: 获取显示器的序列号

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 显示器的当前接口 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回显示器的序列号，返回的字符串需要被 free 释放 |
| `NULL` | 获取失败 |


**备注**: 无
释放由kdk_edid_get_max_brightness和kdk_edid_get_current_brightness返回的显示器亮度(自

---

2.4.1.0版本启用)
> **子模块**: 获取显示器信息 | **接口类型**: C

**原型**:
```c
extern void kdk_free_brightnessInfo(BrightnessInfo *info);
```

**描述**: 释放由kdk_edid_get_max_brightness和kdk_edid_get_current_brightness返回的显示器亮
度

**参数**:

| 参数 | 说明 |
|------|------|
| `info` | 由kdk_edid_get_max_brightness和kdk_edid_get_current_brightness返回的结构体 |
| `指针` | - |


**返回值**: 无

**备注**: 无
释放由kdk_edid_get_red_primary、kdk_edid_get_green_primary、
kdk_edid_get_blue_primary、kdk_edid_get_white_primary返回的色度坐标结构体(自2.4.1.0版本

---


> 📌 启用)
> **子模块**: 获取显示器信息 | **接口类型**: C

**原型**:
```c
extern void kdk_free_chromaticityCoordinates(ChromaticityCoordinates *info);
```

**描述**: 释放由kdk_edid_get_red_primary、kdk_edid_get_green_primary、
kdk_edid_get_blue_primary、kdk_edid_get_white_primary返回的色度坐标结构体

**参数**:

| 参数 | 说明 |
|------|------|
| `info` | 由kdk_edid_get_red_primary、kdk_edid_get_green_primary、 |
| `kdk_edid_get_blue_primary、kdk_edid_get_white_primary返回的结构体指针` | - |


**返回值**: 无

**备注**: 无

---


> 📌 设置屏幕分辨率(自2.5.1.0版本启用)
> **子模块**: 获取显示器信息 | **接口类型**: C

**原型**:
```c
extern int kdk_edid_set_resolution(char *name, char *xresolv, char *yresolv);
```

**描述**: 设置屏幕分辨率

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 显示器名称 |
| `xresolv` | x分辨率 |
| `yresolv` | y分辨率 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `1` | - |
| `成功` | - |
| `0` | 失败 |


**备注**: 无

---

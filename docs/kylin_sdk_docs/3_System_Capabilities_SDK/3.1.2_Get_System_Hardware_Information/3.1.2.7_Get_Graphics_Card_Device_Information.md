# 3.1.2.7 获取显卡设备信息

封装 C 接口获取到显卡设备硬件信息。

**头文件路径：**

` #include "kysdk/kysdk-system/libkydisplay.h"`

**so库路径：**


```
/usr/lib/aarch64-linux-gnu/libkyhw.so
```


**子模块信息：**


> 📌 获取显卡的制造商(自2.2.3.5版本启用)
> **子模块**: 获取显卡信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_display_get_vendor();
```

**描述**: 获取显卡的制造商

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回显卡的制造商，返回的字符串需要被 free 释放。 |
| `NULL` | 获取失败 |


---


> 📌 获取显卡的型号(自2.2.3.5版本启用)
> **子模块**: 获取显卡信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_display_get_product();
```

**描述**: 获取显卡的型号

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回显卡的型号，返回的字符串需要被 free 释放。 |
| `NULL` | 获取失败 |


---


> 📌 获取显卡的说明(自2.2.3.5版本启用)
> **子模块**: 获取显卡信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_display_get_description();
```

**描述**: 获取显卡的说明

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回显卡的说明，返回的字符串需要被 free 释放。 |
| `NULL` | 获取失败 |


---


> 📌 获取显卡的物理id(自2.2.3.5版本启用)
> **子模块**: 获取显卡信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_display_get_physical_id();
```

**描述**: 获取显卡的物理id

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回显卡的物理id，返回的字符串需要被 free 释放。 |
| `NULL` | 获取失败 |


---


> 📌 获取显卡的总线地址(自2.2.3.5版本启用)
> **子模块**: 获取显卡信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_display_get_bus_info();
```

**描述**: 获取显卡的总线地址

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回显卡的总线地址，返回的字符串需要被 free 释放。 |
| `NULL` | 获取失败 |


---


> 📌 获取显卡的设备版本(自2.2.3.5版本启用)
> **子模块**: 获取显卡信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_display_get_version();
```

**描述**: 获取显卡的设备版本

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回显卡的设备版本，返回的字符串需要被 free 释放。 |
| `NULL` | 获取失败 |


---


> 📌 获取显卡的数据宽度(自2.2.3.5版本启用)
> **子模块**: 获取显卡信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_display_get_width();
```

**描述**: 获取显卡的数据宽度

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回显卡的数据宽度，返回的字符串需要被 free 释放。 |
| `NULL` | 获取失败 |


---


> 📌 获取显卡的频率(自2.2.3.5版本启用)
> **子模块**: 获取显卡信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_display_get_clock();
```

**描述**: 获取显卡的频率

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回显卡的频率，返回的字符串需要被 free 释放。 |
| `NULL` | 获取失败 |


---


> 📌 获取显卡的功能(自2.2.3.5版本启用)
> **子模块**: 获取显卡信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_display_get_capabilities();
```

**描述**: 获取显卡的功能

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回显卡的功能，返回的字符串需要被 free 释放。 |
| `NULL` | 获取失败 |


---


> 📌 获取显卡的配置(自2.2.3.5版本启用)
> **子模块**: 获取显卡信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_display_get_configuration();
```

**描述**: 获取显卡的配置

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回显卡的配置，返回的字符串需要被 free 释放。 |
| `NULL` | 获取失败 |


---


> 📌 获取显卡的资源(自2.2.3.5版本启用)
> **子模块**: 获取显卡信息 | **接口类型**: C

**原型**:
```c
extern char* kdk_display_get_resources();
```

**描述**: 获取显卡的资源

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `char*` | 成功返回显卡的资源，返回的字符串需要被 free 释放。 |
| `NULL` | 获取失败 |


---

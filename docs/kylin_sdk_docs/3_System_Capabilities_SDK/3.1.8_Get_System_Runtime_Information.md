# 3.1.8 获取系统运行时信息


**安装命令：**

sudo apt-get install libkysdk-realtime libkysdk-realtime-dev

**构建示例：**

（1）.pro 文件构建项目
`qt 项目.pro 文件中增加：`
CONFIG += link_pkgconfig
PKGCONFIG += kysdk-realtime
（2）CMakeLists.txt 构建项目
`cmake_minimum_required(VERSION 3.5)`
find_package(PkgConfig REQUIRED)
pkg_check_modules(KYSDKREALTIME kysdk-realtime)
target_include_directories(demo PRIVATE ${KYSDKREALTIME_INCLUDE_DIRS})
target_link_directories(demo PRIVATE ${KYSDKREALTIME_LIBRARY_DIRS})
target_link_libraries(demo PRIVATE ${KYSDKREALTIME_LIBRARIES})

#### 3.1.8.1 获取系统运行时信息

封装C接口获取网速、cpu 温度、硬盘温度等。

**头文件路径：**

` #include "kysdk/kysdk-system/libkyrealtimeinfo.h"`

**so库路径：**


```
/usr/lib/aarch64-linux-gnu/libkyrealtime.so
```


**子模块信息：**


> 📌 获取上传的瞬时网速(自2.0.0.0版本启用)
> **子模块**: 获取系统运行时信息 | **接口类型**: C

**原型**:
```c
extern float kdk_real_get_net_speed(const char *nc);
```

**描述**: 获取上传的瞬时网速信息

**参数**:

| 参数 | 说明 |
|------|------|
| `nc` | 网卡名称，如eno1 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `float` | 成功返回上传的瞬时网速 |
| `-1` | 获取失败 |


**备注**: 无

---


> 📌 获取下载的瞬时网速信息(自2.2.3.5版本启用)
> **子模块**: 获取系统运行时信息 | **接口类型**: C

**原型**:
```c
extern float kdk_real_get_if_speed(const char *nc);
```

**描述**: 获取下载的瞬时网速信息

**参数**:

| 参数 | 说明 |
|------|------|
| `nc` | 网卡名称，如eno1 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `float` | 成功返回下载的瞬时网速 |
| `-1` | 获取失败 |


**备注**: 无

---


> 📌 获取cpu温度(自2.0.0.0版本启用)
> **子模块**: 获取系统运行时信息 | **接口类型**: C

**原型**:
```c
extern double kdk_real_get_cpu_temperature();
```

**描述**: 获取CPU温度

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `double` | 成功返回CPU温度 |
| `-1` | 获取失败 |


**备注**: 无

---


> 📌 获取磁盘温度(自2.0.0.0版本启用)
> **子模块**: 获取系统运行时信息 | **接口类型**: C

**原型**:
```c
extern int kdk_real_get_disk_temperature(const char *name);
```

**描述**: 获取磁盘温度

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 磁盘名称，应当是例如/dev/sda这种绝对路径 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 成功返回磁盘温度 |
| `-1` | 获取失败 |


**备注**: 无

---


> 📌 获取磁盘转速(自2.0.0.0版本启用)
> **子模块**: 获取系统运行时信息 | **接口类型**: C

**原型**:
```c
extern int kdk_real_get_disk_rate(const char *name);
```

**描述**: 获取磁盘转速

**参数**:

| 参数 | 说明 |
|------|------|
| `name` | 磁盘名称，应当是例如/dev/sda这种绝对路径 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 成功返回磁盘转速 |
| `-1` | 获取失败 |


**备注**: 无

---


> 📌 获取指定磁盘的IO读取字节数(自3.0.1.0版本启用)
> **子模块**: 获取系统运行时信息 | **接口类型**: C

**原型**:
```c
extern unsigned long long kdk_real_get_disk_read(const char *diskname);
```

**描述**: 获取指定磁盘的 IO 读取字节数

**参数**:

| 参数 | 说明 |
|------|------|
| `diskname` | 磁盘名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `unsigned long long` | 返回读取的字节数 |
| `0` | - |
| `表示失败` | - |


**备注**: 无

---


> 📌 获取指定磁盘的IO写入字节数(自3.0.1.0版本启用)
> **子模块**: 获取系统运行时信息 | **接口类型**: C

**原型**:
```c
extern unsigned long long kdk_real_get_disk_write(const char *diskname);
```

**描述**: 获取指定磁盘的 IO 写入字节数

**参数**:

| 参数 | 说明 |
|------|------|
| `diskname` | 磁盘名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `unsigned long long` | 返回写入的字节数 |
| `0` | - |
| `表示失败` | - |


**备注**: 无

---


> 📌 获取指定进程的CPU执行的指令数(自3.0.1.0版本启用)
> **子模块**: 获取系统运行时信息 | **接口类型**: C

**原型**:
```c
extern int kdk_real_get_cpu_instructions(int pid, int sleepTime);
```

**描述**: 获取指定进程的 CPU 执行的指令数

**参数**:

| 参数 | 说明 |
|------|------|
| `pid` | 进程ID |
| `sleepTime` | 采样间隔时间（秒） |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 指令数 |
| `-1` | - |
| `表示失败` | - |


**备注**: 默认 sleepTime = 1

---


> 📌 获取指定进程的CPU缓存未命中次数(自3.0.1.0版本启用)
> **子模块**: 获取系统运行时信息 | **接口类型**: C

**原型**:
```c
extern int kdk_real_get_cpu_cache_misses(int pid, int sleepTime);
```

**描述**: 获取指定进程的 CPU 缓存未命中次数

**参数**:

| 参数 | 说明 |
|------|------|
| `sleepTime` | 采样间隔时间（秒），默认 sleepTime = 1 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 缓存未命中次数 |
| `-1` | - |
| `表示失败` | - |


**备注**: 无

---


> 📌 获取指定进程的CPU分支预测未命中的次数(自3.0.1.0版本启用)
> **子模块**: 获取系统运行时信息 | **接口类型**: C

**原型**:
```c
extern int kdk_real_get_cpu_branch_misses(int pid, int sleepTime);
```

**描述**: 获取指定进程的 CPU 分支预测未命中的次数

**参数**:

| 参数 | 说明 |
|------|------|
| `sleepTime` | 采样间隔时间（秒），默认 sleepTime = 1 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 分支预测未命中次数 |
| `-1` | - |
| `表示失败` | - |


**备注**: 无

---


> 📌 获取指定进程的CPU系统总线周期数(自3.0.1.0版本启用)
> **子模块**: 获取系统运行时信息 | **接口类型**: C

**原型**:
```c
extern int kdk_real_get_cpu_bus_cycles(int pid, int sleepTime);
```

**描述**: 获取指定进程的 CPU 系统总线周期数

**参数**:

| 参数 | 说明 |
|------|------|
| `sleepTime` | 采样间隔时间（秒），默认 sleepTime = 1 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 系统总线周期数 |
| `-1` | - |
| `表示失败` | - |


**备注**: 无

---


> 📌 获取指定进程的CPU时钟周期数(自3.0.1.0版本启用)
> **子模块**: 获取系统运行时信息 | **接口类型**: C

**原型**:
```c
extern int kdk_real_get_cpu_cycles(int pid, int sleepTime);
```

**描述**: 获取指定进程的 CPU 时钟周期数

**参数**:

| 参数 | 说明 |
|------|------|
| `sleepTime` | 采样间隔时间（秒），默认 sleepTime = 1 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 时钟周期数 |
| `-1` | - |
| `表示失败` | - |


**备注**: 无

---


> 📌 获取等待磁盘 I/O 操作完成的请求数量(自3.0.1.0版本启用)
> **子模块**: 获取系统运行时信息 | **接口类型**: C

**原型**:
```c
extern unsigned long kdk_real_get_disk_queue_length(char *diskname);
```

**描述**: 获取等待磁盘 I/O 操作完成的请求数量

**参数**:

| 参数 | 说明 |
|------|------|
| `diskname` | 磁盘名 |


**返回值**:

| 返回值 | 说明 |
|--------|------|
| `unsigned long` | 等待的 I/O 请求数量 IO_ERROR 表示失败 |


**备注**: 无

---


> 📌 获取cpu执行内核态的时间比例(自3.0.1.0版本启用)
> **子模块**: 获取系统运行时信息 | **接口类型**: C

**原型**:
```c
extern float kdk_real_get_cpu_kernel_modes_per();
```

**描述**: 获取 CPU 执行内核态的时间比例

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `float` | CPU 执行内核态的时间比例 |
| `0` | - |
| `表示失败` | - |


**备注**: 无

---


> 📌 获取cpu执行用户态的时间比例(自3.0.1.0版本启用)
> **子模块**: 获取系统运行时信息 | **接口类型**: C

**原型**:
```c
extern float kdk_real_get_cpu_user_modes_per();
```

**描述**: 获取 CPU 执行用户态的时间比例

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `float` | CPU 执行用户态的时间比例 |
| `0` | - |
| `表示失败` | - |


**备注**: 无

---


> 📌 获取等待cpu时间的线程数(自3.0.1.0版本启用)
> **子模块**: 获取系统运行时信息 | **接口类型**: C

**原型**:
```c
extern int kdk_real_get_cpu_queue_length();
```

**描述**: 获取等待 CPU 时间的线程数

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 等待 CPU 时间的线程数 |
| `-1` | - |
| `表示失败` | - |


**备注**: 无

---


> 📌 获取每秒交换到磁盘或从磁盘读回内存的页面数量(自3.0.1.0版本启用)
> **子模块**: 获取系统运行时信息 | **接口类型**: C

**原型**:
```c
extern int kdk_real_get_memory_page();
```

**描述**: 获取每秒交换到磁盘或从磁盘读回内存的页面数量

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `int` | 每秒交换到磁盘或从磁盘读回内存的页面数量 |
| `-1` | - |
| `表示失败` | - |


**备注**: 无

---


> 📌 获取cpu的页错误率（硬页故障）(自3.0.1.0版本启用)
> **子模块**: 获取系统运行时信息 | **接口类型**: C

**原型**:
```c
extern float kdk_real_get_cpu_hard_page_faults();
```

**描述**: 获取 CPU 的硬页错误率（硬页故障）

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `float` | CPU 的硬页错误率 |
| `-1` | - |
| `表示失败` | - |


**备注**: 无

---


> 📌 获取cpu的页错误率（软页故障）(自3.0.1.0版本启用)
> **子模块**: 获取系统运行时信息 | **接口类型**: C

**原型**:
```c
extern float kdk_real_get_cpu_soft_page_faults();
```

**描述**: 获取 CPU 的软页错误率（软页故障）

**参数**: 无

**返回值**:

| 返回值 | 说明 |
|--------|------|
| `float` | CPU 的软页错误率 |
| `-1` | - |
| `表示失败` | - |


**备注**: 无

---

其他接口类型接口：
引用方法
（1）dbus服务名
服务名称： com.kylin.kysdk.service
路径名称： /com/kylin/kysdk/runinfo
接口名称： com.kylin.kysdk.runinfo
（2）python导入方法
from kysdk import Runinfo
（3）websocket调用
<script src="/usr/lib/kysdk-dbus-websocket/html/js/qwebchannel.js"></script>
（4）Java导入方法
import kylin.kysdk.java.RunInfoMethod;
调用子模块：
功能描
述
接口类型
接口
入参
返回值
获取上
传的瞬
时网速
信息
dbus
getNetSpeed (String nc) ↦ (Double arg_0)
nc 网卡名
`Double 返`
回值为实时
网速
python
getNetSpeed(netcard)->int
netcard 网卡
名
返回值为实
时网速
websocket
runinfo.getNetSpeed(pid)
pid 网卡名
返回值为实
时网速
http
http://127.0.0.1:8888/runinfo/getNetSpeed?
netspeed=parameter
parameter为
网卡名
Json 返回值为实时网
速
java
`double getNetSpeed(String nc)`
nc 网卡名
`double 返`
回值为实时
网速
功能描述
接口类型
接口
入参
返回值
获取磁盘
转速
dbus
getDiskRate (String diskpath) ↦ (Int32
arg_0)
diskpath 硬盘
绝对路径
Int32 返回值为磁盘转
速
python
getDiskRate(disk_path)->int
disk_path 硬
盘绝对路径
返回值为磁
盘转速
websocket
runinfo.getDiskRate(pid)
pid 硬盘绝对
路径
返回值为磁
盘转速
http
http://127.0.0.1:8888/runinfo/getDiskRate?
diskpath=parameter
parameter为
硬盘绝对路径
Json 返回值为磁盘转
速
java
`int getDiskRate(String diskpath)`
diskpath 硬盘
绝对路径
`int 返回值`
为磁盘转速
功能描述
接口类型
接口
入参
返回值
获取CPU
温度
dbus
getCpuTemperature () ↦ (Double arg_0)
无
`Double 返`
回值为实时
CPU温度
python
getCpuTemperature()->int
无
返回值为实
时CPU温度
websocket
runinfo.getCpuTemperature()
无
返回值为实
时CPU温度
http
http://127.0.0.1:8888/runinfo/getCpuTemperature
无
Json 返回值为实时
CPU温度
java
`double getCpuTemperature()`
无
`double 返`
回值为实时
CPU温度
功能
描述
接口类型
接口
入参
返回值
获取
磁盘
温度
dbus
getDiskTemperature (String diskpath) ↦ (Double
arg_0)
diskpath 硬
盘绝对路径
Double
返回值为
实时硬盘
温度
python
getDiskTemperature(disk_path)->int
disk_path
硬盘绝对路
径
返回值为
实时硬盘
温度
websocket
runinfo.getDiskTemperature(pid)
pid 硬盘绝
对路径
返回值为
实时硬盘
温度
http
http://127.0.0.1:8888/runinfo/getDiskTemperature?
diskpath=parameter
diskpath为
硬盘绝对路
径
Json 返回值为实时
硬盘温度
java
`double getDiskTemperature(String diskpath)`
diskpath 硬
盘绝对路径
`double 返`
回值为实
时硬盘温
度
示例代码：
#-----------------------------C语言示例-----------------------------
```c
#include "stdio.h"
#include "libkyrealtimeinfo.h"
int main()
{
    printf("netSpeed : %f\n", kdk_real_get_net_speed("enaftgm1i0"));
    printf("cpuTemp: %f\n", kdk_real_get_cpu_temperature());
    printf("diskTemp : %d\n", kdk_real_get_disk_temperature("/dev/nvme0n1"));
    printf("diskRate : %d\n", kdk_real_get_disk_rate("/dev/nvme0n1"));
    printf("ifnetSpeed : %f\n", kdk_real_get_if_speed("enaftgm1i0"));
    return 0;
}
```
#-----------------------------python语言示例-----------------------------
from kysdk import Runinfo
runinfo = Runinfo()
# getCpuTemperature接口
runinfo.getCpuTemperature()
# getNetSpeed接口
runinfo.getNetSpeed(netcard)
# getDiskRate接口
runinfo.getDiskRate(disk_path)
# getDiskTemperature接口
runinfo.getDiskTemperature(disk_path)
```c
//-----------------------------Java语言示例-----------------------------
import kylin.kysdk.java.RunInfoMethod;
```
RunInfoMethod obj = new RunInfoMethod();
System.out.println("NetSpeed:" + obj.getNetSpeed("enp3s0"));
System.out.println("Rate:" + obj.getDiskRate("/dev/sda"));
System.out.println("CpuTemp:" + obj.getCpuTemperature());
System.out.println("DiskTemp:" + obj.getDiskTemperature("/dev/sda"));
```c
//-----------------------------websocket语言示例-----------------------------
    //pid 网卡名
```
var websocket_url = 'ws://localhost:12345';
var websocket = null;
`    if (websocket === null) {`
websocket = new WebSocket(websocket_url);
websocket.onopen = function () {
console.log("connect websocketserver success");
```c
        }
    } else {
```
websocket.close();
websocket = null;
```c
    }
    function xxx() {
        new QWebChannel(websocket,function(channel){
```
var runinfo = channel.objects.runinfo;
`                //返回信息接收`
runinfo.sendText.connect(function(message) {
...
```c
                });
                //获取实时网速
```
runinfo.getNetSpeed(pid);
`                //获取磁盘转速`
runinfo.getDiskRate(pid);
`                //获取实时CPU温度`
runinfo.getCpuTemperature();
`                //获取实时硬盘温度`
runinfo.getDiskTemperature(pid);
`            }`
);
```c
    }
//-----------------------------http语言示例-----------------------------
// parameter 为网卡名
```
1.http://127.0.0.1:8888/runinfo/getNetSpeed?netspeed=parameter
返回值: json
`    {`
Result:连接dbus服务是否成功(0 成功 -1 失败),
ResultMessage:返回dbus方法 getNetSpeed(parameter)信息
```c
    }
// parameter 为硬盘绝对路径
```
2.http://127.0.0.1:8888/runinfo/getDiskRate?diskpath=parameter
返回值: json
`    {`
Result:连接dbus服务是否成功(0 成功 -1 失败),
ResultMessage:返回dbus方法 getDiskRate(parameter)信息
`    }`
3.http://127.0.0.1:8888/runinfo/getCpuTemperature
返回值: json
`    {`
Result:连接dbus服务是否成功(0 成功 -1 失败),
ResultMessage:返回dbus方法 getCpuTemperature()信息
```c
    }
// parameter 为硬盘绝对路径
```
4.http://127.0.0.1:8888/runinfo/getDiskTemperature?diskpath=parameter
返回值: json
`    {`
Result:连接dbus服务是否成功(0 成功 -1 失败),
ResultMessage:返回dbus方法 getDiskTemperature(parameter)信息
`    }`